# Checkpoint

Living-state doc. Update on every meaningful merge to `main`.

**Last updated:** 2026-05-17
**Latest commit on `main`:** `f278da7` (Phase 5.6.1: Cross-cutting interaction primitives + Project Team card). Squash of `claude/phase-5-6-1-primitives` (two feature commits `af5a98a` + `eba9c37`). Three primitives every later 5.6 sub-phase depends on, plus every consumer call site the spec listed, plus a catch-up Team card on ProjectEdit. **RecordCombobox** (`src/components/ui/RecordCombobox.tsx`): Radix Popover + cmdk typeahead with inline "+ Add" pinned at bottom via `forceMount` + literal sentinel value. Two source modes — `lookup` (binds option name; backed by `useLookup` helper) and `record` (binds id; parent-supplied `loadOptions` + optional `onMiniCreate`). Multi-select supported with chip removal. **MiniCreateModal** (`src/components/ui/MiniCreateModal.tsx`): 2-4 field modal driven by RecordCombobox's "+ Add"; name field required + auto-pre-filled from typeahead text. **ClickPillCell** (`src/components/hq/ClickPillCell.tsx`): wireframe-canonical `.hq-pill .hq-pill--<token>` trigger that opens a popover of token-coloured option dots; optimistic UI with rollback on save error; `stopPropagation` to keep row click-to-detail quiet. **phone util** (`src/lib/hq/phone.ts`): `parsePhone` + `formatPhone`; save-time only via `onBlur`; non-parseable inputs (international, extensions, garbage) pass through unchanged. Wired into every consumer the spec listed: ClickPillCell on Projects (status), Tasks (status + priority), Deliverables (status), Outlook EntryPanel (confidence); RecordCombobox replaced every InlineAddSelect call site (ProjectEdit Client + Category + City; VendorEdit Category + City; ClientEdit City; VenueEdit City; TeamMemberEdit Department) plus the custom multi-select chip patterns at Vendor Capabilities and Venue Types (both flagged as spec deviations since the spec assumed they were InlineAddSelect; reality was MultiTagInput / custom chip-builder); `onBlur={formatPhone}` on PersonEdit, VendorEdit, ClientEdit phone fields. Team card catch-up (commit `eba9c37`): ProjectEdit § Team section was a placeholder paragraph since 5.2.1 ("lands when Team page ships in 5.4"); spec 5.6.1 § 5.A called for an Account Managers + Designers picker but treated `no existing picker` as "skip" in the first pass; `eba9c37` builds the picker correctly using two multi RecordCombobox widgets in record mode sourced from `users` (active=true), with diff-on-save against `project_account_managers` + `project_designers` join rows (same VenueEdit pattern). Spec table mismatch flagged: § 5.A said `source.table: "people"` but join FKs target `users.id`; `users` is correct (`people` is for external contacts). projectStatusToken.ts adds `taskPriorityToken` + `outlookConfidenceToken` + `OUTLOOK_CONFIDENCE_VALUES`; `src/lib/tasks/queries.ts` adds `updateTaskPriority`; `src/lib/outlook/queries.ts` adds `updateOutlookConfidence`. One migration applied at the squash gate: `20260517100000_phase_5_6_1_phone_normalization.sql` (data-only fix: pg_temp helper extracts 10 US digits + formats `(XXX) XXX-XXXX`; updates `people.phone`, `vendors.contact_phone`, `clients.contact_phone` only where they don't already match the formatted regex; idempotent + safe to re-run; non-US/extension/garbage pass through unchanged). No schema change → types regen produced no delta. InlineAddSelect now `@deprecated` with no callsites remaining; soak-then-delete in a follow-on pass. Two carry-forward specs drafted in `OUTPUTS/COWORK_SYNC.md` for the next sub-phase: (A) detail-page inline edit pattern — make record detail pages directly editable inline so the Edit form click goes away; the bigger architectural call (Sticky Save Bar replacement, per-field validation, RLS tightness check); (B) list-view inline cell editing + chevron open-affordance — extend `DataTable` to take `onOpen` instead of `onRowClick` + every cell becomes a stop-propagation edit component; mechanical but wide-touching (Projects + Tasks + Deliverables; Outlook entries don't fit the DataTable shape and route through the side panel instead). Predecessor `0f122c1` (Phase 5.5.1: Sign-in page, Surface 01 part 1). Squash of `claude/phase-5-5-1-signin` (single feature commit `19b16ef`). Replaces the Phase 5.1 stealth coming-soon Landing with the wireframe Surface 01 sign-in page: centered Montserrat ExtraBold "MIRROR NYC HQ" wordmark (HQ in coral) + visible 48px white Google Sign-In button with the 4-color G icon on a full-viewport dark background. Two files: `src/pages/Landing.tsx` (full rewrite, optimistic `signingIn` flag prevents double-clicks, already-authed mounts redirect to `/home`) + new `src/components/icons/GoogleColorIcon.tsx` (4-color SVG lifted byte-for-byte from wireframe sprite line 595). Hooks above any early return per design-system § 12.2. No migration, no edge function deploy, no types regen, no schema impact. Slug retention plumbing (`ProtectedRoute` writes sessionStorage("post_signin_redirect"); `useAuth` reads + clears) was already complete pre-spec; grep + App.tsx audit confirmed every route including `/venue-scout/*` and `/talent-scout/*` nests inside the parent `<ProtectedRoute>` group at App.tsx:103-109, so VS/TS deep links survive the OAuth round trip without additional gate code. Skipped formal code-reviewer + security-auditor passes (micro-scope: one SVG paste + ~75-line page rewrite using only existing `useAuth` API; no new auth surface, no new data fetch). Screenshots at 1440px + 375px confirmed visual fidelity. Predecessor `0cc52f3` (Phase 5.5: Notifications + Activity Feed + Search + States Polish). Squash of `claude/condescending-shirley-03cab5` (two branch commits: `8fff2ce` main implementation + `ecac64f` `[skip netlify] regen types from linked DB`). One migration applied at the squash gate: `20260517000000_phase_5_5_notifications_activity_search.sql` (new `user_notification_preferences` table with per-user RLS + GRANTs + `updated_at_auto` trigger; new `notifications.delivered_slack` column; `notifications` joined `supabase_realtime` publication + `REPLICA IDENTITY FULL`; new `notifications_dispatch_writer()` function + AFTER triggers on `tasks` INSERT/UPDATE and `projects` UPDATE that POST to the notifications-dispatch edge function via `public.invoke_edge_function`; `handle_new_user()` rewritten to call `notifications-dispatch` with `event_type='user_pending'` while preserving the inline durable per-admin notifications row writes; three new pg_cron schedules at 11/12/13:00 UTC for `hq-cron-event-date-today` / `hq-cron-task-due-today` / `hq-cron-deliverable-due-3d`). Four edge functions deployed: `notifications-dispatch` (central fan-out, `requireInternalSecret`-only per security audit MUST-FIX, per-user preference + global kill-switch checks, Slack DM via new `_shared/slackDm.ts` helper with mrkdwn special-char escaping, email-only-for-user_pending path preserved), plus the three daily cron functions. types regen delta vs the hand-augmented version was column-order alphabetization (CLI default) + an `Initialising login role...` stdout line stripped from the regen output. Predecessor `f4e4aab` (Phase 5.4: Wiki + Account Logins + Users + Settings). Squash of `claude/zen-mestorf-940d7f` (single feature commit `fa21daa` — initial spec implementation + two feedback rounds + hooks-after-early-return regression fix all merged into one commit at the squash gate). Three migrations applied at the squash gate: `20260516160000_phase_5_4_wiki_team_settings.sql` (4 new tables: departments + wiki_pages + credentials + mirror_holidays; users.id FK to auth.users dropped + department_tags column dropped + 5 new user columns; handle_new_user rewritten with email-match id-swap path; activity_log + updated_at triggers; RLS + GRANTs; 11 seeded wiki pages + 18 seeded mirror holidays; GRANT cleanup on users INSERT + cities DELETE + project_categories DELETE), `20260516170000_phase_5_4_feedback.sql` (TipTap WYSIWYG editor + HTML storage; admin_only visibility option; vendors-at-a-glance -> preferred-vendors slug+title swap; credentials.related_note dropped; handle_new_user link_url /team -> /users; all 8 seeded prose pages converted to HTML), `20260516180000_phase_5_4_feedback_round_2.sql` (credentials INS/UPD/DEL widened to admin+standard; vendors.preferred boolean + partial index). types regen produced delta: new wiki_pages / credentials / departments / mirror_holidays Row/Insert/Update + Relationships; users Row gained role_title / department_id / slack_handle / slack_user_id / last_active_at and lost department_tags. New deps: TipTap + react-markdown (latter unused after the round-1 TipTap switch; prune in polish pass). Branch name was the auto-generated `claude/zen-mestorf-940d7f` rather than the spec's `claude/phase-5-4-wiki-team-settings`; cosmetic per `docs/decisions.md` Phase 5.4 (same precedent as 5.3). Predecessor `1c21ea9` (Phase 5.3: Calendar + Outlook + Project install/removal dates). Squash of `claude/priceless-jang-b6de74` (three branch commits: `96ded4c` main impl; `62004a2` code-reviewer + migration-reviewer cold pass fixes (MUST FIX timezone off-by-one in CalendarPage isoRange via parseDateOnly helper + 4 SHOULD FIX: useCalendarVisibility unmount cleanup, widened CalendarEventKind union to wire `.cal-ev.olk`/`.cal-ev.hol`, explicit ON DELETE RESTRICT on outlook_entries.created_by, cleaner ERRCODE P0001 on the RPC's already-linked refusal); `0e53080` page-header alignment polish (nav cluster right-edges with Category chip via 248px marginRight); `91ac4c6` types regen). Three migrations applied at the squash gate: `20260516150000_phase_5_3_outlook_entries_table.sql` (new outlook_confidence enum + outlook_entries table + 4 indexes + 2 triggers + 4 RLS policies + GRANTs + promote_outlook_to_project SECURITY DEFINER RPC), `20260516150001_phase_5_3_projects_install_removal_dates.sql` (4 new date cols on projects), `20260516150002_phase_5_3_saved_views_calendar_check.sql` (entity_type CHECK widened to include 'calendar'). types regen produced an 85-line delta (new outlook_entries shape + enum + RPC + 4 projects columns). Zero new edge function deploys. Branch name was the auto-generated `claude/priceless-jang-b6de74` rather than the spec's `claude/phase-5-3-calendar-outlook`; cosmetic per `docs/decisions.md` Phase 5.3.
**Active feature branch:** none. `claude/phase-5-6-1-primitives` was squash-merged + its worktree removed.
**Current phase:** **Phase 5.6.1 shipped 2026-05-17.** Cross-cutting interaction primitives (RecordCombobox + MiniCreateModal + ClickPillCell + phone util) + the catch-up Team card on ProjectEdit. RecordCombobox is the Notion-style typeahead + mini-create that replaces every InlineAddSelect call site (lookup mode) and adds the record-mode pattern for FK pickers like ProjectEdit's Client + Team Account Managers + Designers. ClickPillCell wires inline status/priority/confidence edits into ProjectsList + TasksList + DeliverablesList + Outlook EntryPanel with optimistic save + rollback. Phone format fires on `onBlur` only (avoids cursor-jump while typing) and the one-shot data-fix migration normalized existing rows in `people.phone` + `vendors.contact_phone` + `clients.contact_phone`. Two carry-forward specs drafted in COWORK_SYNC for the next sub-phase: detail-page inline edit pattern (architectural call on whether the Edit form goes away entirely) + list-view inline cell editing with a chevron open-affordance (extends what ClickPillCell already proved). InlineAddSelect marked `@deprecated`; delete after one soak cycle. Spec: `OUTPUTS/phase-5-6-1-spec.md`. Next: **Phase 5.6.2 (next list/detail inline-edit spec, drafted in Cowork from the two COWORK_SYNC carry-forward sections)**.

**Predecessor phase:** **Phase 5.5.1 shipped 2026-05-16.** Sign-in page (Surface 01 part 1). Replaces the Phase 5.1 stealth coming-soon Landing (the one with the hidden "STRATEGY / DESIGN / PRODUCTION" trigger) with the wireframe Surface 01 sign-in page. Centered Montserrat ExtraBold "MIRROR NYC HQ" wordmark (HQ in coral) + visible 48px white Google Sign-In button with the 4-color G icon on a full-viewport dark background (`bg-background` token, not hardcoded `bg-black`). Local `signingIn` state flips the button label to "Signing in..." and `disabled` during the OAuth roundtrip to prevent double-clicks. Already-authed mounts redirect to `/home`. Hooks above any early return per design-system § 12.2. Pending state (Surface 01 part 2) was already shipped in Phase 5.1 via `PendingState.tsx` at `/pending`. White button color is intentional (the one HQ surface where coral does NOT win the primary CTA per spec § 6.5, since Google brand guidelines win on the sign-in button). Full-viewport bare layout breaks the standard `max-w-*` containment rule per spec § 6.6 (`screen--bare`). New files: `src/components/icons/GoogleColorIcon.tsx`. Modified: `src/pages/Landing.tsx` (rewrite). No migration, no edge function changes, no schema impact. Slug retention already complete pre-spec; grep + App.tsx audit confirmed no gap. Spec: `OUTPUTS/phase-5-5-1-signin-spec.md`. Next: **Phase 5.6 (smoke test + UX changes + Jimmie's notes per the Phase 5.5 spec § 0 footnote)**.

**Prior-prior phase:** **Phase 5.5 shipped 2026-05-16.** Notifications + Activity Feed + Search + States Polish. TopBar bell stub replaced by `<NotificationBellPanel />` (Radix Popover, 392px, coral badge with unread count + "9+" cap; Realtime `postgres_changes` INSERT/UPDATE on `notifications` filtered to `user_id=eq.{uid}` keeps the badge live and prepends new rows into the open popover; "Mark all read" + per-row optimistic mark-read with rollback toast on failure; click-to-navigate to `link_url`; footer tlinks to `/activity` + `/notifications/preferences`; empty state "You are all caught up. New notifications land here." matches wireframe Surface 23). `/notifications/preferences` (all tiers including Freelance via the outer ProtectedRoute group): 7 trigger rows x 2 channels (In-App / Slack DM), auto-save upsert on every toggle (no save button), Slack status footer reflects `users.slack_user_id` presence, system defaults fall through when no `user_notification_preferences` row exists for `(user_id, trigger_key)`. `/activity` (StandardOrAdminRoute, replaces ComingSoon): `<FilterBar />` chips for Record type / Person (lookup against users) / Date range (Today/Yesterday/Last 7 days/Last 30 days/This month), cursor-paginated "Load more" 30 at a time with `created_at < cursor`; day-bucket headers (Today / Yesterday / dated); per-row actor link (muted coral) + bold-coral record link + bold-foreground deliverable name (deliverables don't link, per build notes § 22) + relative-time grammar ("X hours ago" / "5:02 PM" / "N days ago"). `/search?q=` (StandardOrAdminRoute, replaces ComingSoon): cross-entity ilike against projects/tasks/deliverables/venues/vendors/clients/people/wiki_pages (10 per section, ranked starts-with > contains > alpha within each), URL `?q=` param updates via 300ms debounce on the page-level input + immediate update on TopBar submit. AdminRoute swapped silent /home redirect for in-place `<PermissionDenied>` with route-aware title (Outlook / Settings / Team management / Talent Scout / Wiki editor) + tier-specific description. WikiPage swept: 2 `.empty` inline divs to `<EmptyState>`, 2 `.empty` permission-denied divs to `<PermissionDenied>`. Three new shared state primitives live in `src/components/ui/`: `EmptyState`, `LoadError`, `PermissionDenied`. Dispatch infrastructure: new `notifications-dispatch` edge function as the central fan-out (security-tightened to `requireInternalSecret` only after the security audit caught that `requireInternalOrUserAuth` would let any signed-in user POST a crafted `recipient_user_ids` to spoof notifications to admins; documented in `docs/decisions.md` Phase 5.5); new `_shared/slackDm.ts` helper with mrkdwn `&` `<` `>` escaping per Slack docs; new `notifications_dispatch_writer` trigger on `tasks` (assignee_id newly set or changed -> `task_assigned`; status flips to `Blocked` -> `task_blocked`) + `projects` (status change -> `project_status_changed` with recipients = `project_account_managers`); reads `auth.uid()` for `actor_id` with `COALESCE(actor_uid, NEW.created_by)` fallback for task_assigned; `handle_new_user` rewritten to call `notifications-dispatch` with `event_type='user_pending'` while preserving inline durable per-admin notifications row writes (dispatch special-cases `user_pending` and skips the in-app insert to avoid duplicates, but still fires the Slack DM + the legacy email path). Three daily crons + edge functions: `hq-cron-deliverable-due-3d` (13:00 UTC / 09:00 ET; deliverables due in 3 days; recipients from `project_account_managers`), `hq-cron-task-due-today` (12:00 UTC / 08:00 ET; tasks due today; recipient = `assignee_id`), `hq-cron-event-date-today` (11:00 UTC / 07:00 ET; projects with `install_dates_start | live_dates_start | removal_dates_start = today`; recipients = union of `project_account_managers` + `project_designers`; `extra.kind` carries the event type for the body template). New files: 14 source files + 1 migration + 1 shared edge helper + 4 edge functions. Modified files: 13 (App.tsx, AdminRoute, TopBar, index.css, tailwind.config, types.ts, WikiPage, config.toml, 5 docs). CSS lift: `.notif` block lifted from wireframe lines 3266-3272 into `src/index.css` (`.activity-row` + `.actdot` already shipped in Project Detail's activity sidebar; reused as-is by the Activity Feed page). Tailwind safelist extended with `notif/notif--unread/activity-row/actdot`. Spec: `OUTPUTS/phase-5-5-spec.md`. Acknowledged spec deviations: no live screenshots embedded in the AWAITING block (the new `user_notification_preferences` table is required by the Preferences page on mount, the new `delivered_slack` column is required by NotificationRow, and the Realtime publication add is required for the bell badge to live-update; live DB stayed incompatible with the worktree until db push at the squash gate -- the § 4.1.a invasive-schema scenario; option 2 taken, leaning on code-reviewer + security-auditor + JSDoc wireframe binding + post-squash visual verification in Jimmie's Personal Chrome). security-auditor findings: 1 MUST FIX applied (`requireInternalSecret` instead of `requireInternalOrUserAuth` on dispatch) + 2 SHOULD FIX applied (Slack mrkdwn escape + cron error responses genericized) + 1 SHOULD FIX acknowledged in docstring (cron idempotency partial-unique-index deferred). code-reviewer findings: 1 MUST FIX applied (stale `Auth: requireInternalOrUserAuth` header comment on dispatch fixed) + 1 MUST FIX classified as false positive (`/notifications/preferences` is inside the outer ProtectedRoute group; comment clarified) + JSDoc wireframe binding extended on `NotificationBellPanel` to cite both panel + empty-state line ranges + other SHOULD FIXes deferred or covered. Carry-forwards: cron idempotency partial unique index on `(user_id, type, link_url, date(created_at))` if cron re-fires ever produce duplicate bell items in prod; `notify-admin-of-pending-user` fallback function stays deployed one phase (drop in 5.6 once dispatch is proven stable); @-mention triggers (`mention` event_type) handled by dispatch but no UI fires them yet (wire when mentions ship); email digest deferred (preferences UI says "email digest is a future phase"); DataTable's centralized `.empty` render deliberately left as-is (visual equivalent + zero behavior change vs. swapping for `<EmptyState>` across 7+ call sites). Next: **Phase 5.6 (smoke test + UX changes + Jimmie's notes per spec § 0 footnote)**.

**Prior-prior-prior phase:** **Phase 5.4 shipped 2026-05-16.** Wiki + Account Logins + Users + Settings surfaces replace the four ComingSoon stubs. `/wiki` + `/wiki/:slug` (ProtectedRoute all tiers including Freelance; admin Delete + Edit buttons on the read-view header; freelance hidden from `admin_only` + `no_freelance` pages via nav filter + component gate). `/wiki/new` + `/wiki/:slug/edit` (AdminRoute; TipTap WYSIWYG editor with Bold / Italic / Underline / H1-H3 / Paragraph / bullet+ordered lists / Link toolbar; storage is HTML via `dangerouslySetInnerHTML`; visibility dropdown Everyone / Hide from Freelance / Admin Only). `/wiki/account-logins` (special slug, page_type='account_logins'; component-gated, freelance blocked; CredentialRevealField with 30s idle re-mask via `useIdleTimer` hook; standard + admin can Add/Edit/Delete after round 2; Related column removed; Updated formats `MMM D, YYYY`). `/wiki/preferred-vendors` (special slug, page_type='vendors_glance' renamed in round 1; curated subset of vendors where `preferred = true`, grouped by `category_name`; admin "Manage Preferred List" dialog opens a multi-select picker with search + diff-based UPDATE). `/wiki/team-directory` (read-only active users grouped by department). `/users` (renamed from `/team` in round 1; admin list with inline tier dropdown via Popover + inline Active toggle + FilterBar default chip `Active is True`; `/team*` routes redirect to `/users*` so pre-feedback notification links + bookmarks still land). `/users/new` + `/users/:id/edit` (admin pre-provisioning form with full_name + email@mirrornyc.com + role_title + department via `<InlineAddSelect />` + tier + slack_handle + slack_user_id + read-only account status). `/settings` (AdminRoute; Admins card with picker modal + remove confirmation; grid g2 of Project Categories + Cities tag editors; Other Lookup Lists table with inline-expand editor per row for Venue Types / Vendor Capabilities / Vendor Categories / Departments; Integrations card display-only above collapsed-by-default Mirror Holidays CRUD editor). Cross-cutting: ProtectedRoute checks `useUserRole().isDeactivated` and signs out + renders "Account deactivated" full-screen when `users.active = false`; Calendar holidays rewired from hardcoded `MIRROR_HOLIDAYS` constant to `useMirrorHolidays()` hook reading `mirror_holidays` table (Settings CRUD editor stays in sync); `.cal-ev.hol` background flipped to warn-token amber/yellow with italic text. New components: WikiLayout / WikiNav / WikiPage / WikiPageEdit / WikiProseRenderer / WikiEditor / TeamDirectoryEmbed / VendorsGlanceEmbed (Preferred Vendors curation + ManagePreferredDialog) / CredentialRevealField / AccountLoginsPage / TeamList / TeamMemberEdit / SettingsPage / AdminsCard / LookupListEditor / MirrorHolidaysEditor. New hooks: useIdleTimer (30s idle re-mask). New libs: `src/lib/wiki/queries.ts`, `src/lib/team/{queries,relativeActive}.ts`. Phase 5.3 carry-forward cleaned up: `as never` casts on outlook_entries selects in `src/lib/calendar/queries.ts` removed. Three migrations applied to the linked DB at the squash gate. Branch had one feature commit `fa21daa`. Late regression caught + fixed pre-squash: WikiPage `useCallback` for the new Delete handler was placed after the loading/notFound early returns, causing "Rendered more hooks than during previous render" and unmounting the whole component (the "black wiki" Jimmie smoke-tested). Fix: dropped `useCallback` (single dialog onClick caller, no memoization needed). Spec: `OUTPUTS/phase-5-4-spec.md`. Acknowledged spec deviations: no live screenshots embedded in the AWAITING block (new wiki_pages + credentials + mirror_holidays tables are immediately required by the new surfaces; live DB stays incompatible with the worktree's frontend changes until migration push at the squash gate -- the § 4.1.a invasive-schema scenario; option 2 taken, leaning on code-reviewer + JSDoc + post-squash live verification in user's Personal Chrome). Three locked Jimmie answers at the design gate: (1) drop the users.id FK to auth.users + use id-swap pattern in handle_new_user; (2) drop department_tags + add department_id pointing at new departments lookup with custom seed values; (3) add admin INSERT policy on users. Three feedback-round Jimmie answers: (4) add Delete button to wiki read-view header (not just edit form); (5) widen credentials writes to admin+standard (not admin-only); (6) vendors.preferred boolean + inline picker on wiki page (not junction table). Carry-forwards: react-markdown dep unused (polish-pass prune); 2027 Mirror Holidays cliff (single seeded New Year's Day stub; admin can now add via Settings); Team list "Cards" view button rendered + disabled (deferred per spec § 0d); ID-swap pattern means hard delete in auth.users no longer cascades to public.users (manual cleanup function can land later if ever needed). Next: **Phase 5.5 (Notifications dispatch + Activity Feed + Search)**.

**Prior-prior-prior-prior phase:** **Phase 5.3 shipped 2026-05-16.** Unified Calendar surface (Surface 15) + admin-only Outlook surface (Surface 16). `/calendar` (StandardOrAdminRoute): pulls Install / Live / Removal date ranges from projects + due dates from deliverables + shared outlook entries (`shared_with_team = true`) + Mirror Holidays (hardcoded 2026 calendar + 2027 stubs in `src/lib/calendar/holidays.ts`); 232px right-rail visibility panel with master + per-project toggles persisted per user via the new `saved_views` row (`entity_type='calendar'`, `name='__calendar_default'`); Lead + Category filter chips (component-local); page-header nav cluster right-edges with the Category chip (248px inset to match the body grid). Click routing: Install/Live/Removal + Deliverable banners route to `/projects/{id}`; admin click on shared Outlook routes to `/outlook?year=YYYY&month=MM#entry=<uuid>`; non-admin shared-Outlook banner is non-clickable (cursor default, no hover); holiday banners no-op. `?source=projects|tasks` first-visit defaults Deliverables / Holidays / Outlook-shared toggles off; saved state wins on subsequent visits. `/outlook` (AdminRoute): 12-month grid with `Month / Week 1-4` columns; year tabs (current year + distinct years from data + Add Year confirm dialog); 277px side panel with detail / edit / new modes (inline Save / Cancel; dirty-state Discard dialog). Confidence-color mapping per locked-decisions § 4 (amber/cyan/green/gray for On Radar/Likely/Confirmed/Complete); the wireframe-canonical `.ol-*` CSS classes were lifted but the four color rules flipped in-place so class semantics match the locked mapping (documented in `docs/decisions.md` Phase 5.3). Promote/Unlink/Delete actions: Promote routes through `promote_outlook_to_project(target_entry_id)` SECURITY DEFINER RPC (pre-checks `is_admin()`, FOR UPDATE lookup, atomic projects INSERT + outlook_entries linkage UPDATE; raises P0001 on already-linked). Home admin `OutlookCondensedCard` rewired from "Outlook lands in Phase 5.3" filler to real `outlook_entries` data for the current year; click on a month cell routes to `/outlook?year=YYYY&month=MM`. Projects surfaces wired into the new schema: ProjectEdit gains Install Dates + Removal Dates date pairs in the Event Info card; ProjectDetail Dates kv splits into three rows (Install / Live / Removal) + header stat strip shows full ranges + "Days Until Install" tile prefers `install_dates_start` with fallback to `live_dates_start` for unbackfilled rows; ProjectsList Timeline view builds Install + Live + Removal bars with row filter widened to include projects with only install OR only removal dates set. Wireframe-canonical CSS lift block in `src/index.css`: `.olgrid`/`.ol-head`/`.ol-row`/`.ol-mn`/`.ol-cell`/`.ol-ev`/`.ol-rad`/`.ol-like`/`.ol-conf`/`.ol-comp`/`.ol-legend`/`.tabs` + new `.cal-ev.olk` + `.cal-ev.hol` modifiers. Tailwind safelist extended with `olk/hol/ol-ev/ol-rad/ol-like/ol-conf/ol-comp`. Cross-cutting components reused intact: `<CalendarMonthView />` extended with optional controlled-month props (`activeMonth`, `onActiveMonthChange`) + `hideInternalNav` flag + widened `CalendarEventKind` union (preserves the existing Deliverables Calendar view uncontrolled path); `<FilterBar />` not modified (CalendarPage uses lightweight inline FilterChip popovers for the simple Lead/Category 2-3-item pickers). New cross-cutting hook: `src/lib/hq/useClients.ts` (clients-only sibling of `useClientsAndVendors`; Outlook entries link to clients only, pre-Project). Spec: `OUTPUTS/phase-5-3-spec.md`. Acknowledged spec deviations: no live screenshots embedded in the AWAITING block (the new outlook_entries table + projects install/removal date columns are immediately required by the new surfaces; live DB stays incompatible with the worktree's frontend changes until migration push at the squash gate -- the very § 4.1.a invasive-schema scenario; option 2 taken, leaning on code-reviewer cold pass + JSDoc binding + post-squash eyeball); branch name auto-generated (cosmetic). Carry-forwards documented below. Next: **Phase 5.4 (Wiki + Account Logins + Settings + Team)**.
**Deployed at:** `https://hq.mirrornyc.com` (also `https://mirrornyc-hq.netlify.app`). Auto-deploys on push to main.

## What's live in production

- **Phase 5.6.1 Cross-cutting interaction primitives + Project Team card** (`f278da7`, shipped 2026-05-17): three primitives every later 5.6 sub-phase depends on. `RecordCombobox` is the Notion-style typeahead with inline "+ Add" (Radix Popover + cmdk; lookup mode binds option name + uses `useLookup`; record mode binds id + parent-supplied `loadOptions`; multi-select supported via removable chips); `MiniCreateModal` is the 2-4 field modal driving the "+ Add" path; `ClickPillCell` is the click-to-pick status pill (`hq-pill .hq-pill--<token>` trigger + popover of token-coloured option dots + optimistic save + rollback on error). Wired into every consumer the spec listed: ClickPillCell on Projects (status), Tasks (status + priority), Deliverables (status), Outlook EntryPanel (confidence); RecordCombobox replaced every InlineAddSelect call site (ProjectEdit Client + Category + City; VendorEdit Category + City; ClientEdit City; VenueEdit City; TeamMemberEdit Department) plus Vendor Capabilities + Venue Types custom multi-select chip patterns (deviations: spec assumed those were InlineAddSelect; reality was MultiTagInput + custom chip-builder). Phone util (`src/lib/hq/phone.ts`) added with `formatPhone` save-time-only on `onBlur` for PersonEdit + VendorEdit + ClientEdit; one-shot DB normalize migration (`20260517100000_phase_5_6_1_phone_normalization.sql`) ran at the squash gate to format existing rows in `people.phone` + `vendors.contact_phone` + `clients.contact_phone` where parseable as 10 US digits (idempotent; non-US/extension/garbage pass through unchanged; no schema change). projectStatusToken.ts gained `taskPriorityToken` + `outlookConfidenceToken` + `OUTLOOK_CONFIDENCE_VALUES`; tasks/queries gained `updateTaskPriority`; outlook/queries gained `updateOutlookConfidence`. Project Team card (`eba9c37`): catch-up to spec § 5.A; ProjectEdit § Team had been a placeholder paragraph since 5.2.1; now ships Account Managers + Designers as two multi RecordCombobox widgets in record mode sourced from `users` (active=true) with diff-on-save against `project_account_managers` + `project_designers` join rows (same pattern VenueEdit uses for `venue_venue_types`). Spec table mismatch flagged: § 5.A said `source.table: "people"` but join FKs target `users.id` (`people` is for external contacts; `users` is correct). InlineAddSelect now `@deprecated` (no callsites left); delete after one soak cycle. Two carry-forward specs drafted in `OUTPUTS/COWORK_SYNC.md` for the next sub-phase: (A) detail-page inline edit pattern — make record detail pages directly editable inline so the Edit form click goes away (architectural call on Sticky Save Bar replacement, per-field validation, RLS tightness); (B) list-view inline cell editing + chevron open-affordance — extend `DataTable` to take `onOpen` instead of `onRowClick` + every cell becomes a stop-propagation edit component (mechanical but wide-touching across Projects + Tasks + Deliverables; Outlook entries don't fit the DataTable shape and route through the side panel instead). Spec: `OUTPUTS/phase-5-6-1-spec.md`. Carry-forwards: see COWORK_SYNC's "Carry-forwards" section for the two drafted next-specs; the `MultiTagInput` component still ships for Tags fields (no lookup-table backing) and stays around for now; `InlineAddSelect` slated for delete-after-soak.

- **Mirror brand identity applied site-wide** (Phase 3.5b): coral `#BE4E44`, Montserrat ExtraBold display + Roboto Mono captions + Roboto body, dynamic `HQ` / `TALENT` / `VENUES` caption next to the M wordmark in the top nav based on which sub-app you're in. Brand spec lives in `docs/mirror-style-guide.md` with the deck-template authority (`BLANK DECK TEMPLATE (2026).pptx`).
- Stealth coming-soon landing at `/` for unauthenticated visitors. Hidden sign-in trigger is the bottom "STRATEGY / DESIGN / PRODUCTION" line (no visible affordance, default cursor, not keyboard-focusable).
- Authenticated users land on Dashboard. Top nav reduced to `Dashboard` + `Talent Scout` (admin) only. Projects / Venues / Clients / Tasks are reachable by drilling in from the Dashboard tile grid.
- Talent Scout is fully wired through Phase 3.7 for admin users:
  - Roles index, three-step new-role wizard, role settings (edit + close/reopen) with sticky save bar + re-eval-trigger confirm dialogs.
  - Pull pipeline (manual trigger; scheduled pulls land in Phase 3.8). Running-state UI shows a 4-step checklist (search → dedupe → process → save) live-updating via Realtime.
  - **Referral ingestion**: `ts-pull-candidates` detects forwards from `*@mirrornyc.com` to `jobs@mirrornyc.com`, unwraps the chain to find the original applicant. Handles standard Gmail forwards, Apple Mail desktop + mobile (`Begin forwarded message:` and `On <date> <Name> <<email>> wrote:` reply-quote attributions), and forward-as-attachment (`message/rfc822`). Captures any `@mirrornyc.com` manager's commentary along the chain into `ts_candidates.internal_notes` (Mirror signatures stripped) so it factors into the FIRST evaluation via the `HIRING MANAGER NOTES:` block in the candidate bundle. `mirrornyc.com` blocked from portfolio URL extraction so manager signatures don't leak as portfolio links.
  - RoleDashboard with Mirror-grey section cards, master pool table titled in-card ("MASTER POOL · from all rounds"), stat tiles, pull-round cards, two-tier candidate table with bulk actions, "View Final Review" button when one exists.
  - PullDetail: title leads, RX + status (Running / Failed) + Latest pills follow at matched height. Same shared CandidateTable + round-scoped re-eval (parallel fan-out, cancellable).
  - CandidateTable: Resume + Portfolio collapsed into one stacked text-button column (no header text), candidate column 220px, score bar proportional fill on `bg-input` track, status column elements (StatusDropdown + ReviewedPill + ReferralPill) at 12px text. ReferralPill is electric blue (Mirror coral was tried in 3.7.8.8 and reverted in 3.7.8.13 because the brand color blended into too many other coral surfaces).
  - CandidateDetail with header cluster simplified: Re-evaluate button (160px wide, full-height of left column) + right-column stack (auto/manual + referral pills on top, status dropdown on bottom). Tier pill removed (overlapped with status). Score Breakdown with vertical muted-grey tier dividers + tighter criterion rows. Internal Notes editor pre-populated when the candidate is a referral with manager commentary.
  - **Phase 3.6 final review** (`ts-final-review`): comparative AI ranking of the master pool, returns immediately, streams `step_progress` via Realtime, writes `final_rankings` (`{candidate_id, final_rank, final_tier, rationale, recruiter_note}` where `recruiter_note` is `string[]` max 3) to `ts_final_reviews`. FinalReviewDetail page renders the rankings table with rationale + bulleted recruiter note.
  - **Global Settings** (admin only) under `/talent-scout/settings`: global competitor list edit (Postgres `text[]`, seeded with Mirror's 19-entry default), default applicable to all roles unless overridden per-role. Phase 3.8 added Anthropic Monthly Spend Cap input + inline current-spend display (coral $X.XX).
  - All three Talent Scout Claude prompts consolidated in `supabase/functions/_shared/prompts.ts`. Frontend mirror at `src/lib/talent-scout/defaultEvalPrompt.ts` (manually synced; verified byte-for-byte at 3.7.7.6).
  - Toasts site-wide use solid Mirror coral with white text (Sonner + Radix Toast both default to coral).
- **Phase 3.8 + 3.9 cron + watchdog infrastructure committed** (squash-merge `e855ffb`): six new edge functions (`ts-cron-scheduled-pulls`, `ts-cron-pull-watchdog`, `ts-cron-reeval-watchdog`, `ts-cron-final-review-watchdog`, `ts-cron-storage-cleanup`, `ts-cron-monthly-spend-reset`), `ts-send-pull-notification` for pull-completion email, real cap-alert email path via `_shared/sendEmail.ts`, two migrations (extensions + schedules + dead-column drop). Watchdogs detect-and-flag only (pull 60min stall, re-eval 30min, final-review 20min). Production wiring still pending; see § Carried-forward cleanup below.
- **Phase 3.10 scorecard refinement** (squash-merge `b70f0e9`): new `ts-refine-scorecard` edge function. The wizard step-3 page and Edit Role page both surface a Process / Save button morph: when scorecard has unrefined edits, the action runs the refine pass; once clean, it flips back to Lock (wizard) or Save (Edit Role, which triggers bulk re-eval). Server-side dead-criterion drop (weight=0 OR empty name+describer) plus defense-in-depth merge that restores `tier`/`weight`/`is_disqualifier`/`is_manual` from user input regardless of model output.
- **Phase 3.11 scorecard substance restoration** (squash-merge `cb038fc`): reverted Phase 3.7's over-aggressive 12-word cap on `full_points_rubric`. Each criterion now carries TWO describer fields: `full_points_rubric` (substantive 1-3 sentences, what the per-candidate evaluator reads) and `summary` (≤ 14-word recap for compact UI surfaces). Both generated in same Claude pass. Existing roles upgrade by re-running scorecard generation OR clicking "Process scorecard" on wizard / Edit Role (Phase 3.10).
- **TS Final Review packet** (`6775429` + `7cd27ed`): Generate Packet button is live on the Final Review detail page (PACKET_FEATURE_ENABLED gate removed; button always packets every ranked candidate via `include_all: true`). Hiring-manager email uses subject `[Mirror HQ] Final Review Packet | <Role>`, multipart/alternative MIME with a "Download Final Review packet" hyperlink (no raw URL wall). Cover page eyebrow gap bumped to 48pt; candidate title page rank gap bumped to 50pt (no overlapping text). When a candidate has no cover_letter attachment but has `email_body_text`, the application email body is rendered as a "Cover Letter Email" content page in the cover-letter slot ahead of the resume. Real Mirror M PNG used as cover wordmark (was a Helvetica-M approximation).
- **Venue Scout (full 1:1 port from `mirror-nyc-venue-scout-pro`)**, shipped via the 2026-05-13 cutover. Producer flow: New Scout (project pick) -> Brief upload (DOCX + AI parse via `vs-parse-brief` v16) -> Sheet Prompt -> Sheet Upload (CSV/XLSX + AI enrichment via `vs-parse-sheet` v18) OR Researching (web search via `vs-research-venues` v25, `claude-sonnet-4-6 + web_search_20250305` with `pause_turn` continuation; Phase B `max_tokens`=12000 since 2026-05-14, fixes no-sheet `tool_use` truncation) -> Sourcing Report (matrix) -> Shortlist (matrix + photo upload) -> Review (matrix + Notes/Feedback editor) -> Compiling (per-venue 4-bullet summaries via `vs-compile-summaries` v29) -> Deck Prep (matrix + drag-drop photo reordering + edit-everything) -> Generating (Google Slides deck via `vs-generate-deck` v13) -> deck opens in new tab + producer returns to Deck Prep. Settings reachable via persistent gear icon on every action page; Settings Danger Zone exposes a `start_over_scout` RPC. Full ErrorState.tsx covers every failure path. AI surface uses `pause_turn` continuation, `writeFailure` CAS guards on all 3 AI edge functions, `WORK_TIMEOUT_MS=360s` for the Supabase Pro plan, URL-fallback helpers (`extractWebSearchResults`, `findVenueWebsite`, `findBestSearchResultUrl`) when models leave `website_url` null. Storage buckets: `briefs`, `sourcing_sheets`, `vs_venue_photos` (private; signed-URL reads). Schema: `vs_scouts` (brief-inline shape with `current_step` state machine + `generated_decks` jsonb history + `pipeline_error` channel), `vs_candidate_venues` (single-round per scout, `source` enum among research/sheet/manual, `derived_attrs` jsonb), `vs_venue_photos` (ON DELETE CASCADE). RLS open to authenticated; `vs_scouts` in `supabase_realtime` publication with REPLICA IDENTITY FULL.
- **Phase 4 Revision - Intake** (`20e17ec`, shipped 2026-05-14): the Venue Scout Brief is now a 3-step stepper. `New Scout -> /brief` redirect index dispatches to `/brief/event` (Step 1: brief PDF upload + event details), `/brief/venue` (Step 2: venue-side fields -- target neighborhoods + strict toggle, venue-type chips, square-footage sliders, ideal features, priority toggles; `city` is now required), `/brief/report` (Step 3 + permanent Brief surface: inline-editable cards, hero band, Generated Decks section, Event Overview generated by `vs-generate-brief-overview` -- trigger model revised in pass 3, see below). New `current_step` value `brief` (migration `20260514110000`, also flips the new-row default to `brief`). The Revisit nav (`ScoutStepThroughNav`) is always-visible now with active/reached/unreached chip states. `vs-parse-brief` parses the new fields; `vs-research-venues` Phase B injects them into the sourcing prompt. All 16 new fields persist to `vs_scouts.brief_data` jsonb (no column migration). Spec: `OUTPUTS/phase-4-revision-intake-spec.md`; decisions in `docs/decisions.md` § Phase 4 Revision.
- **Phase 4 Revision pass 2** (`970c881`, shipped 2026-05-14): three corrections to pass 1. `vs-parse-brief` (deployed v16) constrains `objectives` to short tag-style phrases via the tool schema description + a dedicated system-prompt rule + a `sanitizeObjectives` post-parse pass, so the Brief `TagInput` gets clean tags instead of narrative paragraphs. The `ScoutStepThroughNav` chip strip: the Brief chip now routes into the intake stepper at `/brief/event` ("edit brief" mode), a new Overview chip pinned last routes to the canonical Brief report at `/brief/report` (reached once `current_step >= sheet_prompt`), the trailing Generated Deck link is gone, and active-chip resolution is route-based (`useLocation` prefix match) instead of step-based. `BriefReport.tsx` no longer renders the `Stepper` (it belongs to the intake walkthrough Steps 1-2 only). Spec: `OUTPUTS/phase-4-revision-2-spec.md`; decisions in `docs/decisions.md` § Phase 4 Revision pass 2.
- **Phase 4 Revision pass 3** (`bb58053`, shipped 2026-05-14): the Event Overview generation trigger moved from report-entry auto-fire to the Submit Brief click in `BriefVenue.tsx`. Submit Brief persists the form, then invokes `vs-generate-brief-overview` only when the overview is empty, has no stored hash, or `brief_data.overview_source_hash` no longer matches a freshly-computed hash of the 15 overview-driving brief fields (`computeOverviewSourceHash`: a 16-char SHA-256 prefix, arrays sorted, empty-to-null normalized, computed identically in `src/lib/venue-scout/briefForm.ts` and server-side). Two-stage spinner ("Submitting..." -> "Generating overview...") when a regen runs, single-stage when skipped. `vs-generate-brief-overview` deployed **v2**: atomic single-UPDATE write of `event_overview` + `brief_data.overview_source_hash`, response returns both. `BriefReport.tsx` dropped the auto-fire `useEffect`; its `OverviewBlock` renders an empty-state "Generate overview" button when `event_overview` is empty, and the Regenerate link still re-invokes unconditionally. No migration (the `brief_data` jsonb is open); pre-pass-3 scouts carry no hash and regenerate once on their first post-pass-3 Submit Brief. Spec: `OUTPUTS/phase-4-revision-3-spec.md`; decisions in `docs/decisions.md` § Phase 4 Revision pass 3.
- **Phase 5.4 Wiki + Account Logins + Users + Settings** (`f4e4aab`, shipped 2026-05-16): four new tables (`departments` lookup, `wiki_pages` with 4-value `page_type` enum + 3-value `visibility` enum, `credentials` plaintext-at-rest behind RLS, `mirror_holidays` replacing the Phase 5.3 hardcoded constant). `public.users` reshape: dropped FK to `auth.users(id)` to enable admin pre-provisioning via id-swap pattern in `handle_new_user` (Option A locked Q1); dropped `department_tags text[]` + replaced with `department_id uuid FK` into the new `departments` lookup (seeded with Leadership / Accounts / Creative / Design / Event Production per locked Q2); added `role_title` + `slack_handle` + `slack_user_id` + `last_active_at` columns. New admin INSERT RLS policy on users (locked Q3) + GRANT INSERT. GRANT cleanup on `cities` + `project_categories` DELETE (matches Phase 5.2 cleanup posture for `vendor_capabilities`). New surfaces: `/wiki` + `/wiki/:slug` (ProtectedRoute all tiers including Freelance; admin Edit + Delete buttons on read-view header; nav filters `admin_only` from non-admins + `no_freelance` from freelance), `/wiki/new` + `/wiki/:slug/edit` (AdminRoute; TipTap WYSIWYG editor with Bold / Italic / Underline / H1-H3 / Paragraph / bullet+ordered lists / Link; storage as HTML via `dangerouslySetInnerHTML`; visibility dropdown Everyone / Hide from Freelance / Admin Only), `/wiki/account-logins` (special page; component-gated, freelance blocked; CredentialRevealField with 30s idle re-mask via new `useIdleTimer` hook; standard + admin can Add/Edit/Delete after round 2), `/wiki/preferred-vendors` (curated subset where `vendors.preferred = true`, grouped by category; admin "Manage Preferred List" dialog with multi-select picker + search + diff-based UPDATE), `/wiki/team-directory` (read-only active users grouped by department), `/users` + `/users/new` + `/users/:id/edit` (renamed from `/team`; admin list with inline tier dropdown via Popover + Active toggle + filter chips; `/team*` URLs redirect to `/users*`), `/settings` (Admins card with picker + remove confirmation; Project Categories + Cities tag editors; Other Lookup Lists table with inline-expand editor per row for Venue Types / Vendor Capabilities / Vendor Categories / Departments; Integrations card display-only above collapsed-by-default Mirror Holidays CRUD editor). Cross-cutting: ProtectedRoute checks `useUserRole().isDeactivated` + signs out + shows "Account deactivated" full-screen when `users.active = false`; Calendar holidays rewired from `MIRROR_HOLIDAYS` constant to `useMirrorHolidays()` hook reading `mirror_holidays` table; `.cal-ev.hol` background flipped to warn-token amber/yellow (italic); 11 seeded wiki pages + 18 seeded mirror holidays migrated in. Carry-forward cleanup: `as never` casts on outlook_entries selects in `src/lib/calendar/queries.ts` removed (Phase 5.3 cleanup item). Phase ran three migrations applied at squash gate; types regen wrote the new tables + reshape into `src/integrations/supabase/types.ts`. New deps: TipTap (`@tiptap/react` + starter-kit + extension-underline + extension-link); `react-markdown` added in round 1 then unused after TipTap switch (polish-pass prune). Two feedback rounds applied between spec implementation and squash: round 1 ([20260516170000](supabase/migrations/20260516170000_phase_5_4_feedback.sql) — TipTap rich-text editor; admin_only visibility; vendors-at-a-glance -> preferred-vendors slug+title; credentials.related_note dropped; handle_new_user link_url updated /team -> /users; all 8 seeded prose pages converted from markdown to HTML); round 2 ([20260516180000](supabase/migrations/20260516180000_phase_5_4_feedback_round_2.sql) — credentials INS/UPD/DEL widened to admin+standard; vendors.preferred boolean + partial index). Late regression caught + fixed pre-squash: WikiPage `useCallback` for the new Delete handler was placed after the loading/notFound early returns, causing "Rendered more hooks than during previous render" + unmounting the whole component (the "black wiki" Jimmie smoke-tested in Personal Chrome); fix was dropping `useCallback` (single dialog onClick caller, no memoization needed). Spec: `OUTPUTS/phase-5-4-spec.md`. Decisions: `docs/decisions.md` Phase 5.4 (id-swap pattern; department_tags drop; credentials plaintext rationale; page_type enum; freelance wiki access except Account Logins; mirror_holidays table replaces constant; Integrations card display-only; Team Cards view deferred; GRANT fixes carried) + Phase 5.4 feedback round (TipTap switch; admin_only visibility; related_note drop; vendors-at-a-glance rename; team -> users rename; Settings reorder; calendar yellow) + Phase 5.4 feedback round 2 (Delete on read-view; credentials writes for standard; vendors.preferred + Manage picker). Acknowledged spec deviations: no live screenshots in the AWAITING block (new wiki_pages + credentials + mirror_holidays tables are immediately required by new surfaces; live DB stays incompatible until migration push at squash gate -- § 4.1.a invasive-schema scenario; option 2 taken, post-squash live verification done in Personal Chrome); branch name auto-generated `claude/zen-mestorf-940d7f` (cosmetic, same pattern as 5.3). Carry-forwards: react-markdown unused dep prune; 2027 Mirror Holidays cliff (admin can add via Settings now); Team list "Cards" view button rendered + disabled.

- **Phase 5.3 Calendar + Outlook** (`1c21ea9`, shipped 2026-05-16): unified Calendar surface (Surface 15, StandardOrAdminRoute) + admin-only Outlook surface (Surface 16, AdminRoute) replace the ComingSoon stubs at `/calendar` and `/outlook`. New `outlook_entries` table (admin-CRUD via RLS; standard/freelance SELECT only on `shared_with_team = true`) + `outlook_confidence` enum + `promote_outlook_to_project(uuid)` SECURITY DEFINER RPC. Four new date columns on `projects` (install_dates_start/end, removal_dates_start/end) drive Calendar Install + Removal banners and the Projects Timeline view's Install + Removal bars. `saved_views.entity_type` CHECK widened to include `'calendar'` so per-user Calendar visibility toggles (Deliverables / Mirror Holidays / Shared Outlook / per-project) persist via one implicit row per user (`name='__calendar_default'`). Mirror Holidays seeded as a hardcoded constant (`src/lib/calendar/holidays.ts`) sourced from the 2026 Mirror Holiday Calendar PDF with the Christmas / New Year window expanded to one entry per non-weekend closed day; Settings CRUD editor lands in 5.4. Confidence-color mapping per locked-decisions § 4 (amber/cyan/green/gray for On Radar/Likely/Confirmed/Complete); wireframe `.ol-*` CSS classes lifted but color rules flipped in-place so class semantics match the locked mapping (documented in `docs/decisions.md` Phase 5.3). Home admin OutlookCondensedCard rewired to real data. ProjectEdit gains Install + Removal date pairs; ProjectDetail Dates kv splits into three rows (Install / Live / Removal); ProjectsList Timeline view renders all three bars. New `<CalendarVisibilityPanel />` + `<OutlookYearGrid />` + `<OutlookEntryPanel />` components. New `useClients` hook (clients-only sibling of `useClientsAndVendors`; Outlook entries link to clients only, pre-Project). `<CalendarMonthView />` extended with optional controlled-month props + `hideInternalNav` flag + widened `CalendarEventKind` union; existing Deliverables Calendar view continues to use the uncontrolled path. Wireframe-canonical CSS lift in `src/index.css` (`.olgrid` / `.ol-head` / `.ol-row` / `.ol-mn` / `.ol-cell` / `.ol-ev` / `.ol-rad` / `.ol-like` / `.ol-conf` / `.ol-comp` / `.ol-legend` / `.tabs` + `.cal-ev.olk` / `.cal-ev.hol`); Tailwind safelist extended with the new dynamic-token classes. CalendarPage page-header nav cluster (prev/next/month/Today) right-edges with the Category filter chip below via 248px right inset. Three migrations applied to the linked DB at the squash gate. Code-reviewer cold pass caught 1 MUST FIX (timezone off-by-one in `isoRange`: date-only DB strings parsed as UTC midnight shifted every Install/Live/Removal bar one day early in NYC; fixed via `parseDateOnly` helper) + 4 SHOULD FIX (useCalendarVisibility unmount cleanup, dead `.cal-ev.olk`/`.cal-ev.hol` CSS classes wired up by widening `CalendarEventKind` union, explicit ON DELETE RESTRICT on `outlook_entries.created_by`, cleaner ERRCODE P0001 on the RPC's already-linked refusal) all applied in `62004a2` before the squash. Migration-reviewer: zero MUST FIX; 2 SHOULD FIX folded into the fix-pass. types regen produced an 85-line delta vs the hand-crafted version. Spec: `OUTPUTS/phase-5-3-spec.md`. Acknowledged spec deviations: no live screenshots embedded in the AWAITING block (new outlook_entries table + projects install/removal date columns are immediately required by the new surfaces; live DB stays incompatible until migration push at the squash gate -- the very § 4.1.a invasive-schema scenario; option 2 taken, leaning on code-reviewer + JSDoc + post-squash eyeball); branch name auto-generated (cosmetic; squash to main is what ships). Carry-forwards: `<CalendarMonthView />` now has two render paths (controlled vs uncontrolled; not load-bearing); CalendarPage FilterChip popovers don't close on outside click (observation Frontend #5; promote to shadcn `<Popover>` later); `<EntryPanel />` fires `useClients` + `useLookup("cities")` queries even in detail mode (observation deferred, admin-only + cheap); `loadDistinctOutlookYears` selects all rows (deferred, small table); `ProjectDetail` "Days Until Install" preference flip (observation Frontend #7; document for producers); concurrent first-visit `saved_views` race (deferred, low impact); 2027 Mirror Holidays cliff (single New Year's Day stub; extend in 5.4 Settings editor). Branch had 3 worktree commits + 1 types-regen commit before squash.

- **Phase 5.2 cleanup** (`65159a2`, shipped 2026-05-16): six small carry-forwards from the 5.2.x rounds bundled into one branch. (1) People List Affiliation column header relabeled to **Organization** to match wireframe Surface 11 line 2019; cell extends for the venue-contact case (single venue name or "{N} venues" plain `.cap` text); sort comparator mirrors what the cell renders. (2) PersonDetail kv label flipped "Affiliation" -> "Organization". (3) New `src/lib/hq/useClientsAndVendors.ts` composite-lookup hook merges clients + vendors into a single `ClientOrVendorOption[]` (name + sublabel `"Client"` / `"Vendor"`). (4) `FilterBar` gains `"lookup"` field type with ops `["is", "is not"]`; chip stores the picked uuid as `value`; new `chipDisplayValue()` helper resolves uuid -> name via `field.lookupOptions` for display. PeopleList Organization filter chip uses the new type. Row decoration adds `organization_id = client_id ?? vendor_id` (mutex CHECK guarantees only one is set); the engine's existing `is` op handles the match natively. (5) New `IconClients` briefcase glyph in `HQIcons`; `LeftRail` Clients item swaps from `IconOrgs` to `IconClients` (Vendors keeps `IconOrgs` for the office-building operations-side semantic). (6) `vendors.primary_address` column added (text, nullable; matches clients shape); VendorEdit Primary Address textarea added below the contact grid in the Primary Contact card; VendorDetail Primary Address row added to the Details kv above City. Matched-pair textarea-class fix applied: ClientEdit + VendorEdit both swap `className="input"` -> `className="input textarea"` so `.input{height:44px}` stops overriding `rows={2}`. (7) `vendor_capabilities` GRANT extended to include DELETE for `authenticated`, making the admin-only DELETE RLS policy reachable after the 5.2.3.A `org_capabilities` -> `vendor_capabilities` rename. Workflow doc amendments to `docs/working-with-claude.md`: § 4.5 step 5 split into 5/5b/5c/5d/5e codifying the manual `git push origin main` hand-off pattern (the auto-mode classifier blocks autonomous push at the Bash-tool layer regardless of chat approval; 5.2.3 round confirmed empirically; Code now runs everything autonomously through 5d, surfaces a "Ready to push" message at 5e for Jimmie to run from terminal); new § 4.1.a Carve-out for invasive-schema sub-phases (local Supabase via `supabase start` OR skip live screenshots + rely on code-reviewer + JSDoc wireframe-binding + post-squash eyeball; default to (2); 5.2.3 cited as working example). schema.md: drift callout added to the organizations section + delta bullets for the new column + GRANT (full 5.2.3 structural rewrite carried forward as Docs observation #1). One additive migration (`20260516140000_phase_5_2_cleanup_primary_address_and_vendor_capabilities_grant.sql`) applied to the linked DB at the squash gate. types regen produced zero delta vs. the hand-crafted version. Item D (Projects Touched PostgREST `.contains()` pushdown) deferred per spec § 0d as premature perf optimization. Code-reviewer cold pass + migration-reviewer (parallel runs): 1 MUST FIX (schema.md) + 2 SHOULD FIX (textarea class + sort comparator) applied; 4 CONSIDER items logged to `code-observations.md` (Frontend #2: useClientsAndVendors single-shot load; Frontend #3: FilterBar chipDisplayValue raw-uuid flash race; Docs #1: schema.md larger 5.2.3 split rewrite; Docs #2: § 4.5 stale-sync risk if Jimmie aborts at 5e). First sub-phase to exercise the very § 4.1.a screenshot carve-out it introduces: no live screenshots in the AWAITING block (vendors.primary_address column is immediately required by VendorDetail + VendorEdit queries; live DB stays incompatible until migration push at the squash gate). Composite-lookup filter scope expanded beyond spec § 0c's "no edits to data/*" with AskUserQuestion confirmation. Spec: `OUTPUTS/phase-5-2-cleanup-spec.md`.
- **Phase 5.2.3 clients + vendors split** (`59c81ba`, shipped 2026-05-16): the unified `organizations` table from 5.2.2 splits into two purpose-built tables. `vendors` (renamed from organizations) drops the `type` enum + `org_capabilities` (renamed to `vendor_capabilities`) and gains a `category_id` FK pointing at the new `vendor_categories` lookup table; Internal partners collapse into vendors as the literal `'Internal Partner'` string in tags[] (locked Q1). `clients` rebuilt fresh with a slim shape (name + industry + contact + city + address + tags + website). `people` reshaped: `organization_id` splits into nullable `client_id` + nullable `vendor_id` with DB mutex CHECK (locked Q4: at most one org type); `affiliations[]` array + `person_affiliation` enum dropped. `notes_log` parent_type CHECK widened to `('client', 'vendor', 'person', 'venue')` with row redistribution. `venues.exclusive_vendors_org_ids` renamed to `exclusive_vendor_ids`. 5 migrations apply in dependency order (5.2.3.A through .E); 5.2.3.C took a hotfix at the squash gate (projects FK already named `projects_client_id_fkey`; people.client_id FK ADD failed on Priya Nair's vendor-pointing row; reordered so redistribute UPDATE runs before FK ADDs). 6 new page files replace the 3 Org pages: `src/pages/clients/{ClientsList,ClientDetail,ClientEdit}.tsx` + `src/pages/vendors/{VendorsList,VendorDetail,VendorEdit}.tsx`; each carries a JSDoc DEVIATION wireframe-binding comment per design-system § 11 (the Phase 5 wireframe Surface 10 was drawn as one unified Organizations surface; wireframe-v2 redraw deferred to a polish pass). People surfaces modified: PeopleList Type column resolved from FKs (Client / Vendor / Venue contact / Unaffiliated; folds venue_contact_people join client-side via Promise.all); PersonDetail Type kv + Affiliation tlink resolved the same; PersonEdit's affiliations multi-chip replaced by a single Type radio (Client picker / Vendor picker / Venue contact venue-multi-select / Unaffiliated). LeftRail order flipped to `Home / Projects / Tasks / Deliverables / Calendar / Venues / Vendors / People / Clients / Activity Feed / Search` per locked Q2. App.tsx: 8 new /clients + /vendors routes; 4 backward-compat /organizations URL redirects via `OrganizationsRedirect` (looks up id in clients then vendors, 301-replaces). Cross-cutting grep flip across every shipped consumer (home cards, lib/projects + lib/tasks + lib/deliverables queries, projects/tasks/deliverables/venues pages, ViewSwitch Surface union, useLookup table union). Reused intact: `<InternalNotesEditor />` (parentType widened to `'client' | 'vendor' | 'person' | 'venue'`), `<StarRating />`, `<InlineAddSelect />`, `<MultiTagInput />`, `useLookup` (LookupTable union added `vendor_capabilities` + `vendor_categories`; dropped `org_capabilities`). types.ts regenerated from the post-push linked DB. Code-reviewer cold pass caught + applied 2 MUST FIX (vendor_categories DELETE GRANT, notes_log migration header comment) + 1 SHOULD FIX (PersonDetail Promise.all parallelization). First sub-phase to execute the autonomous ship flow per `docs/working-with-claude.md` § 4.5. Spec: `OUTPUTS/phase-5-2-3-spec.md`. Carry-forwards: distinct rail icon for Clients (currently shares IconOrgs with Vendors); `vendors.primary_address` column + VendorEdit field (spec § 4.B.3 gap); `vendor_capabilities` GRANT missing DELETE (renamed from org_capabilities; same dead-policy posture as pre-fix vendor_categories was; widen alongside Settings page CRUD in 5.4); VendorDetail "Projects Touched" client-side filter (switch to PostgREST `.contains(...)` when scale warrants); drop `/organizations/*` backward-compat redirect once Mirror bookmarks update.
- **Phase 5.2.2 entity-trio frontend** (`0356a85`, shipped 2026-05-16): Organizations + People + Venues surfaces come online with list + detail + edit (9 page files; wireframe-bound via JSDoc to Surfaces 09 / 10 / 11). Four small lookup-table migrations (`cities`, `project_categories`, `org_capabilities`, `venue_contact_people`) + an `activity_log_writer_status_guard` migration that fixes a shipped-trigger crash on UPDATE to status-less tables (CREATE OR REPLACE with nested-IF + `to_jsonb()->>'status'` defense-in-depth). Two cross-cutting components: `<InternalNotesEditor parentType parentId />` (polymorphic across organization | person | venue; reads + writes `notes_log` with append-only semantics + author/admin delete; uses `users!notes_log_author_id_fkey` defensive embed to avoid the PGRST201 multi-FK trap) and `<StarRating value editable size onChange />`. Two helper components (`<InlineAddSelect />` + `<MultiTagInput />`) and one hook (`useLookup`). Organizations List uses Type filter pill row above the toolbar (All / Clients / Vendors / Internal); Vendor + Internal types surface Capabilities + Star Rating columns + Internal Rating sidebar card on detail (all-authed write per locked Q4 of the 5.2.2 spec § 12). People List shows multi-affiliation pills + muted-coral Organization tlink + `mailto:` Email links with the design-system § 12.4 truncate pattern. Venues List shows multi-pill venue types via Venue Scout `TYPE_STYLES` lift + per-row `.btn-coral` Website button. Venue Detail Rates render as "Event Day Rate $X as of <date>" + "Prod Day Rate $Y as of <date>" pulled from `venue_rate_history` newest-per-`(venue_id, rate_kind)`; Edit form "Log new rate" mini-dialog INSERTs append-only history rows (no UPDATE; rates are immutable). Inline-add affordance on City / Project Category / Capability / Venue Type dropdowns INSERTs to the corresponding lookup table on save (any authed user; admin-only DELETE so a misclick doesn't blow away a city referenced by 30 projects). App.tsx route flips: `/organizations`, `/people`, `/venues` (+ sub-routes) off `ComingSoon` stubs. types.ts regenerated from the linked DB at the squash gate. First sub-phase under the new pre-squash discipline (`docs/working-with-claude.md` § 4): migrations stayed LOCAL during implementation; `supabase db push --linked` ran at the squash gate. The Internal Rating column + Type enum survive to 5.2.3 (organizations table renames to vendors; type column drops). Carry-forward items for 5.2.3 documented below. Spec: `OUTPUTS/phase-5-2-2-spec.md`.
- **Phase 5.2.1 Revision: wireframe-fidelity rebuild of Projects + Tasks + Deliverables surfaces** (`62f610e`, shipped 2026-05-16): forward-fix pass on top of the original 5.2.1 squash. Schema + routes + queries from `15511af` stay intact; the visual layer for the seven Phase 5.2.1 surfaces is rebuilt against the locked Phase 5 wireframe (`OUTPUTS/phase-5-hq-wireframe-v1-LOCKED.html`) byte-for-byte. New CSS lift block in `src/index.css` (UNPREFIXED wireframe-canonical classes: `.viewswitch`, `.filterbar > .fchip > .k/.op/.v/.x`, `.tbl[.tbl--flat]`, `.rb-<token>`, `.bcol`, `.board / .board-stack`, `.tl / .tl-bar`, `.calgrid / .cal-ev`, `.kv`, `.savebar`, `.stat`, `.pill p-<token>`, `.input / .field`, `.btn / .btn-primary / ...`, `.av-i`, `.tag`, `.empty`, `.bulkbar`; coexists with the 5.1 `hq-` prefixed chrome). HQIcons gains `IconList / Board / Timeline / Filter / X / ChevronDown / ArrowLeft / Drive / Slack / Slides / Link`. Seven data components rewritten to the wireframe DOM: `<ViewSwitch />` (icon-segmented), `<FilterBar />` (chip builder + new `hidden?: boolean` field-def flag), `<SavedViewsDropdown />` (new `onNavigate` prop that routes to the picked view's variant — closes original-5.2.1 code-reviewer C1), `<DataTable />` (new `flat?` prop strips colored row left-border for top-level list pages), `<BoardView />` (new `layout: "horizontal" | "stacked"` prop), `<TimelineView />`, `<CalendarMonthView />`. `<StickySaveBar />` re-rendered as `.savebar`. ProjectsList / ProjectDetail / ProjectEdit / TasksList / TaskDetail / TaskEdit / DeliverablesList / DeliverableDetail / DeliverableEdit re-orchestrated. Deliverables Board flipped to one-column-per-project per build notes Surface 14 (closes original-5.2.1 code-reviewer C2). Step 0 DB reconciliation bundled in: the four 5.2.2 .sql files (organizations rename + people + venues extensions + projects extensions) recreated locally + registered via `supabase migration repair --status applied` (they were already applied to the live linked DB during a halted 2026-05-15 worktree attempt; this is local-only DDL bookkeeping). Every shipped `clients` / `client_id` / `client:clients` reference flipped to `organizations` / `organization_id` / `organization:organizations`; `projects.notes` reads flipped to `status_notes`; `client_notes` body added to ProjectDetail + ProjectEdit. `/clients` ComingSoon stub replaced with `Navigate to /organizations` redirect. `applyFilters` gains an `ApplyFiltersContext { meUserId }` arg with a `resolveMeValue` helper that substitutes any chip value whose lowercased text equals `"me"` with the caller-provided user id at compare time (falls back to literal `"me"` when meUserId is null so session-expired state matches nothing). Tasks default filter shipped as `{ field: 'assigneeId', op: 'is', value: 'Me' }` consuming that context. PostgREST FK-name embed (`assignee:users!tasks_assignee_id_fkey(...)`) flipped in 4 sites to disambiguate the multi-FK `tasks → users` relationship (`tasks.assignee_id` + `tasks.created_by`). Tailwind safelist added for all 20 dynamic-token classes constructed via template literals; verified present in `dist/assets/index-*.css`. MyProjectsCard reads `projects.job_number` directly via regenerated types + rows route on full-row `onClick` mirroring AllActiveProjectsCard. Deliverables Calendar legend gains the 4th status (Skipped, gray + line-through). `docs/schema.md` documents the 5.2.2 schema entries (organizations rename + type enum + columns, projects 5.2.2 extensions + status_notes rename + client_notes add, venues extensions, venue_venue_types, venue_rate_history, people table, notes_log CHECK widening). `docs/decisions.md` gains two new blocks: "Phase 5.2.1 Revision" + "Phase 5.2.2" (the 5.2.2 schema decisions for locked Q3 + recommended Q8 / Q9 / Q10). Spec: `OUTPUTS/phase-5-2-1-revision-spec.md`.
- **Phase 5.2.1 HQ Core databases: project workflow trio + cross-cutting components + rail amendment** (`15511af`, shipped 2026-05-15): the canonical HQ Core database-list pattern (List / Board / Timeline / Calendar with per-surface subsets) lands across Projects, Tasks, Deliverables. Four migrations: `project_status` reshape to the locked 14 values (drops `Awaiting FB` / `Awaiting Files` / `Awaiting Approval` / `Event Live` / `Proof Out` / `In Review`; adds `Approved` / `Install` / `Removal` / `Queued` / `Awaiting Feedback` / `Cancelled`; renames `Awaiting FB` -> `Awaiting Feedback`) + `task_status` reshape to mixed-case (`To Do` / `Doing` / `Blocked` / `Done`) + `tasks_completed_at_set` CREATE OR REPLACE to compare against the new `Done` literal + `tasks` / `projects` added to `supabase_realtime` with `REPLICA IDENTITY FULL`; new `deliverable_status` enum + new `deliverables` table (multi-assignee `uuid[]`, open-authenticated RLS, `completed_at` trigger, AFTER INSERT/UPDATE/DELETE activity-log trigger) + `activity_log_writer` CREATE OR REPLACE adds a `TG_OP = 'DELETE'` branch (existing triggers untouched, payload `{ old: to_jsonb(OLD) }`); new `saved_views` per-user table with RLS scoped to `user_id = auth.uid()`; `tasks.priority` (CHECK Urgent/High/Normal/Low) + `tasks.blocked_by uuid[]` (GIN index). Cross-cutting components built once for reuse across every 5.2 list page: `<ViewSwitch />`, `<FilterBar />` (Notion-style AND/OR chip builder), `<SavedViewsDropdown />`, `<DataTable />` (two-tier collapse + status-color row borders + shift-click range select), `<BoardView />` (HTML5 drag-drop, optimistic + revert, Realtime), `<TimelineView />` (8-month gantt), `<CalendarMonthView />` (month grid, 140px cells, 3 two-row banners per cell), `<StickySaveBar />`. Shared helpers `src/lib/hq/{savedViews,filterStateApply,dates}.ts` and per-surface `src/lib/{projects,tasks,deliverables}/queries.ts`. Surfaces: `/projects`, `/projects/board`, `/projects/timeline`, `/projects/new`, `/projects/:id`, `/projects/:id/edit`; `/tasks`, `/tasks/board`, `/tasks/new`, `/tasks/:id`, `/tasks/:id/edit`; `/deliverables` (default = Calendar per build notes Surface 14), `/deliverables/list`, `/deliverables/board`, `/deliverables/new`, `/deliverables/:id`, `/deliverables/:id/edit`. Per-surface Calendar tabs (Projects + Tasks) route to the unified `/calendar?source=...` stub; unified Calendar surface lands in 5.3. Tasks List default filter is "assignee is me AND status is not Done"; inline quick-add row on List; the "Notes / Blocks" column merges `description` + `blocked_by`. Deliverables Board groups by project (one row per project with 4 status columns inside so drag-drop changes status within a project). Skipped deliverable titles + Done task titles render with `line-through opacity-60`. Rail amendment: `LeftRail.tsx` collapses to a single ordered Tools group with per-item `adminOnly?: boolean` (Wiki, Talent Scout, Venue Scout, Team, Outlook, Settings); tool-app variant detected via `pathname.startsWith('/talent-scout' | '/venue-scout')` swaps the Primary group for `[HQ Home, Activity Feed]` (route-based, not state-based). `projectStatusToken.ts` dropped the legacy alias rows, flipped task tokens to mixed-case, added `deliverableStatusToken` + `statusTextDecoration` helpers. Existing Home queries flipped: AppShell tasks-count query to `["To Do", "Doing"]`; MyTasksThisWeekCard query + Done update to mixed case + reads `priority` from the new column; AllActiveProjectsCard filter widened to `NOT IN ('Complete', 'Cancelled')`; MyWeekStrip now unions deliverable due dates this week alongside project Live / Removal dates. AppShell sticky-rail hot fix bundled in: outer flex wrapper went from `min-h-screen` to `h-screen overflow-hidden` so the rail + top bar are viewport-locked and `hq-content` is the only scroll source. Three queued REPO_DOC_UPDATES Pending entries flipped to Done in this commit: roadmap 5.2.1 + 5.2.2 split, COWORK_SYNC two-write convention + pre-flight drift check, AppShell sticky-rail hot fix. Spec: `OUTPUTS/phase-5-2-spec.md` §§ 0-5.B + 5.C + 7 + 11 + 12 + 13 + 14; rail amendment: `OUTPUTS/phase-5-2-rail-amendment.md`; locked decisions: `OUTPUTS/phase-5-locked-decisions-2026-05-15.md`.
- **Phase 5.1 HQ Core foundations** (`665a311` + migration fix `1d7b8b6`, shipped 2026-05-15): the cross-cutting HQ Core layer comes online. Tier model rewritten from (`member`/`producer`/`admin`) to (`admin`/`standard`/`freelance`/`pending`) with `pending` as the new default; backfill on existing users mapped admin -> admin, producer -> admin, member -> standard. `handle_new_user` now writes one durable `notifications` row per active admin on every new signup AND fires `notify-admin-of-pending-user` (deployed 2026-05-15) so admins get a one-time email. New polymorphic `notes_log` table (`parent_type IN ('organization', 'person')`); immutable except deletion (RLS: SELECT-all-auth, INSERT-self-author, DELETE-author-or-admin, NO UPDATE). The shipped top-nav `AppShell` is replaced by the locked left-rail shell (232px rail + slim top bar). New routes: `/pending` (Surface 01 bare card; 30s poll; hard-nav to /home on role change), `/home` (Surface 02 Standard + Surface 03 Admin variants in one tier-branched page component; greeting + quick-add cluster + My Week strip + (admin) pipeline counts row + My Projects + My Tasks This Week (Standard only) + All Active Projects + (admin) Outlook condensed placeholder + Recent Activity (Standard "mine" / Admin "cross-team")). `/` redirects authed users to `/home`. New route gate `<StandardOrAdminRoute>` blocks Freelance from HQ Core surfaces with a friendly empty state; `<ProtectedRoute>` redirects `pending` users to `/pending`. Bell icon ships as visual chrome only (no panel until 5.5). Design-system gained a new "Status color mapping" section (4 enums; Project / Task / Deliverable / Outlook Confidence per locked-decisions § 4) plus a Component sizing block (button 40/36px, input 44px, `.h-card` 18px). Mirror-style-guide stripped of the conflicting page-title 32px / section-title 22px / `p-5` card-padding rows. Phase 4 NIT ride-along: ScoutChrome dead query-string branch dropped, `vs-parse-brief` v17 deployed (sanitizeObjectives comma split), `vs-generate-brief-overview` v3 deployed (canonical-numeric hash for `budget` + `expected_guest_count`). Spec: `OUTPUTS/phase-5-1-spec.md`; locked decisions: `OUTPUTS/phase-5-locked-decisions-2026-05-15.md`.
- Netlify auto-deploys on push to `main`.

## What's NOT live yet

- Cron schedules are committed but require runtime activation: production GUCs (`app.supabase_url` + `app.internal_api_secret`) need to be set in Supabase SQL editor before the schedules will fire. Until then they're scheduled-but-noop.
- All Phase 3.8 edge functions need to be deployed via `supabase functions deploy <name>` (cron functions + `ts-send-pull-notification` + re-deploys for `ts-pull-candidates` and any function that imports `_shared/anthropic.ts`).
- Phase 3.8 migrations need to be pushed: `supabase db push --linked`.
- HQ Core surfaces still stubbed: Activity Feed (5.5), Search (later), Wiki (5.4), Team (5.4), Settings (5.4). Projects + Tasks + Deliverables + Clients + Vendors + People + Venues are all live as of 5.2.3. ~~Calendar (unified, 5.3)~~ + ~~Outlook (5.3)~~ shipped 2026-05-16; per-surface Calendar tabs on Projects + Tasks route to `/calendar?source=projects|tasks` and pre-filter the visibility panel on first visit.
- In-app notifications bell (Phase 5.5). The bell icon ships in 5.1 as visual chrome only; pull-completion email + the new `notify-admin-of-pending-user` admin email are live, but the in-app panel + per-user prefs come later.

## Known issues / drift

- `auto_rejected` enum value is deprecated (Phase 3.7.2.1 backfilled to `reject` + `manually_reviewed=false`) but kept in the enum for safety. New writes never use it. Cleanup requires enum rebuild; not worth it now.
- **Phase 4 Revision deferred items** (not blocking): defense-in-depth sanitization of producer free-text before Claude prompt interpolation in `vs-research-venues` / `vs-generate-brief-overview` (uniform pass, should also cover the pre-existing `brief_data.notes` injection); `vs-generate-brief-overview` (like every VS function) uses the service-role client rather than a caller-JWT client (security-auditor SHOULD-FIX, deferred per spec § 5d "same auth posture" -- harden as a uniform pass); `SCOUT_SELECT` column string duplicated across the three Brief pages. Flagged by security-auditor / code-reviewer and recorded in the squash commit messages.
- **Phase 5.2.3 deferred items** (not blocking): ~~distinct rail icon for Clients (currently shares `IconOrgs` with Vendors)~~ RESOLVED in Phase 5.2 cleanup `65159a2` (new `IconClients` briefcase glyph); ~~`vendors.primary_address` column + VendorEdit field (5.2.3 spec § 4.B.3 visual contract gap; lands in the same polish pass that handles `clients.primary_address` UX uniformity)~~ RESOLVED in Phase 5.2 cleanup `65159a2` (migration `20260516140000` + VendorEdit textarea + VendorDetail kv row); ~~`vendor_capabilities` GRANT lacks DELETE (renamed from org_capabilities; same dead-policy posture; widen alongside Settings page CRUD in 5.4)~~ RESOLVED in Phase 5.2 cleanup `65159a2` (migration `20260516140000` adds the GRANT); VendorDetail "Projects Touched" inefficient client-side filter through `venues.exclusive_vendor_ids` (switch to PostgREST `.contains(...)` push-down when scale warrants, OR add a dedicated `project_vendors` join table if direct project-vendor relationships surface); drop `/organizations/*` backward-compat redirect (`OrganizationsRedirect`) once Mirror bookmarks have updated. Wireframe-v2 redraw of Surface 10 (currently shipped as DEVIATION from the locked wireframe; deferred polish pass).
- **Phase 5.2 cleanup deferred items** (not blocking): `useClientsAndVendors` single-shot load (no cross-tab refresh; observation Frontend #2); `FilterBar.chipDisplayValue` raw-uuid flash race if a saved view restores a lookup chip before lookupOptions has loaded (observation Frontend #3); `docs/schema.md` full 5.2.3 split structural rewrite + new lookup-tables documentation (observation Docs #1; cleanup only added a drift callout + delta bullets for its own changes); ~~§ 4.5 manual-push stale-sync risk if Jimmie aborts at step 5e after the SHIPPED block has already written (observation Docs #2)~~ RESOLVED in `5b14bfe` + the `Bash(git push origin main)` allowlist (`85cb765`); Tags filter `"any-of"` upgrade + `applyFilters` array-vs-array intersection fix (deferred until a real UX driver).
- **Phase 5.3 deferred items** (not blocking): `<CalendarMonthView />` now has two render paths (controlled-vs-uncontrolled; observation Frontend #6); CalendarPage Lead + Category FilterChip popovers do not close on outside click (observation Frontend #5; promote to shadcn `<Popover>` later); `<OutlookEntryPanel />` fires `useClients()` + `useLookup("cities")` queries even in detail mode where neither is used (deferred, admin-only + cheap); `loadDistinctOutlookYears` selects all rows for distinct extraction (deferred, small table); `ProjectDetail` "Days Until Install" stat tile now prefers `install_dates_start` over `live_dates_start` (observation Frontend #7; document for producers so the countdown behavior change isn't surprising for projects with only live dates set); concurrent first-visit `saved_views` race on the Calendar page (two tabs simultaneously on first visit could both attempt the INSERT; second falls back silently to default state; deferred, low impact); `MIRROR_HOLIDAYS` carries only a single Jan 1 2027 stub (cliff in early 2027; extend in 5.4 Settings editor when the 2027 PDF is published); `outlook_entries` not in the `supabase_realtime` publication (single-admin edit pattern; add later if multi-admin concurrent editing surfaces as a need); CalendarMonthView's `outlook_entries` query against the linked DB uses generic-string casts (`as never`) in `src/lib/calendar/queries.ts` because the types weren't yet regenerated when the file was written; post-squash regen now backs them but the casts remain in source.
- **Phase 5.2.2 activity_log_writer fix** (not blocking, applied as `20260516120004`): the shipped trigger function evaluated `OLD.status IS DISTINCT FROM NEW.status` inside an AND expression that doesn't short-circuit RECORD field access. UPDATE on tables without a status column (people, organizations, venues, the new lookups) crashed. Fix: nested-IF guard so OLD.status only resolves for `('projects', 'tasks', 'deliverables')`, plus `to_jsonb()->>'status'` defense-in-depth (returns NULL on missing keys instead of erroring). Pasted into Supabase Studio at the squash-approval gate before the seed; backfilled into the migration ledger via the `_004` file so future fresh checkouts see it as applied DDL.
- **Phase 5.1 deferred items** (not blocking): `notes_log` Realtime publication + `REPLICA IDENTITY FULL` will need to land when 5.2's Notes UI ships if it uses `postgres_changes` subscriptions (migration-reviewer flag); `notes_log` SELECT policy is currently `USING (true)` to authenticated, which includes `pending` users — narrow to `is_standard_or_admin OR is_admin` in 5.4 when the Team page locks tier-access patterns (migration-reviewer flag); server-side `hashAsStringArray` in `vs-generate-brief-overview` does not filter empty-trimmed strings while the client `asStringArray` does, so a direct DB edit injecting `""` into a brief_data array would cause a hash mismatch and one wasted regen (security-auditor flag); existing pages (Talent Scout, Venue Scout) lost the previous `max-w-7xl` shell wrapper and now expand to the full content width inside `.hq-content` (32px padding) — acceptable for 5.1 per spec § 2 "body content untouched"; revisit if any TS / VS surface looks broken at >1440px.
- **Phase 4 Revision pass 3 hash-parity edge case** RESOLVED in Phase 5.1 NIT pickup: `computeOverviewSourceHash` + the server hash now use canonical numeric values for `budget` + `expected_guest_count`, so non-canonical input (no `$` / commas) hashes the same as canonical input. Both sides deployed as v3 of `vs-generate-brief-overview`. `code-observations.md` Frontend #1 closed.

## Recent commits (main)

```
f278da7  Phase 5.6.1: Cross-cutting interaction primitives (RecordCombobox + ClickPillCell + phone + Project Team card)
31c0345  [skip netlify] Phase 5.6 plan (5.6.1 through 5.6.5) + 5.7 through 5.11 forward roadmap
0c52061  [skip netlify] Backfill 0f122c1 Phase 5.5.1 into CHECKPOINT.md
0f122c1  Phase 5.5.1: Sign-in page (Surface 01 part 1)
5f89b5f  [skip netlify] Backfill 0cc52f3 Phase 5.5 into CHECKPOINT.md
0cc52f3  Phase 5.5: Notifications + Activity Feed + Search + States Polish
e36217d  [skip netlify] Backfill f4e4aab Phase 5.4 into CHECKPOINT.md
```

(A `[skip netlify]` CHECKPOINT.md backfill commit lands on top of `f278da7`; it is the administrative tail of this merge per § 4.5.b two-push pattern.)

The 2026-05-13 cutover replaced the failed-attempt Phase 4 stack on main with the full vs-port-fresh history (Phase 4.1-port through 4.10.6-port). 42 commits on main with no patch-equivalent on vs-port-fresh were intentionally dropped. See `docs/decisions.md` and `docs/venue-scout-port-plan.md` § "Done when" for the cutover trail.

## Recent migrations

```
20260517100000_phase_5_6_1_phone_normalization.sql           Phase 5.6.1: data-only normalize-existing-rows migration. pg_temp.normalize_phone(raw text) helper extracts exactly 10 US digits via `regexp_replace(raw, '[^0-9]', '', 'g')`, strips a leading 1 for 11-digit inputs, returns the formatted `(XXX) XXX-XXXX` string when it lands on exactly 10 digits, else passes the raw value through unchanged (international, extensions, garbage all survive). Three UPDATEs target `people.phone`, `vendors.contact_phone`, `clients.contact_phone` with a WHERE clause skipping rows that already match `^\(\d{3}\) \d{3}-\d{4}$` (idempotent + safe to re-run). pg_temp function is session-scoped + auto-drops after the migration transaction. No schema change, no GRANT/RLS impact, no types regen delta. Backs the `formatPhone` save-time-only onBlur handler shipped on PersonEdit + VendorEdit + ClientEdit; existing rows get caught up at migration push so the UI never has to retroactively migrate user data. (APPLIED 2026-05-17)
20260517000000_phase_5_5_notifications_activity_search.sql  Phase 5.5: CREATE TABLE user_notification_preferences (id PK, user_id FK ON DELETE CASCADE, trigger_key text, in_app bool default true, slack_dm bool default false, timestamps, UNIQUE(user_id, trigger_key)) + per-user RLS on all CRUD verbs + GRANT SELECT/INSERT/UPDATE/DELETE TO authenticated + ALL TO service_role + updated_at_auto trigger + (user_id) index. ALTER TABLE notifications ADD COLUMN delivered_slack boolean NOT NULL DEFAULT false. ALTER PUBLICATION supabase_realtime ADD TABLE notifications (idempotent via pg_publication_tables guard) + ALTER TABLE notifications REPLICA IDENTITY FULL. CREATE OR REPLACE FUNCTION notifications_dispatch_writer() trigger function (SECURITY DEFINER; reads auth.uid() for actor_id with COALESCE fallback to NEW.created_by for task_assigned; fires task_assigned on assignee_id newly set or changed, task_blocked on status='Blocked', project_status_changed on project status change with recipients = project_account_managers) + AFTER INSERT OR UPDATE trigger on tasks + AFTER UPDATE trigger on projects. CREATE OR REPLACE FUNCTION handle_new_user() rewrite: same inline notifications-row writes per active admin for the durable bell signal + calls notifications-dispatch with event_type='user_pending' instead of notify-admin-of-pending-user directly (link_url remains /users from the 5.4 rename). Three pg_cron schedules: hq-cron-deliverable-due-3d (0 13 * * *), hq-cron-task-due-today (0 12 * * *), hq-cron-event-date-today (0 11 * * *). (APPLIED 2026-05-16)
20260516180000_phase_5_4_feedback_round_2.sql                Phase 5.4 feedback round 2: DROP credentials_insert_admin / _update_admin / _delete_admin policies + CREATE _non_freelance variants gating writes via permission_role IN ('admin', 'standard'). ALTER TABLE vendors ADD COLUMN preferred boolean NOT NULL DEFAULT false. CREATE INDEX vendors_preferred_idx ON vendors (preferred) WHERE preferred = true (partial index for the curated list lookup). Backs the Wiki Preferred Vendors curation (admins toggle the flag inline via multi-select dialog; renders only preferred=true rows grouped by category_name). (APPLIED 2026-05-16)
20260516170000_phase_5_4_feedback.sql                        Phase 5.4 feedback round: ALTER TABLE credentials DROP COLUMN related_note. ALTER TABLE wiki_pages DROP CONSTRAINT wiki_pages_visibility_check + re-ADD widened to ('all', 'no_freelance', 'admin_only'). UPDATE wiki_pages SET slug='preferred-vendors', title='Preferred Vendors' WHERE slug='vendors-at-a-glance'. UPDATE all 8 seeded prose pages converting markdown body to HTML (dollar-quoted for the substantive How We Work content). CREATE OR REPLACE handle_new_user changing the admin-notification link_url from /team to /users. App-side: react-markdown -> TipTap (HTML storage); WikiNav + WikiPage filter admin_only from non-admins. (APPLIED 2026-05-16)
20260516160000_phase_5_4_wiki_team_settings.sql              Phase 5.4 spec implementation: 4 new tables (departments + wiki_pages + credentials + mirror_holidays); ALTER TABLE users DROP CONSTRAINT users_id_fkey (FK to auth.users dropped for id-swap pre-provisioning) + DROP COLUMN department_tags + DROP CONSTRAINT users_department_tags_valid + ADD COLUMN role_title / department_id (FK departments) / slack_handle / slack_user_id / last_active_at; CREATE POLICY users_insert_admin via is_admin(). CREATE OR REPLACE handle_new_user with email-match id-swap path (pre-provisioned users get their placeholder id swapped to NEW.id on first sign-in; idempotent stamp when ids already match; fresh signup falls back to the Phase 5.1 pending + admin-notification flow). Seeded 5 departments (Leadership / Accounts / Creative / Design / Event Production) + 11 wiki pages (Welcome & Mission / Team Directory / How We Work / Vendors at a Glance / Key Partners / Forms & Important Documents / Account Logins / Pricing & Markup Guide / Design File Prep & Specs / Billing & PO Workflow / Shipping & Messengers) + 18 mirror_holidays rows (from the 5.3 hardcoded MIRROR_HOLIDAYS constant + Christmas/New Year window expansion + 2027 New Year stub). updated_at_auto + activity_log_writer triggers on the new tables. RLS: open-SELECT for all + admin INS/UPD/DEL via is_admin() (wiki_pages, departments, mirror_holidays); SELECT for admin+standard only on credentials (freelance blocked entirely) + admin-only INS/UPD/DEL on credentials (widened to admin+standard in round 2). GRANT cleanup: INSERT on users (admin pre-provisioning), DELETE on cities + project_categories (matches the 5.2 cleanup vendor_capabilities posture fix). (APPLIED 2026-05-16)
20260516150002_phase_5_3_saved_views_calendar_check.sql        Phase 5.3: DROP CONSTRAINT saved_views_entity_type_check + re-ADD widened list including 'calendar'. Lets per-user Calendar visibility persist via one implicit saved_views row per user (name='__calendar_default', view_kind='calendar'). Pure additive, no data migration. (APPLIED 2026-05-16)
20260516150001_phase_5_3_projects_install_removal_dates.sql    Phase 5.3: ALTER TABLE projects ADD COLUMN install_dates_start / install_dates_end / removal_dates_start / removal_dates_end (date, nullable). Pure additive, no backfill, no constraints. Existing projects start with NULL Install + Removal dates; producers fill them in via ProjectEdit. Drives Calendar Install + Removal banners + Projects Timeline view's Install + Removal bars + ProjectDetail "Days Until Install" preference (install_dates_start with fallback to live_dates_start). (APPLIED 2026-05-16)
20260516150000_phase_5_3_outlook_entries_table.sql             Phase 5.3: new outlook_confidence enum (On Radar / Likely / Confirmed / Complete) + new outlook_entries table (name, client_id FK ON DELETE SET NULL, city, year, month CHECK 1-12, week CHECK 1-4, date_text, budget, confidence default On Radar, notes, linked_project_id FK ON DELETE SET NULL, shared_with_team default false, created_by NOT NULL ON DELETE RESTRICT, timestamps). 4 indexes: (year, month) full + partial on client_id / linked_project_id / shared_with_team WHERE true. Triggers: trg_outlook_entries_updated_at (updated_at_auto), trg_activity_log_outlook_entries (AFTER INSERT/UPDATE/DELETE via the post-5.2.2-hotfix activity_log_writer). RLS: admin full CRUD; standard + freelance SELECT only on shared_with_team=true rows. GRANTs SELECT/INSERT/UPDATE/DELETE to authenticated + ALL to service_role. NOT added to supabase_realtime publication (single-admin edit pattern; deferred per spec § 3g). New promote_outlook_to_project(target_entry_id uuid) RETURNS uuid SECURITY DEFINER RPC: pre-check is_admin() (raises 42501 on non-admin), FOR UPDATE lookup, raises P0001 if already linked, INSERTs projects (name + client_id + city from entry + status='Queued' + created_by=auth.uid()) then UPDATEs entry's linked_project_id to the new project id, returns the new project id. GRANT EXECUTE TO authenticated. (APPLIED 2026-05-16)
20260516140000_phase_5_2_cleanup_primary_address_and_vendor_capabilities_grant.sql  Phase 5.2 cleanup: ALTER TABLE public.vendors ADD COLUMN primary_address text (nullable; no backfill; matches the clients.primary_address shape lifted from 5.2.3.A). GRANT DELETE ON public.vendor_capabilities TO authenticated (admin-only DELETE RLS policy from the 5.2.3.A org_capabilities -> vendor_capabilities rename was unreachable because the table-level GRANT omitted DELETE; this brings posture in line with clients / cities / project_categories / vendor_categories). Wrapped in BEGIN/COMMIT. (APPLIED 2026-05-16)
20260516130004_phase_5_2_3_venues_exclusive_vendor_ids.sql        Phase 5.2.3.E: ALTER TABLE venues RENAME COLUMN exclusive_vendors_org_ids TO exclusive_vendor_ids. Defensive cleanup: UPDATE strips any element values that no longer exist in vendors (no Client IDs were in the array from the 5.2.2 seed, but the UPDATE is safe to run on hand-edited dev data). Column type stays uuid[]; element values are now interpreted against the renamed vendors table (preserved row IDs through 5.2.3.B's table rename). (APPLIED 2026-05-16)
20260516130003_phase_5_2_3_notes_log_check.sql                    Phase 5.2.3.D: DROP CONSTRAINT notes_log_parent_type_check; UPDATE notes_log SET parent_type='client' WHERE parent_type='organization' AND parent_id IN (SELECT id FROM clients); same for 'vendor'; DELETE any leftover 'organization' rows whose parent_id no longer resolves; ADD CONSTRAINT notes_log_parent_type_check CHECK (parent_type IN ('client', 'vendor', 'person', 'venue')). Defensive cleanup matches the 5.2.3.A + .B reshapes. (APPLIED 2026-05-16)
20260516130002_phase_5_2_3_fk_reshape.sql                         Phase 5.2.3.C: projects.organization_id -> client_id (DROP CONSTRAINT projects_client_id_fkey first since the shipped FK was already named that from the 5.2.2 rename; tolerate projects_organization_id_fkey defensively); RENAME idx_projects_organization_id -> idx_projects_client_id; re-ADD FK pointing at clients. people: DROP CONSTRAINT people_organization_id_fkey; RENAME column to client_id + RENAME index people_org_idx -> people_client_idx; ADD COLUMN vendor_id uuid (no inline FK; populated by the UPDATE below); UPDATE redistributes people whose renamed client_id points at a vendors row over to vendor_id + clears client_id (Priya Nair case); NOW ADD both FKs (people_client_id_fkey -> clients, people_vendor_id_fkey -> vendors) against satisfied data; CREATE INDEX people_vendor_idx WHERE vendor_id IS NOT NULL; ADD CONSTRAINT people_org_mutex_check CHECK (NOT (client_id IS NOT NULL AND vendor_id IS NOT NULL)) (locked Q4: at most one org type per person); DROP COLUMN people.affiliations + DROP TYPE person_affiliation. Ordering hotfix at the squash gate: original migration ADDed FKs before the redistribute UPDATE; the FK ADD failed (SQLSTATE 23503) on the Priya row. Reordered + committed `6ec6c6c` before the squash merge. (APPLIED 2026-05-16)
20260516130001_phase_5_2_3_organizations_to_vendors.sql           Phase 5.2.3.B: UPDATE organizations SET tags = tags || '{Internal Partner}' WHERE type='Internal' (locked Q1: Internal collapses into tags[]); DELETE Client rows from organizations (they live in clients now via 5.2.3.A); DROP INDEX organizations_type_idx; DROP COLUMN organizations.type; DROP TYPE org_type; ALTER TABLE organizations RENAME TO vendors; rename organizations_pkey + organizations_city_idx for tidiness; ADD COLUMN vendors.category_id uuid REFERENCES vendor_categories(id) ON DELETE SET NULL + partial idx WHERE NOT NULL. (APPLIED 2026-05-16)
20260516130000_phase_5_2_3_setup.sql                              Phase 5.2.3.A: ALTER TABLE org_capabilities RENAME TO vendor_capabilities (+ index rename); CREATE TABLE vendor_categories (lookup, same shape as cities; SELECT/INSERT/UPDATE open-auth, DELETE admin-only via public.is_admin(); GRANT includes DELETE so the policy is reachable -- catches a code-reviewer MUST FIX); CREATE TABLE clients (slim shape: name + industry + contact_name/email/phone + primary_address + city + website_url + tags[] + standard timestamps; open-auth RLS; updated_at + activity_log_writer triggers); INSERT INTO clients SELECT FROM organizations WHERE type='Client' preserving UUIDs (so projects.organization_id can flip its FK target without re-mapping in 5.2.3.C). (APPLIED 2026-05-16)
20260516120004_phase_5_2_2_activity_log_writer_status_guard.sql   Phase 5.2.2 frontend (hotfix): CREATE OR REPLACE public.activity_log_writer() with a nested-IF guard so `OLD.status IS DISTINCT FROM NEW.status` only evaluates for tables in ('projects', 'tasks', 'deliverables'). Plus `to_jsonb(OLD)->>'status'` / `to_jsonb(NEW)->>'status'` defense-in-depth (returns NULL on missing keys instead of erroring). The shipped function crashed on UPDATE to people / organizations / venues / lookups because the AND expression didn't short-circuit RECORD field access. Pasted into Supabase Studio at the squash gate; this file backfills the ledger so future fresh checkouts see the fix as applied DDL. (APPLIED 2026-05-16)
20260516120003_phase_5_2_2_venue_contact_people.sql               Phase 5.2.2 frontend: new venue_contact_people composite-PK join (venue_id, person_id), ON DELETE CASCADE on both sides. RLS open-auth SELECT/INSERT/DELETE; no UPDATE policy (rows are insert-or-delete only). Drives Venue Detail Contacts card pulling people via the join. Will reseat in 5.2.3 alongside the org table split but stays at the same shape (people.client_id / vendor_id split is separate). (APPLIED 2026-05-16)
20260516120002_phase_5_2_2_org_capabilities.sql                   Phase 5.2.2 frontend: new org_capabilities lookup table. Same shape as cities (id uuid PK, name text NOT NULL, case-insensitive unique idx, RLS open-auth SELECT/INSERT/UPDATE + admin-only DELETE). Backs the Capabilities multi-tag input on Org Edit for Vendor + Internal types. Will likely rename to vendor_capabilities in 5.2.3 alongside the orgs->vendors rename. (APPLIED 2026-05-16)
20260516120001_phase_5_2_2_project_categories.sql                 Phase 5.2.2 frontend: new project_categories lookup table. Same shape as cities. Backs the Project Category dropdown on Project Edit. Drives the Project List view's Category column. (APPLIED 2026-05-16)
20260516120000_phase_5_2_2_cities.sql                             Phase 5.2.2 frontend: new cities lookup table. Open-authenticated SELECT/INSERT/UPDATE; admin-only DELETE (so a misclick cannot blow away a city referenced by 30 projects). Unique-case-insensitive index on name prevents duplicate entries. Backs the inline-add City dropdown on Project Edit + Organization Edit + Venue Edit. (APPLIED 2026-05-16)
20260515140003_phase_5_2_2_project_extensions.sql                 Phase 5.2.2.D: ALTER TABLE projects RENAME COLUMN notes TO status_notes; ADD COLUMN client_notes text + job_number text + category text + city text + tags text[] NOT NULL DEFAULT '{}' + budget numeric. Partial btree indexes on job_number / city / category WHERE NOT NULL. Surface 04 List view columns + Surface 07 detail title row coral `#<job_number>` + "Pop-Up · LA" meta + tag chips + $budget reference. Budget rule: planning reference figure NOT an invoice amount; never on pipeline-summary surfaces (locked-decisions Q6). (APPLIED 2026-05-15 via the halted worktree; registered locally 2026-05-16 via `supabase migration repair --status applied` as part of the 5.2.1 Revision Step 0 reconciliation)
20260515140002_phase_5_2_2_venues_extensions.sql                  Phase 5.2.2.C: new venue_venue_types join table (multi-select venue type; backfilled from + replaces the single venues.venue_type_id FK + drops idx_venues_venue_type_id). ADD COLUMN venues.city + venue_slide_url + total_sq_ft + exclusive_vendors_org_ids uuid[]. Partial btree index on city WHERE NOT NULL. New venue_rate_kind enum (event_day, prod_day) + new venue_rate_history append-only table (SELECT + INSERT for authenticated only; no UPDATE / no DELETE; drives "Event Day Rate $X as of <date>" on Surface 09 detail via most-recent row per (venue_id, rate_kind)). Widen notes_log_parent_type_check to include 'venue' so the shared <InternalNotesEditor /> serves Venue detail. ADD COLUMN people.venue_id uuid REFERENCES venues(id) ON DELETE SET NULL (Q8 recommendation; venue-contact attribution without a join table). (APPLIED 2026-05-15 via halted worktree; registered locally 2026-05-16)
20260515140001_phase_5_2_2_people.sql                             Phase 5.2.2.B: new person_affiliation enum (Client / Vendor / Internal / Venue). New people table for external humans (Internal Mirror staff stay in public.users + surface on the Team page in 5.4). Multi-affiliation array (GIN-indexed). organization_id nullable FK (ON DELETE SET NULL). created_by NOT NULL with default ON DELETE RESTRICT (matches deliverables.created_by posture). Indexes: people_org_idx WHERE NOT NULL, people_affiliation_gin_idx, people_full_name_idx (lower-cased). Open-authenticated RLS. AFTER INSERT/UPDATE/DELETE activity-log trigger. (APPLIED 2026-05-15 via halted worktree; registered locally 2026-05-16)
20260515140000_phase_5_2_2_organizations.sql                      Phase 5.2.2.A (locked Q3): ALTER TABLE clients RENAME TO organizations + ALTER TABLE projects RENAME COLUMN client_id TO organization_id + ALTER INDEX idx_projects_client_id RENAME TO idx_projects_organization_id. New org_type enum (Client / Vendor / Internal; Venue Owner intentionally dropped). ADD COLUMN organizations.type org_type NOT NULL DEFAULT 'Client' + city + capabilities text[] + website_url + tags text[] + internal_rating int CHECK 0-5 (Vendor-only, admin-write-only RLS deferred to 5.4). RENAME the shipped notes column to legacy_notes so existing client-row notes survive while Internal Notes flips to the polymorphic notes_log table. AFTER INSERT/UPDATE/DELETE activity-log trigger. RLS / GRANTs / triggers carry through the rename by table OID; policy identifiers stay clients_* but access posture is preserved. (APPLIED 2026-05-15 via halted worktree; registered locally 2026-05-16)
20260515130003_phase_5_2_1_tasks_priority_blocks.sql              Phase 5.2.1: ALTER TABLE tasks ADD COLUMN priority text NOT NULL DEFAULT 'Normal' CHECK IN ('Urgent', 'High', 'Normal', 'Low') + ADD COLUMN blocked_by uuid[] NOT NULL DEFAULT '{}'. GIN index on blocked_by. Surfaces the Surface 13 Priority pill + "Notes / Blocks" cell. App is responsible for validating blocked_by elements point to valid task ids (Postgres cannot FK an array element). (APPLIED 2026-05-15)
20260515130002_phase_5_2_1_saved_views.sql                        Phase 5.2.1: new saved_views per-user table. RLS scoped to user_id = auth.uid() on SELECT/INSERT/UPDATE/DELETE (unlike every other HQ Core table; saved views are personal preferences, not team state). is_default per (user_id, entity_type) enforced in app via transactional "clear then set" upsert. (APPLIED 2026-05-15)
20260515130001_phase_5_2_1_deliverables.sql                       Phase 5.2.1: new deliverable_status enum (Upcoming / In Progress / Complete / Skipped per locked-decisions § 4). New deliverables table (project_id FK CASCADE, multi-assignee uuid[], GIN index on assigned_user_ids, completed_at, open-authenticated RLS). New deliverables_completed_at_set trigger (mirror of tasks_completed_at_set). activity_log_writer CREATE OR REPLACE'd with a TG_OP = 'DELETE' branch (action_val 'deleted', payload {old: to_jsonb(OLD)}; existing projects/venues/tasks triggers untouched on AFTER INSERT OR UPDATE only). deliverables added to supabase_realtime publication with REPLICA IDENTITY FULL. (APPLIED 2026-05-15)
20260515130000_phase_5_2_1_project_task_enum_reshape.sql          Phase 5.2.1: ALTER COLUMN TYPE USING (CASE ...) swap for public.project_status (14 legacy values -> locked 14: drops Awaiting FB / Awaiting Files / Awaiting Approval / Event Live / Proof Out / In Review, adds Approved / Install / Removal / Queued / Awaiting Feedback / Cancelled, renames Awaiting FB -> Awaiting Feedback; catch-all backfills: Awaiting Files -> In Progress, Proof Out -> In Production, In Review -> In Progress) + identical swap for public.task_status (lowercase -> mixed case: To Do / Doing / Blocked / Done, ELSE catch-all 'To Do'). idx_projects_status + idx_tasks_status auto-rebuild during ALTER COLUMN TYPE. tasks_completed_at_set body rewritten via CREATE OR REPLACE so the literal comparison flips 'done' -> 'Done' (same OID; existing trigger keeps resolving). New default for projects.status = 'Queued', tasks.status = 'To Do'. tasks + projects added to supabase_realtime publication with REPLICA IDENTITY FULL for the Board view drag-drop. (APPLIED 2026-05-15)
20260515120001_phase_5_1_notes_log.sql                            Phase 5.1: notes_log table polymorphic across organization + person. RLS: SELECT-all-auth, INSERT-self-author, DELETE-author-or-admin, NO UPDATE policy (notes are immutable except for deletion per locked-decisions § 3). Index on (parent_type, parent_id, created_at DESC). Forward-compatible with the Organizations + People tables that ship in 5.2. (APPLIED 2026-05-15)
20260515120000_phase_5_1_tier_model_rewrite.sql                   Phase 5.1: ALTER TYPE swap for public.permission_role from member/producer/admin to admin/standard/freelance/pending. Backfill via ALTER COLUMN TYPE USING (CASE): admin -> admin, producer -> admin, member -> standard, ELSE pending. New default 'pending'. handle_new_user trigger rewritten to insert 'pending', write notifications rows per active admin, and invoke notify-admin-of-pending-user via public.invoke_edge_function. is_producer_or_admin() body rewritten to permission_role::text IN ('admin', 'standard') (new semantics, enum-agnostic via ::text cast) via CREATE OR REPLACE so the master-venue-photos storage policies that depend on it keep resolving (the initial DROP-based version of this migration was rejected by Postgres on the first push; the fix landed as commit 1d7b8b6). current_user_role() recreated around the type swap. (APPLIED 2026-05-15)
20260514110000_phase_4_revision_intake_current_step.sql              Phase 4 Revision - Intake: DROP + re-ADD vs_scouts_current_step_check with a 10th value `brief` prepended; ALTER COLUMN current_step SET DEFAULT 'brief'. Additive -- existing rows untouched. `brief` is the in-flight 3-step intake step; Step 3 Confirm flips it to `sheet_prompt`. (APPLIED)
20260514100000_phase_4_10_6_port_reset_scout_for_deck_regenerate.sql  Phase 4.10.6-port: reset_scout_for_deck_regenerate(target_scout_id uuid) RPC for the Deck Prep regenerate flow. Atomic single-statement UPDATE that sets current_step='deck_prep', strips deck_generation_started_at from brief_data via the jsonb `-` operator, clears status + pipeline_error, bumps last_touched_at. Replaces a frontend read-then-write that had a TOCTOU race window. SECURITY INVOKER; GRANT EXECUTE TO authenticated. (APPLIED)
20260514000002_phase_4_10_3_port_vs_storage_policies.sql        Phase 4.10.3-port: relax storage policies for briefs / sourcing_sheets / vs_venue_photos buckets from is_producer_or_admin() to authenticated. Matches vs_* table RLS posture per port plan § 8.6 (collaborative agency-wide workflow). vs_venue_photos collapsed from 4 split policies to a single FOR ALL policy. IF EXISTS on DROPs handles Studio-side rename drift. (APPLIED)
20260514000001_phase_4_10_3_port_start_over_scout_pipeline_error.sql  Phase 4.10.3-port: CREATE OR REPLACE start_over_scout to clear pipeline_error instead of research_error post-rename. Body otherwise unchanged from 4.9-port migration. (APPLIED)
20260514000000_phase_4_10_3_port_pipeline_error_rename.sql      Phase 4.10.3-port: ALTER TABLE vs_scouts RENAME COLUMN research_error TO pipeline_error. Aligns column name with actual usage (single AI-pipeline error channel across vs-research-venues + vs-compile-summaries + vs-generate-deck since 4.7.2 / 4.8.2). All existing values preserved by RENAME. (APPLIED)
20260513000000_phase_4_9_port_start_over_rpc.sql                Phase 4.9-port: start_over_scout(target_scout_id uuid) RPC for Scout Settings Danger Zone. Transactional cascade-delete of vs_candidate_venues (photos cascade via FK), reset scout state back to sheet_prompt + clear research_error / derived_columns / sheet_storage_path / deck_order + strip brief_data idempotency timestamps. Keeps brief fields, project_id, generated_decks history, brief_data.uploaded_files. SECURITY INVOKER; GRANT EXECUTE TO authenticated. (APPLIED; pipeline_error rename via 20260514000001 in Phase 4.10.3-port)
20260512240000_phase_4_7_1_port_vs_venue_photos_bucket.sql      Phase 4.7.1-port: CREATE vs_venue_photos storage bucket (private) + 4 RLS policies (SELECT/INSERT/UPDATE/DELETE gated on is_producer_or_admin()). Bucket carries Venue Scout deck photos uploaded via PhotoUploadModal; reads use createSignedUrl(path, 3600). Distinct from the public venue_photos bucket reserved for HQ Core's master venues table. (APPLIED)
20260512230000_phase_4_6_port_shortlist_sync_trigger.sql        Phase 4.6-port: re-introduce vs_candidate_venues_shortlist_sync trigger at simplified shape (shortlisted false→true only). Matches HQ venues by website_url first, then case-insensitive name+neighborhood. SECURITY DEFINER; INSERTs new venues rows when no match. (APPLIED)
20260512220000_phase_4_5_port_research_error.sql                Phase 4.5-port: ALTER TABLE vs_scouts ADD COLUMN research_error text (persisted error channel for the EdgeRuntime.waitUntil + Realtime flow on vs-research-venues) (APPLIED)
20260512210000_phase_4_1_port_drop_orphan_helper.sql            Phase 4.1-port follow-up: DROP _jsonb_array_to_text_array (orphan helper from failed Phase 4.3.1's create_scout_with_brief, missed by the main port migration; surfaced by code-reviewer) (APPLIED)
20260512200000_phase_4_1_port_schema.sql                        Phase 4.1-port: DROP failed-attempt vs_briefs / vs_sourcing_rounds / vs_pitch_decks; reset vs_scouts to brief-inline shape with current_step state machine + generated_decks jsonb history; vs_candidate_venues simplified (single-round per scout); vs_venue_photos with ON DELETE CASCADE; RLS open to authenticated; vs_scouts in supabase_realtime publication with REPLICA IDENTITY FULL (APPLIED)
20260508150000_phase_3_11_pull_watchdog_2min.sql                Phase 3.11.1: pull-watchdog cadence 5min→2min, threshold 60min→5min (APPLIED)
20260508140000_phase_3_8_add_updated_at_to_pull_rounds.sql      Phase 3.8: updated_at trigger on ts_pull_rounds for watchdog heartbeat (APPLIED)
20260508130000_phase_3_8_vault_for_internal_secret.sql          Phase 3.8: Vault stores internal_api_secret (APPLIED)
20260508120001_phase_3_8_drop_dead_reeval_progress_column.sql   Phase 3.8: drop ts_pull_rounds.reeval_last_progress_at (APPLIED)
20260508120000_phase_3_8_cron_extensions_and_schedules.sql      Phase 3.8: pg_cron + pg_net + invoke_edge_function helper + six cron schedules (APPLIED)
```

The nine `phase_4_*` migrations that landed on `main` between Phase 4.1 (Scout Dashboard) and Phase 4.6 (Deck Prep) were repaired to `reverted` in the remote `supabase_migrations.schema_migrations` table on 2026-05-12 ahead of the Phase 4.1-port push. The actual table state is now whatever the port migration produced, regardless of the abandoned history.

## Next up

**Phase 5.6.2 (next list/detail inline-edit spec).** Drafted in Cowork from the two COWORK_SYNC carry-forward sections at the end of the 5.6.1 SHIPPED block. Two candidates: (A) **detail-page inline edit pattern** — make ProjectDetail / VendorDetail / ClientDetail / VenueDetail / PersonDetail directly editable inline (ClickPillCell for status, RecordCombobox for FK fields, `<input>` with onBlur save for free text), with a decision on whether the matching Edit form survives as bulk-edit / `/new` create only. Architectural call on per-field validation, RLS tightness, dirty-state replacement. (B) **list-view inline cell editing** — extend `DataTable` to take `onOpen` instead of `onRowClick` + every cell becomes a stop-propagation edit component, with a chevron-icon column at row end carrying the navigate affordance; mechanical but wide-touching (Projects + Tasks + Deliverables; Outlook entries stay tile + side-panel because the 12x4 grid tiles are too small for inline edit). Carry-forwards from earlier 5.6 framing also still in play if Jimmie wants them in 5.6.2 scope: real-cron verification of the three new hq-cron-* jobs once a deliverable hits the 3-day-out window + a task hits today's due-date + a project hits an event date; @-mention trigger wiring into notes_log / project notes (dispatch already handles the `mention` event_type, no surface fires it yet); decision on whether to drop the legacy `notify-admin-of-pending-user` function (the Phase 5.5 fallback). Carry-forwards from the 5.5 reviews listed in the Phase 5.5 paragraph above.

**Production wiring for Phase 3.8** still pending (GUCs, db push, 12 edge function deploys). Can happen in parallel since it's all out-of-band (no Netlify). Steps: set `app.supabase_url` and `app.internal_api_secret` GUCs in the Supabase SQL editor, then `supabase db push --linked` for the cron migrations, then `supabase functions deploy <name>` for each of the six cron functions, `ts-send-pull-notification`, plus re-deploys for `ts-pull-candidates` and any function importing `_shared/anthropic.ts`.

**Carried-forward cleanup queued (from cutover):**
- **vs-research-venues duplicate-invocation race.** CAS guards mask the symptom but the underlying race is real. Proper fix: Postgres advisory lock OR kickoff CAS on `brief_data.research_started_at`. Not blocking; both invocations produce valid scouts, just double-spend.
- **`MAX_PAUSE_CONTINUATIONS=1` cap on `callClaude`.** Conservative. If "no structured output" failures surface in prod, raise to 2-3 with a wall-clock budget instead of a continuation-count budget.
- **Real-cron test** of `ts-cron-scheduled-pulls` + watchdogs in production (requires GUCs set in Supabase SQL editor). Tied to the Phase 3.8 production wiring above.
- **Run `to_regclass('public.vs_briefs')` / `vs_sourcing_rounds` / `vs_pitch_decks` in Supabase Studio** to explicit-confirm the 4.1-port migration's drops landed (migration ledger says applied; visual confirmation in Studio is the belt-and-suspenders check).
- **TS wizard `Stepper` consolidation.** The 4a8a5c6 commit was excluded from the cutover (it depended on a `ui/Stepper.tsx` file from the dropped Phase 4.3.1 commit). TS wizard still imports the local `talent-scout/Stepper.tsx`. If we want a canonical `ui/Stepper.tsx` later, hand-write one and migrate the three TS wizard pages.

## How to update this file

Update on every meaningful merge to `main`:
1. Bump **Last updated** to today.
2. Bump **Latest commit on main** to the new HEAD hash + message.
3. Add the new commits to **Recent commits** (keep last 5).
4. Add new migrations to **Recent migrations** if any.
5. Move items between "What's live" / "What's NOT live yet" if behavior changed.
6. Add to "Known issues / drift" anything that needs a future cleanup pass.
7. Adjust **Current phase** + **Next up** when phase boundaries cross.
