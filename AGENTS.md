# Mirror NYC HQ

Codex reads `AGENTS.md` natively. The canonical project index for this repo lives in [`CLAUDE.md`](CLAUDE.md). Read that first.

If you're running Codex and prefer everything in this file, the canonical content is identical to `CLAUDE.md` for tone, conventions, schema/docs index, and working rules. Keeping the two files in sync by editing `CLAUDE.md` only avoids drift; this file is the pointer.

For live state see `CHECKPOINT.md`. For per-phase ship history see `docs/v1-changelog.md`. For decisions see `docs/decisions.md`.
