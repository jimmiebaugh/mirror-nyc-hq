# Edge Functions

Live in `supabase/functions/`. Each function is a directory with `index.ts`. Shared utilities live in `supabase/functions/_shared/`. Per-function settings (especially `verify_jwt`) are in `supabase/config.toml`.

Deploy via `supabase functions deploy <name>`. Don't skip JWT-verification settings: see `docs/auth-model.md` for the self-invocation pattern.

## Talent Scout

### `ts-pull-candidates(role_id, triggered_by)`
Gmail search → attachment download → PDF text extraction → link parsing → portfolio detection → Anthropic scoring. Chunked self-invoking pipeline (`BATCH_SIZE=8`) using `ts_pull_rounds.pending_candidates`. Writes one row per candidate to `ts_candidates` and one row per evaluation to `ts_evaluations`. Persists every attachment to the `candidate_attachments` bucket regardless of size (drift from source: see `docs/decisions.md`). `verify_jwt = false`.

**Phase 3.7.7: referral ingestion.** When the outer Gmail `From:` is `*@mirrornyc.com` (and not `jobs@`), `parseForwardedEmail` walks the body to find the original applicant via `From:` headers and `On <date>... <<email>> wrote:` reply-quote attributions, picking the deepest non-Mirror sender. Handles Gmail forwards, Apple Mail desktop + mobile, and `message/rfc822` forward-as-attachment. The candidate row gets the original applicant's identity; `is_referral=true` and `referrer_email=<manager>` are set. **Phase 3.7.8.16:** `extractManagerNote` walks every `---------- Forwarded message ---------` / `Begin forwarded message:` segment, captures every `@mirrornyc.com` sender's commentary (Mirror sigs stripped), and writes it to `ts_candidates.internal_notes`. The note is also folded into the FIRST evaluation via the `HIRING MANAGER NOTES:` block prepended to the candidate bundle.

### `ts-evaluate-candidate(candidate_id, overwrite_history?)`
Single-candidate eval/re-eval. Inserts a row into `ts_evaluations` and mirrors the latest fields onto `ts_candidates`. `overwrite_history: true` deletes prior `ts_evaluations` rows for the candidate before inserting: used by bulk re-eval (round-scoped or role-scoped) since prompt/scorecard changes invalidate prior evals. Phase 5.13.3: `_shared/buildClaudeEvalRequest.ts` `getClaudeSystemText` now injects `LOCATION_RULES` into any custom prompt that does not already contain "Location Considerations", ensuring the guardrails survive custom `evaluation_prompt` overrides. Also applies to `ts-pull-candidates` (shares the same `buildClaudeEvalRequest` helper).

### `ts-bulk-reevaluate(role_id, status_filter?)`
Re-eval the role's master pool. Chunked self-invoke. Updates `ts_roles.reeval_*` columns as it progresses; `reeval_last_progress_at` is the heartbeat read by the re-eval watchdog. `verify_jwt = false`.

PullDetail's "Re-Evaluate Pool" (round-scoped) uses a different pattern: parallel fan-out of `ts-evaluate-candidate` calls (concurrency=6) with `overwrite_history: true`, driven from the browser, not this function.

### `ts-generate-scorecard(title, job_description, hiring_priorities)`
Drafts a tiered scorecard via Claude. Called from the new-role wizard's step-3 page. Phase 3.11: each criterion now carries TWO describer fields: `full_points_rubric` (substantive 1-3 sentences for the per-candidate evaluator) and `summary` (≤ 14-word condensed recap for compact UI surfaces). One-shot, user-invoked, default `verify_jwt = true`.

### `ts-refine-scorecard(role_title, jd, hiring_priorities, criteria, ...)`: Phase 3.10
Refinement pass over a user-edited scorecard. Two call sites: the new-role wizard's step-3 page (when scorecard is "dirty since last AI pass") and the Edit Role page's RoleSettings (when scorecard has unrefined edits). Sends the current criteria + role context through Claude with `scorecardRefinementPrompt`, which preserves every concept/principle the user provided and standardizes `name` (short noun phrase), `full_points_rubric` (1-3 sentences of concrete evaluator-facing signals), and `summary` (≤ 14-word recap for compact UI surfaces; Phase 3.11). Two server-side guarantees: (1) **dead-criterion drop**: entries with `weight=0` OR with both `name` and `full_points_rubric` empty/whitespace are filtered before the prompt ever runs; the response includes a `removed_count` so the UI can surface it; (2) **defense-in-depth merge**: `tier`, `weight`, `is_disqualifier`, and `is_manual` are restored from the user's filtered input regardless of model output, so the model can never silently overwrite scoring decisions. Output count = filtered input count. Frontend re-sorts each tier highest-weight first after the refine. One-shot, user-invoked, default `verify_jwt = true`.

### `ts-final-review(role_id, top_n?, triggered_by?)`
Comparative final review across the master pool. Returns `{ final_review_id }` immediately; AI work runs in the background via `EdgeRuntime.waitUntil` and streams progress through `ts_final_reviews.step_progress` (Realtime: FinalReviewLoading subscribes). Produces `final_rankings` jsonb (`[{candidate_id, final_rank, final_tier, rationale, recruiter_note, final_overview}]`) + `pool_summary` text. Min 3 candidates; HARD_CAP=50 by total_score for context-window safety. Two-attempt JSON parse retry. Hyphen-tolerant candidate_id matching. Uses `callClaude('talent_scout', ...)`. `verify_jwt = false`.

### `ts-packet-generate(pull_round_id, top_n?, include_fast_track?)`
Round-scoped candidate review packet. Renders cover (Pulled / In Pool / Auto-Rejected / Fast-Track stats) + NYC-only top-15 comparison matrix + Fast-Track / Borderline / Other Recommended writeup pages + per-candidate title + email + attachment merge. Uploads merged PDF to the `packets` Storage bucket, updates `ts_pull_rounds.packet_*`, emails the role's hiring manager from `jobs@mirrornyc.com`. Tier subtotals render an em dash (U+2014) when `score_breakdown` is empty (Phase 3.6 Q4). `verify_jwt = false`.

### `ts-final-review-packet(final_review_id, top_n?, include_fast_track?, include_all?)`
Review-scoped packet for a completed `ts_final_reviews` row. Renders cover (Pool Size / Top Recs / Strong / Backup) + Pool Summary + full Rankings table + Top / Strong / Backup writeup pages with Not Recommended footnote + per-candidate pages. Uploads to `packets`, updates `ts_final_reviews.packet_*`, emails the hiring manager. Defaults `include_all=true` from the FinalReviewDetail UI; FinalReviewDetail's "Include Fast-Track pages" toggle controls `include_fast_track`. `verify_jwt = false`.

#### Shared infrastructure: `_shared/packetRender.ts`
~50% of source's two packet generators (CloudConvert HTML→PDF / DOCX→PDF, BASE_CSS, htmlDoc, MIRROR_LOGO_SVG, candidate title + email + packet divider renderers, Storage attachment fetcher, pdf-lib upload helper, Gmail send-with-attachment helper) lives here. HQ-specific: attachment bytes come from the `candidate_attachments` Storage bucket (Phase 3.4 persists everything on initial pull): no Gmail re-fetch at packet time. Email send uses the service account's `gmail.send` scope (added to `gmailServiceAccount.ts` SCOPES list in 3.6).

External dependency: `CLOUDCONVERT_API_KEY` Supabase secret (paid service). Packet volume is low (5-20/month) so the spend is negligible.

### `ts-send-pull-notification(role_id, pull_round_id)`: Phase 3.9
Emails the role's hiring manager when a pull completes. Tallies `ts_candidates` for the round (`fast_track`, `interview`, `consider`, `reject`), composes a plain-text body via `_shared/sendEmail.ts` and sends via the service account's `gmail.send` scope. Called fire-and-forget from `ts-pull-candidates` at every `status='complete'` write (chunked finalize, dedupe-clears-pending-to-zero, and zero-results paths). Failures are logged, not surfaced: a notification outage shouldn't fail the upstream round. Standalone in 3.9; folds into `notifications-dispatch` in Phase 5 alongside in-app bell notifications. `verify_jwt = false`.

## Talent Scout cron + watchdog (Phase 3.8)

All six are scheduled via `pg_cron` in `20260508120000_phase_3_8_cron_extensions_and_schedules.sql`. Each accepts an empty body and is invoked by `public.invoke_edge_function(fn_name, body)` (SECURITY DEFINER, reads `app.supabase_url` + `app.internal_api_secret` GUCs). All use `requireInternalOrUserAuth`. Cadences are in `docs/cron-jobs.md`.

### `ts-cron-scheduled-pulls`
Walks every `ts_roles.status='open'` AND `auto_pull_schedule != 'off'` row, computes hours-since-last-pull from `max(ts_pull_rounds.started_at)`, and self-invokes `ts-pull-candidates` for any role past its interval. Skips roles with an in-flight `running` round. Per-role failure logs but doesn't abort the loop.

### `ts-cron-pull-watchdog`
5-minute stall threshold (Phase 3.11.1, was 60). Reads `ts_pull_rounds` rows with `status='running'` AND `updated_at < now()-5m`, updates to `status='failed'` with a CAS guard (`.eq('status','running')`) so a late completion isn't overwritten. The pipeline's per-candidate row update means `updated_at` = "last candidate completed at"; a single candidate hanging >5 min is always a stall regardless of total pool size. Cron cadence bumped from every 5 min to every 2 min so detection lands within 5-7 min of stall onset.

### `ts-cron-reeval-watchdog`
30-minute stall threshold. Reads `ts_roles` rows with `reeval_status='running'` AND `reeval_last_progress_at < now()-30m`, updates to `reeval_status='failed'`.

### `ts-cron-final-review-watchdog`
20-minute stall threshold. Reads `ts_final_reviews` rows with `status='generating'` AND `generated_at < now()-20m`, updates to `status='failed'` with an `error_message` explaining the watchdog flag.

### `ts-cron-storage-cleanup`
Three-pass cleanup per `docs/cron-jobs.md`. Uses `STORAGE_BUCKET` from `_shared/attachmentStorage.ts`. Storage `.remove()` errors log and continue: a failed Storage delete leaves an orphan file but never blocks the row delete. Cron-only; no UI trigger.

### `ts-cron-monthly-spend-reset`
Resets `global_settings.anthropic_spend_current_month_usd=0` and `cap_alert_sent_this_month=false`. Logs the previous spend value for audit. **Phase 5.15:** after the global_settings reset, prunes `public.anthropic_call_log` rows older than 12 months (`lt("created_at", now - 365d)`). Prune runs AFTER the reset so a prune failure cannot block the cap-alert re-arm; prune errors are warn-logged and non-fatal (next month's run catches up). Response summary now includes `call_log_pruned_count` + `call_log_prune_cutoff`.

## Venue Scout: Phase 4

- `vs-parse-brief({ scout_id, storage_path })` (Phase 4.3-port rebuild; Phase 4 Revision field widen; Phase 5.12.2 server-side cities resolution; Phase 5.12.5 array-shape audience/vibe + canonical venue_types filter + ideal_features tag tightening + re-parse guard; Phase 5.12.10 venue_types DB-driven via `getVenueTypesCanonicalSet`; static schema description references "the canonical venue-types list provided in the user message"; live list rides in per-call user message; `sanitizeVenueTypesAgainstCanonical` now takes `canonicalSet` param with "industrial warehouse" combined-phrase gate on BOTH "Industrial" AND "Warehouse" in the runtime set): downloads a brief PDF from the `briefs` bucket and parses it via `callClaude('venue_scout', ...)` with a forced `submit_brief` tool call. Returns `{ parsed_fields }` for the Brief page to merge into form state. **Phase 4 Revision - Intake:** the `submit_brief` tool schema + sanitizer widened to also return the venue-side / event-detail fields the 3-step intake collects (`install_dates`, `strike_dates`, `activations_count`, `objectives`, `target_audience`, `vibe_aesthetic`, `target_neighborhoods`, `venue_types`, `ideal_features`) when the PDF states them. Producer judgment calls (priority toggles, strict-neighborhood flag, square-footage sliders) are intentionally NOT parsed. **Phase 4 Revision pass 2 (deployed v16):** `objectives` is constrained to short tag-style phrases -- the tool schema description plus a dedicated system-prompt rule push the model toward 1-5 word phrases (one per array item), and a `sanitizeObjectives` post-parse pass splits joined items on `; ` / ` and `, drops paragraph-length items (>60 chars), and trims/dedupes so the Brief `TagInput` receives clean tags. **Phase 5.12.2:** a top-level `resolveOrCreateCity(sb, rawName, scoutId)` helper runs after `sanitizeParsed` with a 6-step alias-first ladder. (1) Trim the raw input. (2) Strip a trailing 2-letter state suffix via `/,\s*[A-Za-z]{2}\s*$/` (so "Dallas, TX" -> "Dallas"). (3) Lookup `city_aliases` on trimmed; if hit, return the joined canonical city's name. (4) Lookup `city_aliases` on state-stripped (only if step 2 changed the string). (5) Lookup `cities` on trimmed. (6) Lookup `cities` on state-stripped. If everything misses, fetch `vs_scouts.created_by` and INSERT a new `cities` row using the state-stripped value (so the lookup never accumulates alias-style "City, ST" entries). Plain INSERT + 23505 race-loser re-read; the `LOWER(name)` expression unique index can't be targeted via supabase-js `onConflict`. **Alias-first ordering is load-bearing:** a polluting cities row (auto-created by an earlier version of this helper that did direct-cities-first) cannot short-circuit canonicalization; the alias lookup ALWAYS wins when both an alias and a cities row exist for the same string. Best-effort error posture: any failure along the way logs `[vs-parse-brief]` + returns null + the function falls through to the raw trimmed string. The frontend RecordCombobox in BriefVenue reconciles against the cached lookup on mount (and the `useCityAliases` hook surfaces alias rows in the picker) as the second line of defense. Helper lives top-level in the function alongside `stripStateSuffix`, `resolveAlias`, and `resolveCity`; lift to `_shared/citiesLookup.ts` only if a second consumer surfaces. **Phase 5.12.5 tightens five output shapes on the same forced-tool surface.** (1) `target_audience` + `vibe_aesthetic` flip from `string` to `array of string` in the tool schema (one demographic / aesthetic per item, matching the tag-array shape of `objectives` / `target_neighborhoods` / `venue_types` / `ideal_features`); SYSTEM gains two parallel rules (mirrors the existing `objectives` rule per `feedback_tool_choice_collapse`); a new `sanitizeAudienceTags` shares the parameterized `sanitizeTagArray(raw, maxLen)` body with `sanitizeObjectives` (60-char cap, same split / dedupe shape). (2) `venue_types` schema description pins to the canonical list inline via `CANONICAL_TYPES.join(", ")` (imported from `supabase/functions/_shared/venueTypes.ts`); SYSTEM gains one rule; a new `sanitizeVenueTypesAgainstCanonical` runs every emitted entry through `canonicalizeType` (split on `/`, `,`, `;` per part; combined-phrase recovery for "industrial warehouse" → both `Industrial` and `Warehouse`); drops non-canonical entries with a single `console.warn` enumerating the drop list when non-empty. 5.12.10 swaps the hard-coded canonical array to DB-driven. (3) `ideal_features` schema description tightens to 1-4 word tag shape with concrete example tags; SYSTEM gains one rule; a new `sanitizeFeatureTags` shares `sanitizeTagArray` with a 40-char per-item cap (tighter than `sanitizeObjectives`' 60-char cap; the canonical tag set tops out around 30). (4) `sanitizeObjectives` refactored into the parameterized `sanitizeTagArray(raw, maxLen)` body with thin wrappers (`sanitizeObjectives = sanitizeTagArray(_, 60)`, `sanitizeFeatureTags = sanitizeTagArray(_, 40)`, `sanitizeAudienceTags = sanitizeTagArray(_, 60)`); single body, three callers. (5) **Re-parse guard** for `client_name` + `event_name`: after `sanitizeParsed` and before `resolveOrCreateCity`, the function `SELECT`s `vs_scouts.client_name` + `event_name` and drops the corresponding key from `parsed_fields` when the scout's column is already non-empty; logs a `[vs-parse-brief] re-parse guard:` warn line per skipped field (with the scout's current value truncated to 40 chars for traceability). Producer is the source of truth; the BriefEvent intake gates the upload card behind these required fields, so a no-guard parse on an empty-state scout is the legitimate first-parse case. Defense-in-depth: `briefForm.ts` `applyParsedFields` also predicates the `client_name` / `event_name` merge on `!state.<field>.trim()` so the client-side path is guarded too. Best-effort posture on the guard's `SELECT`: any error warn-logs + falls through to today's behavior (parser fills both fields). **Audit, no fix needed:** `vs-parse-brief` is synchronous end-to-end (no kickoff RPC, no `EdgeRuntime.waitUntil`, no `WORK_TIMEOUT_MS`, no async work after the 200 response); the Phase 5.12.4.2 `IN_FLIGHT_GRACE_MS > WORK_TIMEOUT_MS + 60s buffer` fix shape does not apply. A producer refresh during in-flight parse wastes one Claude call (~$0.01-0.04) and mutates no DB state. User-invoked synchronous, `verify_jwt = true`. Replaces the failed-attempt vs-parse-brief in the deployed-function slot; no separate cutover deletion needed.
- `vs-generate-brief-overview({ scout_id })` (Phase 4 Revision - Intake; v2 = pass 3; Phase 5.12.5 array-shape audience/vibe in the hash + pure-JS budget formatter; Phase 5.12.13.4 (`1a01d3e`): SYSTEM_PROMPT rewrite from short bullet rule list to `<voice>` / `<must_not>` / `<rules>` / `<examples>` structure modeled on `ABOUT_VENUE_SYSTEM`'s pattern; hierarchy inverts from "stay faithful to facts" to "creative extrapolation is welcome" with hard-fact lockdown moved to `<must_not>`; `<voice>` block frames 5 structural beats as illustrative-not-checklist (post-smoke amendment 7); `<rules>` carries output mechanics including active-verbs rule with stop list, vary-sentence-openings rule, closing-one-clean-clause rule (three post-smoke amendments folded into the same squash); three-anchor `<examples>` block embeds cleaned A-List Salon + Vaseline Oasis (base spec, em dashes stripped) + Jimmie's manual edit of a first-smoke Cooper Flagg generation (post-smoke amendment 9; verbatim use because the edit complies with every `<rules>` line and models the active-voice + varied-openers + clean-closing patterns the amendments teach); user-message construction reshapes from a flat `BRIEF FIELDS` key/value dump to three labeled blocks: BRIEF FACTS (key/value for hard-fact fields where precision matters), BRIEF CONTEXT (paragraph-shaped segments joining voice-driving arrays into prose the model can weave), PRODUCER NOTES (`additional_notes` free-text as discrete block); `callClaude` call shape byte-unchanged; `buildStub` fallback untouched; full prompt body lives in the function source; two deploys this ship (base on 2026-05-24, post-smoke amendments on 2026-05-25; both out-of-band)): generates the Event Overview paragraph for a Venue Scout brief. Reads the scout's brief fields (named columns + the Phase 4 Revision `brief_data` keys), calls `callClaude('venue_scout', ...)` for a 3-5 sentence overview in Mirror's voice (no tools, plain-text response; voice rules from `anti-ai-writing-style.md` baked into the system prompt), then **atomically** writes `event_overview` + `brief_data.overview_source_hash` back to the scout in a single UPDATE and returns `{ event_overview, overview_source_hash }`. **Trigger model (pass 3):** the primary trigger is the Submit Brief click in `BriefVenue.tsx`, which invokes this only when the overview is empty, has no stored hash, or the stored `brief_data.overview_source_hash` no longer matches a hash freshly computed from the brief fields. `BriefReport.tsx` no longer auto-fires on report entry; its empty-state Generate button and the Regenerate link both re-invoke unconditionally. The hash (`computeOverviewSourceHash`: a 16-char SHA-256 prefix over the 15 overview-driving fields, arrays sorted, empty-to-null normalized) is computed identically client-side in `src/lib/venue-scout/briefForm.ts` and server-side here; the two MUST stay in lockstep (same field set, same canonical form, same object key order). On a Claude failure or empty response, falls back to a deterministic stub (`"${event_name} for ${client_name} in ${city}. Live ${live_dates}."`) and persists that (with the hash) so the producer always lands on a non-empty, editable overview. **Phase 5.12.5 updates the hash + prompt display block.** (a) `hashAsStringArray` extends to coerce a single string into a single-element array (defensive layer for future hand-edits to `brief_data`; the § 5 backfill migration retires legacy strings at deploy time). `computeOverviewSourceHash` now reads `target_audience` + `vibe_aesthetic` via `hashAsStringArray(...).sort()` (mirrors `objectives` / `target_neighborhoods` / `venue_types` / `ideal_features`); object key order stays identical to the client-side hash in `src/lib/venue-scout/briefForm.ts`. (b) The `Budget: $X` line in the user-message context block routes through `formatBudgetForPrompt` from new `_shared/formatBudget.ts` (pure-JS comma formatter) so the prompt display string never depends on V8's `Number.toLocaleString("en-US", ...)` ICU output. Closes `code-observations.md` Edge #2 (silent ICU parity risk on the budget display). Hash itself was parity-clean post-5.1 (uses `hashAsNumberOrNull` on raw `scout.budget`; no formatted string enters the hash); the Phase 5.12.5 helper closes the remaining display-side surface. User-invoked synchronous, `verify_jwt = true` (explicit `config.toml` entry, matches the vs-parse-brief sibling pattern). No self-invoke.
- `vs-parse-sheet({ scout_id, storage_path })` (Phase 4.4-port rebuild + Phase 4.10.3-port AI-strip + parser widen): parse uploaded venue sourcing sheet (XLSX / CSV / PDF) from the `sourcing_sheets` bucket. XLSX / CSV parsed via `npm:xlsx@0.18.5` + a `pick()` fuzzy header matcher. PDF parsing is intentionally naive (returns 0 venues; frontend routes to empty-sheet error). INSERTs matched rows into `vs_candidate_venues` with `source: "sheet"`. Updates `vs_scouts.sheet_storage_path`. **Phase 4.10.3-port:** pick chain widened to include `website_url` + `capacity` columns (both dropped at parse time pre-4.10.3) plus expanded keyword variants per field (`borough`, `web`, `homepage`, `pax`, `headcount`, `square footage`, `amenities`, etc.). website_url passes through `sanitizeWebsiteUrl` at parse time (producer input is trusted; no HEAD check). **AI enrichment moved out of this function in 4.10.3-port** and consolidated into `vs-research-venues` Phase A. Function now returns `{ count }` in sub-second response time. User-invoked synchronous, `verify_jwt = true`. Replaces the failed-attempt vs-parse-sheet in the deployed-function slot.
- `vs-research-venues({ scout_id })` (Phase 4.5-port rebuild; major iteration through 4.10.3 / 4.10.5 / 4.10.6; Phase 5.12.1 HQ pool + race fix + cross-rail dedupe; Phase 5.12.3 cross-rail dedupe via points ladder + log reason; Phase 5.12.3.1 HQ pool fit filter; Phase 5.12.4 about_venue-driven fit rescue + pipeline placement refactor; Phase 5.12.4.2 kickoff grace bump 360_000 -> 420_000 + final UPDATE CAS on `pipeline_error IS NULL`; Phase 5.12.5 array-shape audience/vibe via `asTagArr`; Phase 5.12.7 Feature B research-time `hq_pool` enrichment; Phase 5.12.10 venue_types DB-driven via per-request `getVenueTypesCanonicalSet` fetch; Phase 5.12.13.3 Retail-bias soften + search-scope flexibility: Phase B `SYSTEM` block (lines 1811-1818) drops the "Strongly prioritize" Retail directive + the TYPE CONSTRAINT block's added "prefer Retail when..." sentence + kills the em dash on line 1814 (rewrites to "Mirror often books..." + "Equal-weight the other canonical venue types..."); Phase B userMsg "Return X net-new" line at 2320 drops "Weight... heavily in ranking_score" and replaces with "Rank against the brief's stated criteria; do not over-weight any single venue type when scoring."; `requirementLines` strict-neighborhoods ternary lockstep'd in BOTH call sites (Phase B at 2152-2156 + rescue at 1433-1437) tightens both branches (strict allows 1-2 adjacent; non-strict explicitly allows adjacent + reasonable radius); `requirementLines` venueTypes "must be one of" lockstep'd in BOTH call sites (Phase B at 2159-2161 + rescue at 1440-1442) softens "must" -> "should" with 1-2 outliers permitted when very-high-rank justifies the type mismatch; one-time prompt-cache invalidation on Phase B SYSTEM accepted; Phase 5.12.13.1 `key_features` tag-shape rewrite on the shared `FILL_TOOL.key_features` schema in `_shared/venueFill.ts` (description rewrites to 2-10 evergreen 1-4 word tags + `items.maxLength: 35` + `minItems: 2` + `maxItems: 10`) AND on Phase B `TOOL.venues[].key_features` (same shape mirror); new `sanitizeTagShape` import from `_shared/venueTypes.ts` chains on the outside of every existing `stripPlaceholders` call for `key_features`: Phase A `enrichSheetVenue` (line ~270), Phase A `enrichHqPoolForResearch` (NEW key_features write gate; `key_features` added to the HQ-pool SELECT column list + the `HqPoolRow` type; gates strictly on `existingFeatures.length === 0` so HQ-curated tag data stays authoritative and the AI emit lands only as a fill, never an overwrite or additive), Phase B parse path inside `venuesToInsert.map`; Phase A `enrichSheetVenue` + `enrichHqPoolForResearch` thread `canonicalSet.names` into `buildFillUserMsg` AND post-emit `sanitizeMultiAgainst`; Phase B SYSTEM + TOOL.venues[].type.description swapped to static "the canonical venue-types list provided in the user message"; canonical list rides in the per-call user message via a `CANONICAL VENUE TYPES (CONSTRAINT...)` line appended to `userMsgBriefPrefix`; Phase B post-emit replaces the local `canonicalizeMulti` helper with `sanitizeMultiAgainst(typeRaw, canonicalSet.names)` (venue_type lands null when nothing resolves); the local `canonicalizeMulti` helper is deleted): AI venue work in two parallel phases inside one `EdgeRuntime.waitUntil`. **Phase 5.12.1 kickoff** is now an atomic SECURITY DEFINER RPC: `sb.rpc("vs_research_try_acquire_kickoff", { target_scout_id, grace_seconds: IN_FLIGHT_GRACE_MS/1000 })`. The RPC uses `pg_try_advisory_xact_lock` + a same-transaction read-and-write of `brief_data.research_started_at`, replacing the pre-5.12.1 inline check-then-write at lines 602-621 that race-condition-failed live on concurrent invocations (code-observations Edge #1; confirmed token double-spend). `false` -> 200 `{ ok, skipped: "in_flight" }`. RPC errors return 500 (kickoff couldn't be evaluated). **Phase 5.12.1 HQ Venues pool** runs immediately after the kickoff RPC, inside the request handler before the existing-venues SELECT: `loadHqVenuesIntoPool(sb, scout_id, scout, briefData)` queries `venues` where `city ilike scout.city` and INSERTs each kept row as `vs_candidate_venues` with `source='hq_pool'` + `linked_venue_id` pre-set + `venue_overview` pre-filled from `venues.about_venue`. The same `SELECT FROM vs_candidate_venues` that built the EXISTING VENUES block then automatically picks up the hq_pool rows (no extra SELECT). Best-effort: any SELECT or per-row INSERT failure logs + continues; the scout still runs Phase A + B without the pool. Skip when `scout.city` is empty. **Phase 5.12.3.1 fit filter:** `loadHqVenuesIntoPool` rewritten from "every city-match in, soft signals as sort keys only, cap at 10" to filter + score + threshold + vetoes. Pipeline order: SELECT city-matches -> SELECT existing hq_pool linked_venue_ids for this scout -> filter already-linked venues OUT (idempotency moved UP from post-sort so newly-qualifying venues on re-run can land additively even when the previous run filled the cap) -> compute `applicability` once via `computeApplicability(briefData)` -> score each remaining venue via `scoreVenueFitForBrief(hq, briefData, applicability)` per the spec § 8.2 points table (venue_type 30 / neighborhood 20 / ideal_features 5/match cap 25 / sq_ft 15 / capacity 10; total max 100) with out-of-band vetoes (`wrong_type`, `below_sq_ft_min`, `above_sq_ft_max`, `below_capacity`) + aggregate-gap veto on `unscored > applicable / 2` -> filter to `decision === keep` -> sort by score DESC then alphabetical ASC -> slice top 10 (defensive ceiling; per-run, not lifetime) -> INSERT loop. Threshold defaults to 40 (`>=` keeps). Helpers are PURE (no scout_id, no logging, no IO); caller owns telemetry. New `[hqPoolFilter]` log line per SCORED venue (already-linked rows skip both scoring and per-row logging); summary log line extended to `inserted=W insert_failed=X over_cap=Y dropped_below_threshold=Z vetoed=A already_linked=B of N city-matches (C applicable brief signals)` with the accounting identity `W + X + Y + Z + A + B === N` (unconditional `[hqPoolFilter:accounting]` warn fires on mismatch, in production too; cheap arithmetic check defending against silent counter drift). Pre-5.12.3.1 `skipped` counter renamed to `insert_failed` for clarity. Per-venue try/catch around the scorer treats throws as `drop_veto` so one bad row doesn't sink the pool. Existing 5.12.1 idempotency rule preserved (already-linked hq_pool rows skip without re-scoring); new behavior on re-run is that venues that didn't clear threshold the first time can land additively if the brief edit makes them newly qualify. A venue that cleared on run 1 but would not on run 2 stays in the matrix (producer-edits-survive-across-runs guarantee). `fit_meta` jsonb persistence + producer-visible FitMetaIndicator Popover DEFERRED for v1; carry-forward queued. **Phase A:** SELECT `vs_candidate_venues WHERE scout_id=? AND source='sheet'`. For each row, `callClaude('venue_scout', { tool: fill_venue, tool_choice: auto, web_search_20250305: max_uses=2 })`. Per-row patch guards prevent overwriting producer values; `address` + `neighborhood` fillable in Phase 4.10.6-port. URL fallback via `findBestSearchResultUrl` against the call's own web_search results. Per-row failures tolerated (vs-compile-summaries Pass 1 is the backstop). **Phase B:** AI-driven new-venue research via `callClaude('venue_scout', { tool: submit_research, tool_choice: auto, web_search_20250305: max_uses=4 })`. Returns `{ ok, scout_id }` immediately; AI work runs in `EdgeRuntime.waitUntil`. On completion, sanitizes venues. **Phase 5.12.1 cross-rail dedupe** runs after `cleanVenues` is built but BEFORE `validateWebsiteUrls` / `findVenueWebsite` (both expensive; HEAD requests + per-venue Claude call). **Phase 5.12.3.1 widening:** the pool now queries the full HQ `venues` table for `city ilike scout.city` (mirrors `loadHqVenuesIntoPool`'s SELECT), NOT just the seeded `source='hq_pool'` subset. Pre-5.12.3.1 the cross-rail dedupe only saw the seeded subset, but the 5.12.3.1 fit filter can legitimately veto an HQ venue (wrong_type / aggregate_gaps / etc.) that Claude's research then surfaces with richer data. Without widening, the dedupe pool was starved (live signal: scout 7ad1d004 had only "The Pavilion" seeded; Platform LA wrong_type-vetoed; Claude's "Platform Culver City" landed unlinked at an address-identical match). The function also reads existing `source='hq_pool'` linked_venue_ids to know which HQ ids were seeded. Runs `findVenueDedupeMatch(candidate, hqPool)` from `_shared/venueDedupe.ts` against the widened pool. **Phase 5.12.3** ladder is now the points-based scorer (name 60/25/0 + address 50/20/0 + website 40/20/0 + city +10 conditional; threshold 60); the drop console log line `[vs-research-venues] ... drop research row "..." (collides with seeded hq_pool <id>; <reason>)` carries the scoring reason for debug. **Address-differ VETO fires here too** (catches same-city same-name different-address collisions). **Phase 5.12.3.1 seed-then-drop:** when a match resolves to an HQ id that's NOT in the seeded set, the function INSERTs that HQ venue as `source='hq_pool'` now (bypassing the fit filter for this venue; the dedupe match is independent evidence of relevance), merging Claude's enrichment where HQ has nulls/empties (features when HQ.features is empty array; size_sq_ft via HQ.total_sq_ft ?? HQ.square_footage ?? cand.size_sq_ft; capacity ?? cand.capacity; website_url ?? cand.website_url; HQ-canonical name / address / neighborhood / venue_type / about_venue still win) plus Claude's per-brief judgments (derived_attrs, recommendations, considerations, rank) so the matrix row carries the better data Claude found, then drops the research row. The seeded-set is updated in-loop so a later candidate matching the same HQ row hits drop-only. Seed-then-drop INSERT failure keeps the research row in the matrix as the safety net (vs-generate-deck's pushVenuesToHq catches the dedupe at deck time). New `[vs-research-venues] ... seed-then-drop "<cand>" -> hq_pool <hqId>` log line per seeded match; summary log line extended with seed-then-drop counters. Dropped research rows never reach `vs_candidate_venues` so there's nowhere to land a `dedupe_meta` value (the scoring reason rides the drop log line). Filtered rows never reach URL validation. Best-effort: a SELECT failure on the pool read warn-logs + continues (Claude's EXISTING VENUES block already pushed away from duplicates; HQ-side push in `vs-generate-deck` still matches at deck time). Then INSERTs canonicalized + HEAD-validated venues into `vs_candidate_venues`. **URL fallback chain (Phase 4.10.6-port):** for any venue still with null `website_url`, run `findVenueWebsite(app, { name, address, city })` (targeted per-venue Claude call) in parallel. HEAD-validate before persist. Writes `derived_columns` + `current_step='sourcing_report'` + `status='in_progress'` + `pipeline_error=null` to `vs_scouts`. On any failure (Claude error, no tool output, empty sanitized result, insert error, 360s timeout), writes `status='failed'` + `pipeline_error=<message>` via `writeFailure` with `.eq("current_step", "researching")` CAS guard so a failure can't overwrite a concurrent success. Idempotent on page refresh via `brief_data.research_started_at` 420-second grace window (`IN_FLIGHT_GRACE_MS = 420_000`; 360s timeout + 60s buffer per Phase 5.12.4.2 Fix 1; enforced atomically by the kickoff RPC). Model: `claude-sonnet-4-6` (Phase 4.10.5-port pivot; `claude-sonnet-4-5` wasn't on `web_search_20260209`'s supported model list, and the older `web_search_20250305` tool has lighter per-turn context overhead than the dynamic-filter version). Pause_turn continuation handled in `callClaude` wrapper (max 1 cycle). **Phase 4 Revision - Intake:** the Phase B `userMsg` construction extracts the venue-side `brief_data` keys collected by the new 3-step intake (`target_neighborhoods` + `strict_neighborhoods_only`, `venue_types`, `ideal_features`, `priority_location` / `priority_cost`, `sq_ft_minimum`, `sq_ft_min` / `sq_ft_max`, `activations_count`) and injects them as a "VENUE REQUIREMENTS" block, plus `target_audience` / `vibe_aesthetic` lines alongside `Overview`. User-message-only change; SYSTEM stays frozen per `feedback_tool_choice_collapse`. **Phase 5.12.4 about_venue-driven fit rescue (third lane).** After the 5.12.3.1 deterministic fit filter decides the first tier (deterministic keeps INSERT as `source='hq_pool'` immediately) and BEFORE Phase B runs, a new `rescueHqPoolFitVetoedVenues` helper sends every soft-fit-vetoed (drop_veto on `wrong_type` / `aggregate_gaps`) and drop-below-threshold HQ city-pool venue's structured fields + `about_venue` paragraph to Claude via a forced `submit_fit_evaluation` custom tool (`{ verdicts: array of { venue_id, keep, reason } }`) and INSERTs each keep-verdict venue as `source='hq_pool'` with `linked_venue_id` set (same row shape `loadHqVenuesIntoPool`'s deterministic-keep INSERT uses; recs / cons / rank / derived_attrs left null for Pass 1 thread (a) to fill at compile). Rescue input set: drop_below_threshold + drop_veto on wrong_type or aggregate_gaps only; EXCLUDE drop_veto on hard physical disqualifiers (below_sq_ft_min / above_sq_ft_max / below_capacity; no narrative can overcome physical bounds); EXCLUDE deterministic keeps; EXCLUDE already-linked venues (per the 5.12.3.1 producer-edits-survive-across-runs guarantee); EXCLUDE venues with null/empty `about_venue` (no rescue signal). Cap `RESCUE_INPUT_CAP = 20` venues per call; sort by `about_venue.length` DESC then name ASC so the highest-signal candidates win the slots. Claude call: model `claude-sonnet-4-6`, `max_tokens: 4000`, no web_search, forced `tool_choice: { type: "tool", name: "submit_fit_evaluation" }`. Two ephemeral cache breakpoints (`ttl: "5m"`): `FIT_RESCUE_SYSTEM` in the system content array (cached across every scout, every client) + the brief block in the first user content block (cached within a re-kickoff window for the same scout). Candidate venues block + final instruction stay uncached. `loadHqVenuesIntoPool` refactored to return `HqPoolLoadResult` (`{ hqRows, alreadyLinkedSet, applicability, scored }`) so the rescue helper consumes the same city pool + alreadyLinkedSet + applicability + scored entries without re-querying. Pure helpers stay pure. Pipeline placement: `loadHqVenuesIntoPool` stays in the synchronous handler path (returns `HqPoolLoadResult | null` to a closure-captured variable; handler returns 200 kickoff immediately after); inside `work()` Phase A launches async, rescue awaits concurrent with Phase A, the existing-venues SELECT + curatedExistingCount + targetNet + Phase B userMsg construction all moved INTO `work()` after rescue resolves so rescued + deterministic-keep hq_pool rows land in Phase B's EXISTING VENUES block, then Phase B runs, then `await phaseAPromise` before the final scout UPDATE. Phase B does NOT wait for Phase A. The Phase B userMsg template splits into a handler-built `userMsgBriefPrefix` (no dependency on existing/targetNet) plus the existing-venues block + targetNet-dependent Return line both built inside `work()`. Per-verdict apply path: hallucinated venue_id drops only that verdict; malformed verdicts drop individually; duplicate verdict for same venue ignores subsequent; per-row INSERT failure increments `insert_failed` counter; missing verdicts treated as drop. New `[hqPoolRescue]` telemetry: per-verdict log + summary line `candidates=N rescued=K dropped=M verdicts_missing=X insert_failed=Y` with accounting identity `rescued + dropped + verdicts_missing + insert_failed === candidates`; unconditional `[hqPoolRescue:accounting]` warn on counter drift; single short-circuit line `candidates=0 (no rescue needed)` when the eligible set is empty; single `claude failed: <error> (skipping rescue, deterministic pool stands)` warn on Claude failure. Failure mode: every rescue failure path is best-effort (timeout, 5xx, no tool_use block, malformed verdicts) — warn-log + skip rescue + continue research; deterministic pool stands; research never blocks on rescue. Composition with 5.12.3.1: deterministic fit filter decides first tier; cross-rail seed-then-drop is the second lane (unchanged; reads the post-rescue seeded set on its own SELECT, treats rescued rows as already-seeded matches via the existing `seededHqIds.has(matchedId)` branch); Claude about_venue rescue is the third lane. No `fit_rescue` column on `vs_candidate_venues` for v1 (rescued rows are indistinguishable from deterministic-keep hq_pool rows at the column level; the `[hqPoolRescue]` log is the only audit signal; producer-visible "why this venue surfaced" lever lives in the 5.12.3.1 deferred `fit_meta` carry-forward). User-invoked, `verify_jwt = true`. The Researching page Realtime-subscribes to `vs_scouts` for completion / failure. **Phase 5.12.5 array-shape audience/vibe consumption.** A new module-level `asTagArr(v: unknown): string[]` helper (next to the existing `asArr`) coerces both shapes: arrays pass through with trim + dedupe; a non-empty string returns as a single-element array (defensive layer mirroring `normalizeTagArray` on the client + `hashAsStringArray` in `vs-generate-brief-overview`). Both call sites flip: (a) the `rescueHqPoolFitVetoedVenues` brief block reads `targetAudience` + `vibeAesthetic` via `asTagArr` and interpolates with `value.length > 0 ? value.join(", ") : "(not set)"`; (b) the Phase B `userMsgBriefPrefix` reads the same fields the same way. SYSTEM stays frozen; no FILL_TOOL change; rescue brief block + FIT_RESCUE_SYSTEM untouched. Phase 5.12.5 introduces no other change to this function. **Phase 5.12.7 Feature B research-time `hq_pool` enrichment.** A new `enrichHqPoolForResearch(sb, scout_id, scout)` helper colocated near `runPhaseA` runs sequentially inside `work()` AFTER `await phaseAPromise` and BEFORE the final CAS-guarded UPDATE flipping `current_step='sourcing_report'`. It SELECTs every `source='hq_pool'` row whose `recommendations`/`considerations`/`rank` are still empty (deterministic-keep bare-insert rows from `loadHqVenuesIntoPool` AND rescue-keep inserts from `rescueHqPoolFitVetoedVenues`, since rescue's Claude call only emits keep/drop verdicts and does not patch per-brief judgment), then fires the SAME Pass 1 needsFill enrichment shape `vs-compile-summaries` uses: `buildFillUserMsg` + `FILL_SYSTEM` + `FILL_TOOL` byte-for-byte (with `aboutVenue` passed when `venue_overview` is populated so Claude sees the HQ CANONICAL ABOUT block + narrower research instruction), `tool_choice: auto`, `web_search_20250305 max_uses=2`. Batched in chunks of `PHASE_A_CHUNK_SIZE = 5`. Independent `ENRICH_TIMEOUT_MS = 60_000` deadline wraps the chunk loop so a slow enrichment leg cannot push the function past Supabase's platform wall before the final UPDATE commits; on cap-hit the in-flight chunk completes, remaining chunks skip with `timed_out` accounting, and the final UPDATE proceeds. Apply-side per-field guards mirror Pass 1 byte-for-byte (only fill empty recs/cons/rank; skip address/neighborhood/venue_type/website_url/size_sq_ft/capacity/key_features since hq_pool rows already carry canonical HQ values; skip derived_attrs since FILL_TOOL doesn't carry the scout's `derived_columns` IDs). Failure mode is load-bearing: every failure path (SELECT failure, Claude failure, missing tool_use block, UPDATE failure, per-row throw) warn-logs + skips; NEVER `writeFailure` (would CAS-no-op the final UPDATE per 5.12.4.2 Fix 2 and leave the scout stuck rendering bare-matrix without navigating off the Researching page). New `[hqPoolResearchEnrich]` telemetry line shape + `[hqPoolResearchEnrich:accounting]` accounting-identity warn shape mirror the 5.12.3.1 `[hqPoolFilter]` + 5.12.4 `[hqPoolRescue]` conventions. Compile-time Pass 1 `hq_pool` branch stays in place as defense-in-depth: predicate skips when fields populated (zero net cost per scout when research-time succeeded) but backfills any row this leg skipped or failed on. Cache shape reuses Pass 1's existing prompt-cache breakpoints (FILL_SYSTEM ephemeral 1h cache, shared across scouts). Does NOT slot-replace the failed-attempt `vs-start-sourcing` (different function name); that still queues for cutover deletion. **Phase 5.14 photo pre-population:** `HqVenueRowRaw` type extended with `photos: (string | null)[] | null`. Both venues SELECTs (`loadHqVenuesIntoPool` + seed-then-drop pool) extended to include `photos`. All three hq_pool INSERT paths extended to capture the new candidate id via `.select("id").maybeSingle()` and seed HQ photos: (1) `loadHqVenuesIntoPool` deterministic-keep -- INSERT loop collects `photoSeedCandidates`, `Promise.allSettled` fires `seedHqPhotosToVs` after the loop with `path=load`; (2) Phase B seed-then-drop -- same pattern after the dedupe loop with `path=seed_then_drop`; (3) `rescueHqPoolFitVetoedVenues` -- `photoSeedCandidates` collected during the verdict loop, `Promise.allSettled` fires after Step 8 (verdicts_missing) with `path=rescue`. `seedHqPhotosToVs` downloads from `venue_photos`, uploads to `vs_venue_photos` at `${scout_id}/${candidateId}/slot-${N}-${timestamp}.${ext}`, inserts `vs_venue_photos` row. Seeding fires only on fresh INSERTs (already-linked filter keeps idempotency). Best-effort throughout; never `writeFailure`. Telemetry: `[hqPoolPhotoSeed] scout=<id> path=<load|seed_then_drop|rescue> candidates_with_photos=N photos_seeded=M photos_failed=K`.
- `vs-compile-summaries({ scout_id })` (Phase 4.7.2-port rebuild; iterates through 4.10.1 / 4.10.4 / 4.10.5 / 4.10.6; Phase 5.12.0 Pass 2 convergence; Phase 5.12.1 hq_pool aware; Phase 5.12.4 Pass 1 hq_pool widening; Phase 5.12.4.2 kickoff grace bump 90_000 -> 300_000 + final UPDATE CAS on `pipeline_error IS NULL`; Phase 5.12.10 venue_types DB-driven via per-request `getVenueTypesCanonicalSet` fetch threaded into `buildFillUserMsg` AND the Pass 1 apply guard, which now uses `sanitizeMultiAgainst(f.venue_type, canonicalSet.names)` instead of the legacy `canonicalizeType` tokenizer; Phase 5.12.13.1 Pass 1 `key_features` apply guard chains `sanitizeTagShape(stripPlaceholders(f.key_features))` from `_shared/venueTypes.ts` (drops digits, > 4 words, > 35 chars, case-insensitive dupes) so backfilled key_features land in evergreen tag shape; Pass 2 inherits the `_shared/venueOverview.ts` label sync `Key features:` -> `Key features (tags):` in `buildOverviewUserMsgForVsRow` without a direct edit; Phase 5.12.13.3 redeploys to pick up the `_shared/venueFill.ts` FILL_SYSTEM cache invalidation (drops "Prefer Retail for ground-floor commercial / storefront / vacancy spaces." sentence from FILL_SYSTEM line 121); no direct edit in this function; Phase 5.12.13.2.1 redeploys to pick up the post-.2 smoke correction in `ABOUT_VENUE_SYSTEM` (cap widens 90-120 -> 100-150 + `<input_fields>` `Key features (tags)` tag-priority guidance); Phase 5.12.13.2 redeploys to pick up the `ABOUT_VENUE_SYSTEM` cap widen to 90-120 + 6 example input reshape in `_shared/venueOverview.ts` (Pass 2 consumes this prompt via `buildOverviewUserMsgForVsRow`): `<task>` block widens cap 90-110 -> 90-120 with the hard ceiling restated, `<output_format>` gains a length-check paragraph capping at 120 words, `<input_fields>` `Key features (tags)` line gains a tag-shape description body + line 64 em-dash sweep, and the 6 in-prompt example INPUT lines reshape from narrative `Key features:` to short tag-shaped `Key features (tags):` lists; all 6 example OUTPUTS stay byte-for-byte; one-time prompt-cache invalidation accepted; no direct edit in this function): two-pass venue compilation via `callClaude('venue_scout', ...)`. Pass 1 = `fill_venue` tool (backfills any pitched venue missing fields; populates `venue_type` / `website_url` / `address` / `neighborhood` / `size_sq_ft` / `capacity` / `key_features` / `derived_attrs` / `recommendations` / `considerations` / `rank` from training-knowledge + brief + producer notes + web_search; `canonicalizeType` + `validateWebsiteUrl` + `stripPlaceholders` + `findBestSearchResultUrl` URL fallback applied post-emission). **`FILL_TOOL` + `FILL_SYSTEM` + `buildFillUserMsg`** live in `_shared/venueFill.ts` (single source of truth shared with `vs-research-venues` Phase A). `tool_choice: auto` + `web_search_20250305` `max_uses=2` (Phase 4.10.5-port pivot). **Pass 1 source list (Phase 5.12.4 widening on top of 5.12.1):** the `needsFill` check fires for `source === "manual" || source === "sheet"` (recs / cons / rank / derived_attrs trigger; pre-5.12.4 behavior) AND for `source === "hq_pool"` (recs / cons / rank trigger only; derived_attrs trigger gated to manual+sheet because FILL_TOOL doesn't carry the scout's `derived_columns` IDs so Claude would invent keys that don't render in the matrix UI). The hq_pool branch also gates on `consArr.length === 0` so 5.12.3.1 cross-rail seed-then-drop rows (which already carry recs + cons + rank from Claude's research) do NOT trigger a wasteful re-fire; pure-fit-filter hq_pool inserts have all three null so the predicate fires correctly on the path that needs it. Apply-side per-field guards already preserve any populated field, so writes only ever land on empty columns. One explicit apply-side guard added in 5.12.4: the `derived_attrs` write now gates on `v.source !== "hq_pool"` so a recs-triggered Pass 1 fire on an hq_pool row never lands Claude's invented derived_attrs keys. `consArr` declaration hoisted to the top of the per-venue loop so it's available in both the `needsFill` predicate and the apply-side guard. `'research'` rows are already fully populated by Phase B and naturally fall through the OR list. **5.12.4 user-message extension:** the Pass 1 callsite passes `aboutVenue: v.source === "hq_pool" ? v.venue_overview : null` to `buildFillUserMsg`; the builder renders an HQ CANONICAL ABOUT block + narrower research instruction when `aboutVenue` is non-empty so Claude focuses on per-brief judgment (recs / cons / ranking_score) instead of re-researching identity / character / neighborhood. FILL_SYSTEM untouched per `feedback_tool_choice_collapse`. **Pass 2 (Phase 5.12.0 convergence)** = tool-less + `ABOUT_VENUE_SYSTEM` (the same evergreen HQ prompt) + `web_search_20250305` `max_uses=2` + `tool_choice: auto` + `max_tokens: 2000`. The system block ships as a content-array with a `cache_control: { type: ephemeral, ttl: "1h" }` breakpoint and the `prompt-caching-2024-07-31` + `extended-cache-ttl-2025-04-11` beta headers, so the long evergreen prompt is billed once per scout (cache write on venue 1, cache reads on venues 2+). Pass 2 user message is venue-data-only (no brief block; no considerations), built via `buildOverviewUserMsgForVsRow(merged, scout.city)` from `_shared/venueOverview.ts`; the optional `<producer_inputs>` block carries recommendations + producer notes when set. Reads `result.text.trim()` directly; empty -> shared `buildStub({ name, city })` fallback. **Pass 2 skip-when-prefilled (Phase 5.12.1):** if `source === "hq_pool" && venue_overview.trim().length > 0`, the Claude call is skipped entirely with a `pass=overview skipped (hq_pool prefilled)` log line. Mirrors the HQ-side write-when-blank rule: the hq_pool seed value (`venues.about_venue`) is canonical and should not be regenerated unless the producer explicitly blanks it. A blank/whitespace-only value triggers regen as a recovery path. The legacy `OVERVIEW_TOOL` + `OVERVIEW_SYSTEM` (forced `write_overview` custom tool, brief-bearing user message, considerations included) are gone. `feedback_tool_choice_collapse` no longer applies to Pass 2 (no forced tool); if voice drifts, the lever is `ABOUT_VENUE_SYSTEM`. Returns `{ ok, scout_id }` immediately; AI work runs in `EdgeRuntime.waitUntil`. On completion, writes `current_step='deck_prep'` + `status='in_progress'` + clears `pipeline_error`. On catastrophic failure (DB connection, 240s timeout, final UPDATE error), writes `status='failed'` + `pipeline_error=<msg>` via `writeFailure` with `.eq("current_step", "compiling")` CAS guard. Per-venue Claude errors logged + tolerated. Idempotency via `brief_data.compile_started_at` (300-second grace; `IN_FLIGHT_GRACE_MS = 300_000` per Phase 5.12.4.2 Fix 1: 240s timeout + 60s buffer). Sequential loop; **Phase 5.12.0 `Promise.race` ceiling bumped 180s -> 240s** as an intentional expected-case cap (NOT a worst-case buffer; arithmetic: ~40s for venue 1 + ~25s × N-1 subsequent venues with cache reads; worst-case all-manual scout can exceed). Lever order if real scouts tip over: (1) drop Pass 2 `web_search` `max_uses` 2 -> 1, (2) bump to 300s, (3) parallelize the per-venue loop. Model: `claude-sonnet-4-6` (callClaude wrapper default). User-invoked, `verify_jwt = true`. The Compiling page Realtime-subscribes to `vs_scouts` for completion / failure. **Phase 5.12.0 also removed the HQ Venues match-or-insert from this function**; the push relocated to the top of `vs-generate-deck` so the producer's last-mile edits on DeckPrep flow through.
- `vs-generate-deck({ scout_id })` (Phase 4.8.2-port rebuild; slide indices corrected in 4.8.3-port; Phase 4.10.6-port slide-layout + CAS-guard polish; Phase 5.12.0 HQ Venues push; Phase 5.12.1 dedupe helpers extracted; Phase 5.12.3 points-based ladder + `dedupe_meta` write; Phase 5.12.4.1 atomic kickoff RPC; Phase 5.12.4.2 kickoff grace bump 90_000 -> 240_000 + final UPDATE CAS on `pipeline_error IS NULL` + env-var check reordered before pushVenuesToHq; Phase 5.12.10 venue_types DB-driven via the consolidated `getVenueTypesCanonicalSet` read at handler entry (per OQ #3): the returned `{ names, idByName }` feeds BOTH `sanitizeMultiAgainst` (rejects non-canonical tokens) AND the `venue_venue_types` join inserts (`idByName.get(name.toLowerCase())`); pre-5.12.10 `ensureVenueTypesLoaded` lazy helper retired; Phase 5.12.13.1 amendment C extends `pushVenuesToHq` match-path + pre-linked-path write-when-blank from `about_venue` only to also cover `features` + `total_sq_ft` + `capacity` + `website_url`; `HqVenueRow` type + the venues SELECT + the in-batch pool-push extend to carry the four extra fields; a single consolidated UPDATE per matched venue (replaces the separate `about_venue`-only UPDATE) writes a composed patch object so VS enrichment of HQ-linked rows backfills HQ-side gaps at deck-generation time while producer-edited HQ values stay authoritative; new venue INSERT path unchanged since it already populates every field): Google Slides deck generation. Authenticates via `_shared/googleServiceAccount.getGoogleAccessToken([presentations, drive])` (service account owns the Drive + Slides API calls + is a member of the Mirror Shared Drive). **Phase 5.12.0 scout select grew `created_by`** so the HQ push can attribute newly inserted `venues` rows. **Phase 5.12.0 HQ Venues push (at the top of `generateWork()`, before any Slides API call):** sequential per-venue loop over the pitched + include_in_deck set. **Phase 5.12.1 dedupe helpers extracted to `_shared/venueDedupe.ts`** (second-consumer trigger fired by `vs-research-venues`'s new cross-rail step). The inline three-step cascade now collapses to one `findVenueDedupeMatch(candidate, hqPool)` call against an in-memory pool of every HQ `venues` row; behavior is preserved end-to-end (same name -> address -> website ladder; same cross-field disambiguation on the name step). The ladder replaces the retired `vs_candidate_venues_shortlist_sync` trigger's website-then-name+neighborhood logic: (a) flexible name match — `normalizeVenueName` strips case + leading articles + corporate suffixes + punctuation + "&"-vs-"and" + diacritics; survivors must NOT cross-field-conflict (both candidate and HQ have non-empty addresses that differ, OR both have non-empty cities — scout.city vs hq.city — that differ); (b) plain address match (`normalizeAddress`); (c) plain website match (`normalizeWebsite` lowercases + trims so trailing whitespace / host casing variants match). Single hit wins on every step; multi-hit or no-hit falls through. Match path: UPDATE `vs_candidate_venues.linked_venue_id` + conditional UPDATE `venues.about_venue` only when the existing value is NULL or trimmed-empty (write-when-blank rule preserves producer edits). Insert path: INSERT `venues` row carrying `name` / `address` / `neighborhood` / `city` (from scout) / `website_url` / `features` (from `key_features`) / `total_sq_ft` (from `size_sq_ft`) / `capacity` / `about_venue` (from the candidate's `venue_overview`) / `created_by` (from scout) + per-token `venue_venue_types` join inserts via `canonicalizeType` split-dedupe-resolve, then UPDATE `linked_venue_id`. Pre-linked rows (legacy trigger pre-5.12.0) skip the match cascade and just run the about_venue write-when-blank check. Per-venue link or insert failures fail the deck run via the new `HQ_PUSH_FAILED` error code (the Generating page's regex routes to `/deck/error/HQ_PUSH_FAILED`); only `venue_venue_types` join INSERT failures are warn-and-continue (decorative). Copies `GOOGLE_TEMPLATE_FILE_ID` into `GOOGLE_OUTPUT_FOLDER_ID` as `${event_name} - Venue Pitch Deck v${N}`. Mirror's template has 6 front-matter slides (cover, project info, event overview, section title, venue map with venue-name legend at slide 6). Slide 7 = per-venue detail template; slide 8 = per-venue floor plan template (both duplicated per venue, originals deleted). **Slide 2 ALL-CAPS pass (Phase 4.10.6-port):** scoped `replaceAllText` requests with `pageObjectIds: [slide2Id]` run at the head of the front-matter pass for `client_name` / `event_name` / `event_live_date` / `event_location` with uppercased values; runs before the case-preserving global pass so other front-matter slides keep original casing. **Per-venue scoped text replacements** on the duplicated detail slide (`venue_name` ALL-CAPS, `venue_id_padded`, `venue_address`, `venue_neighborhood`, `venue_overview`, `venue_size`, `venue_capacity`, `venue_features`, `venue_website`). "Website" hyperlinking + image replacement by alt text (`img_1`..`img_4`) on the detail slide. Phase 5.12.11: photo replace method swapped from CENTER_INSIDE to CENTER_CROP so producer uploads fill the 2x2 grid cells; CENTER_INSIDE letterboxed photos whose aspect ratio did not match the template cell. **Slide-order pass (Phase 4.10.6-port):** per-slide `updateSlidesPosition` requests (one slide per request to satisfy "in presentation order" constraint) lay out duplicates as `[V1_detail, V1_fp, V2_detail, V2_fp, ...]` starting at `insertionIndex=FRONT_MATTER_SLIDES`. Then deletes the original template slides 7 + 8 in the same `batchUpdate`. Returns `{ ok, scout_id }` immediately; work runs in `EdgeRuntime.waitUntil`. On success, appends metadata to `vs_scouts.generated_decks` jsonb (`{ deck_id, deck_name, version, generated_at, venue_count, slide_count, edit_url, embed_url }`) + flips `current_step='completed'` + `status='complete'` + clears `pipeline_error`, gated by `.eq("current_step", "deck_prep")` CAS so parallel successes don't both append. On failure, writes `status='failed'` + `pipeline_error=<CODE>: <message>` via `failWithCode` with `.eq("current_step", "deck_prep")` CAS. Error codes: `AUTH_FAILED`, `TEMPLATE_COPY_FAILED`, `SLIDES_API_FAILED`, `NO_VENUES_INCLUDED`, `HQ_PUSH_FAILED` (new 5.12.0), `UNKNOWN`. Idempotency via `brief_data.deck_generation_started_at` (240-second grace); `reset_scout_for_deck_regenerate` RPC atomically clears that key + resets current_step on producer regenerate. **Phase 5.12.4.1 atomic kickoff RPC:** the pre-5.12.4.1 inline check-then-write of `deck_generation_started_at` (at lines 597-616) was a TOCTOU race surfaced by Codex adversarial review — two near-simultaneous invocations (producer double-clicking Generate) could both pass the 90s grace-window check before either committed its timestamp, then both call `pushVenuesToHq` and INSERT duplicate `venues` rows (no UNIQUE constraint on venue identity columns). 5.12.4.1 swaps the inline path for `sb.rpc('vs_deck_try_acquire_kickoff', { target_scout_id, grace_seconds: Math.round(IN_FLIGHT_GRACE_MS / 1000) })`. The RPC mirrors the Phase 5.12.1 `vs_research_try_acquire_kickoff` shape exactly: `pg_try_advisory_xact_lock` under a `vs-deck:<scout_id>` namespace (separate from `vs-research:*` so deck + research kickoffs for the same scout don't collide) + same-transaction read-and-write of `brief_data.deck_generation_started_at` + clear `pipeline_error`. `false` -> 200 `{ ok, skipped: "in_flight" }`. RPC errors return 500. 180-second `Promise.race` ceiling (HQ push is sequential + DB-only, ~2s per venue, well within headroom). User-invoked, `verify_jwt = true`. The Generating page Realtime-subscribes to `vs_scouts`; on success opens the deck's edit_url in a new tab + navigates back to Deck Prep; on failure parses the code from `pipeline_error` and routes to `/deck/error/<code>`. **Phase 5.12.3:** the `pushVenuesToHq` dedupe ladder reshapes from the strict 5.12.0 name→address→website cascade to weighted scoring per `_shared/venueDedupe.ts` (name 60/25/0 + address 50/20/0 + website 40/20/0 + city +10 conditional; threshold 60 `>=`; cross-field VETO preserved on name FULL + (city differ OR address differ); locked website normalizer contract `new URL` + `www` strip + path-trim drives host vs host+path scoring). On a FRESH ladder-resolved match, the candidate UPDATE now writes `dedupe_meta` alongside `linked_venue_id` in a single statement (shape `{ matched_venue_id, matched_venue_name, score: { name, address, website, city, total, threshold }, reason, matched_at }`). Explicit NOT-written set for `dedupe_meta`: pre-linked candidates (else branch skips the ladder), hq_pool rows (linked at insert time in vs-research-venues; never reach this path), fresh-INSERT no-match rows (no match round happened). Failure handling unchanged: the UPDATE is not split, so a write failure still fails the deck via `HQ_PUSH_FAILED`. **Phase 5.14 photo persistence step (`persistVenuePhotosToHq`):** runs AFTER `pushVenuesToHq` succeeds (step 2.7) and BEFORE the photo-load step for Slides. Re-reads `linked_venue_id` fresh from DB for all candidates (in-memory array was loaded before pushVenuesToHq). Batch-reads `vs_venue_photos` for all pitched candidates. Per venue: (A) reads current `venues.photos`, removes all non-null paths from the `venue_photos` bucket to prevent orphans on extension changes; (B) downloads each slot's file from `vs_venue_photos` bucket, uploads to `venue_photos` bucket at `<linked_venue_id>/photo_<slot>.<ext>` with `upsert:true`, builds a 4-element nullable path array; (C) updates `venues.photos` with the new array. Best-effort throughout: every failure path warn-logs and continues; never fails the deck via `HQ_PUSH_FAILED`. Authority rule: candidate's `vs_venue_photos` always overwrite HQ (even when a prior deck already stored photos). Telemetry: `[vsPhotoPersist] scout=<id> candidates=N persisted=M failed=K skipped=S`.
- `vs-delete-scout({ scout_id })` (Phase 5.12.6; NEW 2026-05-24): synchronous scout delete with storage-file cleanup. Replaces the pre-5.12.6 direct `supabase.from("vs_scouts").delete().eq("id", id)` in `ScoutIndex.tsx`'s `confirmDelete` handler, which left brief PDFs (`briefs` bucket), sourcing-sheet uploads (`sourcing_sheets` bucket), and `vs_venue_photos` storage objects orphaned -- FK CASCADE handled the DB rows but never the Supabase Storage buckets. Order is load-bearing: (1) enumerate paths (read `vs_scouts.brief_data.uploaded_files` + `sheet_storage_path` + `vs_venue_photos.storage_path` joined via `vs_candidate_venues!inner(scout_id)`; read-only, fast, runs before any destructive op); (2) DB DELETE on `vs_scouts WHERE id = $1` (cascades to `vs_sourcing_rounds` + `vs_candidate_venues` + `vs_venue_photos` rows via existing FK ON DELETE CASCADE from the Phase 4.1-port schema); (3) per-bucket `storage.from(bucket).remove([...paths])` against `briefs` + `sourcing_sheets` + `vs_venue_photos` in 100-path chunks. Per-bucket `.remove()` errors are collected into the response payload as `storage_errors: [{ bucket, path, message }]` but NEVER thrown; the function ALWAYS proceeds to the next bucket. Cleanup-first ordering was rejected (a storage-cleanup failure followed by successful DB DELETE leaves a half-deleted scout pointing at missing paths). Auth posture: `verify_jwt = true`, service-role supabase client (`createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)`); **no extra in-function authorization gate** -- counter-example to `vs-research-single-venue`'s three-gate posture per `docs/auth-model.md` § Function-level authorization gates on cross-scout poisoning. Justification: `vs-delete-scout` takes only `scout_id` with no cross-entity surface to poison, and any authenticated user can already delete any scout via direct PostgREST today (open-authenticated `vs_*` RLS); the gateway check + 404 on missing scout is sufficient. Response shape: `{ ok: true, scout_id, event_name, counts: { candidate_venues, briefs_removed, briefs_failed, sheets_removed, sheets_failed, photos_removed, photos_failed }, [storage_errors] }` on success (200); `{ ok: false, error: "scout_not_found" }` (404) when the scout SELECT returns null; `{ ok: false, error: "db_delete_failed", message }` (500) when the DELETE itself fails (storage cleanup is skipped because nothing got deleted); `{ ok: false, error: "scout_lookup_failed" | "photo_lookup_failed", message }` (500) on enumeration failure before the DELETE fires; `{ ok: false, error: "scout_id must be a UUID" }` (400) on input validation; `{ ok: false, error: "Invalid JSON body" }` (400) on malformed body. Drive deck files (`vs_scouts.generated_decks[*].deck_id`) and HQ `venues` rows (referenced via `vs_candidate_venues.linked_venue_id ON DELETE SET NULL` from the HQ side) are NOT touched -- both stay permanently by design (Drive cleanup is NOT a deferred carry-forward per Jimmie 2026-05-24; HQ venues are canonical and survive scout deletion). Frontend callsite is `ScoutIndex.tsx confirmDelete` only; success surfaces a per-bucket count toast (`Deleted "X" (N venues, M photos, K files)`), partial-cleanup surfaces an additional destructive-variant toast. Telemetry: `[vsDeleteScout]` log line carries scout_id + per-bucket counts + wall + outcome (`ok` / `partial`), shape mirrors the `[hqPoolFilter]` / `[hqPoolRescue]` / `[hqPoolResearchEnrich]` accounting convention. No kickoff RPC (single-fire user-invoked, no concurrent-invoke race window worth defending). No `EdgeRuntime.waitUntil` (sync end-to-end; typical wall is sub-second; pathological worst case is a deeply-populated scout with ~36 photos still well under 30s). No Realtime. No activity log (pre-existing posture preserved; if scout-delete tracking is wanted later, add an `activity_log_writer` trigger on `vs_scouts` BEFORE DELETE as a separate sub-phase).

- `vs-regenerate-venue-overview({ venue_id, scout_id, notes })` (Phase 5.12.15; NEW): single-venue regenerate of `vs_candidate_venues.venue_overview`. Used by the consolidated Review surface's per-card Regenerate Overview button. User-invoked synchronous (Claude call ~10-20s). Auth posture: `verify_jwt = true` (default; no config.toml entry). **Authorization gates are load-bearing** (open-authenticated `vs_*` RLS leaves function-level checks as the only cross-scout poisoning defense; mirrors `vs-research-single-venue` from Phase 5.12.7 in posture but with two predicates instead of three). Two gates enforced inline before any Claude call: (1) `venue.scout_id === scout_id` (SELECT predicate filters; missing row -> 404); (2) `scout.current_step === 'deck_prep'` (in-fn check; reject from every other state so a stale tab can't poison an in-flight scout's compile / generate pipeline). Both 404 on miss (probe-defense pattern matches `vs-research-single-venue`); 4xx/5xx error shape carries `{ ok: false, error }`. The source filter that `vs-research-single-venue` carries (`venue.source ∈ {'manual', 'hq_pool'}`) does NOT apply here: every pitched venue regardless of source has a `venue_overview` the producer may want to re-roll (including sheet rows whose Pass 2 paragraph came out flat and research rows where the original overview reads stale after notes-driven tuning). Call body lifts `vs-compile-summaries` Pass 2 byte-for-byte: tool-less, `ABOUT_VENUE_SYSTEM` (1h ephemeral cache via `cache_control: { type: "ephemeral", ttl: "1h" }`), `web_search_20250305` with `max_uses=2`, `tool_choice: auto`, `max_tokens: 2000`, `anthropic_beta: ["prompt-caching-2024-07-31", "extended-cache-ttl-2025-04-11"]`, `callClaude('venue_scout', ..., { fn_name: 'vs-regenerate-venue-overview' })`. Imports `ABOUT_VENUE_SYSTEM` + `buildOverviewUserMsgForVsRow` + `buildStub` from `_shared/venueOverview.ts` (no edit to the shared file). The synthetic row passed to `buildOverviewUserMsgForVsRow` substitutes the request body's `notes` value into `venue.notes` so the existing `<producer_inputs>` block in the user message picks it up without a shared-module signature change. **Frontend passes notes explicitly in the request body** so producer-edit-then-immediately-regenerate doesn't race the 600ms notes debounce on the client; the function never reads `vs_candidate_venues.notes` directly. Server-side UPDATE on `vs_candidate_venues.venue_overview` with the resulting paragraph (or `buildStub(venue.name, scout.city)` fallback when Claude returns empty text); returns `{ ok: true, venue_overview }` on success. No kickoff RPC (single-fire user-invoked; no concurrent-invoke race window worth defending; per-regen cost ~$0.05-$0.15). No `EdgeRuntime.waitUntil`, no Realtime, no activity log, no confirm dialog (producers iterate on notes and re-roll; dialog every click adds friction without payoff). 1h ephemeral prompt cache on the system block means a producer regenerating 3 venues in a row pays cache-write on the first call and cache-read on the next two within the cache window.

- `vs-research-single-venue({ venue_id, scout_id })` (Phase 5.12.7 Feature C; NEW; post-smoke amended 2026-05-24; Phase 5.12.10 venue_types DB-driven via per-request `getVenueTypesCanonicalSet` fetch threaded into `buildFillUserMsg` AND the apply guard, which now uses `sanitizeMultiAgainst(f.venue_type, canonicalSet.names)` instead of the pre-5.12.10 verbatim `.trim()` write, matching the WRITE-strict posture of every other AI write path; Phase 5.12.13.1 apply-path `key_features` write chains `sanitizeTagShape(stripPlaceholders(f.key_features))` from `_shared/venueTypes.ts` so manual + hq_pool single-venue research lands key_features in evergreen tag shape; Phase 5.12.13.3 redeploys to pick up the `_shared/venueFill.ts` FILL_SYSTEM cache invalidation (drops "Prefer Retail for ground-floor commercial / storefront / vacancy spaces." sentence from FILL_SYSTEM line 121); no direct edit in this function): single-venue research for producer-added rows on SourcingReport. Lifts the per-row Claude call shape from `vs-research-venues` Phase A's `enrichSheetVenue` path: one `callClaude('venue_scout', { tool: fill_venue, tool_choice: auto, web_search_20250305 max_uses=2 })` call, then per-field defensive patch guards (mirrors Pass 1 + Phase A byte-for-byte; only fills empty columns so producer-entered values stay authoritative; skips `derived_attrs` since FILL_TOOL doesn't carry the scout's `derived_columns` IDs). Auth posture: `verify_jwt = true`, user-invoked synchronous (Claude call ~10-20s; sync fine for v1; if latency surfaces as a UX problem, swap to `EdgeRuntime.waitUntil` + Realtime in a follow-on). **Authorization gates are load-bearing** (open-authenticated `vs_*` RLS leaves function-level checks as the only cross-scout poisoning defense). Three predicates enforced inline before any Claude call: (1) `venue.scout_id === scout_id` (SELECT predicate filters; missing row -> 404); (2) `venue.source ∈ {'manual', 'hq_pool'}` (SELECT predicate filters; both are producer-added paths — manual rows from SourcingReport's manual-add row, hq_pool rows from the HQ Venue picker that land AFTER research-time Feature B enrichment; `sheet` rows go through Phase A, `research` rows are already populated by Phase B); (3) `scout.current_step ∈ {sourcing_report, shortlist, review_selects, deck_prep}` (in-fn check; the primary callsite is SourcingReport's Continue button which fires this for every unenriched producer-added row in parallel before transitioning to shortlist, so `sourcing_report` is in the allowlist; the later states cover back-button re-fires; rejected from researching / compiling / generating_deck so a stale tab can't poison an in-flight scout's pipeline). All three surface a 404 (rather than 403) so a probe surfaces no useful signal. **`aboutVenue` plumbing on hq_pool rows**: when `venue.source === 'hq_pool'` and `venue.venue_overview` is populated, the function passes that paragraph as the `aboutVenue` argument to `buildFillUserMsg` so Claude sees the HQ CANONICAL ABOUT block + the narrower research instruction (focus on per-brief recs / cons / rank rather than re-researching identity). Manual rows pass null (no HQ canonical reference; degrades to the 4.10.3-port behavior). "Already enriched" short-circuit: when the row's `recommendations` is already populated, returns `ok: true, patch: {}` without firing Claude, so re-firing on a previously-enriched row never burns a second call. Cost per fire: ~$0.02-0.05. No kickoff RPC (single-fire user-invoked, no concurrent-invoke race window worth defending), no `EdgeRuntime.waitUntil`, no Realtime. Returns `{ ok: true, patch }` on success; `{ ok: false, error }` on failure. Primary callsite is `SourcingReport.onContinue` which fans out to every unenriched manual + hq_pool row in parallel and blocks the Continue button (spinner) until all complete; per-row failure surfaces a toast but does not block the transition (the row stays bare and the producer can edit inline). Pre-post-smoke this fired from Shortlist's `togglePitch` handler; that path is gone now (single fan-out on Continue is the only consumer).

### Postgres RPCs

- `start_over_scout(target_scout_id uuid) RETURNS jsonb` (Phase 4.9-port): transactional reset of a scout to `current_step='sheet_prompt'`. Cascade-deletes `vs_candidate_venues` (photos cascade via FK ON DELETE CASCADE). Resets `status` to `'draft'`, clears `pipeline_error`, `derived_columns` (-> `[]`), `sheet_storage_path`, `deck_order` (-> `[]`), and strips idempotency timestamps (`research_started_at`, `compile_started_at`, `deck_generation_started_at`) from `brief_data`. Keeps brief fields, `project_id`, `generated_decks` history, `brief_data.uploaded_files`. `SECURITY INVOKER` so the caller's RLS applies. Called from `ScoutSettings.tsx` Danger Zone. Storage objects in `vs_venue_photos` + `sourcing_sheets` buckets orphan and get swept by a future cron (parallel to TS `candidate_attachments` precedent).

- `bulk_import_commit_projects(payload jsonb) RETURNS jsonb` (Phase 5.9.2, migration `20260602110000_phase_5_9_2_projects_importer.sql`): the atomic commit for the Projects bulk importer, called from the `bulk-import` edge function's `project` handler via `sb.rpc('bulk_import_commit_projects', { payload })`. One in-DB transaction owns queued client/venue creates, novel category/city lookup auto-creates, project insert (create) or update (dedupe-update), the `project_venues` join (the only import-owned roster as of 5.9.2.1; people roster is set retroactively), the `bulk_import_sessions` audit row, and the session `activity_log` row. Re-checks `payload.actor_id` is an admin (raises 42501) — defense in depth matching `promote_outlook_to_project`; `actor_id` comes from the payload because the service-role invocation makes `auth.uid()` NULL inside the function. Returns `{ ok, session_id, created_ids, updated_count, created_refs }`. `SECURITY DEFINER`, `GRANT EXECUTE TO authenticated`. Helpers `_bulk_import_resolve_client_ref` / `_bulk_import_resolve_venue_ref` (both `STABLE`). Full body contract in `docs/schema.md` § Postgres functions.

- `bulk_import_commit_vendors(payload jsonb) RETURNS jsonb` (Phase 5.9.3, migration `20260602140000_phase_5_9_3_vendors_importer.sql`): the atomic commit for the Vendors bulk importer, called from the `bulk-import` edge function's `vendor` handler via `sb.rpc('bulk_import_commit_vendors', { payload })`. Same SECURITY DEFINER + admin re-check posture as the Project RPC. One in-DB transaction owns queued `vendor_categories` + `vendor_subcategories` creates (subcategory parent resolved per row against queued-then-existing categories), novel `vendor_capabilities` + `cities` lookup auto-creates, vendor insert (create) or update (dedupe-update), the `bulk_import_sessions` audit row, and the session `activity_log` row. Deltas vs Project: `category` + `subcategory` are FK columns resolved via helpers; `capabilities` is a `text[]` of names written through with novel-value auto-create; `preferred` + `nationwide` (5.9.3.1) each coerced from a `"true"`/`"false"`/`""` string; `general_email` (5.9.3.2) written through as nullable text; (5.9.3.3) the row's contact_* also creates/reuses a vendor-affiliated `people` row (Primary Contact), vendor-scoped dedupe; NO project-style staff-roster (no `vendor_account_managers` analog) and the update path leaves `project_vendors` untouched. Returns `{ ok, session_id, created_ids, updated_count, created_refs }`. `SECURITY DEFINER`, `GRANT EXECUTE TO authenticated`. Helpers `_bulk_import_resolve_vendor_category_ref(text, uuid[])` / `_bulk_import_resolve_vendor_subcategory_ref(text, uuid[], uuid)` (both `STABLE`). Full body contract in `docs/schema.md` § Postgres functions.

- `bulk_import_commit_venues(payload jsonb) RETURNS jsonb` (Phase 5.9.4, migration `20260602180000_phase_5_9_4_venues_importer.sql`): the atomic commit for the Venues bulk importer, called from the `bulk-import` edge function's `venue` handler via `sb.rpc('bulk_import_commit_venues', { payload })`. Same SECURITY DEFINER + admin re-check posture as the Project / Vendor RPCs. One in-DB transaction owns queued `venue_types` creates (case-insensitive existence probe then `ON CONFLICT (name) DO NOTHING`; no `created_by` column), novel `cities` auto-creates, venue insert (create) or update (dedupe-update on `lower(name)|lower(address)`), the `venue_venue_types` join writes, the per-row contact `people` + `venue_contact_people` join writes, the `bulk_import_sessions` audit row, and the session `activity_log` row. Deltas vs Vendor: `venue_types` is a JOIN (REPLACE on update); `features` is plain `text[]` write-through; `exclusive_vendor_ids` is NOT importable (the RPC never reads or writes it — set only on VenueEdit; corrected in migration `20260602190000` so a dedupe-update can't clobber it). Contact divergence: venues link a contact through the `venue_contact_people` JOIN (NOT the dead `people.venue_id` column), so the RPC creates/venue-scoped-dedupe-reuses a `people` row (`affiliation_type='Venue'`) AND inserts a `venue_contact_people` row; dedupe matches THROUGH the join name-first then email; the join is additive on update (never replaced). **Phase 5.9.7 (`20260602210000`, rebased onto the 5.9.6 undo body):** writes `general_email` through on both create + update paths, and appends one `event_day` `venue_rate_history` row per imported venue (dated `current_date`) only when the amount differs from the venue's most-recent `event_day` rate; a new `created_refs.venue_rates` counter tracks it. The `event_day_rate` non-neg-int + `general_email` email-shape checks live in the bundled `_validators/venue.ts` (so the 5.9.7 validator change required a `bulk-import` redeploy). **Phase 5.10.0 (`20260603100000`, rebased onto the 5.9.7 body):** the `venues.notes` column was renamed to `venues.about_venue`; the RPC's three column references (INSERT column list, INSERT value list, UPDATE SET clause) now read `about_venue` from `v_row->>'about_venue'`. The importer commit payload (`src/lib/hq/bulkImport/entities/venue.ts`) + the CSV template header changed `notes` -> `about_venue` in lockstep. Returns `{ ok, session_id, created_ids, updated_count, created_refs }`. `SECURITY DEFINER`, `GRANT EXECUTE TO authenticated`. Helper `_bulk_import_resolve_venue_type_ref(text, uuid[])` (`STABLE`). Full body contract in `docs/schema.md` § Postgres functions.

- `promote_outlook_to_project(target_entry_id uuid) RETURNS uuid` (Phase 5.3, migration `20260516150000_phase_5_3_outlook_entries_table.sql`): atomic promotion of an `outlook_entries` row to a `projects` row. Pre-checks `public.is_admin()` (raises ERRCODE 42501 on non-admin); refuses to re-promote an already-linked entry (raises 23505). INSERTs `projects` with `name`, `client_id`, `city` from the entry + `status='Queued'` + `created_by=auth.uid()`; UPDATEs the entry's `linked_project_id` to the new project id; returns the new project id. Called from `OutlookPage.tsx` Promote to Project flow via `supabase.rpc('promote_outlook_to_project', { target_entry_id })`. `SECURITY DEFINER` so cross-table writes don't trip RLS mid-flight. `GRANT EXECUTE TO authenticated`. Standard / freelance users would be blocked at the page route gate anyway; the in-function `is_admin()` check is defense in depth.

## HQ Core

### `hq-generate-venue-about({ venue_id })`: Phase 5.10.0; Phase 5.12.13.1 inherits the `_shared/venueOverview.ts` label sync (`Key features:` -> `Key features (tags):` in `buildOverviewUserMsgFromVenue` + `<input_fields>` line `- Key features (list)` -> `- Key features (tags)`); redeploys without a direct edit; Phase 5.12.13.2.1 third redeploy this consumer to pick up the post-.2 smoke correction in `ABOUT_VENUE_SYSTEM` (cap widens 90-120 -> 100-150 with `<task>` + `<output_format>` numerical edits + `<input_fields>` `Key features (tags)` line gains tag-priority guidance "favor the most appealing or distinctive for this specific venue, especially when expanding on more would push the paragraph above the 150-word cap"); 5 locked decisions in `docs/decisions.md` § Phase 5.12.13.2.1; no direct edit in this function (the change is entirely in the shared module); Phase 5.12.13.2 second redeploy this consumer to pick up the `ABOUT_VENUE_SYSTEM` cap widen + example reshape: `<task>` block rewrites with cap widened 90-110 -> 90-120 (hard ceiling 110 -> 120 + cut-or-merge fallback + count-before-returning), `<output_format>` gains a length-check paragraph ("Before returning, count the words in your draft. If above 120, cut a sentence or merge two clauses and recount. Do not return a paragraph above 120 words."), `<input_fields>` `Key features (tags)` line gains a tag-shape description body + line 64 em-dash sweep, and the 6 in-prompt example INPUT lines reshape from narrative `Key features:` to short tag-shaped `Key features (tags):` lists (Studio 525 / Chelsea Industrial / 8626 Melrose Ave / The Flat NYC / AT&T Discovery District / Astor Place); all 6 example OUTPUTS stay byte-for-byte; one-time prompt-cache invalidation accepted per `feedback_tool_choice_collapse`; no direct edit in this function (the change is entirely in the shared module)
Generates (or regenerates) the "About Venue" deck-copy paragraph (`venues.about_venue`) for a single venue. User-invoked synchronous from the About Venue card Generate / Regenerate button on `VenueDetail` + the About Venue field button on `VenueEdit`. Opens a **user-JWT** supabase-js client (forwards the caller's `Authorization` header) so the SELECT (venue row + `venue_venue_types` -> `venue_types` join) and the final `about_venue` UPDATE both run under the caller's session, RLS-enforced; venues UPDATE is open-authenticated, so no service role / SECURITY DEFINER needed. Builds the user-message venue-data block via `buildOverviewUserMsgFromVenue(venue)` and calls `callClaude('hq', { system: ABOUT_VENUE_SYSTEM, tools: [web_search_20250305 max_uses=2], tool_choice: auto, max_tokens: 2000 })`. **TOOL-LESS by design (Jimmie, 2026-05-21):** the HQ generator uses no custom tool — `ABOUT_VENUE_SYSTEM` (an evergreen, brief-less prompt in `_shared/venueOverview.ts`) instructs Claude to reply with the paragraph as **plain text**, and the function reads `result.text` directly. web_search is the only tool, so Claude searches when the venue row is sparse, then writes. Going tool-less removes the `feedback_tool_choice_collapse` failure class entirely (no forced custom tool to collapse). Mirrors the `vs-generate-brief-overview` plain-text pattern. Falls back to the shared `buildStub({ name, city })` deterministic stub only if the reply is empty. pause_turn continuation handled in the `callClaude` wrapper. **Phase 5.12.0:** `buildStub` lifted to `_shared/venueOverview.ts` so this function and `vs-compile-summaries` Pass 2 share one fallback; the local helper was deleted and the import added. VS Pass 2 also converged onto `ABOUT_VENUE_SYSTEM` in 5.12.0 (tool-less + web_search + prompt-cache breakpoint), so the legacy `OVERVIEW_TOOL` + `OVERVIEW_SYSTEM` are gone from the shared module. `ABOUT_VENUE_SYSTEM` gained a short paragraph inside `<input_fields>` acknowledging the optional `<producer_inputs>` block VS passes; HQ never sends that block, and the prompt's "if absent, ignore" rule keeps voice identical. The venues UPDATE trigger writes the `activity_log` row automatically; no inline log. Returns `{ ok: true, about_venue }` or `{ ok: false, error }` (all business outcomes, including expected failures, return HTTP 200 so the descriptive error reaches the caller's toast; only the method guard uses a non-2xx code). **First `callClaude('hq', ...)` consumer in HQ** — spend bucket `'hq'` maps to the `ANTHROPIC_API_KEY_HQ` secret (already wired in `_shared/anthropic.ts` `KEY_BY_APP`; set in Supabase 2026-05-06). `verify_jwt = true` (no self-invoke; no admin re-check inside — any authenticated user who can view a venue can generate, matching the `vs-parse-brief` / `vs-generate-brief-overview` posture).

## Cross-cutting

### `notifications-dispatch(event_type, entity_type, entity_id, entity_name, recipient_user_ids, actor_id?, extra?)`: Phase 5.5
Central notification fan-out. Receives event payloads from:
1. `public.notifications_dispatch_writer` trigger (tasks + projects, via `invoke_edge_function`)
2. `public.handle_new_user` trigger (`event_type='user_pending'`)
3. The three daily crons: `hq-cron-deliverable-due-3d`, `hq-cron-task-due-today`, `hq-cron-event-date-today`

For each recipient: validates `event_type` against an allowlist, drops the actor + dedupes, checks `global_settings.in_app_notifications_enabled` + `email_notifications_enabled`, reads the per-user `user_notification_preferences` row (falls back to system defaults when no row exists), writes a `notifications` row (skips for `user_pending` since `handle_new_user` writes inline; avoids the duplicate), sends a Slack DM via `_shared/slackDm.ts` if `slack_dm` pref + `users.slack_user_id` is populated, and for `user_pending` only also fires the admin email via `_shared/sendEmail.ts` (preserves legacy behavior).

Auth: **`requireInternalSecret` only** (no user-JWT path). Phase 5.5 security audit MUST-FIX: `requireInternalOrUserAuth` would let any signed-in user POST a crafted `recipient_user_ids` array to spoof notifications to admins. All legitimate callers send `x-internal-secret`. `verify_jwt = false`.

Returns `{ ok: true, sent_in_app: N, sent_slack: M, skipped: K }`. Slack mrkdwn special chars in title/body are escaped before send.

### `hq-cron-deliverable-due-3d`: Phase 5.5
Daily 13:00 UTC (09:00 ET). Queries `deliverables WHERE due_date = CURRENT_DATE + 3 AND status NOT IN ('Complete','Skipped')`. Resolves recipients from `project_account_managers` per deliverable, POSTs to `notifications-dispatch` per row. `verify_jwt = false`. Scheduled via pg_cron in migration `20260517000000_phase_5_5_notifications_activity_search.sql`.

### `hq-cron-task-due-today`: Phase 5.5
Daily 12:00 UTC (08:00 ET). Queries `tasks WHERE due_date = CURRENT_DATE AND status <> 'Done' AND assignee_id IS NOT NULL`. POSTs to `notifications-dispatch` per task with the single assignee as recipient. `verify_jwt = false`.

### `hq-cron-event-date-today`: Phase 5.5
Daily 11:00 UTC (07:00 ET). Three parallel queries for `projects` where `install_dates_start`, `live_dates_start`, or `removal_dates_start` equals today. Recipients per project = union of `project_account_managers` + `project_designers` (deduped). `extra.kind` carries `'Install' | 'Live' | 'Removal'` for the body template. `verify_jwt = false`.

### `bulk-import(entity_type, mode, rows, queued_refs, column_set, session_id?)`: Phase 5.9.1 (project handler 5.9.2, vendor handler 5.9.3)
Shared cross-cutting bulk-import primitive. User-invoked synchronous from `BulkImportPage`'s commit step; no self-invoke. `verify_jwt = true` (explicit `config.toml` block, matches the `vs-parse-brief` / `vs-parse-sheet` sibling pattern).

**Auth:** the gateway verifies the JWT; the function then re-checks `permission_role = 'admin'` server-side via a user-JWT client (`auth.getUser()` + a `users` SELECT). Defense in depth on top of the `AdminRoute` gate. Returns 401 for an invalid user, 403 for a non-admin (`{ ok: false, error: "Forbidden: admin only" }`).

**Signature:** `POST { entity_type: 'project'|'vendor'|'venue', mode: 'preview'|'commit'|'undo', rows, queued_refs, column_set, session_id?, dry_run? }` -> success `{ ok: true, mode, session_id?, parsed_count, created_ids?, created_refs? }` (preview/commit) or `{ ok: true, mode:'undo', dry_run, counts }` (undo); failure `{ ok: false, error, validation_errors?, unresolved_refs? }`.

**Undo mode (Phase 5.9.6):** reverts a committed import. The admin re-check runs first (same block as preview/commit), then the function requires `session_id` (else 400) and calls the SECURITY DEFINER `bulk_import_undo` RPC via the service-role client with `{ p_session_id: session_id, p_actor_id: userId, p_dry_run: dry_run ?? false }`. `entity_type` is NOT required for undo (the RPC reads it from the session row). On `dry_run: true` the RPC returns `{ ok, dry_run:true, counts }` without writing (drives the cascade-warning dialog); on `dry_run: false` it performs the destructive delete + hard-deletes the session row in one transaction and returns `{ ok, dry_run:false, counts }`. The function spreads the RPC JSON and adds `mode`. On RPC error it returns `{ ok: false, error }` with a 400 (no `failed_rollback` row — that is commit-only). See `docs/schema.md` § `bulk_import_undo` for the gates (admin, committed, 7-day window, non-empty trail) and the counts shape.

**Entity-handler registry:** a module-level `handlers: Record<string, EntityHandler>` maps each `entity_type` to a `{ validate, commit }` pair. `commit` returns `{ created_ids, created_refs, session_id? }`. Registered: `_smoke` (no-op round-trip prover), `project` (5.9.2), `vendor` (5.9.3), `venue` (5.9.4). All three entity importers are now wired (the `_smoke` handler + route is slated for removal in 5.9.5). Any unregistered `entity_type` returns `{ ok: false, error: "Entity handler not registered for '<entity>'" }` cleanly.

**RPC-owned session writes (5.9.2 / 5.9.3 / 5.9.4):** the `project`, `vendor`, and `venue` handlers' `commit()` are thin wrappers over the SECURITY DEFINER RPCs `bulk_import_commit_projects` / `bulk_import_commit_vendors` / `bulk_import_commit_venues`, which own the *entire* atomic write INCLUDING the `bulk_import_sessions` audit row + the session `activity_log` row (a PostgREST chain can't roll those back together; memory `feedback_postgrest_no_multi_statement_tx`). The handler returns the RPC's `session_id`. **When `commit()` returns a `session_id`, the edge function MUST skip its own session + activity_log inserts** to avoid a duplicate session row. The `_smoke` handler returns no `session_id`, so the legacy path applies (and it skips the session row anyway because the table CHECK rejects `'_smoke'`). The handler passes `{ actor_id: userId, column_set, rows, queued_refs }` as the RPC payload.

**Transaction model (commit):** validate the payload via the handler; on `preview` return the parsed count without writing. On `commit`: for RPC-backed handlers (project, vendor, venue) the RPC is the transaction (see `docs/schema.md` § `bulk_import_commit_projects` / `bulk_import_commit_vendors` / `bulk_import_commit_venues`). For non-RPC handlers, the service-role client writes one `bulk_import_sessions` row (`status='committed'`) + one `activity_log` row. On any handler failure, a separate `bulk_import_sessions` row with `status='failed_rollback'` is written for incident traceability (outside the rolled-back work, since the RPC's own transaction has already rolled back). The `_smoke` handler skips both audit inserts.

**Server-side validators (`_validators/`):** the project handler's `validate` runs `_validators/project.ts` — a Deno mirror of the client-side rules (name required, single resolvable `account_lead`, valid status, ISO dates, numeric budget). The vendor handler runs `_validators/vendor.ts` (name required, `preferred` is `"true"`/`"false"`, valid email shape, parseable `website_url`, a `subcategory` requires a `category` on the same row). Last-line defense before the RPC; most validation already happens client-side in MapStep / DedupeStep / ImportGrid.

**Anthropic calls:** none. Bulk import is pure-CSV in, pure-DB out.

The HQ-side sheet parser (`_shared/hqSheetParser.ts`, fresh build) is deliberately separate from `vs-parse-sheet` so HQ-side changes can't regress the venue-scout sourcing-sheet path. The browser parses the file via the `xlsx` browser build (`src/lib/hq/bulkImport/parser.ts`) and posts already-parsed rows; per-entity re-validation runs in `_validators/`.

### `notify-admin-of-pending-user(user_id, email)`: Phase 5.1 (deprecated 5.5)
Sends a one-time email to every active admin when a new user signs in for the first time. **Phase 5.5: `handle_new_user` now calls `notifications-dispatch` instead.** This function stays deployed one phase as a fallback in case dispatch fails for the user_pending event type. Composes a plain-text + HTML body via `_shared/sendEmail.ts` and sends via the service account's `gmail.send` scope from `jobs@mirrornyc.com`. Failures are logged, not thrown. `verify_jwt = false`. Removable in Phase 5.6 once dispatch is proven stable in prod.

### `auth-on-signup` (deprecated: use `handle_new_user` trigger)
The original spec mentioned an `auth-on-signup` Edge Function. Phase 2 implemented this as a Postgres trigger on `auth.users` instead (`handle_new_user`), running with service-role privileges. No Edge Function needed.

## Shared modules (`_shared/`)

### `anthropic.ts`: `callClaude(app, messages, options)`
Wraps the raw fetch to `api.anthropic.com`. `app` is `'talent_scout' | 'venue_scout' | 'hq'` and selects the per-app secret (`ANTHROPIC_API_KEY_TS` / `_VS` / `_HQ`). After each successful call:
1. Computes cost from the response usage block (incl. cache-read/write discounts).
2. Increments `global_settings.anthropic_spend_current_month_usd` (`trackSpendAndAlert`).
3. Emails the admin once per cap crossing (gated by `cap_alert_sent_this_month`).
4. **Phase 5.15:** writes one row to `public.anthropic_call_log` via `logCallToTable` (app, fn_name, model, all token columns, cost_usd, scout_id, role_id). Runs AFTER `trackSpendAndAlert`; both steps are best-effort, errors swallowed so a logging outage never blocks the caller. Failed `callClaude` calls (`!res.ok` early return) skip both the spend increment and the log row, matching the existing posture.
5. **Does NOT refuse calls when over cap**: graceful degradation, not a hard failure.

**Phase 5.15: `CallClaudeOptions` gains `scout_id?: string | null` + `role_id?: string | null`.** Optional; null when the caller has no scout or role in scope. Threaded through every VS caller (`scout_id` from `req.body.scout_id`) and every TS caller with a role available (`role_id` from `req.body.role_id` or `cand.role_id`). Pre-create flow callers (`ts-generate-scorecard`, `ts-refine-scorecard`) and `hq-generate-venue-about` log with both nulls. Drives the per-tool drilldown stored in `anthropic_call_log` (UI surfaces the aggregate today; per-scout / per-role drilldown deferred).

Phase 3.8 wires the real cap-alert email path via `_shared/sendEmail.ts`. Recipient lookup: first `users` row with `permission_role='admin'` (oldest by `created_at`), fallback `jobs@mirrornyc.com`. The cap-alert is gated by `cap_alert_sent_this_month` so it fires once per cap crossing; `ts-cron-monthly-spend-reset` re-arms it on the 1st.

Use 1-hour prompt caching (`cache_control: { type: 'ephemeral', ttl: '1h' }`) on stable system + role-context blocks. Same cache key per role for the bulk re-eval / final-review hot path.

**Phase 4.10.5-port: pause_turn continuation loop.** When Anthropic's server-tool loop returns `stop_reason="pause_turn"`, the wrapper appends the assistant's content blocks as a new message and re-calls with the same model + tools + tool_choice. Accumulates content + usage tokens + cost across cycles. Capped at `MAX_PAUSE_CONTINUATIONS = 1`. Logs `pause_continuations=N` in the per-call diagnostic when at least one fires. Without this loop, server-tool-heavy calls (web_search, web_fetch, code_execution) would return partial results with no final tool_use block, surfacing as "no structured output" failures downstream.

**Phase 4.10.6-port: `extractWebSearchResults(content)` helper.** Walks a Claude response's content blocks and pulls every `{ url, title }` from `web_search_tool_result` blocks. Used by venue-research callers as a fallback when Claude's tool output left `website_url` null but search results contained usable URLs.

**Phase 4.10.6-port: `findVenueWebsite(app, { name, address, city })` helper.** Runs a targeted, focused Claude call per venue with `web_search` + a tight "Find the official website URL for {name} at {address}" prompt. Parses the first http(s) URL from the text response; falls back to the first search-result URL. Used as Phase B's URL fallback after `validateWebsiteUrls` returns null. ~10-15s per call, runs parallel across all null-URL venues. **Phase 5.15: signature gains `scout_id?: string | null`** in its options so the inner `callClaude` records the parent scout against the URL-fallback call (otherwise the row would land with `scout_id IS NULL` despite the caller knowing the scout). `vs-research-venues:url-fallback` passes the scout id through.

**Phase 5.12.1: per-fetch wall-clock ceiling bumped 60s -> 180s + AbortError disambiguation.** `ANTHROPIC_TIMEOUT_MS` now 180_000 (was 60_000 since 5.8.6). Pre-bump the 60s cap tripped Phase B before its own `WORK_TIMEOUT_MS=360s` budget had any chance, because `web_search_20250305 max_uses=4` legitimately runs 90-180s. With pause_turn continuation (max 1 cycle) the new ceiling allows total wall-clock up to 360s, still inside the Supabase Pro 400s isolate wall. Also replaced `AbortSignal.timeout` with an explicit `AbortController` + sentinel `Symbol("anthropic_timeout_180s")` reason so network-level AbortErrors (TLS drops, DNS failures, connection resets) no longer mislabel as wrapper timeouts. Real wrapper timeouts return `anthropic_timeout_180s`; network failures return `anthropic_fetch_<RealName>: <message>` carrying the underlying DOMException name + message so `pipeline_error` reads stay actionable.

### `internalAuth.ts`: `requireInternalOrUserAuth(req)`
Three-path auth check for self-invoking Edge Functions. See `docs/auth-model.md` § Edge Function self-invocation auth.

### `googleServiceAccount.ts`: Phase 4.8.1-port
Generic Google OAuth2 access-token helper via service account JWT bearer flow. `getGoogleAccessToken(scopes, { impersonateUser? })` supports both domain-wide-delegation impersonation (Gmail) and non-impersonation flows (Drive + Slides). Reads `GOOGLE_SERVICE_ACCOUNT_KEY` from Supabase secrets, mints an RS256-signed JWT, exchanges it for an access token. Module-level cache keyed by `${impersonateUser ?? ""}|${sortedScopes}`, 5-minute pre-expiry buffer. Used by `_shared/gmailServiceAccount.ts` (Gmail impersonation of `jobs@mirrornyc.com`) and `vs-generate-deck` (Drive + Slides, no impersonation; landed in 4.8.2-port). Cherry-picked from failed-attempt main `be30168`.

### `gmailServiceAccount.ts`
Gmail-scoped wrapper around `googleServiceAccount.ts`. `getGmailAccessToken()` calls `getGoogleAccessToken([gmail.readonly, gmail.send], { impersonateUser: 'jobs@mirrornyc.com' })`. Public API preserved at the 4.8.1-port refactor; internal implementation thinner (~30 lines vs ~130 pre-refactor). Callers: `ts-pull-candidates`, `ts-evaluate-candidate`, `_shared/sendEmail.ts`, `_shared/packetRender.ts`. Template for any service-account-authenticated Google API call.

### `sendEmail.ts`: Phase 3.8
Generic Gmail send helper for transactional notifications outside the packet path. `sendGmail({to, subject, bodyText, bodyHtml?, fromName?})` builds a MIME message (plain or `multipart/alternative`), gets a Gmail token via `gmailServiceAccount.ts`, POSTs to `users/me/messages/send`. Returns `boolean`: never throws. Also exports `getAdminEmail(sb)` which finds the oldest active admin in `public.users`. Used by `_shared/anthropic.ts` (cap alerts), `ts-send-pull-notification`, and `notifications-dispatch` (Phase 5.5 user_pending path). The packet-render module keeps its own `sendPacketEmail` since it appends the signed-URL footer.

### `slackDm.ts`: Phase 5.5
Slack `chat.postMessage` helper. `sendSlackDm(slackUserId, text)` POSTs to Slack with the bot token from `SLACK_BOT_TOKEN`. Channel = user id (the bot must have `im:write`). Returns `boolean`: never throws. Logs missing token as a warning + Slack API errors as `console.error`. Sole caller: `notifications-dispatch`. The dispatch fn pre-escapes mrkdwn special chars (`&`, `<`, `>`) on the title/body before composing the message to prevent crafted entity names from rendering misleading autolinks.

### `parseClaudeJson.ts`
Lifted from the source repo. Strips markdown fences and parses the model's JSON response with a fallback for trailing-comma issues.

### `venueFill.ts`: Phase 4.10.1-port (extracted from vs-compile-summaries); Phase 5.12.4 (HQ canonical about_venue extension)
Shared `FILL_TOOL` + `FILL_SYSTEM` + `buildFillUserMsg` + `VenueForFill` + `ScoutBrief` for the "fill missing venue fields" pass. Two consumers: `vs-research-venues` Phase A (per-row sheet enrichment, `enrichSheetVenue`) and `vs-compile-summaries` Pass 1 (post-shortlist backfill of manual / sheet / hq_pool rows). Single source of truth so the Pass 1 prompt + schema never drift across the two callers. `FILL_TOOL` declares the structured custom tool (`fill_venue`: address / neighborhood / venue_type / website_url / size_sq_ft / capacity / key_features / derived_attrs / recommendations / considerations / ranking_score; key_features + recommendations + considerations + ranking_score required); `FILL_SYSTEM` lifted verbatim from VS Pro per `feedback_tool_choice_collapse` (output-quality levers live on schema descriptions + post-emission sanitization, NOT on SYSTEM). **Phase 5.12.4 thread (a):** `VenueForFill` gains optional `aboutVenue?: string | null`; `buildFillUserMsg` renders a new HQ CANONICAL ABOUT block (above the existing VENUE block) + replaces the wider web_search-mandatory research instruction with a narrower one that tells Claude the about paragraph is authoritative for identity / character / neighborhood and to focus on per-brief judgment (recs + considerations + ranking_score) instead of re-researching. When `aboutVenue` is null / undefined / empty, the byte-for-byte 4.10.3-port behavior is preserved for every existing caller (vs-parse-sheet, manual + sheet rows in Pass 1). The HQ CANONICAL ABOUT block lands via the user message only; FILL_SYSTEM untouched per `feedback_tool_choice_collapse` (user-message scoping is the legal lever).

### `venueDedupe.ts`: Phase 5.12.1 (extracted); Phase 5.12.3 (algorithm rewrite + locked normalizers + producer-visible meta); Phase 5.12.4.1 (identity-signal gate)
Shared venue name / address / website normalizers + the points-based match ladder used to dedupe candidate venues against an existing pool. Exports `normalizeVenueName` / `normalizeAddress` (unchanged from 5.12.1) plus `normalizeWebsiteParts(raw): { host, path } | null` (Phase 5.12.3 new structured normalizer: `new URL` parse + `https://` prepend on missing scheme + lowercase host + leading `www.` strip + query/hash ignored + trailing-slash trim; root collapses to `/`) and `normalizeWebsite(raw): string` (Phase 5.12.3 backwards-compat one-line wrapper: `parts ? parts.host + parts.path : ""`; zero consumers outside this module as of 5.12.2 tip, kept defensively). New type exports `DedupeScoreBreakdown` + `DedupeMatchResult<T>`. `findVenueDedupeMatch(candidate, pool, options?)` return type changed from `T | null` to `DedupeMatchResult<T> | null` carrying `{ match, score: { name, address, website, city, total, threshold }, reason }`. Algorithm runs the points-based ladder per the spec § 8.1 weights table (name 60/25/0; address 50/20/0; website 40/20/0; city +10 conditional on at least one other signal contributing ≥1); threshold defaults to 60 (`>=` merges); both consumers pass no options for v1 lockstep. Cross-field VETO preserved fully from 5.12.0 on name FULL match + (city differ OR address differ) when both sides carry those fields (Plaza-NYC-vs-LA + same-city-same-name-different-address hard stops); vetoes fire per pool entry. Deterministic tiebreaker chain on ties at the same total above threshold: (1) normalized name ascending; (2) pool entry `id` ascending lexicographic; (3) pool insertion order as defensive fallback only when no `id`. Per-call telemetry emits `[venueDedupe] candidate="..." best="..." score=name:X+address:X+website:X+city:X=TOTAL threshold=N decision=merge|no_merge|no_merge_identity_gate` for the best non-vetoed scorer (suppressed on empty pool); the `no_merge_identity_gate` decision is new in Phase 5.12.4.1 and surfaces when telemetry-best clears threshold but fails the identity-signal gate (see below). **Phase 5.12.4.1 identity-signal gate:** the eligible filter requires `s.score.name >= 1 || s.score.website >= 1` on top of `!s.vetoed && s.score.total >= threshold`. Pre-fix: address (50) + city (10) = 60 cleared the threshold alone, so two distinct venues at the same building address with the same city ("75 Rockefeller Plaza - Top of the Rock" and "75 Rockefeller Plaza - Studio 8H") would have merged. Post-fix: fall through to `no_merge_identity_gate`. The gate is on identity signals, not on raw point count; the cross-field VETO on name FULL match (Plaza-NYC-vs-LA + same-city-same-name-different-address hard stops) is preserved unchanged and fires before the gate. `dedupe_meta` write shape unchanged; pre-5.12.4.1 historical rows with `name=0+address=50+city=10` are not backfilled (gate filters before match resolution, so no new gated-rejection row ever lands as a `dedupe_meta` write). Two consumers: `vs-generate-deck`'s `pushVenuesToHq` (5.12.0; dedupes VS candidates against the master HQ `venues` pool at Generate Deck click; INSERTs new HQ rows on no-match + writes `linked_venue_id` on every candidate; **5.12.3 also writes `vs_candidate_venues.dedupe_meta` jsonb** alongside `linked_venue_id` ONLY on FRESH ladder-resolved matches — explicit NOT-written set: pre-linked candidates (else branch skips the ladder), hq_pool rows (linked at insert time), fresh-INSERT no-match rows (no match round happened)) and `vs-research-venues` Phase B cross-rail dedupe (5.12.1; drops Claude's net-new picks that collide with `vs_candidate_venues` rows already inserted as `source='hq_pool'`; **5.12.3 drop log line carries the scoring reason for debug**). The extraction was the trigger for the "lift if a second consumer appears later" note in the 5.12.0 vs-generate-deck header.

### `database.types.ts`: Phase 5.16.1.2
Generated `Database` type for the Deno tree (`supabase gen types typescript --linked`, mirrored from the frontend `src/integrations/supabase/types.ts` via `/tmp` + `test -s` + `mv` per the migrations CLAUDE.md rule — never pipe `gen types` straight into a tracked file). New edge functions that read a full table row import `Database` from `../_shared/database.types.ts` and use `Database["public"]["Tables"][...]["Row"]`. Existing hand-rolled narrow `.select(...)` interfaces (e.g. `RoundRow = {id, round_number}`, `HqVenueRow`) are intentionally narrow or joined/derived shapes and are NOT swept to the full Row — that would over-fetch the type surface; retrofit opportunistically when a function is touched for real reasons. Caveat: no local `deno` typecheck, and edge deploys transpile via swc (no typecheck), so these types are source-level documentation, not a build gate. Closes `code-observations.md` Edge #18; reverses the Phase 5.16.1.1 "not wired" note (`decisions.md` § 5.16.1.2 D12).

## Conventions

- New Edge Functions that self-invoke: add `[functions.<name>] verify_jwt = false` to `supabase/config.toml` AND use `requireInternalOrUserAuth` from `_shared/internalAuth.ts`.
- Edge row typing: prefer `Database["public"]["Tables"][...]["Row"]` from `_shared/database.types.ts` for full-row selects (Phase 5.16.1.2); keep intentionally-narrow per-`.select()` interfaces where they read only a column subset or a joined shape. `logClaudeUsage` (a dead, caller-less log helper) + its `EvalUsage` type were pruned from `_shared/buildClaudeEvalRequest.ts` in 5.16.1.2 (Edge #19).
- All outbound email from `jobs@mirrornyc.com` via the service account's `gmail.send` scope, not from individual users.
- Anthropic calls go through `callClaude`, never `fetch('https://api.anthropic.com/...')` directly. Per-app keys + spend tracking + caching all flow from there.
- Use 1-hour prompt caching for the role-context block whenever the same role is being processed N times (initial pull batches, bulk re-eval, final review).
