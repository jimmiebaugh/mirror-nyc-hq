# HQ v1 changelog

Mirror NYC HQ shipped its v1 cut on 2026-05-18 at the `v1.0.0` tag and has continued shipping through Phase 5.12 (Venue Scout review, 2026-05-27). This changelog covers every sub-phase from Phase 3 (Talent Scout port) through Phase 5.12 (Venue Scout review).

Format: chronological ship table at the top, feature-area sections below.

**Commit hashes:** the chronological ship table preserves the original sub-sub-phase hashes for historical fidelity, but git history has since been rebased so each X.X sub-phase is now ONE squashed commit on `main` (Phase 5.1 = `fe04a5d`, 5.2 = `56b0484`, 5.3 = `af59516`, 5.4 = `e528999`, 5.5 = `9c73788`, 5.6 = `b62af98`, 5.7 = `9d2bdc4`, 5.8 = `8a468d2`, 5.9 = `81cdaf2`, 5.10 = `5d57b26`; 5.11.0 = `a2adbaf`, 5.11.1 = `38f5374`, 5.11.2 = `c19b332` — these three retain .Y granularity). The pre-rebase per-sub-sub-phase hashes in the table below are useful as historical labels but no longer resolve in `git log`.

## Chronological ship table

| Sub-phase | Date | Commit | One-line summary |
|---|---|---|---|
| 5.1 | 2026-05-15 | [`39bdd8c`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/39bdd8c) | HQ Core foundations: permission-tier rewrite (admin / standard / freelance / pending); polymorphic `notes_log` table; AppShell left rail; Home dashboards. |
| 5.2.1 | 2026-05-15 | [`2938aa1`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/2938aa1) | Project workflow trio (Projects / Tasks / Deliverables) plus first-cut DataTable + BoardView + TimelineView + CalendarMonthView primitives. |
| 5.2.1 Revision | 2026-05-16 | [`0ce758f`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/0ce758f) | Wireframe-fidelity rebuild of Projects + Tasks + Deliverables surfaces against the locked Surface 04 / 07 / 13 designs. |
| 5.2.2 | 2026-05-16 | [`6214002`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/6214002) | Organizations + People + Venues entity trio plus four lookup migrations (`cities`, `project_categories`, `org_capabilities`, `venue_types`); `<InternalNotesEditor>` + `<StarRating>` added. |
| 5.2.3 | 2026-05-16 | [`519a46c`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/519a46c) | `organizations` split into `clients` + `vendors` with FK reseat across projects + people + notes_log; six new pages and five migrations. |
| 5.2 cleanup | 2026-05-16 | [`9b63650`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/9b63650) | Six small carry-forwards: `IconClients` glyph, `vendors.primary_address`, `vendor_capabilities` DELETE grant, Tags filter "any-of", DataTable lookup-hydration race fix. |
| 5.3 | 2026-05-16 | [`740ad7e`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/740ad7e) | Calendar + Outlook entries + project install / removal date ranges; per-source visibility panel persisted via implicit `saved_views` row; `promote_outlook_to_project` RPC. |
| 5.4 | 2026-05-16 | [`0e0f0ef`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/0e0f0ef) | Wiki + Account Logins + Users + Settings: four new tables (`departments`, `wiki_pages`, `credentials`, `mirror_holidays`); id-swap signup; 11 seeded wiki pages; admin pre-provisioning. |
| 5.5 | 2026-05-16 | [`82ad5d8`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/82ad5d8) | Notifications + Activity Feed + Search + States polish; bell panel; `user_notification_preferences`; Slack DM dispatch infrastructure; three `pg_cron` schedules for deliverable / task / event reminders. |
| 5.5.1 | 2026-05-16 | [`8e043ff`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/8e043ff) | Sign-in page (Surface 01). |
| 5.6.1 | 2026-05-16 | [`65e45ea`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/65e45ea) | Cross-cutting interaction primitives: `<RecordCombobox>`, `<ClickPillCell>`, phone normalization helper, Project Team card. |
| 5.6.2 | 2026-05-16 | [`04e9588`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/04e9588) | List-table reshapes across HQ Core; new `vendor_subcategories` lookup + `project_vendors` join table; parent-scoped `useLookup`. |
| 5.6.2.1 | 2026-05-16 | [`01d35ac`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/01d35ac) | 5.6.2 follow-on fixes. |
| 5.6.2.2 | 2026-05-16 | [`e1b8713`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/e1b8713) | PostgREST embed-FK disambiguation hotfix + PersonEdit Venue typeahead. |
| 5.6.3 | 2026-05-16 | [`bdae150`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/bdae150) | PersonDetail inline-edit prototype; `people.affiliation_type` enum + mutex CHECK constraint. |
| 5.6.3.1 | 2026-05-16 | [`ba89adb`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/ba89adb) | Inline-edit sweep across six HQ Core detail pages (Client / Vendor / Person / Venue / Task / Deliverable). |
| 5.6.4 | 2026-05-16 | [`2506d9d`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/2506d9d) | Visual polish batch. |
| 5.6.4.1 | 2026-05-17 | [`276df71`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/276df71) | 5.6.4 smoke-test follow-on (16 items); migration widens `notes_log.parent_type` CHECK to `outlook_entry`. |
| 5.6.5 | 2026-05-17 | [`986b953`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/986b953) | Global default views: `users.is_owner` boolean + `saved_views.scope` ('user' / 'global'); owner-only writes on the global scope via scope-aware RLS. |
| 5.6.5.1 | 2026-05-17 | [`b2de061`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/b2de061) | Owner delegation + sort persistence propagation + Team page account-link fixes; `users_protect_admin_columns` gains an is_owner write gate plus self-revoke block. |
| 5.6.5.2 | 2026-05-17 | [`4f47cfd`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/4f47cfd) | `last_active_at` stamping throttled to a 5-minute floor per user via sessionStorage. |
| 5.7.1 | 2026-05-17 | [`b7a5238`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/b7a5238) | Global cleanup + `<RecordCombobox>` polish + Quick-Add wiring across the rail and Home dashboards. |
| 5.7.2 | 2026-05-17 | [`5e63484`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/5e63484) | @-mentions feed + append-only Notes on Tasks and Deliverables; `note_mentions` table; `notes_log.parent_type` widened to `task` + `deliverable`. |
| 5.7.3 | 2026-05-17 | [`2e61571`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/2e61571) | Detail-page polish + fixed-position `<StickySaveBar>` + Status Notes on projects swapped to `<InternalNotesEditor>` (append-only). |
| 5.7.4 | 2026-05-17 | [`aab31b9`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/aab31b9) | List-page polish + TaskEdit hard-delete; combined "Account / Design Leads" column on Projects; new `purple` StatusToken. |
| 5.7.5 | 2026-05-17 | [`43eeb27`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/43eeb27) | Deliverables status reshape: drop `In Progress` (Upcoming / Complete / Skipped final set); auto-task lifecycle helper writes `tasks.source_deliverable_id` with CASCADE FK. |
| 5.7.6 | 2026-05-17 | [`612c8e8`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/612c8e8) | Global FilterBar distinct-values picker; `distinctValuesByField` prop wired across all nine HQ Core list pages. |
| 5.7.7 | 2026-05-17 | [`c8bc789`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/c8bc789) | `project_members` general-team join + ProjectEdit reshape + Dashboard read-only links + Tags chips + Team filter + DataTable inline quick-add prop. |
| 5.7.8 | 2026-05-17 | [`9bf068e`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/9bf068e) | Venues + Vendors filter overhaul; SavedViews moved inline with FilterBar; FilterBar op locked to "is" globally with `allowIsNot` opt-in for Tasks + Deliverables. |
| 5.7.9 | 2026-05-17 | [`3d5f1cc`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/3d5f1cc) | Calendar overhaul: color sweep + pagehead reshape + My Tasks layer + Day / Week / Month view switcher; activeView + showMyTasks persist via `__calendar_default` saved-view jsonb. |
| 5.7.10 | 2026-05-18 | [`367fa0e`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/367fa0e) | Settings Lookup Lists merge + Wiki image upload (private bucket, 1-year signed URLs, diff-on-save cleanup); Integrations card stubbed to "Coming Soon". |
| 5.7.11 | 2026-05-18 | [`d16c4c1`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/d16c4c1) | Vendor Files & Assets URL list (`vendor_files` table + `<VendorFilesEditor>`); VendorDetail title-width fix; Internal Rating caption removal. |
| 5.7.12 | 2026-05-18 | [`60f6ff0`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/60f6ff0) | Profile + Settings split surfaces; extended `users_protect_admin_columns` to gate `is_owner` / `email` / `full_name`; dropped Creative department; 5.7.2 mention-demotion reverts. |
| 5.7.13 | 2026-05-18 | [`8eea759`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/8eea759) | Vendor team rating: per-user `vendor_ratings` table + team aggregate; drop legacy `vendors.internal_rating`; `<StarRating>` extended with `md` size + half-star rendering. VendorDetail subtitle reshape; "Internal" pill inline. |
| 5.7.14 | 2026-05-18 | [`59a5dac`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/59a5dac) | Leftovers cleanup + carry-forward triage: drop projects legacy notes, `notes_log` orphan sweep migration, MyWeekStrip today-forward + pagination, ProjectActivity Realtime + child-entity rollup, coral sweep, PostgREST embed convention documented. |
| 5.8.0 | 2026-05-18 | [`975568f`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/975568f) | HQ v1 kickoff: roadmap rollover; annotated tag `v1.0.0` pinned; fires the deferred Phase 5.7 Netlify deploy that the 5.7.14 squash suppressed. |
| 5.8.5 | 2026-05-18 | post-rebase: `8a468d2` | Security pass + accept-risk docs: 7 migrations (function search_path pinning x5, REVOKE EXECUTE on SECURITY DEFINER fns, RLS init-plan rewrite of 17 policies, storage bucket listing tighten, F001 credentials column encryption via pgsodium AEAD with three SECURITY DEFINER RPCs). Tier 3 docs additions. 18 edge function redeploys. |
| 5.8.5.1 | 2026-05-18 | post-rebase: `8a468d2` | Hotfix re-GRANT EXECUTE on RLS helper functions (`is_admin` / `is_producer_or_admin` / `current_user_role`) after 5.8.5 REVOKE FROM PUBLIC broke RLS predicate evaluation for authenticated callers. Production-restored within minutes via Dashboard SQL; migration encodes the GRANT for future rebuilds. |
| 5.8.6 | 2026-05-18 | post-rebase: `8a468d2` | Tier 2 surface reduction: `npm audit fix` bumps closing HIGH XSS; DOMPurify wrap on WikiProseRenderer; 28 unused shadcn UI deletes + 23 unused deps uninstalled (CSS bundle -26KB); Anthropic redactSecrets + AbortSignal.timeout(60s); 11 mechanical lint fixes; sonner toaster migrated to shadcn; secrets/ relocated outside repo. |
| 5.8.7 | 2026-05-18 | post-rebase: `8a468d2` | Phase 5.8 close + audit v1.1 quick wins (N001 worktrees ignore + sweep, N002 next-themes uninstall, N003 generated-types eslint ignore, N005 credentials RPC predicate refactor via `is_producer_or_admin()`, N006 credentials reveal audit log). `v1.1.0` annotated tag pinned. |
| 5.8.8 | 2026-05-19 | post-rebase: `8a468d2` | Auth pre-provision sign-in regression hotfix: encode 5.8.5.1's missed GRANTs on `handle_new_user` + `users_protect_admin_columns`; restore the Phase 5.4 swap block; scope new-user email dispatch to active owners only (bell-panel stays admin-wide); add symmetric `trg_users_align_id_to_auth` BEFORE INSERT trigger so pre-provisioning is order-agnostic; one-time data repair on `jobs@mirrornyc.com` mismatched id; OAuth `redirect_uri` sanitization in `useAuth.tsx` + `ProtectedRoute.tsx`. |
| 5.8.8.1 | 2026-05-19 | post-rebase: `8a468d2` | ON UPDATE CASCADE on all 42 FKs pointing at `public.users.id`. Phase 5.4 dropped the outbound FK to `auth.users(id)` but missed adding ON UPDATE CASCADE to the inbound side; the swap UPDATE inside `handle_new_user` would FK-violate the moment a pre-provisioned user had any project_members / tasks.assignee_id / etc. attachment. |
| 5.9.1 | 2026-05-19 | post-rebase: `81cdaf2` | Bulk import shared primitive: `bulk_import_sessions` + `bulk_import_drafts` tables; `bulk-import` edge function scaffold (admin-gated, transactional shell); HQ-side sheet parser; shared UI (`BulkImportPage` + `UploadStep` + `MapStep` + `DedupeStep` + `ImportGrid` virtualized); EntityConfig registry; Settings card. |
| 5.9.2 | 2026-05-19 | post-rebase: `81cdaf2` | Projects importer + `bulk_import_commit_projects` SECURITY DEFINER RPC (atomic cross-table commit) + 8 ImportGrid cell editors; MapStep RecordCombobox typeahead; FilterBar `presence` type + "Bulk Imported" chip. |
| 5.9.3 | 2026-05-19 | post-rebase: `81cdaf2` | Vendors importer + `bulk_import_commit_vendors` RPC. Category/subcategory FK refs, capabilities as text[] of names with auto-create, preferred bool coerced from "true"/"false" string. |
| 5.9.3.1 | 2026-05-19 | post-rebase: `81cdaf2` | `vendors.nationwide` bool + generic `applyFilters` `fieldMatchAll` hook so nationwide vendors surface under every city chip. Green "National" pill on VendorsList. |
| 5.9.3.2 | 2026-05-19 | post-rebase: `81cdaf2` | `vendors.general_email` company-level email distinct from primary contact's; VendorDetail relabels "Email"/"Phone" → "Contact Email"/"Contact Phone". |
| 5.9.3.3 | 2026-05-19 | post-rebase: `81cdaf2` | Vendor importer creates a vendor-affiliated `people` row per contact (Primary Contact); vendor-scoped dedupe. |
| 5.9.4 | 2026-05-20 | post-rebase: `81cdaf2` | Venues importer + `bulk_import_commit_venues` RPC. `venue_types` written through the `venue_venue_types` JOIN; per-row contact creates Venue-affiliated `people` row + `venue_contact_people` join. `exclusive_vendor_ids` deliberately NOT importable. |
| 5.9.5 | 2026-05-20 | post-rebase: `81cdaf2` | Bulk-import audit page at `/settings/bulk-import/history`; `_smoke` route + handler + page + union member removed; Phase 5.9 deploy fired. |
| 5.9.6 | 2026-05-21 | post-rebase: `81cdaf2` | Bulk-import undo (7-day window): two `uuid[]` tracking columns on `bulk_import_sessions`; new `bulk_import_undo` SECURITY DEFINER RPC; edge fn `mode: "undo"` + `dry_run`. UI Undo button in audit-page detail rail with cascade-warning AlertDialog. |
| 5.9.7 | 2026-05-21 | post-rebase: `81cdaf2` | Import Event Day Rate (appends one `event_day` `venue_rate_history` row per venue, dated `current_date`, only when amount changed) + `venues.general_email`. VenueDetail Venue Details card reordered; duplicate Venue Slide link removed. |
| 5.10.0 | 2026-05-21 | post-rebase: `5d57b26` | `venues.notes` → `venues.about_venue` rename (OID-preserving) + new `hq-generate-venue-about` edge function (first `callClaude('hq', ...)` consumer). HQ generator is TOOL-LESS with evergreen `ABOUT_VENUE_SYSTEM` prompt + web_search; Generate / Regenerate buttons on VenueDetail + VenueEdit. Breaking-rename coordinated migration + functions + frontend in one push. |
| 5.10.1 | 2026-05-22 | post-rebase: `5d57b26` | VenueEdit + VenueDetail layout refresh: 2-column label-above Details cards, `card-headbar` headers, Master Venue Deck Slide card, Features as tag input. First responsive `@media` breakpoints; `.label-form` coral → grey app-wide. |
| Codebase audit (v1) | 2026-05-22 | `34b32d4` | Slim-down + legibility pass over the HQ frontend, reviewed cold by Codex. Dead Lovable scaffolding removed; superseded components removed; 23 unused deps + the stray `bun.lockb` dropped; ~66 route components converted to `React.lazy` behind Suspense; bulk-import parser dynamic-imports `xlsx`. Initial bundle 2.24MB → ~586KB. `.env` untracked + `.env.example` added. |
| Triage cleanup | 2026-05-22 | (folded into `34b32d4`) | Verified-row cleanup: `VenueTypePill` extracted to shared, `prettyHost` extracted to shared, em-dash sweep on Settings Integrations caption, `CandidateSearch` renamed to `candidateSearch.ts`, dead `poolStatus.ts` deleted. |
| 5.11.0 | 2026-05-22 | `a2adbaf` | UX/design-system audit + implementation pass. Focus rings, mobile shell (off-canvas hamburger drawer below 1024px) + 44px tap targets, `--info` + `--purple` tokens, coral link contrast via `--primary-hover`, `hq-` chrome convergence (dead Phase 5.2.1 block removed, Home + ClickPillCell migrated to unprefixed canonical), shadcn `rounded-sm`/`rounded-md` resolved to `var(--radius)`, edit-page card headers converted to `card-headbar` + `.h-card`. `src/index.css` net -382 lines. |
| 5.11.1 | 2026-05-23 | `38f5374` | Detail-page polish + list-table standardization + managed Project Tags / Venue Features lookups. Plain titles, `.detail-meta`, DField rows; Project/Venue/Vendor/Client/Person/Task/Deliverable polish; Contacts + relationship-card patterns; de-chipped tag styling; list-table standardization; global token + chrome adjustments. |
| 5.11.2 | 2026-05-23 | `c19b332` | Structural consistency. W1: TaskDetail + DeliverableDetail eyebrow / plain h1 / detail-meta / hero `ClickPillCell` status; `.crumb` back links; stacked-identifier `--primary-hover` color. W2: extracted `DField`, `HQFormField`, `ContactsCard`, `WebsiteActionButton`, neutral `prettyHost`, `ListPageChrome`. W3: VendorEdit Tags dropped (Capabilities is vendor tag set); Task/Deliverable/Project edit shell alignment; `RecordCombobox multi` for `blocked_by` + assignees; PersonDetail Associated Venues = standalone relationship card. W4: docs canon recorded. |
| **5.12 (aggregate)** | **2026-05-27** | **`1a01d3e`** | **Phase 5.12 squash. 28 sub-phases (5.12.0 through 5.12.14.3) collapsed into one commit. Detailed sub-phase rows below.** |
| 5.12.0 | 2026-05-23 | `1a01d3e` | VS Pass 2 converged onto `ABOUT_VENUE_SYSTEM` (tool-less + web_search + prompt cache); legacy `OVERVIEW_TOOL` + `OVERVIEW_SYSTEM` retired. Overview prompt input = recs + producer notes only (no considerations). HQ Venues push moved from the retired `vs_candidate_venues_shortlist_sync` trigger to the top of `vs-generate-deck`, gated by a DeckPrep confirmation modal. Name-first dedupe ladder with cross-field conflict check (then address, then website); `venues.about_venue` write-when-blank preserves producer edits. New `HQ_PUSH_FAILED` error code routed in ErrorState. `WORK_TIMEOUT_MS` on vs-compile-summaries bumped to 240s as an intentional expected-case cap. White Box added to CANONICAL_TYPES (server + frontend lockstep). `buildStub` lifted to `_shared/venueOverview.ts`. `generate()` on DeckPrep hardened with `flushPendingFieldSaves` + explicit error toasts on reset / order / include UPDATEs. |
| 5.12.1 | 2026-05-23 | `1a01d3e` | HQ Venues pool in VS sourcing + vs-research-venues race fix. `vs-research-venues` extended with a pre-Phase-B HQ pool insert (city exact match + neighborhood / venue_type soft-sort signals + 10-row cap; rows insert as `source='hq_pool'` with `linked_venue_id` pre-set + `venue_overview` pre-filled from `venues.about_venue`) + a post-Phase-B cross-rail dedupe against the seeded hq_pool rows (runs before the expensive URL-validation step). New SECURITY DEFINER `vs_research_try_acquire_kickoff` RPC replaces the pre-existing race-prone non-atomic check at lines 602-621 (`pg_try_advisory_xact_lock` + same-transaction read-and-write; GRANT TO service_role only). `vs_candidate_venues.source` CHECK widened to allow `'hq_pool'`. `vs-compile-summaries` Pass 1 skips hq_pool rows (canonical HQ data); Pass 2 skips when `venue_overview` is non-empty (mirrors HQ write-when-blank). Dedupe helpers extracted to `_shared/venueDedupe.ts` (second-consumer trigger fired; `vs-generate-deck`'s `pushVenuesToHq` cascade now calls `findVenueDedupeMatch`). New `SourcePill` state "Venues DB" (teal palette) on SourcingReport + Shortlist; `SOURCE_PRIORITY` extended to manual < sheet < hq_pool < research. Wrapper hot patch folded in: `_shared/anthropic.ts` per-fetch ceiling bumped 60s -> 180s + AbortController + sentinel reason so network-level AbortErrors no longer mislabel as wrapper timeouts (label renamed `anthropic_timeout_60s` -> `anthropic_timeout_180s`); all 10 Claude callers redeployed. Two follow-on sub-phases (5.12.2 Cities combobox + 5.12.3 points-based dedupe) spawned during smoke; existing 5.12.2-5.12.7 renumbered down two slots to 5.12.4-5.12.9. Resolves code-observations Edge #1 (confirmed-live token double-spend). Held under the 5.12 deploy budget. |
| 5.12.2 | 2026-05-23 | `1a01d3e` | VS Cities consistency + alias extension. `BriefVenue.tsx` + `BriefReport.tsx` City inputs swapped to `<RecordCombobox source={{ kind: "lookup", table: "cities" }}>` matching VenueEdit + ProjectEdit verbatim. `vs-parse-brief` `resolveOrCreateCity` ladder (final shape after the alias extension): trim -> state-strip on `/,\s*[A-Za-z]{2}\s*$/` -> alias-on-trimmed -> alias-on-stripped -> cities-on-trimmed -> cities-on-stripped -> insert using stripped value. Best-effort error posture; 23505 race-loser re-read. Three migrations land together: (a) `20260603180000_phase_5_12_2_backfill_scout_cities.sql` does case+trim canonicalization on existing `vs_scouts.city`; (b) `20260603190000_phase_5_12_2_city_aliases.sql` creates the new `city_aliases` lookup (`(alias text, city_id uuid FK to cities, created_by uuid)`, `LOWER(alias)` unique index, RLS open SELECT/INSERT/UPDATE + admin DELETE) and seeds 4 LA/NYC aliases; (c) `20260603200000_phase_5_12_2_city_aliases_cleanup.sql` re-runs the backfill with corrected alias-first ordering across all 5 free-text city columns (vs_scouts / venues / vendors / projects / clients) and DELETEs polluting cities rows whose LOWER(name) matches an alias. Repaired one pre-alias parser polluter ("Los Angeles, CA" cities row + 2 referencing rows: 1 scout + 1 venue). Frontend: new `useCityAliases` hook (module-level cache in `src/lib/hq/lookups.ts`); `LookupCombobox` renders aliases as a read-only "Aliases" CommandGroup below canonical options when `source.table === 'cities'` so typing "Los Angeles" surfaces the alias whose canonical is "LA". Aliases never enter `optionLabelById`, keeping form-value display always canonical. Admin alias curation via Settings -> Lookup Lists carry-forward; new aliases land via SQL today. Closes both the case/trim AND alias variants of the 5.12.1 HQ pool end-to-end gate. Held under the 5.12 deploy budget. |
| 5.12.3 | 2026-05-23 | `1a01d3e` | Points-based venue dedupe ladder + producer-visible merge explainability. `_shared/venueDedupe.ts`'s `findVenueDedupeMatch` reshaped from a strict name → address → website cascade to weighted scoring across name (60 full / 25 partial token / 0) + address (50 full / 20 street-number / 0) + website (40 host+path / 20 host / 0) + city (+10 conditional, only when another signal hits); threshold 60 (`>=` merges); both consumers pass no options for v1 lockstep. Cross-field VETO preserved fully from 5.12.0 (name FULL match vetoed when both sides have non-empty cities OR addresses AND they differ): Plaza-NYC-vs-LA city-veto + same-city-same-name-different-address veto. New `normalizeWebsiteParts(raw): { host, path } | null` (locked website normalizer: `new URL` parse + https prepend on missing scheme + lowercase host + `www.` strip + query/hash ignored + trailing-slash trim); legacy `normalizeWebsite` kept as a one-line wrapper for backwards compat. Deterministic tiebreaker chain (normalized name asc → pool entry id asc → insertion order) so the merge target is stable across reruns even when the DB SELECT order shifts. New `dedupe_meta` jsonb column on `vs_candidate_venues` (migration `20260603220000_phase_5_12_3_vs_candidate_venues_dedupe_meta.sql`; nullable, no backfill); `vs-generate-deck.pushVenuesToHq` writes it alongside `linked_venue_id` ONLY on FRESH ladder-resolved matches (explicit NOT-written set: pre-linked candidates, hq_pool rows, fresh-INSERT no-match rows). Cross-rail dedupe in `vs-research-venues` drops the research row entirely; the `[vs-research-venues] ... drop research row` log line now carries the scoring reason for debug. New `[venueDedupe]` per-call telemetry log line (best-scorer breakdown + decision; suppressed on empty pool) so threshold calibration has real data. New `DedupeMetaIndicator` (Popover-backed info icon) + `normalizeDedupeMeta(raw): DedupeMetaShape | null` runtime shape guard in `matrix/primitives.tsx`. `VenueIdentityStack` extended with two optional props (`linkedVenueId` + `dedupeMeta`); indicator co-locates with SourcePill on Shortlist + SourcingReport. DeckPrep adds source-pill + indicator inline at the bottom of `DeckRow`'s venue-summary stack (DeckPrep doesn't consume VenueIdentityStack because its row carries metadata fields the shared primitive doesn't; footer pattern mirrors). All three matrix pages extend Venue type + SELECT + matrix-row JSX explicitly (the indicator does NOT auto-light up from the shared primitive alone). No Realtime subscription added; navigate-away-and-back covers the producer flow (cross-tab live sync folds into 5.12.14). Closes the Platform-LA recall hole the 5.12.1 smoke surfaced. Held under the 5.12 deploy budget. |
| 5.12.6 | 2026-05-24 | `1a01d3e` | VS scout delete cascade: storage-file cleanup on scout delete. New `supabase/functions/vs-delete-scout/index.ts` (`verify_jwt = true`, service-role client; no extra in-function authorization gate beyond the gateway check + 404 on missing scout because `vs_*` RLS is open-authenticated AND the function takes only `scout_id` with no cross-entity surface; counter-example to `vs-research-single-venue`'s three-gate posture per `docs/auth-model.md` § Function-level authorization gates). Order is load-bearing: enumerate storage paths (read-only: `vs_scouts.brief_data.uploaded_files` -> `briefs` bucket paths, `vs_scouts.sheet_storage_path` -> `sourcing_sheets` bucket path, `vs_venue_photos.storage_path` joined via `vs_candidate_venues!inner(scout_id)` -> `vs_venue_photos` bucket paths) -> DB DELETE on `vs_scouts WHERE id = $1` (cascades to `vs_sourcing_rounds` + `vs_candidate_venues` + `vs_venue_photos` rows via existing FK ON DELETE CASCADE) -> per-bucket `storage.from(bucket).remove([...paths])` in 100-path chunks. Per-bucket errors are collected into the response payload as `storage_errors: [{ bucket, path, message }]` but NEVER thrown; the scout row is already gone, so partial storage failures degrade to pure orphans with no DB references (the strictly safer degraded state and a direct match for pre-5.12.6 behavior where ALL files orphaned). Cleanup-first ordering was rejected: a storage-cleanup failure followed by successful DB DELETE leaves a half-deleted scout pointing at missing paths; producer sees a broken state. `[vsDeleteScout]` telemetry log line carries scout_id + per-bucket enumerated/removed/failed counts + wall + outcome (`ok` / `partial`); shape mirrors the `[hqPoolFilter]` / `[hqPoolRescue]` / `[hqPoolResearchEnrich]` accounting convention. Response shape: `{ ok: true, scout_id, event_name, counts: { candidate_venues, briefs_removed, briefs_failed, sheets_removed, sheets_failed, photos_removed, photos_failed }[, storage_errors] }` on success; `{ ok: false, error: "scout_not_found" }` 404 / `{ ok: false, error: "db_delete_failed", message }` 500 on failure. Frontend `src/pages/venue-scout/ScoutIndex.tsx` `confirmDelete` handler swaps the direct `supabase.from("vs_scouts").delete().eq("id", pendingDelete.id)` for `supabase.functions.invoke("vs-delete-scout", { body: { scout_id: pendingDelete.id } })`; the existing `setDeleting(true)` / `setDeleting(false)` lifecycle wraps the new invocation. Success toast renders a per-bucket count summary built from the response (`Deleted "<event_name>" (N venues, M photos, K files)`; singular/plural variants; empty parts omitted; bare title when all counts are zero). Partial-cleanup failures surface a SEPARATE destructive-variant toast (`Project deleted, but storage cleanup failed for: <bucket list>. Safe to ignore.`) so the producer sees both "delete succeeded" and "files may need manual cleanup" without conflating them. The inline comment at the call site rewritten from the pre-5.12.6 "future cleanup cron will sweep orphaned files (parity with TS)" punt to reflect the new synchronous behavior. **HQ `venues` rows + Drive deck files stay permanently by design.** `vs_candidate_venues.linked_venue_id` references `venues(id) ON DELETE SET NULL` (HQ side); cascading the candidate venue leaves the HQ master row untouched. `vs_scouts.generated_decks[*].deck_id` Drive references stay forever (per Jimmie's call 2026-05-24): producers retain deck history outside the app after scout deletion; this is intentional behavior, NOT a deferred carry-forward, and is not planned for any future sub-phase. `supabase/config.toml` gains a `[functions.vs-delete-scout]` block with `verify_jwt = true` matching the sibling vs-* pattern. No migrations (all FK cascades already exist from the Phase 4.1-port schema). Edge function deploys first time with 5.12.6. Resolves the inline comment punt on `ScoutIndex.tsx` line 234 that has been live since Phase 4. Held under the 5.12 deploy budget. |
| 5.12.7 | 2026-05-24 | `1a01d3e` | HQ venues as first-class citizens in the sourcing matrix (HQ Venue picker + research-time hq_pool enrichment + manual venue row relocation + auto-research-on-promotion). Three threads in one squash; subsumes the pre-existing locked 5.12.7 manual-relocation scope under § C-merge resolution 2026-05-24. **Thread A: HQ Venue picker.** New `+ Add Venue from HQ Venue List` row pinned below the data rows on SourcingReport. New `src/components/venue-scout/HqVenuePicker.tsx` wraps the Phase 5.6.1 `RecordCombobox` primitive (`kind: "record"` source variant, `quickCreate={false}`, single-select). `loadOptions` SELECT against `venues` master table mirrors `loadHqVenuesIntoPool`'s SELECT byte-for-byte (column list reflects venues table shape: NO `venue_type` column — venue type lives on `venue_venue_types` join — and NO `size_sq_ft` column; canonical fields are `total_sq_ft` + `square_footage`); city-scoped to scout brief city via `.ilike('city', scoutCity)` when set, all-cities fallback when scout has no city. Pre-INSERT SELECT on `(scout_id, linked_venue_id)` dedupes against existing matrix rows with warn-and-skip toast on hit; no UNIQUE constraint added (concurrent-pick race accepted risk for a 5-user team). INSERT shape mirrors `loadHqVenuesIntoPool` byte-for-byte (`source='hq_pool'`, `linked_venue_id` set, structured fields populated verbatim from venues row, `venues.about_venue` copied verbatim into `venue_overview`); insert-time mapping reuses `hqVenueTypesToSlashJoined` (duplicated client-side to avoid a server round-trip per pick). No auto-research at picker insert (HQ data canonical; per-brief judgment via Pass 1 backstop at compile time or Feature B at next research run). No regenerate-for-brief button (Pass 2 not easily invokable standalone today; producer recovers per-brief tone via inline edit or by blanking the field to trigger Pass 2 regen on next compile per 5.12.1 prefilled-skip recovery). **Thread B: research-time `hq_pool` enrichment.** New `enrichHqPoolForResearch` helper in `vs-research-venues` colocated near `runPhaseA`; invocation sequential after `await phaseAPromise`, before the final CAS-guarded UPDATE flipping `current_step='sourcing_report'`. Reuses `buildFillUserMsg` + `FILL_SYSTEM` + `FILL_TOOL` byte-for-byte from 5.12.4 thread (a); `aboutVenue` passed when `venue_overview` is populated (renders the HQ CANONICAL ABOUT block + narrower research instruction). Batched in chunks of 5 mirroring `PHASE_A_CHUNK_SIZE`. Independent `ENRICH_TIMEOUT_MS = 60_000` deadline wraps the chunk loop so a slow enrichment leg can't push the function past Supabase's platform wall before the final UPDATE; on cap-hit the in-flight chunk completes, remaining chunks skip with `timed_out` accounting, and the final UPDATE proceeds. Apply-side patch guards mirror Pass 1 byte-for-byte: fill `recommendations` + `considerations` only when existing array is empty; fill `rank` (via `Math.round`) only when null; skip `derived_attrs` (FILL_TOOL doesn't carry the scout's `derived_columns` IDs; same posture as Pass 1); skip address/neighborhood/venue_type/website_url/size_sq_ft/capacity/key_features (hq_pool rows already carry canonical HQ values). Warn-log + skip on EVERY failure path (SELECT failure, Claude failure, missing tool_use block, UPDATE failure, per-row throw); NEVER `writeFailure` (would CAS-no-op the final UPDATE per 5.12.4.2 Fix 2). New `[hqPoolResearchEnrich]` telemetry line shape + `[hqPoolResearchEnrich:accounting]` accounting-identity warn mirroring `[hqPoolFilter]` + `[hqPoolRescue]`. Disjoint with rescue (5.12.4 thread b) by construction: rescue's inserts land with empty recs/cons/rank too (rescue's Claude call only emits keep/drop verdicts), so enrichment picks up BOTH deterministic-keep bare-insert rows AND rescue-keep inserts in the same pass. Compile-time Pass 1 `hq_pool` branch stays in place as defense-in-depth (predicate skips when fields populated; zero net cost per scout when research-time succeeded; cheap insurance against research-time failures). Cache shape reuses Pass 1's existing prompt-cache breakpoints (FILL_SYSTEM ephemeral 1h cache, shared across scouts). **Thread C: manual venue row relocation + auto-research-on-promotion.** Manual-add row + `addManualRow` function + `setLastManualId` plumbing moved verbatim from `Shortlist.tsx` to `SourcingReport.tsx` (Shortlist drops the manual-add row, the `addManualRow` function, the `Plus` lucide-react import, the `lastManualId` state + reset effect; pitch toggle gains the auto-research-on-promotion path). New `EditableField` variant `url` in `matrix/primitives.tsx` (renders `<input type="url">` with `placeholder="https://..."`; relies on browser-level URL format validation per spec § 6.1 — no blur-time helper since `validateWebsiteUrl` is Deno-only on the edge side and a duplicated browser helper would fail under CORS) + new `VenueIdentityStack.onWebsiteChange` prop. SourcingReport passes `onWebsiteChange` only for `source='manual'` rows so non-manual rows keep the read-only `WebsiteArrow` display unchanged; `VenuePatch` widens with `website_url`. Persist via `website_url: next.trim() || null` (empty string normalizes to null). New `supabase/functions/vs-research-single-venue/index.ts` edge function (single-venue mode of `vs-research-venues` Phase A pattern; new function rather than a mode of `vs-research-venues` for cleaner factoring given the existing function's lifecycle complexity — kickoff RPC + Phase A/B + rescue + Feature B enrichment + CAS-guarded UPDATE + `EdgeRuntime.waitUntil`). `verify_jwt = true`; user-invoked synchronous (Claude call ~10-20s; sync fine for v1; swap to `EdgeRuntime.waitUntil` + Realtime in a follow-on if latency surfaces as a UX problem). Three load-bearing authorization gates inside the function body since `vs_*` RLS is open-authenticated: `venue.scout_id === scout_id` (SELECT predicate), `venue.source === 'manual'` (SELECT predicate), scout state ∈ `{shortlist, review_selects, deck_prep}` (in-fn check; rejected from researching / compiling / generating_deck so a stale tab can't poison an in-flight scout's pipeline). All three predicates surface a 404 (rather than 403) so a probe surfaces no useful signal. "Already enriched" short-circuit returns `ok: true, patch: {}` when `recommendations` is already populated, so re-pitching a previously-enriched row after un-pitch never burns a second Claude call. Per-field defensive patch mirrors Pass 1 + Phase A byte-for-byte (only fill empty columns; producer-entered values stay authoritative). Shortlist's `togglePitch` handler grows the auto-research-on-promotion dispatch: on pitch-toggle-ON for `source='manual'` rows where recs / cons / features / rank are all empty, fires `vs-research-single-venue` best-effort via per-tab `researchingVenueIds` Set ref (suppresses rapid-click duplicates within this tab; cross-tab duplicates accepted risk thanks to the function's "already enriched" short-circuit). Toast on failure, no toggle block. `supabase/config.toml` gains a `[functions.vs-research-single-venue]` block with `verify_jwt = true` matching the sibling vs-* pattern. **No migrations.** `vs_candidate_venues.source` CHECK already accepts `'hq_pool'` + `'manual'` (Phase 5.12.1 widening); `linked_venue_id` already exists; all picker INSERT columns already exist; Feature B writes to existing columns. **Edge function redeploys out-of-band:** `vs-research-venues` (Feature B enrichment site) + new `vs-research-single-venue` (deploys first time with 5.12.7). **Tailwind safelist note:** no new template-literal token classes introduced per `feedback_tailwind_safelist_token_classes`; all new Tailwind classes are static. **Post-smoke amendments 2026-05-24** (folded into the same squash, verified live against scout 39a9fab2): (a) HQ Venue picker restyled from input-field shape to a full amber-row click affordance (`bg-amber-400/5 hover:bg-amber-400/10`, design-system `--warn` palette), label `Add a {scoutCity} Venue from Venue Database`; click opens a Popover-anchored cmdk typeahead. (b) Newly-added producer rows (manual + hq_pool) pin to the BOTTOM of the matrix via a session-scoped `newlyAddedIds: string[]` partition in `sortedVenues`. (c) Auto-research trigger moved from Shortlist `togglePitch` to SourcingReport `onContinue`; Continue button blocks (spinner + label "Enriching new venues...") until every unenriched producer-added row has fired `vs-research-single-venue` in parallel. Per-row failure surfaces a summary toast but does not block the transition. (d) `vs-research-single-venue` widened: source predicate accepts `manual` OR `hq_pool`; state allowlist gains `sourcing_report`; `aboutVenue` passed to `buildFillUserMsg` when source is hq_pool. (e) New `code-observations.md` Edge row 12 logs retail-bias prompt note (4/4 net-new research rows all `Retail`); deferred to Phase 5.12.13 prompt audit. Held under the 5.12 deploy budget. |
| 5.12.9 | 2026-05-24 | `1a01d3e` | Neighborhoods lookup nested in cities (HQ + VS). Promotes `neighborhood` from free-text to a managed parent-scoped lookup nested under `cities`, mirroring `vendor_subcategories` (Phase 5.6.2) with `city_id` as the parent FK. New `public.neighborhoods` table (`id uuid PK`, `name text NOT NULL`, `city_id uuid NOT NULL REFERENCES cities ON DELETE CASCADE`, `created_by uuid REFERENCES users ON DELETE SET NULL`, `created_at timestamptz`); unique index on `(city_id, LOWER(name))`; btree on `city_id`; RLS open SELECT/INSERT/UPDATE to authenticated, DELETE admin-only via `is_admin()`. Migration `20260606000000` includes a multi-table backfill scraping distinct `(city, neighborhood)` pairs from (A) HQ `venues` rows with both fields, (B) `vs_candidate_venues` joined to parent `vs_scouts.city`, (C) every element of `vs_scouts.brief_data.target_neighborhoods` joined to parent scout's city. `jsonb_typeof = 'array'` guard on source C protects against non-array brief_data; `ON CONFLICT DO NOTHING` on the city-scoped LOWER(name) unique index keeps the backfill idempotent. Initial backfill seeded 15 distinct rows. **Column types on consumer tables stay text** (single + array) per the cities precedent: `venues.neighborhood`, `vs_candidate_venues.neighborhood`, and `vs_scouts.brief_data.target_neighborhoods` are unchanged. The lookup is the typeahead + admin curation source; stored values are still option-name strings. **`useLookup` hook extended** (`src/lib/hq/lookups.ts`): `LookupTable` union gains `"neighborhoods"`; new `PARENT_COLUMN_BY_TABLE` map (`vendor_subcategories: "parent_category_id"`, `neighborhoods: "city_id"`) + `parentColumnFor(table)` helper; the two previously-hardcoded `parent_category_id` references (SELECT filter in `ensureLoaded` line 87 + INSERT payload in `addOption` line 313) read from the map. New `useCityIdForName(cityName: string | null): string | null` helper (case-insensitive trim match against `useLookup("cities")` options; returns null for blank/no-match, keeping the picker disabled until a canonical city is selected). **Settings page** adds a dedicated `<NeighborhoodsLookupEditor>` card between Lookup Lists and Bulk Import (parent-scoped layout doesn't fit the flat `OTHER_LOOKUPS` row table); native `<details>` collapsible per city with count chip; per-city tag editor mounts lazily on first expand (admin curation, low frequency). **Every neighborhood field across HQ + VS swaps to `<RecordCombobox>`:** VenueEdit + VenueDetail (HQ), BriefVenue + BriefReport (VS brief), Review, SourcingReport + Shortlist + DeckPrep (matrix). All parent-scope to the relevant `cities.id` (form.city, venue.city, draft.city, scoutMeta.city via `useCityIdForName`); each disables on `!cityId` so producers can't add a neighborhood without a canonical parent city. **City-change clears neighborhood / target_neighborhoods on every consumer**, mirroring the VendorEdit/VendorDetail subcategory clear (Phase 5.6.2). VenueDetail's new `saveCityName` helper writes `{ city, neighborhood: null }` as a single PostgREST UPDATE for atomicity. BriefVenue uses the existing `patch({ city, target_neighborhoods: [] })` helper. BriefReport's edit card extracts a `<CityNeighborhoodsEditor>` child component (so `useCityIdForName(draft.city)` legally lives at the top of a render body; `renderEditor` can't host hooks because it's invoked conditionally on edit-mode toggle). `strict_neighborhoods_only` is NOT cleared (independent producer preference). **Matrix surfaces**: scout city resolved once at the page level via `useCityIdForName(scoutMeta.city)` and threaded into every row; SourcingReport already loaded `city` (Phase 5.12.7), Shortlist + DeckPrep extend their `vs_scouts` SELECT to include `city`. DeckPrep's previous `hasAnyMeta` blank-meta hiding is overridden for the neighborhood row specifically: the combobox itself is the empty-state affordance, so the row always renders regardless of `(venue.neighborhood ?? "").trim().length`; size + capacity rows keep their blank-skip behavior. **Carry-forwards:** AI-write canonicalization against the lookup (FILL_TOOL.neighborhood + brief parser schema description nudges) deferred to 5.12.13 prompt audit; compact-styling visual tighten for combobox triggers in dense matrix cells lands with 5.12.14's "Venue tables full audit". **No edge function changes; no notification or activity-log triggers** (lookup tables historically silent; add/remove noise low value for admin audit). Doc updates folded in: schema.md gets a new `### neighborhoods (Phase 5.12.9 lookup)` section with cross-references from `venues.neighborhood`, `vs_candidate_venues.neighborhood`, and `vs_scouts.brief_data.target_neighborhoods` lines; decisions.md gets a § Phase 5.12.9 entry; roadmap.md flips 5.12.9 PLANNED → DONE; applied queued REPO_DOC_UPDATES entries (5.12.8 CUT 2026-05-24 row removed from roadmap with a § Phase 5.12.8 cut entry added to decisions.md; working-with-claude.md § 2 pre-flight drift-check grep updated to BSD/macOS `-E` form matching `**Marker commit:** \`<hash>\``). Held under the 5.12 deploy budget. |
| 5.12.5 | 2026-05-24 | `1a01d3e` | Brief parse refinement: array-shape audience/vibe + canonical venue_types filter + ideal_features tag tightening + re-parse guard + observations #1/#2 resolve. `vs-parse-brief` tightens five output shapes on the existing forced-`submit_brief` surface. (1) `target_audience` + `vibe_aesthetic` flip from `string` to `array of string` in the tool schema (one demographic / aesthetic per item; 1-5 words per tag; matches the existing `objectives` shape); SYSTEM gains two parallel one-line rules (mirrors the existing `objectives` rule per `feedback_tool_choice_collapse`); a new `sanitizeAudienceTags` shares the parameterized `sanitizeTagArray(raw, maxLen)` body with `sanitizeObjectives` (60-char cap). (2) `venue_types` schema description pins inline to `CANONICAL_TYPES.join(", ")` (imported from `_shared/venueTypes.ts`); SYSTEM gains one rule; a new `sanitizeVenueTypesAgainstCanonical` runs each emitted entry through `canonicalizeType` (split on `/`, `,`, `;` per part; combined-phrase recovery for "industrial warehouse" → both `Industrial` + `Warehouse`); drops non-canonical entries with a single `[vs-parse-brief] venue_types non-canonical drop:` `console.warn` line when non-empty. 5.12.10 swaps the hard-coded canonical set to DB-driven. (3) `ideal_features` schema description tightens to 1-4 word tag shape with concrete example tags (High Foot Traffic, Garage Doors, Floor-to-Ceiling Windows); SYSTEM gains one rule; a new `sanitizeFeatureTags` shares `sanitizeTagArray` with a 40-char per-item cap. (4) `sanitizeObjectives` refactored onto the parameterized `sanitizeTagArray(raw, maxLen)` with thin wrappers (`sanitizeObjectives = sanitizeTagArray(_, 60)`, `sanitizeFeatureTags = sanitizeTagArray(_, 40)`, `sanitizeAudienceTags = sanitizeTagArray(_, 60)`). (5) **Re-parse guard:** after `sanitizeParsed` and before `resolveOrCreateCity`, the function `SELECT`s `vs_scouts.client_name` + `event_name` and drops the corresponding key from `parsed_fields` when the scout already has a non-empty value. Per-field warn-log with the scout's current value truncated to 40 chars. Producer is the source of truth. Defense-in-depth in `briefForm.ts` `applyParsedFields`: the `client_name` / `event_name` merge predicate also gates on `!state.<field>.trim()`. Best-effort posture; scout-fetch failure logs + falls through. **Frontend shape + intake form swap.** `BriefFormState.target_audience` / `vibe_aesthetic` flip from `string` to `string[]`; `EMPTY_BRIEF_FORM` empty arrays; `ParsedBriefFields` flips to `string[] | null`. New `normalizeTagArray(v: unknown): string[]` helper (legacy-string -> single-element array; array filter+trim; else `[]`) used by `fromScout` for both fields. `toUpdate` `setOrDelete` predicates flip from `.trim().length > 0` to `.length > 0` (matches `objectives` shape). `applyParsedFields` `target_audience` / `vibe_aesthetic` flip from string-overwrite to `mergeUnique` (matches `objectives` shape) so producer-typed tags survive parse-and-apply. `computeOverviewSourceHash` flips both fields to `[...state.value].sort()` (matches `objectives` shape; object key order locked with the server). `BriefEvent.tsx` lines 463-485 swap `<Textarea>` to `<TagInput>` for both fields; placeholder text updated; `Textarea` import dropped (no remaining consumer). `ParsedPreview` `pushStr` -> `pushArr` for both fields. `BriefReport.tsx` ReportCards for both fields swap `renderEditor` from `<Textarea>` to `<TagInput>` and `renderDisplay` from `<span className="whitespace-pre-wrap">` to `<TagList items={v}>`; `isBlank` predicate flips from `v.trim() === ""` to `v.length === 0` (matches the `objectives` ReportCard three rows above). **Downstream consumer edge functions.** `vs-generate-brief-overview/index.ts`: `hashAsStringArray` extends to coerce a single string into a single-element array (defensive layer; § 5 migration retires legacy strings at deploy time); `computeOverviewSourceHash` reads `target_audience` + `vibe_aesthetic` via `hashAsStringArray(...).sort()` (mirrors `objectives` / `target_neighborhoods` / `venue_types` / `ideal_features`); object key order stays identical to the client-side hash. **Optional belt-and-suspenders helper:** new `supabase/functions/_shared/formatBudget.ts` `formatBudgetForPrompt(value)` (pure-JS comma formatter; ~30 lines; single export) used at the budget display line in `vs-generate-brief-overview` so the prompt string never depends on V8's `Number.toLocaleString` ICU output. `vs-research-venues/index.ts`: new module-level `asTagArr(v: unknown): string[]` helper (next to existing `asArr`) accepts both shapes; both call sites flip (rescue path's `targetAudience` / `vibeAesthetic` reads + Phase B `userMsgBriefPrefix` reads); interpolation uses `value.length > 0 ? value.join(", ") : "(not set)"`. SYSTEM stays frozen on every edge function; FILL_TOOL untouched; rescue brief block + FIT_RESCUE_SYSTEM untouched. **Migration.** One data backfill UPDATE (`20260605000000_phase_5_12_5_brief_data_shape_backfill.sql`) on `vs_scouts.brief_data`: legacy string-shape `target_audience` / `vibe_aesthetic` normalize to single-element-array shape via `jsonb_set` + `CASE` on `jsonb_typeof`; `overview_source_hash` resets to JSON null so the first BriefVenue submit post-deploy recomputes the new array-shape hash. Idempotent: rows already in array shape stay, JSON-null hash writes the same value on re-apply. No DDL; no new columns / tables / GRANTs / RLS policies. **Accepted scope cost:** one wasted overview-regenerate on first post-migration BriefVenue submit (production has one test scout carrying a throwaway overview; the regenerated overview replaces a throwaway; steady-state hash gating resumes after). Carry-forward (Frontend code-observations row 40, status `☐ ☐`): cache-aware hash hydration helper colocated with `briefIntakeStore` for the Phase 5.16 hardening pass when real producer load materializes. **`code-observations.md` resolves:** Frontend #1 (`briefForm.ts computeOverviewSourceHash` budget + guest-count canonicalization) flips R `☐` -> `☑` (re-verified during the 5.12.5 audit; the hash already uses canonical numeric form on both sides post-5.1 NIT pickup; the remaining ICU surface was the display block, closed by `formatBudgetForPrompt`). Edge #2 (`vs-generate-brief-overview hashFormatBudget` ICU parity risk) flips R `☐` -> `☑` with the parallel note (hash itself was parity-clean post-5.1; the display block ICU surface closes via `formatBudgetForPrompt`). **`vs-parse-brief` kickoff-race audit, no fix needed.** Synchronous end-to-end (no kickoff RPC, no `EdgeRuntime.waitUntil`, no `WORK_TIMEOUT_MS`, no async work after 200); the 5.12.4.2 fix shape does not apply. Producer refresh during parse wastes one Claude call but mutates no DB state. Documented in `docs/decisions.md` § Phase 5.12.5 so a future audit doesn't redo the analysis. Three VS edge functions redeployed out-of-band: `vs-parse-brief`, `vs-generate-brief-overview`, `vs-research-venues`. Held under the 5.12 deploy budget. |
| 5.12.14.3 | 2026-05-27 | `1a01d3e` | VS UX Audit Pass 4 — E2E smoke + multi-round revisions. 7 producer-smoke rounds + 5 amendments across the VS surface stack on `claude/phase-5-12-14-3`. **Major shippables:** BriefReport one-sheet collapse (hero band → 2-col 70/30 grid → section cards: Overview/Details/Dates+Budget/Generated Decks; new `BriefReportRow` inline-edit primitive with optional `editorLeftActions` slot; GeneratedDecksCard refresh with show-3 + expand toggle); BriefEvent + BriefVenue intake restructure (`.card` canon adoption with nested-card hierarchy: outer Event/Venue card → Upload Brief + Details nested; full-width `stack-6` rhythm; vibe + aesthetic migrated BriefEvent → BriefVenue; sq_ft range pair retired across the data layer with `sq_ft_min` legacy fallback in briefForm + vs-research-venues); new `VSPageField` primitive supporting composite ReactNode labels (string label = `<Label>`, ReactNode label = `<div>` to avoid the nested-label HTML bug + click-on-text-toggles-checkbox bug); ReviewCard chrome refresh (`.card` outer + 3-row top bar: drag handle + checkbox + Venue NN pill + up/down arrows; chevron + checkbox `pointerdown` stopPropagation against dnd-kit; `.hq-explainer-label` canon → Montserrat ExtraBold 13px coral); VS Overview CSV template + Enriching modal copy refresh (single-source CSV at `public/templates/venue-scout-sheet-template.csv` with download link in Overview header; modal title/body/footer/spacing rewrite across two amendments); cross-VS data removals (install/strike dates, sq_ft_max range with `sq_ft_min` legacy fallback in 4 vs-research-venues call sites); VS Settings build at `/venue-scout/settings` (shared `LookupListsCard` extracted from HQ Settings + consumed by both; Anthropic Spend Cap card mirroring TS Settings via `global_settings` row; System Prompts placeholder); global 404 restyled to ErrorState chrome (`/src/pages/NotFound.tsx` matches `/src/pages/venue-scout/ErrorState.tsx` chrome with "404" + Page not found + Back to home + Go back); NewScout collapsed to modal triggered from Overview-as-funnel (route `/venue-scout/scouts/new` + `NewScout.tsx` deleted; modal mounts in Overview only; ScoutIndex + sidebar route to Overview funnel); TopBar 3-zone with Client+Event center label in active scout via `useMatch` + supabase fetch; ScoutIndex Phase pills replacing Status column (5-token map: muted/info/purple/warn/success for brief/researching/shortlist/active-assessment/completed); `.scout-list-tbl` wrapper for ScoutIndex matrix-visual parity (no sticky col, no 1280 min-width); Neighborhoods row joins shared Lookup Lists table on HQ + VS Settings (via new `NeighborhoodsLookupEditor inline` mode); sidebar VS section's "+ New Scout" → "New Scout" (icon conveys affordance); ScoutPageHeader 3-zone primitive on 8 in-scout pages (BriefEvent + BriefVenue + BriefReport + SourcingReport + Shortlist + Review + ScoutSettings + SheetPrompt); back-crumb relocated page-chrome → TopBar globally via `useReferrerCrumb` (21 pages migrated; `showCrumb = href !== "/"` predicate hides on root-tier pages; below-md hidden); ScoutPhaseBreadcrumb single-row enforcement (`flex-nowrap` + responsive scale text-[11/12/13px] + h-5/5/6 circles + hidden below md); BriefReport stage-aware back-crumb routing reinstated via `useReferrerCrumb` route-table extension (hook-internal supabase fetch when pathname matches BriefReport → routes to latest-completed phase target: Brief Intake / Sourcing / Shortlist / Review per current_step); `.tbl--matrix` standalone (decoupled from `.tbl` after composition's specificity conflicts surfaced; matrix primitives drive chrome via Tailwind utilities). 15 code-review findings addressed in R6: BriefVenue Strict-in-label nested-label split, sq_ft_min legacy fallback at 4 call sites in vs-research-venues, BriefReport empty-Generate dirty-draft check, BriefReportRow Escape `defaultPrevented` guard + draft-sync useEffect, ReviewCard Notes controlled value, Overview textarea autosize-on-status-flip + ResizeObserver, # of Spaces 0-normalization, vibe_aesthetic ParsedPreview indicator, ReviewCard chevron + checkbox stopPropagation against dnd-kit, GeneratedDecksCard coral placeholder for in-flight deck. 3 edge functions redeploy out-of-band: `vs-research-venues` (sq_ft_min fallback at 4 call sites; install/strike + sq_ft_max removal residue), `vs-parse-brief` (install/strike + sq_ft_max schema cleanup), `vs-generate-brief-overview` (touched per the same data-shape changes). NO migrations. NO schema changes. **3 architectural decisions logged** (`docs/decisions.md`): `.tbl` canon decouple from matrix tables; back-crumb relocation page-chrome → TopBar (single source of truth + BriefReport stage-aware override); Lookup Lists shared component pattern (HQ + VS Settings consume extracted `LookupListsCard`). **REPO_DOC_UPDATES applies:** Phase 5.14 venue photo persistence inserted into roadmap (originally queued as 5.17; renumbered 2026-05-27); VS research-accuracy C3 (URL accuracy) appended to spec for future sub-phase; Phase 5.15 Anthropic call-log infra placeholder queued for spend-cap per-tool breakdown (originally queued as 5.X; renumbered 2026-05-27). Spec at `OUTPUTS/historical/phase-5-12-14-3-spec.md`. Held under the 5.12 deploy budget (squash carries the literal Netlify deploy-suppression directive; one coordinated deploy at end of full 5.12 batch). |
| 5.12.14.2 | 2026-05-26 | `1a01d3e` | VS audit residual cleanup. Closes the residual carry-forwards from the 5.12.14 v1 / 5.12.14.1 VS UX audit cycle. No producer-visible UI changes. No edge functions. No migrations. No schema. Pure code-hygiene + dead-export deletion + dead-comment sweep. Five items: (1) `DedupeMetaIndicator` + `DedupeMetaShape` + `normalizeDedupeMeta` exports fully pruned from `src/components/venue-scout/matrix/primitives.tsx` (~55 LOC + the now-orphaned `Info` lucide-react import); the `vs_candidate_venues.dedupe_meta` jsonb column + the `supabase/functions/_shared/venueDedupe.ts` write path stay byte-unchanged. (2) `src/components/venue-scout/NotesModal.tsx` deleted entirely (97 LOC); zero JSX consumers post-5.12.14.1; Review (post-5.12.15) renders notes inline. (3) `ScoutStepThroughNav` comment-residue sweep across `ScoutChrome.tsx` + `ScoutPhaseBreadcrumb.tsx` + `ErrorState.tsx` + `Shortlist.tsx` + `SourcingReport.tsx`; the component was already removed in 5.12.14 v1 (replaced by `ScoutPhaseBreadcrumb`), only dead comments remained. (4) `Phase 5.12.16 Stage 2C` forward-reference comments in ~10 files flipped to `Phase 5.12.14.1 Stage 2C` (Code-session stowaway from before the naming was locked). (5) Stale worktree + branch cleanup executed as STEP 0 (BEFORE the fresh worktree was cut, so the sweep couldn't catch its own host); inventory drift surfaced (3 of 4 spec-listed worktrees + 1 of 4 spec-listed branches already pre-cleaned between spec drafting + session open); partial cleanup executed. Verification gate: typecheck + build + test clean; lint shrunk to 188 vs ~192 baseline. Held under the 5.12 deploy-budget pattern (squash carries the literal Netlify deploy-suppression directive; one coordinated deploy at end of full 5.12 batch). Spec: `OUTPUTS/historical/phase-5-12-14-2-spec.md`. |
| 5.12.14.1 | 2026-05-26 | `1a01d3e` | VS UX Audit Pass 3, Stages 2A + 2B + 2C combined squash. Full Stage 2 of `OUTPUTS/historical/ux-vs-audit-pass-2-plan.md`. Stage 2A behavioural fixes (items 1 + 2 + 7): ScoutPhaseBreadcrumb phase-5 "OVERVIEW=reached-for-everyone" bug fix (`reachedAtStep` `sheet_prompt` → `completed` + label rename "Overview" → "Brief Report"); ErrorState bundle (`text-destructive` swap on `x` + `block` failure icons, route-derived eyebrow off pathname so `/deck/error/*` shows "Review" not "Sourcing", `border-warn text-warn` on warn icon, label-section size 10px → 13px); matrix horizontal overflow fix on SourcingReport + Shortlist with `min-w-[1100px]` reshape plus expanded matrix-row reflow on VenueMatrixRow.tsx + primitives.tsx + TagInput.tsx. Stage 2B pattern locks + Stepper-as-title (items 3 + 5 + 6 + 8): canonical `VSLoadingShell` with icon-as-spinner replaces horizontal progress bars across Researching/Compiling/Generating (three CSS keyframes — pencil diagonal-swipe, hammer strike-swing about bottom-left, magnifier scan-sweep) plus honest server-driven progress via new `brief_data.progress_step` field written from vs-research-venues + vs-compile-summaries + vs-generate-deck at every real phase boundary (replaces timed-fake setInterval bump; fallback to step 0 for pre-deploy scouts mid-flight); Stepper-as-title on BriefEvent + BriefVenue + BriefReport (`h-9 w-9` → `h-6 w-6` resize, 24px circles between 20px ScoutPhaseBreadcrumb + original 36px; active type widens 1\|2\|3 → 1\|2\|3\|4 for BriefReport all-done posture; drops `h-page` + eyebrow); new `useReferrerCrumb` hook + `ReferrerCrumb` component (sessionStorage tracking + canonical-parent fallback table + loop protection); coral → grey section labels via canonical `.label-section` on BriefEvent + BriefReport + BriefReportCard. Stage 2C structural + primitives (items 4 + 9 + 10 + polish sweep + HQ Core crumb-from-referrer lift): SheetPrompt + SheetUpload merged into one route (`/sourcing/sheet-upload` Route deleted; `stepToRoute('sheet_upload')` → `/sourcing/sheet-prompt?upload=1` with auto-expand flag; `sheet_upload` DB enum value preserved); new `SheetUploadCard` component extracted from deleted SheetUpload.tsx; ScoutIndex migrated to `<DataTable<Scout> flat twoTier>` with per-row archive ghost-icon as trailing column (Restore + Delete as column-actions on archived rows; static `.pill .pill-sm` for status auto-derived; AlertDialog destructive + vs-delete-scout edge-fn call byte-identical); new `VSPageField` primitive replaces 4 local Field helpers across NewScout + BriefEvent + BriefVenue + ScoutSettings (drift normalized to canonical 12px); `.actionbar` extension on NewScout + Overview + merged SheetPrompt with `pb-32` outer; token sweeps amber-500/200/400 → warn / red-500 → destructive / red-400 → destructive / green-400 → success across 5 pages; card-surface fixes for ScoutSettings Danger Zone (`bg-card` → `bg-surface-alt`) + ScoutGlobalSettings placeholder (`<Card className="bg-surface-alt">` + `.label-section` + `space-y-3 p-6` + `.tlink`); Start Over title bump (`text-sm font-semibold` → `text-base font-semibold text-foreground`) + ellipsis glyph (`Start Over...` → `Start Over…`); Overview width `max-w-3xl` → `max-w-4xl`; ChipMultiSelect selected state coral → success green (BriefVenue + BriefReport only — matrix retains Stage 2B canon since matrix uses different chip primitive `paletteFor` from `useVenueTypes`); HQ Core crumb-from-referrer lift Path A — `useBackHref` folded into `useReferrerCrumb` (state.from priority-1 inside the hook + optional `fallback` prop), HQ Core route table expanded to projects/clients/vendors/venues/people/tasks/deliverables/settings/bulk-import/calendar, ReferrerCrumb relocated `src/components/venue-scout/ReferrerCrumb.tsx` → `src/components/ReferrerCrumb.tsx` with canonical `<IconArrowLeft className="ic ic-sm" /> Back to {label}` chrome, 11 HQ Core detail-page swaps + 7 list-page `backState` import swaps + 1 OverflowList swap + 9 VS page import swaps land cleanly. Three edge functions deployed out-of-band during Stage 2B (vs-research-venues + vs-compile-summaries + vs-generate-deck for progress_step writes); no edge-fn deploys in 2A or 2C. NO migrations. Combined Stage 2 diff: 72 file-touches across ~50 unique files. Held under the 5.12 deploy budget (squash carries the literal Netlify deploy-suppression directive in its subject; one coordinated deploy at end of full 5.12 batch). Specs: `OUTPUTS/historical/ux-vs-audit-pass-2-plan.md` + `OUTPUTS/historical/stage-2-progress.md`. |
| 5.12.14 | 2026-05-25 | `1a01d3e` | VS UX/visual + flow audit + design-system convergence. Mirror of Phase 5.11.2's structural pass scoped to VS surfaces. Locks the canonical VS chrome (3 page-width buckets — `max-w-3xl` for forms/stepper/loading/error/settings/Overview, `max-w-4xl` for ScoutGlobalSettings, full-width for matrix pages, ScoutIndex stays at HQ-canon `max-w-7xl`; standardized header stack of crumb → ScoutPhaseBreadcrumb → eyebrow → h-page with NO caption; new `ScoutPhaseBreadcrumb` component at `src/components/venue-scout/ScoutPhaseBreadcrumb.tsx` replacing the 5-chip `ScoutStepThroughNav` strip with 20px numbered circles + mono labels + `›` separators; brief intake `Stepper.tsx` byte-unchanged and stacks below the breadcrumb on intake pages — documented two-indicator compromise; `ScoutStepThroughNav` export fully retired from `ScoutChrome.tsx`). New `.hq-explainer` design-system primitive (single CSS class with mono coral label + 14px body, lives in `src/index.css` under the new "Phase 5.12.14 HQ Core surfaces" block); v1 consumers BriefEvent (PDF pre-fill explainer above the upload card) + Review (notes-driven regenerate explainer above the venue card grid). Title rewrites (11): Event Details → Event; Venue Details → Venue; Upload Sourcing Sheet → Upload Sheet; Researching Venues → Researching; Venue Shortlist → Shortlist; Compiling Deck Preview → Compiling; Generating Venue Deck → Generating Deck (producer override). Matrix shared 7-column layout via new `VenueMatrixRow` component at `src/components/venue-scout/matrix/VenueMatrixRow.tsx` (both Sourcing + Shortlist render through the same row primitive; per-page differences collapse to col-1 label + col-1 handler + page-level source-of-truth — shortlisted vs pitched). Column shape: Shortlist/Pitch+Source (110px, checkbox top + SourcePill bottom stacked) | Venue (230px, EditableField name + TypeTogglePopover horizontal max-2-per-row) | Location (180px, RecordCombobox neighborhood + EditableField address) | Website (130px, InlineEditText with prettyHost tlink + IconLink + muted `-` empty state) | Features (230px, compact TagInput) | Recommendations (280px, Bullets) | Considerations (280px, Bullets). Total ~1440px. Notes/Feedback column drops from both pages (NotesModal mount + openNotes handler + notesOpen state retire from both consumer files; NotesModal component file stays alive). `STICKY_TH.col2` + `STICKY_TD.col2` constants in `primitives.tsx` update from `left-[60px]` to `left-[110px]` to match the widened col-1 (without this swap the sticky Venue column overlaps col-1 during horizontal scroll). `TypeTogglePopover` horizontal variant enforces max-2-per-row via deterministic `grid grid-cols-2 gap-[6px] justify-items-start` when `currentTypes.length >= 2`; single-pill renders plain centered (no grid wrapper) to avoid off-balance look. TagInput gains compact prop: `compact=true` drops container border/background/padding, chips center via flex-wrap, inline input replaced with reveal-on-click coral `+` button (Enter/blur commits + collapses back to `+`); `compact=false` (default) byte-unchanged for BriefVenue + BriefReport consumers. Matrix `primitives.tsx` sweep DELETES 11 retired exports (VenueIdentityStack, WebsiteArrow, HdrStack, RankDisplay, rankBucket, RANK_TEXT, RANK_BAR, RankTier, EditableVenueName, NotesCellButton, EditableTextarea) plus StackDivider internal helper; drops Pill from exports (kept as internal helper for TypeTogglePopover); drops 4 re-exports (TYPE_STYLES, TYPE_FALLBACK_STYLE, parseTypes, CanonicalType type) — consumers switch to direct `@/lib/venue-scout/venueTypes` imports. `DedupeMetaIndicator` + `normalizeDedupeMeta` + `DedupeMetaShape` stay exported as documented-dead future-state scaffolding (logged in code-observations). New `/venue-scout/overview` route + page (`src/pages/venue-scout/Overview.tsx`): 3-card layout (What this is + How it works numbered list + What you bring/walk away with grid) + "Start a New Scout" button → navigates to existing `/venue-scout/scouts/new`. Both ScoutIndex's "+ New Project" affordance + LeftRail's VS_TOOL_ITEMS "New Project" rail item retarget from `/venue-scout/scouts/new` to `/venue-scout/overview`. NewScout's form + create flow byte-unchanged; only the header restacks (drop caption "Name the project, then fill in the brief on the next step."). BriefReport dynamic forward button + just-generated toast: new `resolveForwardCta(current_step)` helper maps to {Begin Sourcing / Go to Sourcing / Go to Shortlist / Go to Review}; advancesStep=true only on the brief → sheet_prompt transition (delegates to existing `requestConfirm`); every other forward click is pure navigation. `completed` case routes to `stepToRoute(scoutId, 'deck_prep')` not `stepToRoute(scoutId, 'completed')` (latter returns `/brief`, a loop back to BriefReport). Mount-effect on BriefReport fires a one-shot success toast when `?just-generated=true` is present in the URL, then strips the param via `setSearchParams(next, { replace: true })`. Generating.tsx line 190 retargets from `/venue-scout/scouts/${scoutId}/review` to `/venue-scout/scouts/${scoutId}/brief/report?just-generated=true` (`replace: true` preserved). `window.open(editUrl)` auto-tab stays. Inline sticky `sticky bottom-0 -mx-6 mt-6 border-t-2 ...` footer on BriefEvent / BriefVenue / BriefReport / ScoutSettings swaps to canonical `.actionbar` `position: fixed` wrapper (already in `src/index.css:1048`); page wrappers bump `pb-24` → `pb-32` so content scrolls under the fixed bar without clipping. ScoutSettings drops Cancel + the dirty-leave confirmation flow from the sticky bar (top crumb handles back-navigation); BriefReport keeps the back link in the bar by design. DateRangePicker: shadcn Calendar primitive (`src/components/ui/calendar.tsx`) installed via `npx shadcn@latest add calendar`; new dependencies `react-day-picker` + `date-fns` (transitive). New `src/components/ui/DateRangePicker.tsx` wrapper: internal `DateRange | undefined` state during popover open (does NOT round-trip through the formatted-string value on every onSelect); commits via `onChange` only when the range is complete (both `from` AND `to` set) or when popover closes with a partial selection; `parseOwnFormat` round-trips picker's own outputs (single + range-same-month + range-cross-month + range-cross-year regex); legacy free-text values render as-is in the trigger label + popover opens empty. Three VS free-text date fields on BriefEvent (`live_dates`, `install_dates`, `strike_dates`) swap from plain `<Input>` to `<DateRangePicker>`; BriefReport's Live date(s) ReportCard + Install/Strike ReportCard inputs swap to DateRangePicker. Storage shape unchanged: formatted strings ("Oct 15-17, 2026") into existing `text` columns; downstream string consumers (Claude prompts, Slides text, UI display) consume as-is. Multi-date (non-consecutive) explicitly carry-forward. BriefReport god-file decomposition: lines 339-400 extract to new `src/components/venue-scout/GeneratedDecksCard.tsx` (pure presentational; parent owns the post-generate toast effect); the generic `ReportCard<T>` primitive (used 9 times across BriefReport) extracts byte-for-byte to new `src/components/venue-scout/BriefReportCard.tsx`. BriefReport.tsx slims from 1049 lines to ~900 (closes `code-observations.md` Frontend #19's VS slice; ProjectDetail.tsx + RoleSettings.tsx + ProjectEdit.tsx slices remain open). NEW dependencies one-time via shadcn CLI: `react-day-picker` + `date-fns`. NO migrations. NO edge function redeploys. NO schema changes. Held under the 5.12 deploy budget (squash carries the literal Netlify deploy-suppression directive in its subject; one coordinated deploy at end of full 5.12 batch). Spec at `OUTPUTS/historical/phase-5-12-14-spec.md`. |
| 5.12.15 | 2026-05-25 | `1a01d3e` | Final Review + Deck Prep consolidation (rename + route move + per-venue card layout + new vs-regenerate-venue-overview edge function). DeckPrep.tsx renamed to `src/pages/venue-scout/Review.tsx`; today's 618-line FinalReview file deleted at squash time. Per-card body extracts to new `src/components/venue-scout/ReviewCard.tsx` sibling so the parent stays ~600-700 lines (presentational card owns top bar + body 3 sub-columns + 4-photo grid + Regenerate Overview button; parent owns loader + DndContext + state hooks + debounceSave + flushPendingFieldSaves + persistOrder + onRowDragEnd + reloadVenuePhotos + regenerateOverview + generate + the Generate Deck AlertDialog + PhotoUploadModal mount + sticky bottom nav). Route `/deck/prep` + `/sourcing/review` both collapse to `/review`. `current_step` enum + the CAS guards on `vs-compile-summaries` (`.eq("current_step", "compiling")`) and `vs-generate-deck` (`.eq("current_step", "deck_prep")`) are byte-untouched. Shortlist Continue flips `current_step` directly to `'compiling'` (skips the legacy `'review_selects'` state); legacy scouts in `review_selects` on deploy day route forward via `stepToRoute("review_selects") -> /review` (no backfill migration). Per-venue card layout: editable name + drag handle / numbered Venue 01 pill / include checkbox cluster (top-right where Pitch ✓ used to be); body 3 sub-columns (sub-col A: address + type chips + size + website + overview rows=10 + Regenerate Overview button + notes + coral explainer; sub-col B: neighborhood + capacity; sub-col C: 4-photo grid byte-identical to FinalReview's). Numbered pill renumbers against the included set only via a `useMemo`-built `Map<string, number>` for O(1) per-card lookup; dashes don't get numbers; remaining venues renumber contiguously (matches vs-generate-deck's `pitched + include_in_deck` slide-order iteration). Pill chrome shifts from `rounded` to `rounded-full`. Stats header above the cards: "X venues included · X total slides (6 front-end + 2 per venue)" with live recompute (constant `FRONT_MATTER_SLIDES = 6` stays internal; producer-facing copy reads "front-end" per Jimmie). Sticky bottom nav: Back (left) / "X venues included" (middle, single line) / Generate Deck (right); today's DeckPrep's scrollable bulleted venue list drops (the producer reads the slide-math breakdown once above, no need to echo at the bottom). Coral explainer under notes rewrites to "Notes feed the overview when you Regenerate. Never appears on the deck." (stronger Regenerate cue than today's FinalReview copy). New `supabase/functions/vs-regenerate-venue-overview/index.ts` edge function: `verify_jwt = true` (default; no config.toml entry), service-role client, two in-function auth gates (venue.scout_id === scout_id + scout.current_step === 'deck_prep'; both 404 on miss per the vs-research-single-venue probe-defense pattern). Source filter from vs-research-single-venue does NOT apply (every pitched venue is a valid regen target regardless of source). Call body lifts vs-compile-summaries Pass 2 byte-for-byte (tool-less + web_search auto with max_uses=2 + ABOUT_VENUE_SYSTEM with 1h ephemeral cache + max_tokens 2000 + `prompt-caching-2024-07-31` + `extended-cache-ttl-2025-04-11` betas). Imports ABOUT_VENUE_SYSTEM + buildOverviewUserMsgForVsRow + buildStub from `_shared/venueOverview.ts` (no edit to the shared file). Synthetic row substitutes the request body's `notes` value into venue.notes so the existing `<producer_inputs>` block in the user message picks it up without a shared-module signature change. Frontend passes notes explicitly so producer-edit-then-immediately-regenerate doesn't race the 600ms notes debounce on the client. Server-side UPDATE on `vs_candidate_venues.venue_overview`; returns `{ ok, venue_overview }`. No confirm dialog before regen (per-regen cost ~$0.05-$0.15; producers iterate on notes and re-roll; a dialog every click adds friction without payoff). Per-venue in-flight state map disables the button + flips label to "Regenerating…" during the wall (~10-20s). On 404 (stale tab where current_step has advanced past `deck_prep`) the toast surfaces destructively. Source pill + DedupeMetaIndicator drop from the consolidated Review (Sourcing + Shortlist keep them per 5.12.14's audit; both are matrix-debug affordances that belong where the producer is making sourcing decisions, not where they're tuning copy + photos). Producer flow post-5.12.15: brief -> sheet_prompt -> sheet_upload -> researching -> sourcing_report -> shortlist -> compiling -> deck_prep (consolidated Review) -> completed. App.tsx route table drops `/sourcing/review` and changes `/deck/prep` -> `/review`. format.ts `stepToRoute` for `review_selects` + `deck_prep` both resolve to `/venue-scout/scouts/${scoutId}/review`. ScoutChrome.tsx drops the Final Review NAV_CHIPS entry; renames the Deck Prep entry label to "Review"; `resolveActiveChip` prefix table updates (Deck Prep prefix `/deck/prep` -> `/review`; Final Review entry dropped). Compiling.tsx + Generating.tsx + ErrorState.tsx route refs updated to `/review`. Retires `code-observations.md` Frontend #19's DeckPrep.tsx slice (☑ resolved with strikethrough on the `DeckPrep.tsx (905)` god-file row). Zero migrations. One edge function deploys (vs-regenerate-venue-overview; first-time deploy). Held under the 5.12 deploy budget (squash carries the literal Netlify deploy-suppression directive in its subject; one coordinated deploy at the end of the full 5.12 batch after 5.12.14 ships). Spec at `OUTPUTS/historical/phase-5-12-15-spec.md`. |
| 5.12.13.2.1 | 2026-05-25 | `1a01d3e` | Post-.2 smoke correction. Producer smoke after the 5.12.13.2 deploy (Jimmie 2026-05-25; ~5-6 fresh About paragraph regenerates) showed 5 of 6 outputs landing 130-175 words against the .2 120-word hard cap, including The Glasshouse at 175 / NYA Studios at 163 / AT&T Discovery District at 159. The cap-restatement strategy in `<task>` + `<output_format>` did not land; the count-and-cut nudges were treated as soft signals, not strict truncation. Producer call: quality of the longer outputs is right (producer-voice, fact-rich, well-structured); the 120 cap was too tight for venues with material to work with. Two coordinated edits to `supabase/functions/_shared/venueOverview.ts` (the same SOLE source file as .2). (1) `<task>` block points at 150 throughout: "90 to 120 words" -> "100 to 150 words"; "120-word limit is a hard cap" -> "150-word limit is a hard cap"; "exceeds 120 words" -> "exceeds 150 words". The cut-or-merge fallback + count-before-returning nudge + verb-first sentence guidance + "Every sentence must earn its place" closer all stay byte-for-byte. (2) `<output_format>` length-check paragraph points at 150: "If above 120" -> "If above 150"; "above 120 words" -> "above 150 words". (3) `<input_fields>` `Key features (tags)` line gains tag-priority guidance: appends "Not every tag has to make the paragraph; favor the ones most appealing or distinctive for this specific venue, especially when expanding on more would push the paragraph above the 150-word cap." Why: smoke surfaced the model was trying to cover every tag for high-feature-count venues (Glasshouse's 360-degree views + 49 chandeliers + retractable partitions + 1,875-cap + outdoor terraces all got named), which contributed to the overshoot. The positive instruction ("favor the most appealing or distinctive") gives the model permission to skip tags rather than padding. All 6 in-prompt example outputs stay byte-for-byte (Studio 525 at 98 and Astor Place at 92 now sit slightly below the new 100-word soft floor; the hard rule is the 150 ceiling). Two edge functions redeploy out-of-band: `hq-generate-venue-about` (third redeploy this consumer: 5.12.13.1, 5.12.13.2, 5.12.13.2.1) + `vs-compile-summaries` (seventh redeploy: 5.12.4, 5.12.4.2, 5.12.10, 5.12.13.1, 5.12.13.3, 5.12.13.2, 5.12.13.2.1). One-time prompt-cache invalidation per consumer accepted. No migration. No frontend. Server-side `trimToWordCap` helper still deferred; re-open as 5.12.13.2.2 only if post-.2.1 smoke still shows 3+ of 5 overshoot the new 150 ceiling. Spec'd inline (no separate spec doc); 5 locked decisions in `docs/decisions.md` § Phase 5.12.13.2.1. Held under the 5.12 deploy budget. |
| 5.12.13.2 | 2026-05-25 | `1a01d3e` | Fifth ship of the Phase 5.12.13 prompt audit, slotted into the merge order after .1, .6, .4, .3 cleared the working-session iteration. Tunes `ABOUT_VENUE_SYSTEM` in `supabase/functions/_shared/venueOverview.ts` (the SOLE source file; shared by `hq-generate-venue-about` + `vs-compile-summaries` Pass 2). Four coordinated edits to the SYSTEM body. (1) `<task>` block (lines 79-81) rewrites: cap widens from 90-110 to 90-120 words; the hard ceiling moves from 110 to 120; two new sentences inserted before the verb-first sentence-shape guidance ("If your draft exceeds 120 words, cut sentences or merge clauses until it fits before you reply." + "Count the words in your draft before returning."); "Every sentence must earn its place" moves to the tail as a closer; "Cut anything that does not sell the space or the guest experience" drops (redundant with `<audience>`). (2) `<output_format>` block (lines 232-234) gains a new paragraph appended inside the existing block: "Before returning, count the words in your draft. If above 120, cut a sentence or merge two clauses and recount. Do not return a paragraph above 120 words." Pairs with the `<task>` cap restatement; rides on the block the model attends to most closely. (3) `<input_fields>` block (lines 63-77): line 64 em dash killed (`Missing fields will render as "(not set)" — treat...` reshapes to `Missing fields render as "(not set)"; treat...` per the voice rule + the 0bcb5aa em-dash sweep precedent); `- Key features (tags)` line (74) gains a description body ("short evergreen feature tags (1 to 4 words each, usually 1 to 2). Interpret each tag as a feature anchor; expand on what it describes in the paragraph. Do not list the tags verbatim and do not pad the paragraph to cover every tag."). Other bullets stay as bare field names by intent. (4) Six example INPUT lines reshape from long narrative `Key features:` strings to short tag-shaped `Key features (tags):` lists matching the post-.1 user-message label and the canonical tag shape: Studio 525 (line 145) -> `High Ceilings, Skylight, Concrete Floors, Multi-Level, Drive-In`; Chelsea Industrial (line 161) -> `High Ceilings, Floor-to-Ceiling Windows, Multiple Spaces, LED Screen, Built-In Bar`; 8626 Melrose Ave (line 177) -> `Corner Location, High Ceilings, Open Layout, Rooftop`; The Flat NYC (line 193) -> `Exposed Brick, Skylight, Multi-Level, Balcony, Art Installations`; AT&T Discovery District (line 209) -> `Outdoor, Multiple Spaces, LED Screen, Plaza`; Astor Place (line 225) -> `Outdoor, High Visibility, Plaza`. All six example OUTPUTS stay byte-for-byte (Jimmie's hand-revised voice anchors; the cap widening to 120 is the lever that lets them hold without editing — The Flat NYC at 112 words sits inside the new range). Two edge functions redeploy out-of-band: `hq-generate-venue-about` (second redeploy this consumer; first was 5.12.13.1) + `vs-compile-summaries` (sixth redeploy; previous: 5.12.4, 5.12.4.2, 5.12.10, 5.12.13.1, 5.12.13.3). One-time prompt-cache invalidation per consumer accepted (per `feedback_tool_choice_collapse`: SYSTEM edit is acceptable when the lever IS in SYSTEM and there's no per-call equivalent; the cap rule + the example shape both live in SYSTEM). Net SYSTEM size: roughly +50 chars on `<task>`, +165 chars on `<output_format>`, +220 chars on `<input_fields>`, -2.4 KB across the 6 example input lines (tag lines are much shorter than the narrative lines); net SYSTEM shrinks by roughly 1.8 KB. No re-eval implications: cap edit affects newly-generated About paragraphs only; historical `venues.about_venue` + `vs_candidate_venues.venue_overview` rows stay until a producer regenerates from the detail page (HQ) or Pass 2 re-runs (VS). No backfill, no migration, no schema, no frontend. Closes `code-observations.md` Edge #4 (low; About paragraphs trending 120-150 against the prompt's pre-.2 90-110 cap). Held under the 5.12 deploy budget. Spec at `OUTPUTS/historical/phase-5-12-13-2-spec.md`. |
| 5.12.13.3 | 2026-05-25 | `1a01d3e` | Third ship of the Phase 5.12.13 prompt audit, slotted into the merge order after 5.12.13.6 + 5.12.13.4 because the parallel-pilot pair landed first while .3 cleared the working-session iteration. Softens three Retail anchors across the VS write paths AND reshapes the existing `requirementLines` block in `vs-research-venues` so the brief's venue-type preference allows 1-2 high-aligning outliers and the neighborhood-strict toggle produces sharper guidance to Claude on both sides of the flag. Five edits across two files: (1) Phase B `SYSTEM` block in `vs-research-venues/index.ts` lines 1811-1818: "Strongly prioritize ground-floor commercial / storefront / retail-vacancy properties — a significant share of Mirror's selected venues are empty storefronts and ground-floor commercial spaces." rewrites to "Mirror often books ground-floor commercial / storefront / retail-vacancy properties, so surface them when the brief allows. Equal-weight the other canonical venue types (provided in the user message) when they fit the brief." Em dash on line 1814 killed. TYPE CONSTRAINT block's 5.12.10-added sentence "If the venue is most accurately described as a ground-floor commercial / storefront / retail vacancy, prefer Retail when it appears in the canonical list." dropped entirely. (2) Phase B userMsg "Return X net-new" line at 2320: "Weight ground-floor commercial / storefront / retail-vacancy spaces heavily in ranking_score." replaced with "Rank against the brief's stated criteria; do not over-weight any single venue type when scoring." (3) `_shared/venueFill.ts` `FILL_SYSTEM` line 121: drops "Prefer Retail for ground-floor commercial / storefront / vacancy spaces." sentence. (4) `requirementLines` strict-neighborhoods ternary in BOTH call sites (Phase B at 2152-2156 + rescue at 1433-1437) tightens in both branches: strict branch becomes "Restrict candidates to these neighborhoods: X. You MAY include at most 1-2 venues from immediately adjacent neighborhoods, but no further." Non-strict branch becomes "Prefer candidates in these neighborhoods: X. Adjacent neighborhoods and a reasonable radius within the city are acceptable; do not exclude a venue that aligns strongly with every other brief criterion solely on neighborhood." Adjacency relies on the model's training knowledge for major cities; no explicit adjacency data passed. (5) `requirementLines` venueTypes "must be one of" in BOTH call sites (Phase B at 2159-2161 + rescue at 1440-1442) softens to "should be," with 1-2 outliers permitted: "Candidate venue should be one of these types: X. You MAY include 1-2 strong outliers from outside this list ONLY when the venue ranks very high on the brief's other criteria (neighborhood, scale, brand fit, vibe) such that the type mismatch is clearly overcome. Default to within-type." No new brief-stepper flag; always-soften. Three edge functions redeploy out-of-band: `vs-research-venues` + `vs-research-single-venue` + `vs-compile-summaries`. `vs-research-single-venue` + `vs-compile-summaries` consume the updated `FILL_SYSTEM` without a direct edit; redeploy picks up the one-time cache invalidation. `hq-generate-venue-about` does NOT redeploy (reads `_shared/venueOverview.ts` not `_shared/venueFill.ts`). `vs-parse-brief` schema descriptions stay as-is (producer decision: scout-time neighborhoods remain free-text; canonicalization against `public.neighborhoods` only happens at deck-push time, carry-forward to a separate ship). No migrations. No frontend changes. Historical `vs_candidate_venues` + `venues` rows keep their current shape. One-time prompt-cache invalidation per consumer accepted (per `feedback_tool_choice_collapse` honesty: SYSTEM edit is acceptable when the lever IS in SYSTEM and there's no per-call equivalent). Closes `code-observations.md` Edge #12 (Retail bias over-anchor). NEW carry-forward: vs-generate-deck neighborhood auto-add into `public.neighborhoods` at deck-push time (separate ship; slot pending Jimmie's call). Held under the 5.12 deploy budget. Spec at `OUTPUTS/historical/phase-5-12-13-3-spec.md`. |
| 5.12.13.4 | 2026-05-25 | `1a01d3e` | Fourth ship of the Phase 5.12.13 prompt audit (base spec + 3 post-smoke amendments folded into the same squash per `feedback_single_commit_per_ship`). Voice tuning on `vs-generate-brief-overview` so the Event Overview paragraph reads in Mirror's producer voice (warm, evocative, present-tense, scene-setting, with a sense of place) instead of the prior cold news-style summarization producers flagged. Three coordinated edits inside one edge function. (1) `SYSTEM_PROMPT` rewrites from a short bullet rule list (8 lines, rules-only, no exemplars) to a four-block `<voice>` + `<must_not>` + `<rules>` + `<examples>` structure modeled on `ABOUT_VENUE_SYSTEM`'s pattern. Hierarchy inverts from "stay faithful to facts" to "creative extrapolation is welcome"; hard-fact lockdown (client, event, dates, venues, neighborhoods, budgets, headcount) moves to a dedicated `<must_not>` block that anchors at the top as the binding constraint, not one bullet among five. The `<voice>` block names 5 structural beats distilled from the voice signal across all 4 reference past-deck overviews (SheaMoisture Hot Off the Press Newsstand, New Balance Grey Days 2026, A-List Salon, Vaseline Oasis): concept-first opener (lead with what the activation IS, not who the client is), why-this-exists-for-the-brand (strategic intent in one sentence), hero beat named (centerpiece moment called out), guest-experience verb beat (what guests do, choose, or receive), scene-setting via metaphor or branded vocabulary (themed neighborhoods, hashtags, conceptual pairings pulled through as if already part of the world). Adds warm/evocative/present-tense/scene-setting/sense-of-place voice language + a "creative extrapolation is welcome" permission to use sensory and material language (light, texture, scale, material, energy) where the brief allows + a dedicated audience-specificity instruction ("If the brief says 'GenZennial women,' write 'GenZennial women,' not 'consumers' or 'guests'"). `<rules>` carries output mechanics only: one paragraph, 3 to 5 sentences (kept from the original), no bullet points / headings / preamble, present tense (now explicit; was implicit in the original), no em dashes, no filler hype ('exciting', 'amazing', 'world-class', 'unparalleled', 'we're thrilled'), return ONLY the paragraph. Two-anchor `<examples>` block embeds cleaned producer-written reference overviews (A-List Salon mobile salon + Vaseline Oasis Glowasis/Slowasis), both ~4 sentences (matches the 3-5 sentence target), structurally closest to what VS will produce; em dashes stripped from both originals to match the `<rules>` block (the few-shot cannot model what the rules forbid: A-List "you get ready—it's where the night begins" -> "you get ready. It's where the night begins"; Vaseline "glowing skin—all set within" -> "glowing skin, all set within"). SheaMoisture and Grey Days reserved as out-of-sample smoke-comparison references (longer + more brand-platform shaped; useful for verifying voice generalizes but too long for the SYSTEM prompt). (2) User-message construction reshapes from a flat `BRIEF FIELDS` key/value dump into three labeled blocks: BRIEF FACTS (key/value for hard-fact fields where precision matters: Client, Event, City, Live dates, Install dates, Strike dates, Budget, Expected guest count, Activations / spaces), BRIEF CONTEXT (paragraph-shaped segments joining the voice-driving arrays into prose the model can naturally weave: `The activation aims for {objectives}`, `The audience is {audience}`, `The vibe is {vibe}`, `Target neighborhoods: {neighborhoods}`, `Venue types under consideration: {types}`, `Ideal features: {features}`), PRODUCER NOTES (free-text `additional_notes` as its own discrete block). FACTS keeps precision; CONTEXT lets the model weave; NOTES surface free-text producer context that previously got buried as one row in the dump. BRIEF CONTEXT header omits entirely when no segments fire; PRODUCER NOTES block fires only when `briefData.additional_notes` is a non-empty trimmed string. `additional_notes` field-name verified against `src/lib/venue-scout/briefForm.ts ParsedBriefFields` ([line 141](src/lib/venue-scout/briefForm.ts:141)): `vs-parse-brief` still parses the field for backward compat with v14 briefs even though the new intake UI doesn't surface it, so the block fires whenever a brief carries the key. The empty-context guard widens from `contextLines.length > 0` to `factsLines.length > 0 || contextSegments.length > 0 || additionalNotes` so the call still fires when only notes or only context segments are present. Trailing `Write the Event Overview paragraph.` task instruction line unchanged. (3) `callClaude` call shape (`callClaude("venue_scout", [{ role: "user", content: userMsg }], { system: SYSTEM_PROMPT, max_tokens: 600, fn_name: "vs-generate-brief-overview" })`) byte-unchanged. No tools, no tool_choice, no cache_control. (4) `buildStub` deterministic fallback (`${event} for ${client} in ${city}. Live ${live_dates}.`) untouched; stays cold and factual by design as the failure-mode placeholder. Cost / latency: `SYSTEM_PROMPT` grows from ~150 tokens to ~600 tokens; user message stays roughly the same size (same field content); output stays at 3-5 sentences (~80-150 tokens). Net per-call input cost rise ~450 tokens (sub-cent at Anthropic's current pricing; negligible at Mirror's call volume). Latency unchanged in any user-perceptible way. Prompt cache invalidates once on first call after deploy; rebuilds on call 2. One-time cost. Historical overview backfill explicitly out of scope: existing `vs_scouts.brief_data.event_overview` rows stay as-is; producers regenerate per scout from the BriefReport UI when desired; new regenerations are in-voice from deploy forward. **Post-smoke amendments (3) folded into the same squash 2026-05-25** after Jimmie's first-smoke read on a Cooper Flagg / The Art of Cooper Flagg / New Balance brief surfaced three drift dimensions in the base-spec output: template rhythm (the 5 named `<voice>` beats were producing a strict 4-sentence beat-checklist with one sentence per beat in order), stative-verb prevalence (the original `<rules>` block had "Present tense" but said nothing about active voice; the model read present-tense-passive constructions as compliant; output leaned on "is a," "arrives as," "is displayed," "holds" / "the aesthetic holds"), and comma-chained appositional pile-ups in the closing sentence (the "creative extrapolation is welcome" + sensory-language permission was over-firing on the close). **Amendment 1: `<voice>` beats reframe.** Replaces the "Beats that show up in good overviews:" intro + 5 beat bullets with "Beats that often show up in strong overviews. You do not need to hit all of them, and the order is yours. Combine, skip, or fold beats together as the brief calls for. The list is illustrative, not a checklist." Individual bullet phrasings softened in parallel ("Hero beat named. Call out the centerpiece moment" → "A hero beat. The centerpiece moment"). Loosens the structural template-pull. **Amendment 2: Three new `<rules>` lines.** Inserted after the present-tense rule. Active-verbs rule with stop list ("Use active verbs. Lean away from stative or arrival constructions ('is a', 'arrives as', 'holds', 'sits within', 'exists as'). Subjects act on the page; let the activation, the guests, and the brand DO things."); the stop list pulled directly from the Cooper Flagg drift. Vary-openings rule ("Vary sentence openings and structure. Not every sentence needs to start with the concept name or a subject-verb construction. Lead a sentence with a participial phrase, a temporal frame, or an audience cue when the rhythm calls for it."). Closing-clean-clause rule ("Closing sentence is one clean clause. Avoid comma-chained pile-ups of appositional descriptors (e.g., 'X, Y, Z, premium without spectacle, nostalgic in texture but current in energy').") with the exact descriptor-pile-up example from the Cooper Flagg drift's final sentence. **Amendment 3: Third `<example>` from Jimmie's manual Cooper Flagg edit.** Initial recommendation was a cleaned Grey Days 2026 anchor (from the four reference past-deck overviews); Jimmie pushed back with his manual edit of the first-smoke Cooper Flagg generation: "Anchored in the weeks surrounding the 2025-2026 season tip-off, The Art of Cooper Flagg is both a product launch and an immersive portrait that places Cooper Flagg's story in a gallery-forward pop-up that frames New Balance's newest NBA chapter through the lens of the athlete who defines it. His discipline, craft, and quiet intensity as a student of the game center the curated, gallery-style environment where sneakers and apparel are displayed with the weight of collected works. NBA and Mavericks fans and sneaker enthusiasts alike move through the space on their own terms, discovering product, storytelling, and photo moments that feel authored rather than assembled. The space exemplifies quiet confidence through the visual language of a career in its first chapter, yet already worth framing." Used verbatim (4 sentences, present-tense, no em dashes, active verbs throughout, varied openers, clean atmospheric closing clause — complies with all `<rules>` including the three new lines). Anchor reasoning: (a) concrete Mirror-voice rewrite of a generated overview that Jimmie himself made, so unambiguously Jimmie-approved; (b) exhibits exactly the patterns the post-smoke `<rules>` teach; (c) single-activation overview shape rather than the brand-platform shape Grey Days carries. `<examples>` intro line updated to "Three reference overviews. They demonstrate the voice; your output should sit in the same tonal range. The examples vary in opening structure (concept-first / participial-phrase / temporal-frame) on purpose; match the voice, not the surface shape." Trade-off acknowledged: regenerating the Cooper Flagg brief itself will trivially echo the anchor; the SheaMoisture + Grey Days reference overviews remain reserved as out-of-sample smoke-comparison references. Re-smoke after amendments confirmed voice + tone aligned (Jimmie 2026-05-25). One edge function redeploys out-of-band: `vs-generate-brief-overview` (second redeploy; first was Phase 5.12.5; two deploys this ship — once for the base spec on 2026-05-24, once for the smoke amendments on 2026-05-25; both deploys are out-of-band and don't burn Netlify credits). No migration. No schema. No frontend changes. Spec at `OUTPUTS/historical/phase-5-12-13-4-spec.md`; 9 locked decisions (6 base + 3 post-smoke amendments) captured in `docs/decisions.md` § Phase 5.12.13.4. Held under the 5.12 deploy budget. |
| 5.12.13.1 | 2026-05-24 | `1a01d3e` | First ship of the Phase 5.12.13 prompt audit. `key_features` field shifts from address-anchored narrative sentences to short, evergreen, reusable feature tags across every consumer that writes it. Three surfaces in lockstep: (a) shared `FILL_TOOL.key_features` schema in `supabase/functions/_shared/venueFill.ts` (consumed by `vs-research-venues` Phase A's `enrichSheetVenue` + Phase A's `enrichHqPoolForResearch` + `vs-compile-summaries` Pass 1 + `vs-research-single-venue`); (b) Phase B `vs-research-venues` `TOOL.venues[].key_features` mirroring the FILL_TOOL shape; (c) `_shared/venueOverview.ts` user-message label sync. Schema rewrite: description swaps from "3-6 short, concrete physical or experiential features... 5-15 words" with sentence-shaped examples to "2-10 short, evergreen tags... 1-4 words each (usually 1-2)" with the canonical tag list (Courtyard, Skylight, High Ceilings, Garage Doors, Ground Floor, Multi-Level, Waterfront, Rooftop, Garden, Wood Floors, Exposed Brick, Balcony, Storefront, Vaulted Ceilings, Multiple Stages, Outdoor, Floor-to-Ceiling Windows) as the reference shape. Reference list framed as "fine if they match the shape" rather than a closed whitelist so the model can emit valid tags not on the list. Hard exclusions enumerated: specific named neighborhoods, streets, addresses, landmarks, architects; numbers, units, square footage, walk scores; narrative sentences > 4 words; marketing fluff; placeholder tokens. `items.maxLength` tightens 120 -> 35; `minItems` 3 -> 2; `maxItems` 6 -> 10. New deterministic `sanitizeTagShape(items: unknown): string[]` export in `_shared/venueTypes.ts` (adjacent to existing `stripPlaceholders`): drops non-strings, empty strings, items with any digit, items with > 4 whitespace-separated words, items > 35 chars, case-insensitive duplicates (preserves first-occurrence casing). Composes with `stripPlaceholders`: `sanitizeTagShape(stripPlaceholders(...))`. Deliberately does NOT strip items matching a CANONICAL_TYPES token because "Outdoor" is both a canonical venue type AND a legitimate evergreen feature tag in the locked tag list; the digit / word-count rules cover the bad patterns the venue-type exclusion would target. Sanitizer chains at FIVE write sites: Phase A `enrichSheetVenue` (vs-research-venues), Phase A `enrichHqPoolForResearch` NEW key_features write gate (vs-research-venues; pre-5.12.13.1 the HQ-pool branch deliberately skipped key_features writes because "HQ rows already carry canonical values"; Jimmie's call during implementation surfaced an ambiguity in the spec vs the live code and locked the policy: HQ pool rows whose existing `key_features` is null/empty now receive sanitized AI-emitted tags; never overwrites and never appends to an existing tag set, so HQ-curated data stays authoritative for any HQ pool row that already has tags. Required adding `key_features` to the HQ-pool SELECT column list + the `HqPoolRow` type), Phase B parse path inside `venuesToInsert.map` (vs-research-venues), Pass 1 backfill (vs-compile-summaries), apply path (vs-research-single-venue). User-message label sync: `Key features:` -> `Key features (tags):` in both `buildOverviewUserMsgFromVenue` (HQ-side hq-generate-venue-about consumer) + `buildOverviewUserMsgForVsRow` (VS-side vs-compile-summaries Pass 2 consumer). `ABOUT_VENUE_SYSTEM` `<input_fields>` documentation line also syncs: `- Key features (list)` -> `- Key features (tags)`. Both builders update in lockstep so the prompt-cache identity preserves across HQ + VS Pass 2 (the `<venue_input>` block shape is deliberately byte-identical so the long evergreen `ABOUT_VENUE_SYSTEM` is billed once per scout). FILL_SYSTEM body and the six in-prompt `ABOUT_VENUE_SYSTEM` examples stay byte-for-byte unchanged (per `feedback_tool_choice_collapse`; example re-shape pairs naturally with the 90-110 word cap tuning in 5.12.13.2). Historical data backfill explicitly out of scope: existing narrative-shaped `features` rows on `venues` + `vs_candidate_venues.key_features` stay until producer edits or future re-emit overwrites. Four edge functions redeploy out-of-band: `vs-research-venues` + `vs-compile-summaries` + `vs-research-single-venue` + `hq-generate-venue-about` (the fourth consumes the updated `_shared/venueOverview.ts` shared module without a direct edit). No migrations. **Post-implementation amendments (3) folded into the same squash after live-dev smoke surfaced follow-on items:** (A) Features column on Sourcing + Shortlist matrices swaps `EditableTextarea` (rows=6 comma-joined string) for the existing `<TagInput>` so the new 1-4 word tags render as pills with × removal + type-and-Enter to add (matches BriefVenue + BriefReport patterns; the matrix `debounceSave`'s defensive string-to-array branch stays in place for any future string-shaped callers). (B) `addManualRow` + the HQ Venue picker INSERT payload both default `shortlisted: true` (closes the gap where manual + HQ-picker rows appeared on Shortlist via the `v.shortlisted || v.source === "manual"` filter but bypassed `SourcingReport.onContinue`'s research fan-out gate `!v.shortlisted`; producer can still untick to exclude). (C) `vs-generate-deck pushVenuesToHq` match-path + pre-linked-path write-when-blank extends from `about_venue`-only to also cover `features`, `total_sq_ft`, `capacity`, `website_url`; the `HqVenueRow` type + the venues SELECT + the in-batch pool-push extend to carry the four extra fields; single consolidated UPDATE per matched venue (replaces the separate `about_venue`-only UPDATE) so VS enrichment of HQ-linked rows backfills HQ-side gaps at deck-generation time while preserving producer-edited HQ values. Adds `vs-generate-deck` as the 5th edge function redeploy this ship. Five edge functions total: `vs-research-venues` + `vs-compile-summaries` + `vs-research-single-venue` + `hq-generate-venue-about` + `vs-generate-deck`. Three additional `src/*` edits ([SourcingReport.tsx](src/pages/venue-scout/SourcingReport.tsx), [Shortlist.tsx](src/pages/venue-scout/Shortlist.tsx)) for amendments A + B. Verification gate (re-run after amendments): typecheck + build + test all clean; sanitizer ad-hoc unit verification against the 4 inputs documented in spec § 11.1 emitted the expected output (bad: `[]`; good: all 6 pass through; mixed: 3 of 5 survive; dupes: 1 of 3 with first-casing preserved). Closes `code-observations.md` Edge #3 + Edge #14. New code-observations Edge row logs the cross-cutting delimiter standardization carry-forward (locked target: `/` + `,` universally, mirroring what VS Phase A + frontend `parseTypes` already accept; today HQ Bulk Import splits on `\|` and `vs-parse-sheet` doesn't split at all). Held under the 5.12 deploy budget. |
| 5.12.10 | 2026-05-24 | `1a01d3e` | VS canonical venue-types now read at runtime from `public.venue_types` (5 edge functions + shared `useLookup` frontend cache). New `src/lib/venue-scout/useVenueTypes.ts` hook + new server-side helpers (`getVenueTypesCanonicalSet` returning `{ names, idByName }`, `canonicalizeAgainst`, `canonicalizeMultiAgainst` for DISPLAY, `sanitizeMultiAgainst` for WRITE) in `_shared/venueTypes.ts`; legacy `canonicalizeType` + `canonicalizeMultiType` regex helpers DELETED from both `_shared/venueTypes.ts` and `src/lib/venue-scout/venueTypes.ts` after grep verified zero residual callers. Single migration (`20260607000000_phase_5_12_10_venue_types_db_driven.sql`) wraps 5 atomic steps inside one BEGIN/COMMIT: (1) backfill the 9 legacy palette names; (2) DELETE test rows not in legacy 9 with zero `venue_venue_types` references; (3) consolidate any remaining case-variants among survivors via insert-then-delete on `venue_venue_types` (avoids the `(venue_id, venue_type_id)` PRIMARY KEY violation an UPDATE pattern would hit when a venue references multiple losers in the same lower-name group); (4) drop the case-sensitive `venue_types_name_key` constraint, add `venue_types_name_lower_unique_idx ON LOWER(name)` (mirrors cities pattern from Phase 5.2.2); (5) `CREATE OR REPLACE bulk_import_commit_venues` with the 5.10.0 body, applying two substring swaps inside the v_queued_types loop: `ON CONFLICT (name) DO NOTHING` -> `ON CONFLICT DO NOTHING` (the column-level target no longer resolves after step 4) AND `WHERE name = v_qrec->>'name'` -> `WHERE LOWER(name) = LOWER(v_qrec->>'name')` on the race re-read after a swallowed conflict (without this swap, a race-loser inserting "retail" while "Retail" wins gets null back from the exact-case re-read and the downstream venue join insert fails). `vs-generate-deck`'s pre-5.12.10 `ensureVenueTypesLoaded` helper retired in favor of `getVenueTypesCanonicalSet`'s `idByName` field (consolidated single-read pattern per OQ #3). Schema descriptions + system prompts stay static (prompt-cache stability per `feedback_tool_choice_collapse`); live canonical list rides in the per-call user message via `buildFillUserMsg` (new optional `canonicalSet?: readonly string[]` 3rd param) + Phase B requirements block + brief parser user message. `vs-parse-brief`'s `sanitizeVenueTypesAgainstCanonical` widened to take `canonicalSet` param; the "industrial warehouse" combined-phrase special-case gated on BOTH "Industrial" AND "Warehouse" being in the runtime set so a 5.12.13 prompt-audit retirement of either tag doesn't silently re-emit it. `vs-research-single-venue`'s apply guard hardened from verbatim-trim to `sanitizeMultiAgainst` (matching every other AI WRITE path). `parseTypes` signature widened from `CanonicalType[]` to `string[]` with optional `canonicalSet` param so legacy data with names dropped from the runtime set surface as fallback-styled pills. `TypeTogglePopover` unions runtime types with any stale tokens stored on a venue so producers can uncheck stored-but-removed types (stale tokens render at the END of the list, pre-checked, with `TYPE_FALLBACK_STYLE`). New `paletteForType(name)` non-hook palette helper in `src/lib/venue-scout/venueTypes.ts` (case-insensitive `CANONICAL_TYPES` lookup with `TYPE_FALLBACK_STYLE` fallback); `VenueTypePill` migrated to it. **Brief intake chip row swap:** `src/components/venue-scout/ChipMultiSelect.tsx` (consumed by `BriefVenue.tsx` AND `BriefReport.tsx`) replaces its hard-coded `CANONICAL_VENUE_TYPE_CHIPS` array (descriptive labels like "Pop-up retail" / "Industrial / warehouse" / "Gallery / white box") with `useLookup("venue_types")` so the same runtime canonical set drives the brief intake flow too. The `+ Other...` inline-add now INSERTs into `public.venue_types` via `useLookup.addOption` (was per-brief custom chip that didn't persist) so producer adds in the brief intake flow propagate to every VS surface forever. Toggle is case-insensitive: clicking "Retail" when a row carries legacy lowercase "retail" first removes the variant, second click adds canonical case; gradually normalizes stored values without an explicit backfill. Stale per-brief chips (legacy descriptive strings on pre-5.12.10 briefs; admin-deleted lookup rows still stored on a brief) render with selected styling + X-remove affordance per the pre-existing custom-chip path so producers can drop them without re-adding to the lookup. Existing `brief_data.venue_types` strings are NOT backfilled (per OQ #4 posture; testing-record data + the brief parser already filters AI-emitted values against the canonical set, so the only stragglers are producer-typed legacy strings the brief is rarely re-opened on). `TYPE_STYLES` palette stays keyed on the 9 legacy names; producer-added types render via `TYPE_FALLBACK_STYLE` (neutral grey). Frontend page edits propagate the widened signature: `Review.tsx` toggleType widened to `t: string`, chip-row swapped from `CANONICAL_TYPES.map` to `availableTypes.map` (from `useVenueTypes`), `TYPE_STYLES[t]` -> `paletteFor(t)`; `SourcingReport.tsx` + `Shortlist.tsx` add `useVenueTypes`, thread `availableTypes` into `parseTypes`, widen TypeTogglePopover `onChange` callback to `(next: string[]) => void`. `matrix/primitives.tsx` re-exports of `CANONICAL_TYPES` + `canonicalizeType` dropped (TYPE_STYLES + TYPE_FALLBACK_STYLE + parseTypes + `type CanonicalType` re-exports retained). Five edge function redeploys out-of-band: `vs-research-venues`, `vs-compile-summaries`, `vs-generate-deck`, `vs-parse-brief`, `vs-research-single-venue`. Closes `code-observations.md` Edge #5. Held under the 5.12 deploy budget. |
| 5.12.12 | 2026-05-24 | `1a01d3e` | Rail nav is now VS-context-aware: when on /venue-scout/*, the brand reads inline "Venue SCOUT" (white "Venue" + coral "Scout"; same chrome + 56px row as the HQ "Mirror HQ" brand) and the rail Tools group + heading are hidden; three VS rows render directly under Activity Feed (Venue Scout parent + indented New Project + indented Settings) with all three force-coral throughout /venue-scout/* via a new `forceActive` RailItem field. Brand link routes to /venue-scout. New `.hq-ri--active:hover` CSS rule (white-tint background + coral text) gives active rows a distinct hover affordance instead of looking identical to rest state (applies to HQ Tools' single-row active match too). TopBar hides the global search bar inside /venue-scout/* (global search is HQ-Core-scoped: Projects / Venues / Vendors / People / Clients / Outlook; not VS scouts / candidate venues); cmd-k guards on input mount so it passes through to the browser shortcut on VS pages. New /venue-scout/settings tool-app-wide stub page (ScoutGlobalSettings; AdminRoute) plus a SURFACE_LABELS entry on AdminRoute for route-aware denied copy. Extracted resolveSettingsHref util in src/lib/shell/ for HQ + TS contexts. TS + HQ contexts otherwise unchanged. Seven source surfaces touched (LeftRail.tsx + index.css + TopBar.tsx + App.tsx route + AdminRoute.tsx SURFACE_LABELS + new ScoutGlobalSettings page + new resolveSettingsHref util). No migration, no edge function, no schema. Decisions doc captures two post-implementation revision rounds off Jimmie's feedback (round 1: dropped stacked-three-line brand + "Venue Scout" group heading, added forceActive; round 2: full coral on active rows + new white-tint hover, hide TopBar search in VS; an interim muted-coral persist variant was prototyped and discarded). |
| 5.12.11 | 2026-05-24 | `1a01d3e` | Deck image placeholder sizing (CENTER_CROP fills photo grid cells). `vs-generate-deck` per-venue photo replacement on slide 7 swapped from `imageReplaceMethod: "CENTER_INSIDE"` to `imageReplaceMethod: "CENTER_CROP"`. CENTER_INSIDE letterboxed photos whose source aspect ratio did not match the template cell aspect ratio (visible whitespace bars on the short axis); CENTER_CROP preserves aspect, fills the cell fully, and crops the long-axis overflow. One-line code change to the `replaceImage` request constant on the per-venue photo loop; no template doc edit, no schema migration, no UI work, no telemetry add. Producer impact: the 2x2 photo grid on each per-venue detail slide now renders every uploaded photo filling its cell, matching the template's grid geometry as drawn. Re-Generate produces the same layout deterministically. Failure surface unchanged: missing photos still fall back to the template placeholder bitmap; URL fetch failures still route to `SLIDES_API_FAILED` via the existing outer catch. `vs-generate-deck` redeployed out-of-band. Parallel-pilot cycle 1 ship (with 5.12.5). Held under the 5.12 deploy budget. |
| 5.12.4.2 | 2026-05-24 | `1a01d3e` | Codex adversarial review fixes round 2 (kickoff grace windows + final-UPDATE CAS guards + vs-generate-deck non-mutating-prereq reorder). Three correctness fixes from a second adversarial review on the 5.12.4.1 squash. All three target pre-existing lifecycle / cancellation bugs that span the VS pipeline; none introduced by 5.12.4 or 5.12.4.1. **Fix 1: `IN_FLIGHT_GRACE_MS` MUST exceed `WORK_TIMEOUT_MS` + writeFailure-buffer.** Pre-5.12.4.2 all three VS edge functions had `IN_FLIGHT_GRACE_MS <= WORK_TIMEOUT_MS`, so once the grace check expired BUT the first work() invocation was still mid-flight, a producer refresh could reacquire the kickoff and start a SECOND parallel run — re-opening the duplicate-write race the 5.12.4.1 kickoff RPC was meant to close. Bumped: `vs-generate-deck` 90_000 -> 240_000 (180s timeout + 60s buffer), `vs-compile-summaries` 90_000 -> 300_000 (240s + 60s), `vs-research-venues` 360_000 -> 420_000 (360s + 60s; equal-grace-and-timeout was hairline since RPC uses `<` for the age check). Producer's explicit Re-Generate flow unaffected (reset RPCs clear the kickoff timestamp directly). **Fix 2: final success UPDATE CAS on `pipeline_error IS NULL`.** `Promise.race([work, timeoutPromise])` rejects on timeout + writes failure, but JavaScript can't cancel the losing work() promise — a slow Claude call (per-fetch ceiling 180s + 1 pause_turn continuation = up to ~360s real wall-clock) can resolve after the timeout, then apply per-venue patches, then overwrite the failure stamp with `current_step='deck_prep' / 'sourcing_report' / 'completed' + status='in_progress' / 'complete' + pipeline_error=null`. All three final success UPDATEs (`vs-compile-summaries`, `vs-research-venues`, `vs-generate-deck`) now CAS on `current_step=<expected>` AND `pipeline_error IS NULL`. The kickoff RPC clears `pipeline_error` to NULL at acquisition (5.12.1 + 5.12.4.1 pattern); writeFailure / failWithCode stamps a non-null `pipeline_error` (the error message); so `pipeline_error IS NULL` is exactly the "no failure has been stamped yet" predicate, independent of `status` and `current_step` state-machine evolution. Each final UPDATE uses `{ count: "exact" }` so the caller can branch on `count === 0` (CAS no-op); the no-op branch logs a warn + returns cleanly so the outer try/catch doesn't double-write failure on top of failure. `vs-generate-deck` keeps its existing `.eq("current_step", "deck_prep")` predicate AND adds the `pipeline_error IS NULL` predicate (the existing CAS caught only parallel-success races; failWithCode leaves current_step unchanged so the new predicate is needed for the timeout-then-late-success path). **Fix 3: `vs-generate-deck` non-mutating-prereq reorder.** Pre-5.12.4.2 `pushVenuesToHq` ran at step 2.5 (immediately after the venues load), BEFORE the env-var check at step 4. A missing-env-var `TEMPLATE_COPY_FAILED` would leave HQ state mutated (new `venues` rows + `linked_venue_id` writes) for a deck the producer was told was NOT generated. The env-var existence check (`GOOGLE_TEMPLATE_FILE_ID` + `GOOGLE_OUTPUT_FOLDER_ID`) pulled into a new step 2.5 (before `pushVenuesToHq`); the actual `copyTemplate` Drive call stays at step 4. Remaining HQ-mutated-but-deck-failed window: Drive `copyTemplate` throw or Slides API throw AFTER the push — both fail via `TEMPLATE_COPY_FAILED` / `SLIDES_API_FAILED`; `pushVenuesToHq` is idempotent on `linked_venue_id` so the producer's Re-Generate path re-runs without re-INSERTing. The stronger guarantee (move HQ push after Slides success) is logged as carry-forward (a) in COWORK_SYNC. No schema migration. Three edge functions redeployed out-of-band: `vs-generate-deck`, `vs-compile-summaries`, `vs-research-venues`. Held under the 5.12 deploy budget. |
| 5.12.4.1 | 2026-05-24 | `1a01d3e` | Codex adversarial review fixes (deck kickoff race + dedupe identity-signal gate). Both findings surfaced during the 5.12.4 review pass; neither was introduced by 5.12.4 itself (deck kickoff TOCTOU traces to 5.12.0 when `pushVenuesToHq` moved into `vs-generate-deck`'s background work; dedupe threshold gap traces to 5.12.3 when the points-based ladder replaced the strict cascade). **Fix 1: deck kickoff race.** New `vs_deck_try_acquire_kickoff(target_scout_id uuid, grace_seconds int default 90)` SECURITY DEFINER RPC (migration `20260604120000_phase_5_12_4_1_deck_kickoff_lock.sql`); mirrors the Phase 5.12.1 `vs_research_try_acquire_kickoff` shape exactly. Uses `pg_try_advisory_xact_lock` on `hashtextextended('vs-deck:' || scout_id::text, 0)` (separate namespace from `vs-research:*` so deck + research kickoffs for the same scout don't collide on the lock space) + same-transaction read-and-write of `brief_data.deck_generation_started_at` + clear `pipeline_error`. SECURITY DEFINER, `SET search_path = public`, REVOKE from PUBLIC / anon / authenticated, GRANT to service_role only. `vs-generate-deck/index.ts` swaps the pre-5.12.4.1 inline check-then-write at lines 597-616 for `sb.rpc('vs_deck_try_acquire_kickoff', { target_scout_id: scout_id, grace_seconds: Math.round(IN_FLIGHT_GRACE_MS / 1000) })`; response shape on the kickoff path matches `vs-research-venues`'s pattern (RPC error -> 500; `acquired === false` -> 200 `{ skipped: "in_flight" }`; `acquired === true` -> proceed). Local `briefData` declaration removed (no downstream consumers after the kickoff block). Without the lock, two near-simultaneous invocations (producer double-clicking Generate) could both pass the 90s grace-window check before either committed its timestamp, then both call `pushVenuesToHq` and INSERT duplicate `venues` rows (no UNIQUE constraint on venue identity columns). **Fix 2: dedupe identity-signal gate.** `_shared/venueDedupe.ts` `findVenueDedupeMatch` eligible filter gains `s.score.name >= 1 || s.score.website >= 1` on top of `!s.vetoed && s.score.total >= threshold`. Address (50) + city (10) = 60 alone is no longer enough to merge; two distinct venues at the same building address with the same city ("75 Rockefeller Plaza - Top of the Rock" and "75 Rockefeller Plaza - Studio 8H") would have merged pre-fix and now fall through to `no_merge_identity_gate`. New telemetry decision string `no_merge_identity_gate` surfaces in the `[venueDedupe]` log line when telemetry-best clears threshold but fails the gate, so smoke logs make the rejection reason obvious (separate from the existing `merge` / `no_merge`). Cross-field VETO on name FULL match preserved unchanged. Both consumers (`vs-generate-deck`'s `pushVenuesToHq` + `vs-research-venues`'s cross-rail dedupe) pick up the change automatically. `dedupe_meta` jsonb write shape unchanged; pre-fix historical rows are not backfilled (gate filters before match resolution, so no new gated-rejection row ever lands as a `dedupe_meta` write). No frontend touched. Migration applied to live; `vs-generate-deck` + `vs-research-venues` redeployed; `src/integrations/supabase/types.ts` regenerated to surface the new RPC. Held under the 5.12 deploy budget. |
| 5.12.4 | 2026-05-24 | `1a01d3e` | HQ pool per-brief enrichment + about_venue-driven fit rescue. **Thread (a):** `vs-compile-summaries` Pass 1 `needsFill` predicate widened to include `source='hq_pool'` rows (recs + cons + rank trigger; derived_attrs trigger gates manual+sheet only because FILL_TOOL doesn't carry the scout's `derived_columns` IDs so Claude would invent keys that don't render in the matrix UI). Apply-side `derived_attrs` write guard extended with `v.source !== "hq_pool"` so a recs-triggered Pass 1 fire on an hq_pool row never lands invented derived_attrs keys. `consArr` declaration hoisted to the top of the per-venue loop so it's available in both the `needsFill` predicate and the apply-side guard. `_shared/venueFill.ts` `VenueForFill` gains optional `aboutVenue?: string | null`; `buildFillUserMsg` renders an HQ CANONICAL ABOUT block (above the existing VENUE block) + a narrower research instruction when `aboutVenue` is non-empty, telling Claude the about paragraph is authoritative for identity / character / neighborhood and to focus on per-brief judgment (recs + considerations + ranking_score). When `aboutVenue` is null / undefined / empty the byte-for-byte 4.10.3-port behavior is preserved for every existing caller. FILL_SYSTEM untouched per `feedback_tool_choice_collapse` (the about_venue arrives via the user message; the legal lever). Pass 1 callsite in `vs-compile-summaries` passes `aboutVenue: v.source === "hq_pool" ? v.venue_overview : null` so the HQ CANONICAL ABOUT block only renders for HQ-sourced rows. **Thread (b):** new pre-Phase-B rescue pass inside `vs-research-venues`. `loadHqVenuesIntoPool` refactored to return `HqPoolLoadResult` (`{ hqRows, alreadyLinkedSet, applicability, scored }`) so the new `rescueHqPoolFitVetoedVenues` helper consumes the same city pool + alreadyLinkedSet + applicability + scored entries without re-querying; the `ScoredEntry` type hoists to module scope. Pure helpers (`computeApplicability`, `scoreVenueFitForBrief`, `isApplicableArray`, `isApplicableFiniteNumber`) stay pure. New constants `RESCUE_INPUT_CAP = 20`, `FIT_RESCUE_SYSTEM` (~250-word fit-evaluation prompt, invented for this surface; no em dashes; "errs toward dropping" voice rule baked in), `FIT_RESCUE_TOOL` (`submit_fit_evaluation` returning `{ verdicts: array of { venue_id, keep, reason } }`). Rescue input set: every HQ city-pool venue whose deterministic decision is `drop_below_threshold` OR `drop_veto` with veto `wrong_type` / `aggregate_gaps`. EXCLUDE: `drop_veto` on `below_sq_ft_min` / `above_sq_ft_max` / `below_capacity` (hard physical disqualifiers), deterministic keeps, already-linked venues, venues with null/empty `about_venue`. Cap 20 venues per call; sort by `about_venue.length` DESC then name ASC so the highest-signal candidates win the slots. Claude call: model `claude-sonnet-4-6`, `max_tokens: 4000`, no web_search, forced `tool_choice: { type: "tool", name: "submit_fit_evaluation" }`. Two ephemeral cache breakpoints (`ttl: "5m"`): FIT_RESCUE_SYSTEM in the system content array, the brief block in the first user content block. The candidate venues block + final instruction stay uncached. Defensive predicates around `brief_data` jsonb reads (asArr / asNum / asStr matching the Phase B userMsg builder shape). Per-verdict apply path: hallucinated venue_id drops only that verdict (warn); malformed verdict shape drops only that verdict; duplicate verdict for the same venue ignores subsequent (warn); per-row INSERT failure increments `insert_failed` (warn-log); missing verdicts (Claude returned no verdict for a candidate) warn-log + treated as drop. Keep verdicts INSERT `vs_candidate_venues` with the same row shape `loadHqVenuesIntoPool`'s deterministic-keep INSERT uses (source='hq_pool', linked_venue_id set, recs / cons / rank / derived_attrs left null for Pass 1 thread (a) to fill at compile). New `[hqPoolRescue]` telemetry: per-verdict log line carries scout_id + venue_id + name + verdict + reason; summary line `candidates=N rescued=K dropped=M verdicts_missing=X insert_failed=Y` with accounting identity `rescued + dropped + verdicts_missing + insert_failed === candidates`; unconditional `[hqPoolRescue:accounting]` warn fires on counter drift (cheap arithmetic check; production too). On empty rescue input set: single short-circuit line `candidates=0 (no rescue needed)`; per-candidate + summary logs skipped. On Claude failure (timeout / 5xx / no tool_use / malformed verdicts array): single `claude failed: <error> (skipping rescue, deterministic pool stands)` warn-log; deterministic pool stands; research never blocks on rescue. **Pipeline placement:** `loadHqVenuesIntoPool` stays in the synchronous handler path (returns `HqPoolLoadResult | null` to a closure-captured variable; handler returns 200 kickoff immediately after). Inside `work()`: Phase A launches async (`phaseAPromise`); rescue awaits concurrent with Phase A; existing-venues SELECT + curatedExistingCount + targetNet + Phase B userMsg construction all moved INTO `work()` after rescue resolves so rescued + deterministic-keep hq_pool rows land in Phase B's EXISTING VENUES block; Phase B prompt + Claude call follow; `await phaseAPromise` after Phase B completes, before the final scout UPDATE. Phase B does NOT wait for `phaseAPromise`. The Phase B userMsg template splits into a handler-built `userMsgBriefPrefix` (no dependency on existing/targetNet) + the existing-venues block + the targetNet-dependent Return line, both built inside `work()`. Net latency overhead near-zero when Phase A is the longer leg. Composition with 5.12.3.1: deterministic fit filter decides first tier; cross-rail seed-then-drop is the second lane (unchanged; reads the post-rescue seeded set on its own SELECT, treats rescued rows as already-seeded matches via the existing `seededHqIds.has(matchedId)` branch); Claude about_venue rescue is the third lane. Closes the 5.12.3.1 first-smoke recall hole where Platform-LA-shape rows (wrong HQ tagging + rich about_venue + brief alignment) couldn't land unless Claude independently surfaced them via cross-rail seed-then-drop. No `fit_rescue` column on `vs_candidate_venues` for v1 (rescued rows indistinguishable from deterministic-keep hq_pool rows at the column level; producer-visible "why this venue surfaced" lever lives in the 5.12.3.1 deferred `fit_meta` carry-forward). No schema migration, no UI work. Held under the 5.12 deploy budget. |
| 5.12.3.1 | 2026-05-23 | `1a01d3e` | HQ pool fit filter + cross-rail seed-then-drop. `loadHqVenuesIntoPool` (in `vs-research-venues/index.ts`) rewritten from "every city-match in, soft signals as sort keys only, cap at 10" to filter-already-linked-out + score + threshold + vetoes. Five signals: venue_type 30 (≥1 token overlap) + neighborhood 20 + ideal_features 5/match (cap 25) + sq_ft 15 (venue.total_sq_ft in range) + capacity 10 (venue.capacity ≥ expected_guest_count); total max 100; threshold 40 (`>=` keeps). Out-of-band vetoes drop disqualified venues outright: `wrong_type` (brief specifies types AND venue has types AND zero overlap), `below_sq_ft_min` / `above_sq_ft_max` (venue.total_sq_ft out of range when brief side set), `below_capacity` (venue.capacity < expected when both set + venue.capacity > 0). Aggregate-gap veto `unscored > applicable / 2` (strictly more than half) drops skeleton HQ rows. Four new pure helpers added inline to the function: `isApplicableArray`, `isApplicableFiniteNumber`, `computeApplicability`, `scoreVenueFitForBrief` (no scout_id, no logging, no IO; caller owns telemetry). Pipeline order locked: SELECT city-matches -> SELECT existing hq_pool links for scout -> filter already-linked OUT before scoring -> compute applicability once -> score each remaining venue (per-venue try/catch defensive) -> filter to `decision === keep` -> sort score DESC then alphabetical ASC -> slice top 10 -> INSERT loop (per-row try/catch). Idempotency moved UP from post-sort (5.12.1 ordering) so newly-qualifying venues on re-run land additively even when the previous run filled the cap (closes the Codex spec-review bug). 10-row cap is PER RUN, not lifetime: matrix can grow beyond 10 across re-runs by design (additive guarantee preserves producer-edits-survive-across-runs). New `[hqPoolFilter]` telemetry log per SCORED venue (already-linked rows aren't re-scored or re-logged) carrying score breakdown + applicable + unscored + threshold + decision + veto. Summary log line extended with new counters (`inserted + insert_failed + over_cap + dropped_below_threshold + vetoed + already_linked === city_matches`); pre-5.12.3.1 `skipped` renamed to `insert_failed` for clarity; unconditional `[hqPoolFilter:accounting]` warn fires when the sum doesn't match (cheap arithmetic check defending against silent counter drift). `fit_meta` jsonb column + producer-visible FitMetaIndicator Popover DEFERRED (rationale: producer cannot act on it directly, telemetry log carries calibration data, brief snapshot would freeze stale data on producer brief-edit re-runs); carry-forward for 5.13 or a 5.12.14 follow-on. Zero-applicable-signals brief drops every row as `drop_below_threshold` (v1 strict empty-pool behavior; lossless-skip lever queued for smoke-driven flip). **Cross-rail dedupe seed-then-drop (folded in post-first-smoke):** the 5.12.1/5.12.3 cross-rail dedupe pool widened from the seeded `source='hq_pool'` subset to the full HQ `venues` city pool. Live smoke (scout 7ad1d004) surfaced the fit filter starving the dedupe: Platform LA in HQ was `wrong_type`-vetoed (HQ venue_types don't overlap with the brief's), so the only seeded hq_pool row was "The Pavilion", and Claude's "Platform Culver City" research row (same address) landed unlinked because the dedupe pool didn't include Platform LA. The 5.12.3 ladder would have scored 25 + 50 + 0 + 10 = 85 ≥ 60 with Platform LA in the pool; the pool was wrong, not the ladder. Fix: query the full HQ city pool; on match against an HQ id NOT in the seeded set, INSERT it as `source='hq_pool'` now (bypassing the fit filter for this venue), merging Claude's enrichment where HQ has nulls/empties (features when HQ.features is empty; size_sq_ft / capacity / website_url first-non-null fallback) plus Claude's per-brief judgments (derived_attrs, recommendations, considerations, rank) so the matrix row is rich on day one. HQ-canonical identity fields (name, address, neighborhood, venue_type, venue_overview from about_venue) still win. Seeded-set tracking includes newly-inserted ids so a later candidate matching the same HQ row hits drop-only (no double-insert). Seed-then-drop INSERT failure keeps the research row in the matrix as the safety net; vs-generate-deck's pushVenuesToHq catches the dedupe at deck time. New telemetry: `[vs-research-venues] ... seed-then-drop "<cand>" -> hq_pool <hqId>` per seeded match; summary log line extended with seed-then-drop counters. No schema migration, no UI work. Closes the 5.12.1 smoke note where pool venues over-surfaced in cities with sparse HQ coverage AND the 5.12.3.1 first-smoke recall hole on fit-vetoed HQ matches. Held under the 5.12 deploy budget. |

## Foundations

Phase 5.1 established the HQ Core spine: the new four-value `permission_role` enum
(admin / standard / freelance / pending) replaced the legacy member / producer / admin
trio, the polymorphic `notes_log` table landed (parent_type + parent_id; insert-self,
delete-author-or-admin, no UPDATE), the AppShell left-rail shipped as the persistent
chrome for every HQ surface, and Home dashboards composed the per-tier landing cards.
Phase 5.5.1 backfilled the public sign-in page (Surface 01) on top of the existing
admin-pre-provisioned signup flow.

- **Tables.** `notes_log`, `permission_role` enum rewrite.
- **Components.** `AppShell`, `LeftRail`, `RailItem`, `RailFooter`, `TopBar`, `HomeAdminDashboard`, `HomeStandardDashboard`, `HomeFreelanceDashboard`, `SignInPage`.
- **Triggers.** `handle_new_user` rewrite (pending tier; admin notification fan-out; id-swap path added later in 5.4).
- **Edge functions.** `notify-admin-of-pending-user` (later wrapped by `notifications-dispatch` in 5.5).
- **Sub-phases.** [`39bdd8c`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/39bdd8c) (5.1), [`8e043ff`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/8e043ff) (5.5.1).

## Database surfaces

Projects, Tasks, Deliverables, Clients, Vendors, People, and Venues each got a list page
(table / board / timeline / calendar views per surface), a detail page (read + inline-edit
on all but ProjectDetail's h1), and an edit page (form-style fallback). Inline-edit
primitives let producers update fields without leaving the detail page; status pills are
click-to-change with optimistic UI + Realtime rollback. Phone numbers normalize on save;
PostgREST embed-FK disambiguation is documented in `docs/conventions.md`.

Highlights by surface:

- **Projects.** ProjectsList table + board + timeline + calendar views; ProjectDetail
  with Status Notes + Activity card + child-entity rollup; ProjectEdit with `project_members`
  general join (5.7.7). Install / Live / Removal date ranges (5.3). Status enum locked at
  14 values (5.2.1). Legacy `status_notes` + `client_notes` columns dropped in 5.7.14
  after the 5.7.3 UI swap to `notes_log` and 5.7.7 removal of client_notes UI.
- **Tasks.** TasksList table + board; in-table quick-add (5.7.7); per-row inline title
  edit; TaskDetail with hard-delete from edit page (5.7.4); auto-task lifecycle linked to
  deliverables via `tasks.source_deliverable_id` (5.7.5).
- **Deliverables.** DeliverablesList table + board + calendar; status enum reshape
  (dropped `In Progress` in 5.7.5; Upcoming / Complete / Skipped final set); auto-task
  helper writes `tasks.source_deliverable_id` with ON DELETE CASCADE (5.7.5 follow-up).
- **Vendors.** VendorsList table; VendorDetail with Internal Rating (replaced by Team
  Rating in 5.7.13); Files & Assets URL list (5.7.11); `vendor_categories` +
  `vendor_subcategories` + `vendor_capabilities` lookup tables; "Internal" pill inline
  with the subtitle (5.7.13).
- **Clients.** ClientsList table; ClientDetail with Primary Contact composite +
  Deliverables + Projects columns; vendors split out of the legacy `organizations` table
  (5.2.3).
- **People.** PeopleList table; PersonDetail inline-edit prototype (5.6.3) became the
  pattern for the rest. Affiliation enum + mutex CHECK on `client_id` / `vendor_id`.
- **Venues.** VenuesList table; VenueDetail with Venue Type pill + subtitle reshape
  (5.7.13); `venue_types` + `venue_venue_types` join; `venue_rate_history` append-only
  table.

Tables shipped: `projects`, `tasks`, `deliverables`, `clients`, `vendors`, `people`,
`venues`, `project_account_managers`, `project_designers`, `project_members`,
`project_vendors`, `project_venues`, `venue_venue_types`, `venue_contact_people`,
`venue_rate_history`, `vendor_files`, `vendor_ratings`, `vendor_categories`,
`vendor_subcategories`, `vendor_capabilities`, `cities`, `project_categories`,
`venue_types`, `departments`, `notes_log`.

Sub-phases: [`2938aa1`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/2938aa1) (5.2.1),
[`0ce758f`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/0ce758f) (5.2.1 Revision),
[`6214002`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/6214002) (5.2.2),
[`519a46c`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/519a46c) (5.2.3),
[`9b63650`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/9b63650) (5.2 cleanup),
[`04e9588`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/04e9588) (5.6.2),
[`01d35ac`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/01d35ac) (5.6.2.1),
[`e1b8713`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/e1b8713) (5.6.2.2),
[`aab31b9`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/aab31b9) (5.7.4),
[`43eeb27`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/43eeb27) (5.7.5),
[`c8bc789`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/c8bc789) (5.7.7),
[`9bf068e`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/9bf068e) (5.7.8),
[`d16c4c1`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/d16c4c1) (5.7.11),
[`8eea759`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/8eea759) (5.7.13).

## Calendar + Outlook

Phase 5.3 shipped the unified `/calendar` route plus the admin-only `/outlook` pipeline
view. CalendarMonthView renders projects + tasks + deliverables + outlook entries on a
shared month grid; per-source visibility persists via an implicit `saved_views` row keyed
`__calendar_default`. The new `outlook_entries` table carries Outlook pipeline rows with
year + month + week + budget + confidence; `promote_outlook_to_project` is a SECURITY
DEFINER RPC that converts an entry to a Queued project atomically. Phase 5.7.9 overhauled
the Calendar surface: pagehead reshape, color sweep, My Tasks personal layer, Day / Week /
Month view switcher, and persisted activeView via the same saved-view jsonb.

- **Tables.** `outlook_entries`, `mirror_holidays` (5.4).
- **Enums.** `outlook_confidence` (On Radar / Likely / Confirmed / Complete).
- **RPCs.** `promote_outlook_to_project`.
- **Routes.** `/calendar` (per-source presets via `?source=projects|tasks|deliverables`), `/outlook`.
- **Sub-phases.** [`740ad7e`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/740ad7e) (5.3), [`3d5f1cc`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/3d5f1cc) (5.7.9).

## Wiki + Settings + Account Logins

Phase 5.4 shipped four new tables (`departments`, `wiki_pages`, `credentials`,
`mirror_holidays`) plus the `/wiki`, `/settings`, `/users`, and `/account-logins` routes.
Eleven wiki pages seeded (Welcome & Mission, Team Directory, How We Work, Preferred
Vendors, Key Partners, Forms & Important Documents, Account Logins, Pricing & Markup
Guide, Design File Prep & Specs, Billing & PO Workflow, Shipping & Messengers).
Credentials table is SELECT-gated to admin + standard (freelance blocked); writes opened
to non-freelance in feedback round 2. `handle_new_user` gained an email-match id-swap
path so admins can pre-provision users by email. Phase 5.7.10 merged Lookup Lists into a
single Settings surface, added the Wiki image upload pipeline (private `wiki_images`
bucket, 1-year signed URLs, diff-on-save cleanup), and stubbed the Integrations card to
"Coming Soon".

- **Tables.** `departments`, `wiki_pages`, `credentials`, `mirror_holidays`.
- **Storage buckets.** `wiki_images` (private; admin-only writes; 1-year signed URLs).
- **Routes.** `/wiki`, `/wiki/:slug`, `/settings`, `/users`, `/users/:id`, `/account-logins`.
- **Sub-phases.** [`0e0f0ef`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/0e0f0ef) (5.4), [`367fa0e`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/367fa0e) (5.7.10).

## Notifications + Activity Feed + Search

Phase 5.5 shipped the notifications spine: durable `notifications` table with `delivered_slack`
column; per-user `user_notification_preferences` (in_app + slack_dm per trigger_key);
`notifications_dispatch_writer` trigger function fanning out to `notifications-dispatch`
edge function; `notifications` added to `supabase_realtime` with REPLICA IDENTITY FULL so
the bell panel re-renders on insert. Three pg_cron schedules cover deliverable-due-3d /
task-due-today / event-date-today reminders. Activity Feed at `/activity` reads the
`activity_log` write-side that the `activity_log_writer` trigger has been populating since
5.1. Search at `/search` matches names + tags across projects + tasks + deliverables +
people + clients + vendors + venues. Phase 5.7.2 added @-mentions: `note_mentions` join
table, parent_title resolved per parent_type, dispatch trigger fires `mention` event_type,
activity_log writer logs one row per mention.

- **Tables.** `notifications`, `user_notification_preferences`, `note_mentions`, `activity_log`.
- **Edge functions.** `notifications-dispatch`.
- **Triggers.** `notifications_dispatch_writer`, `activity_log_writer_note_mention`.
- **Cron jobs.** `hq-cron-deliverable-due-3d`, `hq-cron-task-due-today`, `hq-cron-event-date-today`.
- **Routes.** `/activity`, `/search`, bell panel in TopBar.
- **Sub-phases.** [`82ad5d8`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/82ad5d8) (5.5), [`5e63484`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/5e63484) (5.7.2).

## Profile + Team management

Phase 5.6.5 added the owner concept: `users.is_owner` boolean (single seeded owner) plus
`saved_views.scope` ('user' / 'global') with owner-only writes on the global branch via
scope-aware RLS. Phase 5.6.5.1 extended `users_protect_admin_columns` with an is_owner
write gate plus a self-revoke block so the last-owner-locks-themselves-out failure mode is
structurally impossible. Phase 5.7.12 split Profile + Settings into separate surfaces;
extended the admin-columns trigger to gate `is_owner` / `email` / `full_name`; dropped the
legacy Creative department (merged into Design); and added the choose-only Departments
picker via a new `<RecordCombobox allowCreate={false}>` prop. Slack handle field with `@`
prefix glyph + hint caption; Slack DM dispatch verified end-to-end after `SLACK_BOT_TOKEN`
landed.

- **Tables.** `users` (gains `is_owner`, `role_title`, `department_id`, `slack_handle`, `slack_user_id`, `last_active_at`).
- **Triggers.** `users_protect_admin_columns` (extended).
- **Routes.** `/users`, `/users/:id`, `/settings/profile`, `/settings/notifications`.
- **Sub-phases.** [`986b953`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/986b953) (5.6.5), [`b2de061`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/b2de061) (5.6.5.1), [`60f6ff0`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/60f6ff0) (5.7.12).

## Cross-cutting primitives

The cross-cutting primitives evolved across the 5.6.X and 5.7.X rounds, settling into a
canonical interaction kit by the end of Phase 5.7. DataTable carries table / board /
timeline / calendar variants per surface; FilterBar gained a global distinct-values picker
(5.7.6) and a globally-locked "is" op with `allowIsNot` opt-in (5.7.8); SavedViewsDropdown
moved inline with FilterBar (5.7.8); StickySaveBar switched to fixed positioning (5.7.3);
RecordCombobox absorbed phone normalization (5.6.1), inline quick-add (5.7.7), and
`allowCreate` opt-out (5.7.12); ClickPillCell handles click-to-change status pills with
optimistic UI + Realtime rollback; InlineEditText + InlineTagInput power detail-page
inline-edit; MiniCreateModal handles parent-scoped inline-add; StarRating gained `md` size
+ half-star read-only rendering (5.7.13).

- **Components.** `DataTable`, `FilterBar`, `SavedViewsDropdown`, `BoardView`,
  `TimelineView`, `CalendarMonthView`, `RecordCombobox`, `ClickPillCell`,
  `InlineEditText`, `InlineTagInput`, `MiniCreateModal`, `StickySaveBar`, `StarRating`,
  `EmptyState`, `LoadError`, `PermissionDenied`, `InternalNotesEditor`,
  `VendorFilesEditor`, `MyWeekStrip`, `ProjectActivity`.
- **Sub-phases.** [`65e45ea`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/65e45ea) (5.6.1), [`bdae150`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/bdae150) (5.6.3), [`ba89adb`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/ba89adb) (5.6.3.1), [`2506d9d`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/2506d9d) (5.6.4), [`276df71`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/276df71) (5.6.4.1), [`b7a5238`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/b7a5238) (5.7.1), [`2e61571`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/2e61571) (5.7.3), [`612c8e8`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/612c8e8) (5.7.6), [`59a5dac`](https://github.com/jimmiebaugh/mirror-nyc-hq/commit/59a5dac) (5.7.14).

---

## Phase 5.8 — HQ v1 release pass + security audit + cleanup

Phase 5.8 closed the HQ v1 release window and ran a full Tier 1 / Tier 2 / Tier 3 audit pass plus an auth-pre-provision hotfix sweep. **Security (5.8.5):** seven migrations covering function search_path pinning, REVOKE EXECUTE on SECURITY DEFINER fns, RLS init-plan rewrite across 17 policies, storage bucket listing tighten, and `credentials` column encryption via pgsodium AEAD with three SECURITY DEFINER RPCs (`credentials_create` / `credentials_set_password` / `credentials_reveal_password`). **Hotfix (5.8.5.1):** re-GRANT EXECUTE on `is_admin` / `is_producer_or_admin` / `current_user_role` after the 5.8.5 REVOKE FROM PUBLIC silently broke RLS predicate evaluation for authenticated callers. **Surface reduction (5.8.6):** CVE bumps closing HIGH XSS GHSA-2w69-qvjg-hvjx, DOMPurify wrap on wiki HTML, 28 shadcn UI deletes + 23 unused deps uninstalled (CSS bundle -26KB), Anthropic timeouts + redaction, sonner → shadcn toast migration, `secrets/` moved out of repo. **Audit v1.1 close (5.8.7):** worktree gitignore + sweep, `next-themes` uninstall, generated-types eslint ignore, credentials RPC predicate refactor via the helper, credentials reveal audit-log row; `v1.1.0` tag pinned. **Auth hotfix (5.8.8 + 5.8.8.1):** restore the Phase 5.4 swap block dropped during the 5.5 notifications rewrite, add the symmetric `trg_users_align_id_to_auth` BEFORE INSERT trigger so pre-provisioning is order-agnostic, scope new-user email dispatch to active owners only (bell-panel stays admin-wide), sanitize the OAuth `redirect_uri` poisoning failure mode, flip all 42 FKs pointing at `public.users.id` to ON UPDATE CASCADE. Now lives under the single squashed commit `8a468d2`.

## Phase 5.9 — Bulk Import (Projects + Vendors + Venues)

Cross-cutting bulk-import primitive plus per-entity wiring. CSV-only across all three entities; map step for unknown refs; admin-only; dedupe defaults to skip with per-row override; configurable column picker; fresh HQ-side parser (`_shared/hqSheetParser.ts`, deliberately separate from `vs-parse-sheet`).

- **Tables.** `bulk_import_sessions`, `bulk_import_drafts`; per-entity `bulk_import_session_id` columns on `projects`, `vendors`, `venues` (partial indexes); `vendors.nationwide`, `vendors.general_email`, `venues.general_email` added during the per-entity rounds.
- **RPCs.** `bulk_import_commit_projects`, `bulk_import_commit_vendors`, `bulk_import_commit_venues`, `bulk_import_undo` — all SECURITY DEFINER, all admin-re-checked, atomic cross-table commits.
- **Edge function.** `bulk-import` (verify_jwt = true; admin re-check; entity-handler registry; `mode: "preview" | "commit" | "undo"`).
- **UI.** Shared `BulkImportPage` orchestrator + `UploadStep` + `MapStep` + `DedupeStep` + virtualized `ImportGrid` + `ColumnPickerDrawer` + audit page at `/settings/bulk-import/history`. "Bulk Imported" `presence` filter chip on each list page.
- **Cross-cutting:** Stepper component moved from `talent-scout/` to `ui/`; FilterBar gains `presence` field type; lookup cache `invalidateLookup` (so import-created lookups appear without reload); generic `applyFilters` `fieldMatchAll` hook (powers `nationwide` vendors auto-passing every city chip).

Squashed under `81cdaf2`.

## Phase 5.10 — Venue work (about_venue rename + AI generator + layout refresh)

**5.10.0:** `venues.notes` renamed to `venues.about_venue` (OID-preserving) — `notes` collided with the polymorphic `notes_log` table. New `hq-generate-venue-about` edge function is the first `callClaude('hq', ...)` consumer; **tool-less by design** (an evergreen `ABOUT_VENUE_SYSTEM` prompt in `_shared/venueOverview.ts` + `web_search` + `tool_choice: auto`; Claude replies as plain text). Removes the `feedback_tool_choice_collapse` failure class outright (no forced custom tool to collapse). Generate / Regenerate About Paragraph buttons on VenueDetail (card header) + VenueEdit (About Venue section header; disabled while the form is dirty). Regenerate gates behind an AlertDialog. Breaking-rename coordination: migration + function deploy + frontend ship as one Jimmie-approved push.

**5.10.1:** VenueEdit + VenueDetail layout refresh — 2-column label-above Details card with `card-headbar` section headers; standalone Venue Types card folded into Details; "Links & References" → "Master Venue Deck Slide" card; Features became a tag input. First responsive `@media` breakpoints (form pairs collapse below 640px). `.label-form` coral → grey app-wide.

Squashed under `5d57b26`. **VS convergence to the shared overview prompt** (relocate generation to deck-prep, drop the brief, cut considerations, add web_search, push to `venues.about_venue` on Generate Deck) is deferred to Phase 5.12 — full plan in `docs/roadmap.md` § 5.12.

## Phase 5.11 — UX and structural consistency

Design-system audit sequence after the 5.10 venue work. Three sub-phases shipped between 2026-05-22 and 2026-05-23; each is a separate commit on `main`.

**5.11.0 (`a2adbaf`) — UX/design-system audit + implementation pass.** Parallel Claude + Codex audits, then combined implementation. Focus rings (shared `:focus-visible` coral ring on `.btn` family + view switcher / tabs / toggle / checkbox / filter chips / saved-views / rail nav / text links / combobox picker / topbar controls). Mobile shell: rail collapses to off-canvas hamburger drawer below 1024px; `.savebar` / `.actionbar` freed from `left:232px`; primary + secondary tap targets ≥44px on mobile. Tokens: `--info` set to canonical `#06B6D4`, new `--purple` `#B57BF5`, both exposed as Tailwind utilities; 16 hardcoded cyan/purple hex literals replaced. Coral link contrast: in-content links switched from `--primary` to `--primary-hover`, clearing AA on `--surface-alt` at 4.50:1. `hq-` chrome convergence: dead Phase 5.2.1 `hq-` block removed; Home + ClickPillCell migrated to the unprefixed canonical `.card` / `.pill .p-<token>` / `.stat` / `.tlink` / `.tbl` / `.activity-row` / `.actdot` / `.block-lbl`; `.hq-pill-lg` renamed to `.pill-hero`; shell-only `.hq-rail` / `.hq-topbar` / `.hq-iconbtn` / `.hq-content` etc. kept as designed. Edit-page card headers converted to `card-headbar` + `.h-card`. Tailwind `rounded-sm` / `rounded-md` now resolve to `var(--radius)`. `src/index.css` net -382 lines. Closes `code-observations.md` Frontend #13 / #21 / #22 / #23 / #24 / #25 + Docs #5; opens Frontend #26 (`--surface-raised` AA borderline, accept-risk).

**5.11.1 (`38f5374`) — Detail-page polish + list-table standardization + managed tag fields.** Follow-on round of UX-audit smoke fixes building on 5.11.0. Detail pages converged around plain titles, `detail-meta`, DField rows. Project / Venue / Vendor / Client / Person / Task / Deliverable polish. Contacts + relationship-card patterns. Managed Project Tags + Venue Features lookups (`project_tags` + `venue_features` tables, backed by the same managed-lookup pattern as `vendor_capabilities`). De-chipped tag styling. List-table standardization. Global token + chrome adjustments. Opened code-observations Frontend #27 + #28 + #29.

**5.11.2 (`c19b332`) — Structural consistency.** Four waves. **W1 (visible chrome):** TaskDetail + DeliverableDetail eyebrow + plain h1 + `detail-meta` + hero `ClickPillCell` status + short due dates + compact Details rows; TaskDetail editable Blocked By. Project / Task / Deliverable detail back links use `.crumb`. List headers `row between list-head`. Stacked identifiers (Projects / Tasks / People) on `hsl(var(--primary-hover))`, 13.5px leads, 12px sublines. Quiet count footers. **W2 (shared lifts):** extracted `DField`, `HQFormField`, `ContactsCard`, `WebsiteActionButton`, neutral `prettyHost` in `src/lib/url.ts`, and `ListPageChrome`. Venues + Vendors share search + chip-radio chrome; PeopleList stays the documented colored-affiliation variant. Removed the venue-scout `prettyHost` export. **W3 (product-call changes):** VendorEdit Tags removed (Capabilities is the visible vendor tag-like set). TaskEdit + DeliverableEdit moved to 880px centered Details shells. ProjectEdit gained the Project eyebrow + Details card grouping while staying wide. Task `blocked_by` + Deliverable `assignees` use `RecordCombobox multi`. PersonDetail Associated Venues moved to its own standalone relationship card; Affiliation field reads "Venue contact." **W4 (docs):** `docs/design-system.md` documents list / detail / multi-record / edit / Venue Slide / extracted-component canon. `docs/decisions.md` records the 5.11.2 decisions. Resolved code-observations rows #30 to #37 + #39; #38 deferred to a separate primary-contact product / data-model round.

---

## Phase 3 — Talent Scout port (siloed reference)

The 11 sub-phases that ported Talent Scout from `mirror-talent-scout` to HQ Core are summarized in `docs/roadmap.md` § Phase 3. Talent Scout itself was originally shipped as a standalone Lovable app; Phase 3.X lifted it into HQ as an admin-only sub-route. Highlights: 3.4 ran the chunked Gmail pull pipeline (`BATCH_SIZE=8`); 3.5 added CandidateDetail + status dropdown + re-eval; 3.6 added the Final Review packet (pdf-lib + signed-URL email); 3.7 added referral ingestion (forward-chain walker); 3.8 added pg_cron + 6 cron jobs (scheduled pulls + 3 watchdogs + storage cleanup + monthly spend reset); 3.10 added scorecard refinement; 3.11 restored substantive describer fields. Per-sub-phase decisions live in `docs/decisions.md` under the Phase 3 sections. The original sub-phase commits were squashed into Phase 3.2 = `9badf38`, Phase 3.3 = `8331a6a`, Phase 3.4 = `2d8dfc7`, Phase 3.5 = `62147f7`, Phase 3.5b = `ef75f15`, Phase 3.6 = `a01cacb`, Phase 3.7 = `7cf213f`, Phase 3.8-3.10 = `d138dbc`, Phase 3.11 = `4f0810c`.

## Phase 4 — Venue Scout port (cutover + revision)

Full 1:1 port from `mirror-nyc-venue-scout-pro` shipped to production 2026-05-13 (`7cd27ed` was the pre-rebase cutover SHA). Producer flow: New Scout → Brief upload (DOCX + AI parse) → Sheet Prompt → Sheet Upload (XLSX/CSV + AI enrichment) OR direct AI venue research → Sourcing Report → Shortlist (matrix + photo upload) → Review → Compiling (per-venue overview generation) → Deck Prep → Generating (Google Slides deck) → deck opens in new tab. AI surface uses `pause_turn` continuation, `writeFailure` CAS guards on all 3 AI edge functions, 180-360s `WORK_TIMEOUT_MS`, URL-fallback helpers. **Phase 4 Revision — Intake** (post-port) rebuilt the Brief as a 3-step stepper (Event → Venue → Review), added venue-side fields the AI sourcing prompt needs, added `vs-generate-brief-overview`, made the Revisit nav always-visible. Per-sub-phase decisions live in `docs/decisions.md` § "Phase 4 cutover + port plan locked decisions" + § "Phase 4 Revision". Squashed under Phase 4.1 = `262d213`, 4.2 = `4faa968`, 4.3 = `477b583`, 4.4 = `d7f6fe0`, 4.5 = `aaabccd`, 4.6 = `f7eb639`, 4.7 = `f54f3cd`, 4.8 = `1e2c8ce`, 4.9 = `f1c127f`, 4.10 = `3a197b0`, Final Review packet = `ad2cb77`, VS cutover sweep = `0f92c62`, Phase 4 Revision = `d9f642d`.

---

For current sub-phase status see `CHECKPOINT.md`. For the forward plan see `docs/roadmap.md`. Decisions per sub-phase live in `docs/decisions.md`.
