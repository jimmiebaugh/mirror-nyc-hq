# Mirror NYC HQ — local env template. Copy to .env and fill in.
# All vars are VITE_-prefixed so Vite inlines them into the client bundle at
# build time. The publishable key is the public anon key (safe to ship); it is
# NOT the service_role secret. Production values live in the Netlify dashboard.
VITE_SUPABASE_PROJECT_ID=amipjjmphblfxpghjnel
VITE_SUPABASE_URL=https://amipjjmphblfxpghjnel.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
