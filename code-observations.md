# Code observations

Living log of noteworthy unresolved follow-up items Claude or anyone notices while working in the codebase. Intent is passive: log it here, keep moving, triage later during a calm afternoon. The file persists across sessions.

Do not add a new row for an issue fixed in the same task, a resolved hotfix, or a narrative summary of work already handled. Those belong in the relevant repo docs, CHECKPOINT, changelog, or commit message. Existing rows can be marked resolved when later work fixes them.

## Format

Each section below is a table. Columns:

| # | Date | File | Hash | V | R | Severity | Note |

- **#**: monotonically increasing within the section. Append-only. Never reorder.
- **Date**: YYYY-MM-DD when logged.
- **File**: path relative to repo root. Include line number when useful (`src/foo.tsx:42`).
- **Hash**: introducing commit's short hash, backtick-wrapped, from `git blame -L <line>,<line> <file>`. Use `n/a` for non-code observations.
- **V**: `☐` unverified, `☑` verified (someone confirmed the observation is real).
- **R**: `☐` open, `☑` resolved (fix landed, or observation retracted with strikethrough).
- **Severity**: `[low]`, `[med]`, `[high]`. High = probable bug or security smell; med = consistency or clarity issue; low = polish.
- **Note**: one sentence. Add a `(retracted: reason)` suffix if retracting.

## Lifecycle

1. Claude or anyone logs an observation. New rows start V `☐`, R `☐`.
2. A verifier reads the code and either confirms (V `☑`) or retracts (strikethrough the row, append `(retracted: reason)` to the Note).
3. When a fix lands, mark R `☑` and link the commit hash in the Note.

Append-only. Never delete rows. Never reorder.

## What to log

Only things actually encountered during a task. Do not go hunting:

- **Unfinished implementations**: TODOs, stubs, placeholder logic, partially implemented features.
- **Dead code**: unused functions, unreachable branches, commented-out blocks.
- **Possible bugs**: unchecked errors, race conditions, off-by-ones, logic errors.
- **Unusual patterns**: inconsistent conventions, surprising workarounds, anti-patterns.

---

## Frontend

(React + Vite + Tailwind + shadcn/ui. Talent Scout, Venue Scout, HQ Core surfaces.)

| # | Date | File | Hash | V | R | Severity | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-14 | `src/lib/venue-scout/briefForm.ts` (`computeOverviewSourceHash`) | `n/a` | ☑ | ☑ | [low] | Phase 4 Revision pass 3: the overview-staleness hash includes `budget_text` + `expected_guest_count` as raw producer display strings, while the server (`vs-generate-brief-overview`) recomputes them from the canonicalized DB number; if a producer types a non-canonical form (comma in guest count, missing `$` in budget) and resubmits within the same in-memory session before any `fromScout` re-seed, the hashes diverge into one wasted (self-healing) Claude regen. Bounded per spec § 3 risk tolerance; flagged by code-reviewer, deferred. Proper fix: hash `toUpdate(state)` output (the DB representation) instead of raw form strings. (5.8.2 triage: verify-and-defer to Phase 5.10 Venue Scout review. Self-healing one wasted regen, producer never sees corruption; lift only if a producer surfaces the wasted-regen turnaround as a real annoyance.) (RESOLVED `1a01d3e`: re-verified during the Phase 5.12.5 audit. `computeOverviewSourceHash` already uses `parseBudget(state.budget_text)` + `parseGuestCount(state.expected_guest_count)` (Phase 5.1 NIT pickup; canonical numeric form on both sides). Remaining ICU surface was the budget DISPLAY string in vs-generate-brief-overview (one-way, read by Claude, no hash compare); closed paired with Edge #2 by extracting `_shared/formatBudget.ts` pure-JS comma formatter so the display block no longer depends on `Number.toLocaleString` / ICU.) |
| 2 | 2026-05-16 | `src/lib/hq/useClientsAndVendors.ts` | `523e1d6` | ☑ | ☑ | [low] | New composite-lookup hook for the PeopleList Organization filter has `useEffect` dep `[]`, so the clients+vendors picker loads once on mount and never refreshes. Creating a new Client / Vendor in another tab and navigating back to People won't surface it in the filter picker until a full page reload. Acceptable for Phase 5.2; revisit if Saved Views with Organization chips ship + cross-tab staleness becomes a real UX issue. (5.8.2 triage: verify-and-defer. No saved views with Organization chips exist through Phase 5.7.x, so the cross-tab staleness window has no production trigger yet. Pairs with Frontend #3; both unlock together when Organization-chip saved views become a real need.) (RESOLVED `<5.16.1.1>`: `invalidateClientsAndVendors()` added to `useClientsAndVendors.ts` (module-level subscriber registry + reloadKey); called from the Client + Vendor create-success paths so a still-mounted Organization picker refreshes without a reload. Pairs with #3.) |
| 3 | 2026-05-16 | `src/components/data/FilterBar.tsx` (`chipDisplayValue`) | `523e1d6` | ☑ | ☑ | [low] | When a saved view restores a lookup chip before its `lookupOptions` array has loaded, the chip momentarily renders the raw uuid before the hook resolves and re-renders with the name. No saved Organization-chip views exist in 5.2 cleanup (only the default `type=Client` chip is preset), so the race window is currently theoretical. Worth revisiting if Saved Views grow to persist Organization chips. (5.8.2 triage: verify-and-defer. Race window still has no production trigger as of Phase 5.7.x. Pairs with Frontend #2; both unlock together when Organization-chip saved views become a real need.) (RESOLVED `<5.16.1.1>`: pairs with #2; the faster picker hydration closes the chip-uuid race. The residual race stays theoretical until Organization-chip saved views exist.) |
| 4 | 2026-05-16 | ~~`src/pages/calendar/CalendarPage.tsx` (event rendering)~~ | ~~`n/a`~~ | ☑ | ☑ | ~~[low]~~ | (retracted: code-reviewer cold pass flagged the dead CSS; in-commit fix widened `CalendarEventKind` to include `"olk" + "hol"` so the new modifier classes are wired up. Outlook and Holiday banners now render with the distinct gray-tones spec § 12 prescribed.) |
| 5 | 2026-05-16 | `src/pages/calendar/CalendarPage.tsx` (FilterChip popovers) | `n/a` | ☑ | ☑ | [low] | The Lead + Category filter-chip popovers on the Calendar page do not close on outside click; the user has to click the chip again or pick an item to dismiss. Acceptable for 5.3 (the chips are simple 2-3-item pickers), but the `<FilterBar />` pattern has outside-click handling and the Calendar chips don't. Worth promoting to the FilterBar pattern in a future polish pass. (5.8.2 triage: moot. Phase 5.7.9 (`3d5f1cc`) dropped the Lead + Category filter chips from /calendar entirely; the popover surface no longer exists. Only a stale doc comment at CalendarPage.tsx:45 references the removed chips. See memory `project_calendar_lead_category_chips_dropped.md` + the 5.7.9 COWORK_SYNC archive.) |
| 6 | 2026-05-16 | `src/components/data/CalendarMonthView.tsx` | `n/a` | ☑ | ☑ | [low] | Phase 5.3 extended the component with optional controlled-month props (`activeMonth`, `onActiveMonthChange`) + a `hideInternalNav` flag so the new Calendar page can drive nav from its page header. The component now has two render paths (internal nav vs no nav); the existing Deliverables Calendar view still uses the uncontrolled path. Not a bug, just two code paths where there was one before. (5.8.2 triage: verified; accepted as design choice. Both render paths remain in active use: Deliverables Calendar consumes the uncontrolled internal-nav path; the /calendar surface consumes the controlled `hideInternalNav` path with its own page-header nav. No consolidation planned.) (RESOLVED `<5.16.1.1>` as-designed: both render paths remain canonical for their respective consumers.) |
| 7 | 2026-05-16 | `src/pages/projects/ProjectDetail.tsx` (`installCountdownIso`) | `n/a` | ☑ | ☑ | [low] | The "Days Until Install" stat tile now prefers the new `install_dates_start` column with a fallback to `live_dates_start` for unbackfilled rows. This is a behavior change for every existing project: the countdown previously showed days-until-live; for projects with no install dates yet it still does. Document for Jimmie so producers aren't surprised when "days until install" stops matching their old mental model once they start filling in install dates. (5.8.2 triage: verified; accepted as designed. Phase 5.3 schema reshape was intentional; behavior is consistent with the install_dates_start column add. Producers transitioning to install-dates see the correct semantics; pre-backfill projects fall back to live-dates cleanly. Communicated to Jimmie at the time.) (RESOLVED `<5.16.1.1>` as-designed: behavior intentional; producer comms already happened.) |
| 8 | 2026-05-17 | multiple HQ surfaces | `n/a` | ☑ | ☑ | [low] | Phase 5.7.3 followup-6 design call (Jimmie): coral (`hsl(var(--primary))`) is reserved for hyperlinks. Only ProjectDetail's Overview Job # value (`text-primary mono`) was flipped to foreground in this pass; other non-link coral usages flagged for a future sweep — `src/components/home/MyWeekStrip.tsx:145` ("My Week" heading), `src/pages/people/PeopleList.tsx:50` (Client tier swatch color), `src/components/calendar/VisibilityPanel.tsx:93` (panel accent), `src/components/data/SavedViewsDropdown.tsx:248` (dropdown accent), `src/components/wiki/WikiNav.tsx:65` (nav accent), `src/components/ui/PermissionDenied.tsx:42` (denial heading). Each needs a per-site call on whether it's an accent-by-design or a stale coral-text holdover. (5.8.2 triage: per-site sweep closed. 3 of 6 flipped in 5.7.14 (`59a5dac`): VisibilityPanel clientName label flipped to foreground; SavedViewsDropdown "default" / "global default" caption flipped to subtle-foreground; PermissionDenied lock badge recolored to destructive (semantic match, not a flip). 3 of 6 kept by design: MyWeekStrip "My Week" heading (5.7.14 refactored the markup to inline style + bumped font-size and intentionally kept the coral as visual emphasis on Home; live at MyWeekStrip.tsx:164); PeopleList Client tier swatch (tier identity color matching the Affiliation pill scheme); WikiNav "+ New Page" (it IS a `<Link>`, coral correct per `feedback_coral_reserved_for_hyperlinks.md`).) |
| 9 | 2026-05-19 | `src/pages/settings/SettingsPage.tsx:132` | `n/a` | ☑ | ☑ | [low] | Encountered while slotting the Phase 5.9.1 Bulk Import card: the Integrations card header renders `— Coming Soon` with a literal em dash, which violates the project "no em dashes anywhere" rule. Pre-existing (predates 5.9.1); not touched in this commit. Swap to a comma/parens or drop the dash in a future polish pass. (RESOLVED `a998afe`: changed to "(Coming Soon)".) |
| 10 | 2026-05-20 | `src/lib/hq/lookups.ts` (`ensureLoaded`) | `n/a` | ☑ | ☑ | [med] | The shared lookup cache never refetches once populated (`ensureLoaded` returns early when `entry.options !== null`), and there is no exported invalidate/clear. So a lookup row created server-side by the bulk-import RPC (vendor_categories / vendor_subcategories / vendor_capabilities / cities) is absent from the cache for the rest of the session — until a full page reload. Surfaced in Phase 5.9.3.2 follow-up: an imported vendor's VendorDetail showed "No category" even though category_id was set (subcategory happened to fetch fresh because its parent-scoped cache key was new). Mitigated the *display* by falling back to the joined `vendor.category_name` / `vendor.subcategory_name` in VendorDetail's name memos. NOT mitigated: the Category/Subcategory dropdown options still won't list the brand-new lookup row until reload. Proper fix: export an `invalidateLookup(table, parentScopeId?)` and call it from the bulk-import commit success path (and inline-add already handles its own case via addOption). (RESOLVED 5.9.3.2 follow-up 3: added `invalidateLookup` to `lookups.ts` — nulls cached options + refetches live subscribers — and call `invalidateLookup()` on the BulkImportPage commit-success path, so import-created lookups now refresh on the detail/edit surfaces AND their dropdowns without a reload.) |
| 11 | 2026-05-21 | `src/pages/venues/VenueEdit.tsx` + `src/pages/venues/VenueDetail.tsx` | `n/a` | ☑ | ☑ | [low] | Encountered while adding `venues.general_email` (Phase 5.9.7): `venues.contact_name`/`contact_email`/`contact_phone` are write-only from the manual surfaces. The bulk-import RPC writes them (and derives a `venue_contact_people` join contact), but VenueEdit/VenueDetail never read or edit them (venue contacts surface only via the join, in the Contacts sidebar). So an imported venue's `contact_*` text columns are invisible and uneditable in the UI, diverging from the vendor side where the same columns are first-class fields. Not a bug (the join is the canonical contact link), just a write-only-column inconsistency worth a future call on whether to drop them or surface them. (RESOLVED `<5.16.1.1>`: keep write-only (Round 2). The `venue_contact_people` join is the canonical contact link; the columns stay as a documented write-only divergence. See `docs/decisions.md` § Phase 5.16.1.1 D4.) |
| 12 | 2026-05-22 | `src/pages/venues/VenueEdit.tsx` + `src/pages/venues/VenueDetail.tsx` + `src/pages/venues/VenuesList.tsx` | `e8f8cdc` | ☑ | ☑ | [med] | Phase 5.10.1 added a third local copy of `VenueTypePill` (now duplicated across VenueEdit, VenueDetail, and VenuesList) plus a second local copy of `prettyHost` (VenueEdit + VenueDetail); cleanup is to extract `VenueTypePill` to a shared `src/components/venues/` component and `prettyHost` to a shared venues url util so pill styling + host-formatting can't drift across the three venue surfaces. (RESOLVED `a998afe`: `VenueTypePill` extracted to `src/components/venues/VenueTypePill.tsx` (with `small?` prop); `prettyHost` extracted to `src/lib/venue-scout/venueTypes.ts`. See new row #20 for the remaining client/vendor `prettyHost` copies.) |
| 13 | 2026-05-22 | `src/pages/venues/VenueDetail.tsx` (page grid) | `n/a` | ☑ | ☑ | [low] | VenueDetail's outer page layout is a fixed inline `grid-template-columns: 1fr 332px` (main + right sidebar) with no breakpoint, so on a narrow window the main column is squeezed and the sidebar never drops below it; Phase 5.10.1 added `@media (max-width:640px)` collapse for the in-card `.g2`/`.venue-addr-row*` grids but left this outer split untouched. Cleanup: convert the inline grid to a class that stacks the sidebar under the main column around ~900px (part of the deferred Phase 5.13 mobile sweep). (RESOLVED `f622d50`: replaced the inline grid with a shared `.detail-2col` class on Venue/Person/Client/Vendor detail pages and `.detail-2col--wide` on Project Detail, stacking below 900px.) |
| 14 | 2026-05-22 | `src/hooks/use-mobile.tsx` | `c50d4b1` | ☑ | ☑ | [low] | Codebase-audit pass: this shadcn default hook is never imported anywhere. Jimmie's call (2026-05-22) is to KEEP it for likely future responsive work rather than delete now. Revisit in a future triage: either it gets a first consumer (the deferred Phase 5.13 mobile sweep is the natural one) or it gets deleted as dead. (RESOLVED `<5.16.1.1>`: deleted `src/hooks/use-mobile.tsx`; zero imports repo-wide.) |
| 15 | 2026-05-22 | `src/lib/talent-scout/poolStatus.ts` | `e6e7f38` | ☑ | ☑ | [low] | Codebase-audit pass: exported pool-status helper(s) never imported anywhere (whole file is dead). Held back from deletion pending a future triage call on whether this is staged for an upcoming Talent Scout surface or genuinely abandoned. If the latter, delete the file. (RESOLVED `a998afe`: confirmed genuinely abandoned (zero imports repo-wide for every export); file deleted.) |
| 16 | 2026-05-22 | `src/components/icons/HQIcons.tsx:305` (`IconStar`) | `59a5dac` | ☑ | ☑ | [low] | Codebase-audit pass: `IconStar` is exported but never imported. Left in place as icon-set completeness (HQIcons is a hand-curated palette, treated like the shadcn re-exports). Remove only if a future pass decides the icon set should be usage-pruned rather than kept whole. (RESOLVED `<5.16.1.1>`: kept for palette completeness; annotated at the `IconStar` export + `docs/decisions.md` § Phase 5.16.1.1 D12.) |
| 17 | 2026-05-22 | ~~`src/components/venue-scout/matrix/primitives.tsx`~~ | `1a38c92` | ☑ | ☑ | ~~[low]~~ | ~~Codebase-audit pass: ~12 exports are unused by current callers~~. (RESOLVED `1a01d3e`: matrix `primitives.tsx` swept. DELETED 11 retired exports (`VenueIdentityStack`, `WebsiteArrow`, `HdrStack`, `RankDisplay`, `rankBucket`, `RANK_TEXT`, `RANK_BAR`, `RankTier`, `EditableVenueName`, `NotesCellButton`, `EditableTextarea`) + the `StackDivider` internal helper; dropped `Pill` from exports (kept as internal helper for `TypeTogglePopover`); dropped 4 re-exports (`TYPE_STYLES`, `TYPE_FALLBACK_STYLE`, `parseTypes`, `CanonicalType` type) — consumers (`SourcingReport`, `Shortlist`, `ReviewCard`) now import directly from `@/lib/venue-scout/venueTypes`. `DedupeMetaIndicator` + `normalizeDedupeMeta` + `DedupeMetaShape` survive as documented-dead future-state scaffolding (potential Review or HQ Venues use; revisit in a dedicated cleanup pass). File slims ~830 → ~500 lines.) |
| 18 | 2026-05-22 | `src/components/talent-scout/CandidateSearch.tsx` | `n/a` | ☑ | ☑ | [low] | Codebase-audit pass: after removing the dead `CandidateSearch` component, this file now exports only `matchesSearch` (a pure search-text predicate) and the `Searchable` type, but keeps a `.tsx` extension and a component-implying filename. Future polish: rename to `candidateSearch.ts` and/or relocate to `src/lib/talent-scout/` so it reads as the search util it now is. (RESOLVED `a998afe`: `git mv` to `src/lib/talent-scout/candidateSearch.ts`; RoleDashboard + PullDetail imports + the stale CandidateTable comment updated.) |
| 19 | 2026-05-22 | multiple page components | `n/a` | ☑ | ☐ | [low] | Codebase-audit pass: several god-files over ~900 lines doing many unrelated things, ripe to split for legibility (not a bug): `ProjectDetail.tsx` (1413), ~~`BriefReport.tsx` (1049)~~, `RoleSettings.tsx` (946), `ProjectEdit.tsx` (927), ~~`DeckPrep.tsx` (905)~~. `src/integrations/supabase/types.ts` (2843) is generated and excluded. No action taken; flagged for incremental extraction during normal feature work on each surface. (DeckPrep.tsx slice RESOLVED `1a01d3e`: file renamed to `src/pages/venue-scout/Review.tsx` in the Final Review + Deck Prep consolidation; the per-card body extracted to the new `src/components/venue-scout/ReviewCard.tsx` sibling per spec § 8.J; parent now ~600-700 lines.) (BriefReport.tsx slice RESOLVED `1a01d3e`: lines 339-400 extracted to new `src/components/venue-scout/GeneratedDecksCard.tsx`; the generic `ReportCard<T>` primitive (used 9 times on BriefReport) extracted byte-for-byte to new `src/components/venue-scout/BriefReportCard.tsx`. Parent slims accordingly.) Other god-file slices remain open. Scheduling: ProjectDetail.tsx + RoleSettings.tsx + ProjectEdit.tsx remaining slices target 5.12.16 per Jimmie's call 2026-05-25; opportunistic extraction on whatever HQ-Core surface they touch next. |
| 20 | 2026-05-22 | `src/pages/clients/ClientDetail.tsx:511` + `src/pages/vendors/VendorDetail.tsx:852` | `6214002` | ☑ | ☐ | [low] | Frontend #12 follow-up (found by code-reviewer during the triage cleanup): after extracting the shared `prettyHost` to `src/lib/venue-scout/venueTypes.ts`, ClientDetail + VendorDetail still carry byte-identical local `prettyHost` copies. #12 scoped only the three venue surfaces, so these were left untouched. Next dedupe points both at the shared util; resolve the module-home question first — the util currently lives under `venue-scout/` but client/vendor are non-venue surfaces, so either accept the cross-module import or relocate `prettyHost` to a neutral `src/lib/url.ts`. |
| 21 | 2026-05-22 | `src/index.css` (5.1 `hq-` block vs 5.2 unprefixed block) | `n/a` | ☑ | ☑ | [med] | UX audit (F1.6/F14.1): two parallel chrome class families render near-identical components — `.hq-card`/`.hq-pill`/`.hq-tbl`/`.hq-dt` (Phase 5.1) and `.card`/`.pill`/`.tbl` (Phase 5.2). `design-system.md §11` warns against parallel layers; this is two. They already differ subtly (pill dot markup, table header treatment), so a new surface can pick the wrong one and drift. Triage: converge on the unprefixed 5.2 family (+ `.hq-dt` for the status-left-border variant) and retire the `hq-` duplicates after auditing consumers (Home dashboard + Outlook are the live `hq-` consumers). Larger refactor; not bundled with the Pass-3 safe fixes. (RESOLVED `ada555f`/`a9da5a5`/`b1eb7b9`: removed the dead Phase 5.2.1 hq- block (viewswitch/filterbar/dt/board/timeline/calendar), migrated Home + ClickPillCell consumers of .hq-card/.hq-pill/.hq-stat/.hq-tlink/.hq-tbl/.hq-activity-*/.hq-block-lbl to the unprefixed canonical classes, renamed .hq-pill-lg → .pill-hero, and dropped the now-unused .hq-* CSS rules + obsolete safelist entries. Shell-only .hq-rail/.hq-topbar/.hq-iconbtn/.hq-content/.hq-quickadd/.hq-weekstrip/.hq-ol-* kept as designed.) |
| 22 | 2026-05-22 | `.tlink` / `.combo-link` + record links on `.card` surfaces | `n/a` | ☑ | ☑ | [med] | UX audit (F4.1): coral `#BE4E44` small text (~12px links: client/website/record links) on `--surface-alt` `#141414` card backgrounds computes ~3.7:1 contrast, under WCAG AA 4.5:1 for normal text (`mirror-style-guide.md §6` notes coral is already only 4.6:1 on pure black). These are the most-clicked links in HQ. Triage with a real contrast tool: persistent underline (currently hover-only), weight bump, or a lighter on-card coral. Needs a brand-aesthetic call before changing the app-wide link treatment. (RESOLVED `5a0c129`: kept brand semantics but switched small in-content/card links to existing `--primary-hover`; verified computed contrast ~4.50:1 on `--surface-alt`.) |
| 23 | 2026-05-22 | `src/index.css` interactive controls (`.btn-tertiary`, `.btn-sm`, `.hq-qa`, `.viewswitch button`, `.fchip`, `.btn-quickadd`, `.hq-iconbtn`) | `n/a` | ☑ | ☑ | [low] | UX audit (F7.1): multiple interactive targets fall below the 44×44 mobile minimum (quick-add 24px, filter chips 30px, view-switch 34px, tertiary/sm buttons 36/32px, icon buttons ~28px). Only meaningfully fixable alongside the deferred Phase 5.13 mobile sweep (see #13/#14); enforcing min hit-area at mobile breakpoints there. Naive desktop min-height changes would distort the intended density. (RESOLVED `f622d50`: enforced 44px min-height on `.btn-tertiary`/`.btn-sm`/`.hq-qa`, 44px height on `.viewswitch button`, 40px on `.fchip`/`.savedviews`, and 40px min on `.hq-iconbtn` under `@media (max-width:1023px)` — mobile-scoped so desktop density is untouched.) |
| 24 | 2026-05-23 | `src/index.css` remaining small controls (`.checkbox`, `.toggle`, `.ca`, `.wiki-tbtn`) | `n/a` | ☑ | ☑ | [low] | Codex UX audit follow-up to #23: the primary mobile control set was fixed, but several secondary controls still rendered below the 44x44 target on mobile. They show up in notification settings, team lists, credential reveal fields, and the wiki editor toolbar. Safer fix: add mobile-only hit-area padding or wrapper targets so desktop density stays intact. (RESOLVED `ca00601` + `e9c8dc0`: mobile-only hit areas added for `.checkbox`, `.toggle`, `.ca`, `.wiki-tbtn`, and the topbar avatar locked to 44x44 so flex shrink cannot undercut the target.) |
| 25 | 2026-05-23 | `tailwind.config.ts` radius tokens + `src/components/ui/*` primitives | `n/a` | ☑ | ☑ | [med] | Codex UX audit (COD-F2.2): shadcn primitives still read sharper than HQ chrome because the Tailwind radius mapping yields `rounded-sm` near 0px and `rounded-md` near 2px while HQ primitives use the canonical `--radius`. This is most visible on TS/VS buttons, inputs, and cards beside canonical HQ surfaces. Future fix should align primitive variants with HQ radius tokens, then visually sweep dialogs, selects, and popovers. (RESOLVED `7e607ff`: `rounded-sm` and `rounded-md` now resolve to `var(--radius)`, matching the HQ 4px primitive radius.) |
| 26 | 2026-05-22 | `src/index.css` (`.tlink` / `.combo-link` / `.notif .nbody .src` / `.wikipage.prose a` etc. on `--surface-raised`) | `n/a` | ☑ | ☑ | [low] | Borderline-context note. Frontend #22 resolved the in-card link contrast by pointing in-content links at `--primary-hover` (`#CA5B53`), which clears WCAG AA on `--surface-alt` (`#141414`) at 4.50:1 but renders sub-AA on `--surface-raised` (`#1F1F1F`) at 4.03:1. Most coral links sit on `.card` (surface-alt); the borderline case is coral links inside popovers / dropdowns / combobox menus where the body is surface-raised. Accept-risk for now; if a future popover surface puts coral links on raised, bump the lightness another notch or scope a separate `--primary-link-raised` token. (RESOLVED `<5.16.1.1>` accept-risk: no current production consumer puts coral links on `--surface-raised`; the in-card `--primary-hover` fix from #22 is canonical. Bump lightness or add `--primary-link-raised` only if a popover surface ever needs it.) |
| 27 | 2026-05-23 | `src/pages/clients/ClientDetail.tsx` (Details card) | `23534b8` | ☑ | ☑ | [low] | Phase 5.11.1 layout: per Jimmie's restructure spec, the Details card now binds `contact_phone` to two visible labels (Website/Phone pair on row 2, and the Primary Contact / Email / Phone 3-col row). Both controls write to the same column, so editing one immediately mirrors into the other (no data drift, just visual redundancy). Acceptable for v1 — revisit during a Client/Vendor consistency pass to either collapse to a single Phone field or split `contact_phone` from a separate `primary_phone` if the auto-fill-from-primary semantic gets formalized. (RESOLVED `<5.16.1.1>`: removed the duplicate Phone control from the Primary Contact 3-col row (now 2-col: Primary Contact + Email); the Website/Phone pair on row 2 is the canonical Phone.) |
| 28 | 2026-05-23 | `src/pages/people/PersonDetail.tsx` (title-row type pill) | `8c5df96` | ☑ | ☐ | [low] | The type pill in the Person Detail title row navigates to the Edit page via `navigate(`/people/${person.id}/edit`)`. Per plan decision 25 the type itself is derived from FK presence, so direct in-place edit would need to flip client_id/vendor_id/is_venue_contact atomically — Edit form is the safe escape hatch for now. A future inline pill-popover that lets producers reassign affiliation type without leaving the detail page would shorten the loop, but it needs UX design for the "type changed → which FK to clear / set" disambiguation. (verify-and-defer per 5.16.1.1: keep the Edit-nav escape hatch; inline reassignment needs the FK-disambiguation UX, which does not fit a triage phase. See `docs/decisions.md` § Phase 5.16.1.1 D5.) |
| 29 | 2026-05-23 | `src/pages/clients/ClientsList.tsx` (Active Projects column) | `n/a` | ☑ | ☑ | [low] | Phase 5.11.3 renamed the column "Projects" → "Active Projects" to match `loadClients()`'s existing `NON_ACTIVE_PROJECT_STATUSES` filter (Complete / Cancelled / On Hold drop out). The functionality already matches the label, but the relationship is implicit. Future phase: surface the active-project lifecycle definition in `docs/decisions.md` and/or expose a column hover tooltip noting the filter rule; consider an "Inactive Projects" link/drill-down on the Total Projects cell so producers can dig into what's hidden from the active count. (RESOLVED `<5.16.1.1>`: added an Active-Projects column-header tooltip (new DataTable `headerTitle` prop) noting the `NON_ACTIVE_PROJECT_STATUSES` rule; lifecycle defined in `docs/decisions.md` § Phase 5.16.1.1 D6. Tooltip lives on ClientsList where the column actually is; the spec's "ProjectsList" was a typo. Inactive-Projects drilldown not built; out of triage scope.) |
| 30 | 2026-05-23 | `src/pages/{projects,venues,vendors,clients,people,tasks,deliverables}/*Detail.tsx` (DField local helpers) | `n/a` | ☑ | ☑ | [med] | Phase 5.11.2 audit (Pass 1 D7): the same 8-line `DField` label-above-field wrapper is defined locally in all seven HQ Core detail pages — ProjectDetail:1435–1442, VenueDetail:879–886, VendorDetail:844–851, ClientDetail:539–545, PersonDetail:519–526, TaskDetail:327–334, DeliverableDetail:312–319. Byte-equivalent today; any label-style change has to land seven times. Lift to `src/components/hq/DField.tsx`. See `OUTPUTS/phase-5-11-2-audit-pass-2-plan.md` item #1. (RESOLVED `<5.11.2>`: extracted `src/components/hq/DField.tsx` and replaced all seven local helpers.) |
| 31 | 2026-05-23 | `src/pages/{venues,vendors,clients}/*Detail.tsx` (Contacts card local copies) | `n/a` | ☑ | ☑ | [med] | Phase 5.11.2 audit (Pass 1 D9): same Contacts-card pattern duplicated across VenueDetail:718–754, VendorDetail:710–746, ClientDetail:462–498 (`.card` + "Contacts" h-card + `.stack-3` of `.row-c` link rows with avatar initials + name 15px + email 13px `--primary-hover`). Lift to `src/components/hq/ContactsCard.tsx`. See `OUTPUTS/phase-5-11-2-audit-pass-2-plan.md` item #2. (RESOLVED `<5.11.2>`: extracted `src/components/hq/ContactsCard.tsx` and kept all three detail pages on their local contacts state.) |
| 32 | 2026-05-23 | `src/pages/{projects,venues,vendors,clients,people,tasks,deliverables}/*Edit.tsx` (FormField/Field local helpers) | `n/a` | ☑ | ☑ | [med] | Phase 5.11.2 audit (Pass 1 E3): byte-equivalent `FormField` / `Field` helper defined locally in all seven HQ Core edit pages (ProjectEdit:909–927, VenueEdit:836–854, VendorEdit:608–626, ClientEdit:377–395, PersonEdit:535–553, TaskEdit:428–446, DeliverableEdit:407–425). Two compact pages (Task / Deliverable) call it `Field`; the other five call it `FormField` — naming inconsistency on top of the duplication. The shared `src/components/ui/Field.tsx` uses coral labels (TS/VS convention) and is therefore not a drop-in replacement under the Phase 5.10.1 grey-HQ-Core-label rule. Lift to `src/components/hq/HQFormField.tsx` and document the two-component split. See `OUTPUTS/phase-5-11-2-audit-pass-2-plan.md` item #3. (RESOLVED `<5.11.2>`: extracted `src/components/hq/HQFormField.tsx`; `src/components/ui/Field.tsx` remains the TS/VS coral-label wrapper.) |
| 33 | 2026-05-23 | `src/pages/{projects,people}/*List.tsx` (sub-line raw RGBA literals) | `n/a` | ☑ | ☑ | [med] | Phase 5.11.2 audit (Pass 1 L2): ProjectsList (lines 336, 349) and PeopleList (lines 340, 352, 364, 373) render the stacked-identifier sub link with `style={{ color: "rgba(190,78,68,0.85)", fontSize: 12 }}` while the Phase 5.11.3 list-table canon block in `docs/design-system.md` explicitly requires `style={{ color: "hsl(var(--primary-hover))", fontSize: 12 }}`. The reference page (ProjectsList) disagrees with its own canon block; TasksList already uses the semantic token. Side-by-side delta visible (raw RGBA ≈ `#A1453B` vs `--primary-hover` `#CC5C52`). See `OUTPUTS/phase-5-11-2-audit-pass-2-plan.md` item #6. (RESOLVED `<5.11.2>`: Projects, Tasks, and People stacked identifier cells now use `hsl(var(--primary-hover))`, 13.5px leads, and 12px sublines.) |
| 34 | 2026-05-23 | `src/pages/{vendors,clients,people}/*Detail.tsx` (tags loaded but not rendered) | `n/a` | ☑ | ☑ | [med] | Phase 5.11.2 audit (Pass 1 D13): VendorDetail (vendor.tags fetched lines 46, 93), ClientDetail (tags in type at line 35), PersonDetail (tags loaded at line 48) all load the tag arrays but never render them — producers can't see tags from the detail page, even though ProjectDetail (lines 1032–1043) shows tags as `.field-chips` chips. Either render them on the missing surfaces using the de-chipped `.tag` rule (src/index.css:988–990) or drop the columns. See `OUTPUTS/phase-5-11-2-audit-pass-2-plan.md` item #10. (RESOLVED `<5.11.2>`: documented as an intentional exception. Capabilities remains the vendor tag-like surface; Client, Person, and Vendor detail tags stay hidden.) |
| 35 | 2026-05-23 | `src/pages/{tasks,deliverables}/*Detail.tsx` (missing `.detail-meta` + hero pill) | `n/a` | ☑ | ☑ | [med] | Phase 5.11.2 audit (Pass 1 D3 + D4): TaskDetail (lines 204–218) and DeliverableDetail (lines 212–226) skip the `.detail-meta` caption row and the inline hero status pill near h1 that the other five HQ Core detail pages render. As a result the compact-record detail pages feel meaningfully different from Project / Venue / Vendor / Client / Person. Add caption (project · assignee · due / project · client · due) and move the existing Details-card ClickPillCell into the header for parity. See `OUTPUTS/phase-5-11-2-audit-pass-2-plan.md` item #8. (RESOLVED `<5.11.2>`: TaskDetail and DeliverableDetail now have eyebrow, plain title, detail-meta, hero status pill, short due dates, and no duplicate status field.) |
| 36 | 2026-05-23 | `src/pages/venues/VenuesList.tsx:380` + Vendor/Client list | `56b0484` | ☑ | ☑ | [low] | Phase 5.11.2 Codex audit: VenuesList, VendorsList, and ClientsList each implement the same centered `btn btn-coral btn-sm` Website action button with identical disabled styling. Lift to a shared WebsiteActionButton so URL/no-URL behavior, rel/target handling, and stopPropagation stay aligned. (RESOLVED `<5.11.2>`: extracted `src/components/hq/WebsiteActionButton.tsx` and replaced all three list-page copies.) |
| 37 | 2026-05-23 | `src/pages/venues/VenuesList.tsx:421` + `src/pages/vendors/VendorsList.tsx:424` | `9d2bdc4` | ☑ | ☑ | [low] | Phase 5.11.2 Codex audit: VenuesList and VendorsList duplicate page-local `SearchInput` and `ChipRadioGroup` helpers, while PeopleList carries a third tuned chip-radio variant. This list-page chrome has become a shared pattern; lift the reusable pieces or document the People variant. (RESOLVED `<5.11.2>`: extracted `src/components/data/ListPageChrome.tsx` for Venues and Vendors; PeopleList remains documented as the colored-affiliation variant.) |
| 38 | 2026-05-23 | `src/pages/clients/ClientEdit.tsx:273` + `src/pages/vendors/VendorEdit.tsx:480` | `56b0484` | ☑ | ☐ | [med] | Phase 5.11.2 Codex audit: ClientEdit and VendorEdit edit primary contacts as raw text fields, while ClientDetail and VendorDetail use People-backed RecordCombobox controls. Decide whether edit pages should adopt the relationship model or document the text-field edit route as intentional. (DEFERRED `<5.11.2>`: held for a separate product and data-model round.) |
| 39 | 2026-05-23 | `src/pages/people/PersonDetail.tsx:447` + `src/pages/projects/ProjectDetail.tsx` Vendors card | `b62af98` | ☑ | ☑ | [med] | Phase 5.11.2 Codex audit: standalone multi-record sections are not using one shell. VenueDetail Exclusive Vendors uses `combo-as-link` plus bullet-separated coral links; PersonDetail Associated Venues embeds the same interaction inside Details, and ProjectDetail Vendors uses a separate sidebar list/add flow. Standardize or document the section-vs-field rule. (RESOLVED `<5.11.2>`: PersonDetail Associated Venues now uses the standalone card shell; ProjectDetail Vendors sidebar is documented as the add-Popover exception.) |
| 40 | 2026-05-24 | `src/pages/venue-scout/BriefVenue.tsx` | `1a01d3e` | ☑ | ☐ | [low] | Phase 5.12.5 carry-forward: the § 5 backfill migration resets `brief_data.overview_source_hash` to JSON null, so the first BriefVenue submit post-deploy triggers `vs-generate-brief-overview` once even when the producer hasn't edited anything. Acceptable today (one test scout in production carrying a throwaway overview). When real producer load materializes (post-Phase 5.16 hardening pass), harden via a cache-aware hash hydration helper colocated with `briefIntakeStore` so BriefEvent / BriefVenue / BriefReport all see the freshly-recomputed hash without a wasted regenerate on a shape-migration boundary. (verify-and-defer per 5.16.1.1: one wasted regen on the shape-migration boundary is self-healing; lift only if producer load surfaces it as a real annoyance.) |
| 41 | 2026-05-24 | `src/pages/venue-scout/ScoutIndex.tsx` (`openDelete`) lines 208-227 | `4faa9689` | ☑ | ☑ | [med] | Phase 5.12.6 round-3 codex finding (pre-existing; not a 5.12.6 regression). `openDelete` fetches candidate ids with a single unpaged `.select("id").eq("scout_id", ...)` and counts photos with one unchunked `.in("candidate_venue_id", candidateIds)`. PostgREST's default `db-max-rows` cap (Supabase default 1000) silently truncates the candidate list; the photo `.in()` URL also blows past the conservative 8KB ceiling once `candidateIds.length` runs into the high hundreds. Net effect: the AlertDialog can show producers an UNDERSTATED count before they approve an irreversible delete. The actual delete (Phase 5.12.6 `vs-delete-scout` edge function) DOES paginate correctly, so no data is lost — just preview accuracy degrades on extreme scouts (>1000 candidates). For Mirror's typical scout shape (<50 candidates) the modal renders accurate counts and the bug is theoretical. Surfaced during the Phase 5.12.6 ship cycle; defer to a Phase 5.12.14 VS UX pass or a standalone hardening sub-phase. Fix would mirror `vs-delete-scout`'s pagination loop (PAGE_SIZE 1000 + POSTGREST_IN_CHUNK 200 on the photo count) or move counts into a `vs-delete-scout?dry_run=true` mode that returns the same response shape without the destructive ops. (RESOLVED `<5.16.1.1>`: `openDelete` now pages the candidate-id fetch (PAGE_SIZE 1000 `.order`/`.range` loop) and chunks the photo count (`.in()` in POSTGREST_IN_CHUNK 200 slices), mirroring `vs-delete-scout`; the delete-preview count is accurate on >1000-candidate scouts.) |
| 42 | 2026-05-24 | ~~`src/pages/venue-scout/SourcingReport.tsx` + `src/pages/venue-scout/Shortlist.tsx`~~ | `1a01d3e` | ☑ | ☑ | ~~[low]~~ | ~~Phase 5.12.13.1 amendment A landed `<TagInput>` pills in place of the prior `<EditableTextarea>`~~. (RESOLVED `1a01d3e`: matrix Features column reshape lands with the "Venue tables full audit". `TagInput` gains a `compact` prop (no container chrome, centered chips, reveal-on-click coral `+` button); SourcingReport + Shortlist consume `<TagInput compact />` through the shared `VenueMatrixRow` primitive. BriefVenue + BriefReport keep the default bounded-box rendering.) |
| 43 | 2026-05-25 | `src/components/ui/DateRangePicker.tsx` (Phase 5.12.14 NEW) | `1a01d3e` | ☑ | ☐ | [low] | Phase 5.12.14 added a new range-mode date picker on top of shadcn `Calendar` + `react-day-picker` + `date-fns`. Consumed by Venue Scout brief intake (BriefEvent's 3 date `<Field>` blocks) + BriefReport (live_dates ReportCard + install/strike ReportCard). HQ surfaces still use native `<input type="date">` for single-date entry (TaskEdit `due_date`, ProjectEdit live/install/strike date pairs, `src/components/data/bulk-import/cells/DateCell.tsx`, `MirrorHolidaysEditor.tsx`). Future-lift opportunity: align HQ surfaces on the new `DateRangePicker` for cross-cutting date-range consistency. Tracking surfaces: TaskEdit (single date — would use `single` mode if added), ProjectEdit (live_dates_start + live_dates_end + install_dates_start + install_dates_end + strike_dates_start + strike_dates_end — natural range candidates), DateCell, MirrorHolidaysEditor. No deadline; revisit when next HQ surface lands a date-range field that benefits from the picker. Scheduling: target slot 5.12.16 per Jimmie's call 2026-05-25 (VS-audit-cycle ships 5.12.14.2 + 5.12.14.3 stay VS-finalization-only; HQ lift is a cross-cutting follow-on outside the audit workstream). (verify-and-defer per 5.16.1.1: DateRangePicker is `value:string` display-only + range-only, incompatible with HQ's ISO `date` columns, and §4 forbids schema changes; the faithful lift needs a dual-contract picker extension with a `single` mode. DEFERRED to its own sub-phase (Jimmie 2026-05-28). See `docs/decisions.md` § Phase 5.16.1.1 D7.) |
| 44 | 2026-05-25 | `src/hooks/useReferrerCrumb.ts` (audit pass 2 NEW) + HQ Core back-links across `src/pages/projects/ProjectDetail.tsx`, `src/pages/venues/VenueDetail.tsx`, `src/pages/people/PersonDetail.tsx`, `src/pages/vendors/VendorDetail.tsx`, `src/pages/clients/ClientDetail.tsx`, `src/pages/talent-scout/*` | `1a01d3e` | ☑ | ☑ | [low] | Stage 2B item 6 landed the crumb-from-referrer pattern on every Venue Scout page (the inline `<Link className="crumb">← Back to X</Link>` calls swapped to `<ReferrerCrumb />` consuming `useReferrerCrumb`). HQ Core surfaces (Project / Venue / Person / Vendor / Client detail pages + Talent Scout role flow) still ship hardcoded back-link `to` props with a static literal. Producers navigating Person → Project → back end up at "← People" instead of "← Project", which loses the trail. Same hook can lift directly: extend `resolveRoute` for HQ Core route shapes (canonical parents per detail page) and replace the inline crumbs with `<ReferrerCrumb />`. Hook is per-tab via sessionStorage with current-path loop protection, so the pattern is safe to copy as-is. Scope is ~6-8 detail pages + 4-5 Talent Scout pages; defer to a dedicated HQ Core sub-phase. **RESOLVED in Phase 5.12.14.1 Stage 2C (`1a01d3e`):** Path A lift landed — `useReferrerCrumb` generalized to be app-tier-agnostic with HQ Core route table added; `useBackHref` folded into the hook (state.from is priority-1 inside the hook); optional `fallback` prop for caller-specific overrides; `ReferrerCrumb` relocated `src/components/venue-scout/ReferrerCrumb.tsx` → `src/components/ReferrerCrumb.tsx` with canonical IconArrowLeft chrome. 11 HQ Core detail-page swaps (ProjectDetail/ClientDetail/VendorDetail/TaskDetail/VenueDetail/PersonDetail/DeliverableDetail + BulkImportPage/BulkImportHistoryPage/BulkImportEntityPage×2/NotificationPreferences) + 7 list-page `backState` import swaps + 1 OverflowList swap + 9 VS page import swaps land cleanly. Talent Scout pages not touched in 2C — no inline crumb back-links found in `src/pages/talent-scout/*` during the scan. |
| 45 | 2026-05-26 | `src/components/ui/DateRangePicker.tsx` (Phase 5.12.14 NEW) | `1a01d3e` | ☑ | ☐ | [low] | Multi-date (non-consecutive) DateRangePicker mode — carry-forward from 5.12.14 v1 spec § 8.9; add `mode="multiple"` toggle if smoke surfaces real producer need. Scheduling: target slot 5.12.16 per Jimmie's call 2026-05-25. (verify-and-defer per 5.16.1.1: no producer trigger; defer. Pairs with #43's deferred picker work.) |
| 46 | 2026-05-26 | ~~`src/components/venue-scout/matrix/primitives.tsx` + `src/components/venue-scout/NotesModal.tsx` + 5 files w/ `ScoutStepThroughNav` comment residue~~ | `1a01d3e` | ☑ | ☑ | ~~[low]~~ | ~~Carry-forward dead exports + files from the 5.12.14 v1 sweep: `DedupeMetaIndicator` + `DedupeMetaShape` + `normalizeDedupeMeta` survived row #17 as "documented-dead future-state scaffolding" pending a dedicated cleanup pass; `NotesModal.tsx` survived 5.12.14 v1 in case a consumer surfaced; `ScoutStepThroughNav` comment residue persisted across `ScoutChrome` / `ScoutPhaseBreadcrumb` / `ErrorState` / `Shortlist` / `SourcingReport` after the component itself was deleted~~. (RESOLVED `1a01d3e`: dedicated cleanup pass. All three DedupeMeta exports + the now-orphaned `Info` lucide import pruned from `primitives.tsx` (~55 LOC); the `vs_candidate_venues.dedupe_meta` jsonb column + the `_shared/venueDedupe.ts` write path stay byte-unchanged for a future re-consumer. `NotesModal.tsx` deleted entirely (97 LOC); zero JSX consumers post-5.12.14.1 + Review renders notes inline post-5.12.15. `ScoutStepThroughNav` comment residue swept across the 5 files. Supersedes row #17's "survive as documented-dead future-state scaffolding" note about DedupeMetaIndicator. Spec: `OUTPUTS/historical/phase-5-12-14-2-spec.md`.) |
| 47 | 2026-05-27 | `src/pages/settings/SettingsPage.tsx` + `src/pages/wiki/AccountLoginsPage.tsx` + `src/pages/projects/ProjectDetail.tsx` (lines 1057 + 1111) + `src/pages/team/TeamList.tsx` + `src/pages/notifications/NotificationPreferences.tsx` + `src/pages/talent-scout/FinalReviewDetail.tsx` (line 323) | `n/a` | ☑ | ☑ | [cleanup] | R7 § A `.tbl` canon flip queued HQ-wide. VS Sourcing + Shortlist matrix tables migrated to `.tbl .tbl--matrix` (modifier captures matrix-specific deltas: 1280px min-width, table-layout fixed, row-hover suppression, cell-divider pseudo disabled). Other HQ tbl consumers (HQ Core Settings, Wiki Account Logins, Project Detail × 2 inline tables, TeamList, NotificationPreferences) already use `.tbl` directly so the canon stays; flag is that the FinalReviewDetail TS table uses `w-full` without the `.tbl` class at all + the global tbl rule deserves an audit pass for cross-cutting visual drift between consumers. Apply the VS-canon visual contract HQ-wide in a follow-on sub-phase. **R7 amendment v1 update:** matrix tables now use `.tbl--matrix` standalone (detached from `.tbl` per producer call — `.tbl` base specificity was burying matrix Tailwind utilities for header alignment, td padding, row hover, and the `::after` cell-divider pseudo bar was painting over content). HQ-Core `.tbl` consumers untouched this round. **R7 amendment v2 plan (queued):** when the HQ-wide canon-flip sub-phase ships, rewrite `.tbl` rule to match matrix primitives (currently in `src/components/venue-scout/matrix/primitives.tsx` Th/Td), then roll out HQ-wide. Update VS matrix tables to recouple with `.tbl` if needed; if matrix-specific deltas remain (sticky col, min-width 1280, table-fixed), keep `.tbl--matrix` as an additional modifier. (RESOLVED `<5.16.1.1>` per R7 amendment v2: global `.tbl` rewritten to match matrix Th/Td: thead center + `--surface` bg (`.l` left opt-out), cell-divider `::after` now opt-in via `.tbl--with-dividers`, Td padding already matched, row-hover stays HQ default. `.tbl--matrix` kept STANDALONE (recoupling would re-bury matrix utilities). Bare-`.tbl` consumers (AccountLogins / ProjectDetail×2 / TeamList / NotificationPreferences) flip to canon; SettingsPage has no bare `.tbl`; FinalReviewDetail already uses `.tbl` (the "w-full without .tbl" premise was stale). See `docs/decisions.md` § Phase 5.16.1.1 D8.) |
| 48 | 2026-05-27 | `src/pages/venue-scout/Review.tsx` | `n/a` | ☑ | ☑ | [low] | R7 § A spec listed Review as a `.tbl .tbl--matrix` consumer but Review.tsx is a `<ReviewCard>` list (not a `<table>`), so the canon classes don't apply. Flagged to confirm whether Review should grow a table consumer (no current need; the card list is the right surface for editable per-venue chrome including the photo grid) or whether the spec mistakenly grouped Review with Sourcing + Shortlist. R7 ship leaves Review untouched; revisit if a future producer call asks for table-shape Review. (RESOLVED `<5.16.1.1>`: confirmed Review.tsx is a `<ReviewCard>` list, not a table; the card list is the right surface. The spec's grouping of Review with Sourcing + Shortlist was the mistake.) |
| 49 | 2026-05-28 | `src/hooks/useUserRole.tsx` | `n/a` | ☑ | ☑ | [low] | Surfaced during the Phase 5.16.0 migration-reviewer pass. When the caller's `public.users` row is unreadable, the query returns null and `role` resolves to `null` — NOT `'pending'` — so `isPending` is `false` and `ProtectedRoute` would skip the `/pending` redirect. 5.16.0 mitigated the specific RLS trigger for this (the `users_select` policy keeps an `OR id = auth.uid()` self-read clause so pending/deactivated users can always read their own row), but the underlying null-role -> not-pending mapping stays a latent fragility if the users-read path is ever changed again. A defensive option: treat a null role for an authed user as pending (redirect) rather than falling through. Defer to 5.16.1 triage. (RESOLVED `<5.16.1.1>`: `useUserRole.tsx` now sets `isPending: true` when `role === null` for an authed, non-loading user, so `<ProtectedRoute>` redirects to /pending instead of falling through to all-false. Deactivation flow unaffected (it needs a readable row with `active=false`, mutually exclusive with the unreadable-row null-role case). Effect also rekeyed on `userId` not the `user` object to clear the exhaustive-deps warning.) |
| 50 | 2026-05-28 | `src/components/bulk-import/UploadStep.tsx` + `src/lib/hq/bulkImport/` (pipeline) | `81cdaf2` | ☑ | ☑ | [med] | Surfaced during the Phase 5.16.1.2 xlsx smoke (Jimmie, HQ Bulk Venue Import): the Review grid rendered the correct row count with every cell BLANK when the uploaded sheet's headers didn't exactly match the internal column keys. The whole pipeline reads `row[col.key]`, the shipped templates use the internal keys verbatim as CSV headers, and there was NO header→key normalization anywhere — so a modified / friendly-header sheet produced rows keyed by the raw headers and every `row[col.key]` resolved to `undefined`. Latent since the Phase 5.9 bulk-import primitive; NOT a regression (the correct template fills in, confirmed by Jimmie). A code-reviewer + general-purpose trace reproduced the parser against the templates and ruled out the carry-over/grid/virtualizer code. (RESOLVED `<5.16.1.2>`: two layers in new `src/lib/hq/bulkImport/normalizeHeaders.ts` (`buildAutoHeaderMapping` token-match seed + `applyHeaderMapping` re-key) + a manual column-mapping UI in `UploadStep` — each uploaded header → a field dropdown (auto-matches pre-filled, "Don't import" default), required-field-unmapped guard, duplicate-target warning, live mapped-preview. The mapped sheet flows downstream via `onParsed` so the whole pipeline (ref enumeration, grid, dedupe, validation, commit) sees entity-keyed rows. Auto-match covers case/space/underscore/label variance; the UI handles synonyms (Venue→Name, Venue Type→Venue Types, Size→Square Footage) surfaced in Jimmie's smoke. Upload-step placement (Jimmie's call). Entity-agnostic. 9 unit tests. See `docs/decisions.md` § Phase 5.16.1.2 D14.) |
| 51 | 2026-05-29 | `src/components/home/MyTasksThisWeekCard.tsx:157` | `fe04a5d` | ☐ | ☐ | [low] | Phase 6.1 table-alignment pass: the leading checkbox column's `<td>` uses Tailwind `text-center` rather than the canonical `.c` class the rest of the `.tbl` system (and this card's other cells, post-6.1) use. Left as-is in 6.1: Part B named only Task + Priority and "columns not listed stay as-is"; spec §2 flagged it as current-state. Visually identical (`text-center` == `.c`), so a pure convention inconsistency; swap to `.c` in a future `.tbl` polish pass. |
| 52 | 2026-05-29 | `src/components/outlook/YearGrid.tsx:82` | `af59516` | ☐ | ☐ | [low] | Encountered while doing the Phase 6.2 outlook-grid restyle: the per-cell loading guard is a no-op ternary `{loading && cellEntries.length === 0 ? null : null}`; both branches render `null`, so the `loading` prop has no effect inside `.ol-cell` (no skeleton/spinner ever renders). Looks like a placeholder for a per-cell loading state that was never implemented. Either wire up a real loading affordance or drop the dead expression (and reconsider whether `loading` belongs on the component at all). |
| 53 | 2026-05-29 | `src/pages/projects/ProjectDetail.tsx:271` (`saveFields`) + `src/components/projects/ProjectDetailsCard.tsx:38` | `n/a` | ☐ | ☐ | [low] | Phase 6.3 correctness review (DateField lift). `saveFields(patch: Partial<Project>)` types `patch` as `Partial<Project>`, which structurally permits relation keys (`client`, `venues`, `account_managers`, etc.) that are NOT real `projects` columns; `.update(patch)` would forward any such key to PostgREST and 400. Safe today because the only three callers pass date-column pairs, but the loose type would let a future caller silently break the update. Tighten to a date-pair-only union (or `Pick<Project, the six date columns>`). Same review: an empty-draft "Done" or the always-rendered footer "Clear" on an already-empty field fires a redundant `onChange({null,null})` → `saveFields` → one wasted null/null DB write (no data harm). And the shared-closure revert (`setProject(prev)` over the render's `project`) can clobber a concurrent second DateField edit if two saves overlap and the first errors, the same non-atomic pattern as `saveClientId`/`saveVenueIds`, not new. No timezone/off-by-one bug found: `toIso` (getFullYear/getMonth/getDate) is the exact inverse of `parseIso`, round-trips clean, no UTC shift. Draft re-seed (R3), stay-open (R5), reopen-to-month (R6), Clear stopPropagation (R7), and no-commit-on-outside-click/Esc all verified correct. |
| 54 | 2026-05-29 | `src/components/home/QuickAddCluster.tsx` + `src/index.css` (`.hq-qa`, mobile rule ~:1459) | `n/a` | ☐ | ☐ | [low] | Phase 6.4 (H1) swapped the quick-add buttons from `.hq-qa` to `.btn .btn-coral`, leaving `.hq-qa` with no component consumer (only a doc comment + the mobile 44px tap-target rule still reference it); the CSS rule was kept rather than removed. Knock-on: that mobile tap-target rule (`.btn-tertiary,.btn-sm,.hq-qa{min-height:44px}`) does NOT list bare `.btn`, so the now-`.btn` quick-add buttons sit at 40px (below the 44px mobile target). Either add `.btn` to the mobile rule or drop `.hq-qa` in a cleanup pass. |
| 55 | 2026-05-29 | `src/pages/talent-scout/RoleSettings.tsx:409` + `ts_roles.reeval_*` columns (no `.tsx` consumer) | `n/a` | ☑ | ☐ | [low] | Surfaced verifying the Phase 6.0 F035 fix: `ts-bulk-reevaluate` writes `ts_roles.reeval_status` / `reeval_total` / `reeval_processed` / `reeval_failed`, but no frontend component renders them (grep of `src/**/*.tsx` returns 0 read sites; the only reference is the fire-and-forget comment at RoleSettings.tsx:409), and `ts_roles` is not in the `supabase_realtime` publication. So the bulk re-eval has no progress UI; the audit's "progress bar" framing for F035 was hypothetical, and the F035 `reeval_total` count fix is latent (correct-but-unrendered) until a progress surface ships. Note the visible "Re-evaluate" button (CandidateTable.tsx:451) is a different path: `ts-evaluate-candidate` per selected row, client-side fan-out, no `reeval_*` columns. A future "add bulk re-eval progress UI" task now inherits a correct denominator. See `docs/decisions.md` § Phase 6.0 tech-debt D4 (audit finding F035). |
| 56 | 2026-05-29 | `src/components/talent-scout/CandidateTable.tsx:343-371` | `n/a` | ☐ | ☐ | [high] | `reevalSelected` fires one `ts-evaluate-candidate` Claude invoke per selected candidate via unbounded `Promise.all` over the whole selection (selecting the Master Pool launches N simultaneous Claude-backed edge invocations); `PullDetail.tsx:249` caps the identical op at CONCURRENCY=6. Rec: bound concurrency with a 6-wide worker pool, extracting a shared `mapWithConcurrency` since both sites now do the identical fan-out. (audit F009) |
| 57 | 2026-05-29 | `src/hooks/useUserRole.tsx:54-99` | `n/a` | ☐ | ☐ | [med] | No shared cache: each of 14 call sites fires its own `SELECT permission_role,active,is_owner FROM users` (the shell mounts it 3x per authed page before page consumers), while siblings `lookups.ts` and `useClientsAndVendors.ts` have module caches for exactly this. Rec: add a module-level cache plus subscriber set keyed by `userId`, mirroring `useClientsAndVendors.ts`, with no caller API change. (audit F012) |
| 58 | 2026-05-29 | `src/pages/vendors/VendorDetail.tsx:136-376`; `src/pages/clients/ClientDetail.tsx:98-219` | `n/a` | ☐ | ☐ | [med] | The Primary-Contact resolve heuristic, `applyPrimaryContact` writer, `savePrimaryContact` guard, and `createXContact` handler are byte-for-byte equivalent between Vendor and Client detail (aside from table name), so two copies will drift on any denormalization fix. Rec: extract a `usePrimaryContact({parentTable,parentId,contacts})` hook into `src/lib/hq`/`people`, keeping per-page picker JSX and sharing only resolve/write logic. (audit F014) |
| 59 | 2026-05-29 | `src/components/home/MyWeekStrip.tsx:54-67`; `src/components/home/RecentActivityCard.tsx:247-260` | `n/a` | ☐ | ☐ | [med] | The "projects this user is assigned to" lookup (parallel `project_account_managers` + `project_designers` by `user_id` into a Set) is reimplemented independently in 3+ Home surfaces (the F022 perf concern folds in here), so any assignment-semantics change must touch each copy. Rec: extract `loadAssignedProjectIds(userId)` into `src/lib/home`/`hq`. (audit F015, F022) |
| 60 | 2026-05-29 | `src/pages/venue-scout/BriefReport.tsx:73`; `BriefVenue.tsx:44`; `BriefEvent.tsx:63` | `n/a` | ☐ | ☐ | [med] | The 25-column `SCOUT_SELECT` string is duplicated verbatim across the 3 intake pages, so a column add/rename must hit all 3 in lockstep and a miss silently drops a field from one page's `fromScout()`. Rec: hoist `SCOUT_SELECT` into `src/lib/venue-scout/briefForm.ts` (which already owns the brief column contract) and import in all 3. (audit F016) |
| 61 | 2026-05-29 | `src/components/home/RecentActivityCard.tsx:82-130` | `n/a` | ☐ | ☐ | [med] | `singularizeEntityType`/`entityWord`/`hrefForEntity` are near-verbatim re-impls of the same three in `lib/activity/formatSentence.ts` and have already drifted (this copy returns `/wiki` with no slug while `formatSentence` returns `/wiki/:slug`, so the same activity row links to different places on Home vs the feed). Rec: export the three mappers from `formatSentence.ts`, import them here, and delete the parallel copy. (audit F017) |
| 62 | 2026-05-29 | `src/lib/search/queries.ts:70-74` | `n/a` | ☐ | ☐ | [med] | `ilikeNeedle` escapes only `%`/`_` but the value is interpolated into PostgREST `.or(...)` strings whose grammar treats `,` `(` `)` `.` as delimiters, so searching `Smith, Co` or `(draft)` breaks the filter into a server error that each `searchX` catches and returns `[]`, making that section silently vanish (not SQLi, but a real correctness/trust-boundary bug). Rec: strip/escape PostgREST `.or()` delimiters from the needle, or move multi-column search to a tsvector/RPC that does not embed user text in the or-grammar. (audit F020) |
| 63 | 2026-05-29 | `src/lib/hq/savedViews.ts:57,78,88,150,154` | `n/a` | ☐ | ☐ | [med] | `saved_views` rows are `as unknown as SavedView` at every boundary and `filter_state` (jsonb) is fed into `applyFilters`/FilterBar with no runtime validation, so a schema-drifted row (unsupported op, stale `organization` entity_type) flows through untyped and can silently filter a list to nothing or throw in `chipDisplayValue`. Rec: add a shape validator in `listSavedViews`/`getDefaultSavedView` that drops/normalizes non-conforming chips before returning (this is the jsonb-to-filter-engine trust boundary). (audit F023) |
| 64 | 2026-05-29 | `src/lib/hq/lookups.ts:119-128` | `n/a` | ☐ | ☐ | [med] | On a lookup SELECT failure `ensureLoaded` sets `entry.options=[]` plus `console.warn` only, and because `[]` is not null the cache treats the table as loaded and never retries, so the picker shows "No results" indistinguishable from genuinely empty until a full reload or explicit invalidate. Rec: on error leave `entry.options` as `null` so a remount retries, optionally surfacing an error flag for a retry affordance. (audit F025) |
| 65 | 2026-05-29 | `src/components/outlook/EntryPanel.tsx:100-752` | `n/a` | ☐ | ☐ | [med] | 752-LOC component handling detail/edit/new inline with the City/Client/Budget field markup duplicated between the detail inline-edit block and the edit-mode form, over the 500-LOC god-file threshold so targeted changes are error-prone. Rec: split into `OutlookEntryDetail` + `OutlookEntryForm` under `components/outlook/`, keeping the mode switch and dialog state in the parent (no behavior change). (audit F026) |
| 66 | 2026-05-29 | `src/components/data/InternalNotesEditor.tsx:123-144` | `n/a` | ☐ | ☐ | [med] | Fetches its own `permission_role` + user id on every mount via a raw `users` SELECT across 8 parent surfaces, a duplicate self-read on top of the shell's `useUserRole`. Rec: read role/id from `useUserRole` + `useAuth` instead of a second per-mount `users` query. (audit F027) |
| 67 | 2026-05-29 | `src/hooks/useReferrerCrumb.ts:100-369` | `n/a` | ☐ | ☐ | [med] | `resolveRoute` is a ~270-line hand-maintained route table duplicating App.tsx's route structure and already stale (`/venue-scout/scouts/new` retired in App.tsx:512 but still mapped here, plus an unreachable `/brief/report` arm), so new routes silently fall to the "HQ" default crumb. Rec: trim the dead cases now and cross-link App.tsx in a comment, longer term deriving labels from a shared route-meta map. (audit F028) |
| 68 | 2026-05-29 | `src/components/outlook/EntryPanel.tsx:55`; `YearGrid.tsx:29`; `src/components/projects/ProjectDetailsCard.tsx:18`; `src/components/data/bulk-import/cells/MoneyCell.tsx:15`; `src/lib/venue-scout/briefForm.ts:162` | `n/a` | ☐ | ☐ | [med] | 5+ local `formatBudget`/`formatMoney` helpers (all `$${n.toLocaleString('en-US')}` variants with divergent empty-states) alongside the Phase 6.4 `hq/currency.ts:formatCurrencyDisplay` declared "single source of truth", with the display-only `'-'`/blank cases overlapping it directly. Rec: replace the `'-'`/blank-empty-state copies (ProjectDetailsCard, MoneyCell) with `formatCurrencyDisplay` and reconcile the TBD/M-abbrev variants or scope the "single source of truth" claim. (audit F033) |
| 69 | 2026-05-29 | `src/pages/clients/ClientDetail.tsx:345-401` | `n/a` | ☐ | ☐ | [med] | Primary Contact uses a click-to-reveal toggle here but an always-inline `RecordCombobox` in VendorDetail (two UX/code shapes for the identical relation), and the toggle's `onChange` calls `savePrimaryContact(next)` without awaiting before `setPrimaryContactEditing(false)` so a failed save still collapses the editor. Rec: pick one pattern (always-inline is simpler); if keeping the toggle, await `savePrimaryContact` before collapsing. (audit F040) |
| 70 | 2026-05-29 | `src/pages/people/PersonDetail.tsx:149-221` (+ Venue/Vendor/Client Detail `saveField`) | `n/a` | ☐ | ☐ | [med] | Inline-edit saves capture `person` from the render closure for optimistic rollback (`setPerson({...person,[field]:prev})`), so two overlapping edits make the second's rollback use a stale snapshot that omits the first, clobbering it. Rec: use functional `setState` for both the optimistic write and the rollback (`setPerson(p => p ? {...p,[field]:next} : p)`). (audit F041) |
| 71 | 2026-05-29 | `src/pages/talent-scout/FinalReviewDetail.tsx:44-67,104` | `n/a` | ☐ | ☐ | [med] | Hand-rolled `Review`/`Cand` types + `as unknown as Review[]` bypass the generated `Database` types every sibling TS page uses, and `final_rankings` (jsonb) is asserted to `FinalRanking[]` with no runtime validation so a malformed row crashes render instead of a handled state. Rec: derive `Review` from `Database['public']['Tables']['ts_final_reviews']['Row']` intersected with the jsonb shape, keeping `FinalRanking` as the jsonb element type only. (audit F042) |
| 72 | 2026-05-29 | `src/pages/talent-scout/RoleDashboard.tsx:51-82`; `src/pages/talent-scout/FinalReviewDetail.tsx:90-148` | `n/a` | ☐ | ☐ | [med] | Both load effects destructure only `data` from their parallel queries and check no `.error`, so a transient failure renders as "Role not found."/"No final review yet" indistinguishable from genuine empty, masking failures with no toast/retry. Rec: destructure and check `error` on at least the primary fetch, adding an error branch distinct from the empty state. (audit F043) |
| 73 | 2026-05-29 | `src/pages/projects/DeliverablesList.tsx:119-121,263` | `n/a` | ☐ | ☐ | [med] | The realtime INSERT refetch and `onCreated` refetch call `loadDeliverables().then(setRows)` with no unmount/active guard while the mount and saved-view fetches in the same file are properly active-guarded, so setState-after-unmount can occur (inconsistent with the file's own discipline). Rec: route both through a guarded loader / ref-based active flag scoped to the subscription effect. (audit F046) |
| 74 | 2026-05-29 | `src/pages/tasks/TasksList.tsx:437-440,562`; `src/pages/projects/queries.ts:80-84`; `src/pages/projects/ProjectsList.tsx:72-73` | `n/a` | ☐ | ☐ | [med] | Four different first-name/display-name rules (`firstName` capitalizes email-derived names, `firstNamesOf` splits whitespace, TasksList uses bare `.split(' ')[0]`/`.split('@')[0]`) make the same user render a different short name on Projects vs Tasks vs board cards. Rec: consolidate to one `firstNameOf(name,email)` util in `src/lib/hq`. (audit F047) |
| 75 | 2026-05-29 | `src/lib/deliverables/queries.ts:21-34` | `n/a` | ☐ | ☐ | [med] | `loadDeliverables` embeds `project:projects(...)` with no constraint-named alias and casts straight to `DeliverableListRow[]` with no array-shape normalization, unlike `loadTasks`/`loadProjects` which defensively collapse `Client | Client[]`, so if PostgREST returns the to-one embed as a 1-element array every project/client name renders blank (cast hides it from tsc). Rec: mirror `loadTasks` by adding the constraint-named alias plus the `Array.isArray` collapse before returning. (audit F048) |
| 76 | 2026-05-29 | `src/lib/deliverables/assigneeSync.ts:40-104` | `n/a` | ☐ | ☐ | [med] | `syncDeliverableAssignees` is the untested auto-task lifecycle engine (diff prev/next assignees into task INSERT/DELETE keyed on `source_deliverable_id`+`source_user_id`, with a 23505 swallow and delete-count reconciliation) invoked from 3 call sites, so a diff bug silently creates orphan tasks or deletes the wrong user's task. Rec: add a unit test for the diff math plus the 23505-benign/partial-delete branches with a mocked supabase client. (audit F049) |
| 77 | 2026-05-29 | `src/pages/talent-scout/CandidateDetail.tsx:23-29,154-159` | `n/a` | ☐ | ☐ | [low] | Local `Criterion` type omits `partial_points_rubric`/`summary`/`is_manual` from the canonical wizardStore `Criterion`, plus `role.scorecard as unknown as Criterion[]` (the name-keying fragility is mitigated by forced re-eval on scorecard edits, leave that). Rec: `import { Criterion } from wizardStore` instead of redefining a narrower one. (audit F050) |
| 78 | 2026-05-29 | `src/pages/talent-scout/CandidateDetail.tsx:252-259` | `n/a` | ☐ | ☐ | [low] | `StatusDropdown.onChange` here optimistically mutates local state and never refetches, while the same control in `CandidateTable` triggers a full reload (two consumers handle the same success differently). Rec: either `load()` after the change (consistent with the table) or document that optimistic-only is intentional because no server-derived field changes on a status flip. (audit F051) |
| 79 | 2026-05-29 | `src/pages/venue-scout/ScoutGlobalSettings.tsx:94`; `src/components/settings/AnthropicSpendBreakdownTable.tsx:57` | `n/a` | ☐ | ☐ | [low] | `ScoutGlobalSettings` and its child `AnthropicSpendBreakdownTable` independently fetch the identical `anthropic_spend_breakdown` RPC with the same `window_kind`, two identical round-trips per screen both re-firing on every toggle. Rec: fetch once in the parent and pass rows down as a prop. (audit F060) |
| 80 | 2026-05-29 | `src/pages/venue-scout/Review.tsx:190-196,352`; `src/components/.../PhotoUploadModal.tsx:220` | `n/a` | ☐ | ☐ | [low] | Signed photo URLs are minted one round-trip per photo via `Promise.all` of individual `createSignedUrl` calls (up to 4N storage requests per Review load), while the client exposes `createSignedUrls` (batch) for exactly this. Rec: replace with a single `createSignedUrls(paths, 3600)` and map results back by path. (audit F061) |
| 81 | 2026-05-29 | `src/components/bulk-import/BulkImportPage.tsx:118-154` | `n/a` | ☐ | ☐ | [low] | The autosave `setInterval` effect deps include `gridRows`/`mappings`/etc., so every cell edit tears down and recreates the interval, restarting the 20s clock and potentially preventing autosave indefinitely during continuous editing (the `dirtyRef` pattern was meant to avoid this). Rec: move mutable form state into a ref and depend only on `[user, config.entity_type]`. (audit F062) |

## Edge Functions

(Supabase Edge Functions in `supabase/functions/`. `ts-*`, `vs-*`, `_shared/`.)

| # | Date | File | Hash | V | R | Severity | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-14 | `supabase/functions/vs-research-venues/index.ts:601-621` | `49e03e6` | ☑ | ☑ | [med] | Duplicate-invocation race confirmed live (scout 25b5c921, 4 boots / 2 parallel callClaude runs same scout id); the in_flight check reads `brief_data.research_started_at` then writes it non-atomically, so concurrent invocations both pass before either records the kickoff. Downstream CAS guards mask the DB-write conflict but double-spend tokens. Carry-forward from 2026-05-13 cutover; proper fix is a Postgres advisory lock or a kickoff CAS on `research_started_at`. (5.8.2 triage: verify-and-defer to Phase 5.10 Venue Scout review. High-cost fix (advisory lock or kickoff CAS); downstream CAS guards mask the DB-write conflict but token double-spend remains. 5.10 is the natural slot for the rewrite to land alongside other Venue Scout passes.) (RESOLVED `1a01d3e`: new SECURITY DEFINER `vs_research_try_acquire_kickoff` RPC uses `pg_try_advisory_xact_lock` + same-transaction read-and-write of `brief_data.research_started_at`, making the kickoff atomic. GRANT EXECUTE TO service_role only; vs-research-venues calls it via the service-role client. Token double-spend gone.) |
| 2 | 2026-05-14 | `supabase/functions/vs-generate-brief-overview/index.ts` (`hashFormatBudget`) | `n/a` | ☑ | ☑ | [low] | Phase 4 Revision pass 3: client (`briefForm.ts`) + server budget canonicalization for the overview-source hash both rely on `Number.prototype.toLocaleString("en-US", ...)`; identical on V8 (browser + Deno) today but a silent parity risk if either runtime's ICU output ever shifts. Flagged by security-auditor + code-reviewer, deferred; a pure-JS comma formatter would remove the runtime coupling. (5.8.2 triage: verify-and-defer. Silent parity risk only; both runtimes are V8 so ICU output is identical today and divergence has not surfaced. Lift only if ICU divergence actually shows up, which is unlikely; pure-JS comma formatter is the easy lift when it does.) (RESOLVED `1a01d3e`: re-verified during the Phase 5.12.5 audit. The HASH itself was parity-clean post-5.1 (uses `hashAsNumberOrNull` on raw `scout.budget`; no formatted string enters the hash). The remaining ICU surface was the prompt DISPLAY block (`Budget: $X`); closed by introducing `supabase/functions/_shared/formatBudget.ts` `formatBudgetForPrompt` (pure-JS comma formatter) and wiring it at the budget display line. ICU coupling removed end-to-end on the edge side.) |
| 3 | 2026-05-23 | `supabase/functions/_shared/venueFill.ts:60` (FILL_TOOL `key_features` description) | `3a197b0` | ☑ | ☑ | [med] | Phase 5.12.0 smoke (Jimmie): VS-generated `key_features` arrive as long sentences carrying address-anchored facts ("Standalone creative-conversion building, ~24,806 sq ft total leasable area", "Walk Score of 96 highly walkable block on N Cahuenga Blvd", "Neighbors include Netflix, Red Studios, Paramount Studios..."). Intended shape is a SHORT keyword tag list (1-4 words, usually 1-2) of evergreen architectural / event-experience features that apply across venues — Courtyard, Skylight, High Ceilings, Garage Doors, Ground Floor, Multi-Level, Waterfront, Rooftop, Garden, Wood Floors, Exposed Brick, Balcony, Storefront, Vaulted Ceilings, Multiple Stages, Outdoor, Floor-to-Ceiling Windows. No venue-specific names / neighborhoods / addresses / square footage; no venue-type tokens (those are already covered by venue_type). Target 2-10 features per venue; quality over quantity. Same description is used by both `vs-research-venues` (Phase A + B) and `vs-compile-summaries` Pass 1 — single fix point. Lever: tighten the `key_features` description in `_shared/venueFill.ts` per `feedback_tool_choice_collapse` (descriptions are the output-quality lever for forced tools). Routes to **5.12.6 prompt audit**. (RESOLVED `1a01d3e`: first ship of the Phase 5.12.13 prompt audit. `FILL_TOOL.key_features` description rewrites to "2-10 short, evergreen tags... 1-4 words each (usually 1-2)" with the canonical tag list as reference shape (framed as "fine if they match the shape" rather than closed whitelist); `items.maxLength` tightens 120 -> 35; `minItems` 3 -> 2; `maxItems` 6 -> 10. New deterministic `sanitizeTagShape` helper in `_shared/venueTypes.ts` (drops digits, > 4 words, > 35 chars, case-insensitive dupes) chains on the outside of existing `stripPlaceholders` at every write site: Phase A `enrichSheetVenue`, Phase A `enrichHqPoolForResearch` (new key_features fill gate), Phase B parse path, `vs-compile-summaries` Pass 1, `vs-research-single-venue` apply path. FILL_SYSTEM body byte-unchanged per `feedback_tool_choice_collapse`.) |
| 4 | 2026-05-23 | `supabase/functions/_shared/venueOverview.ts` (`ABOUT_VENUE_SYSTEM` `<task>` block) | `1a01d3e` | ☑ | ☑ | [low] | Phase 5.12.0 smoke (Jimmie): generated About paragraphs trend 120-150 words against the prompt's stated hard cap of 90-110. The cap is restated in `<task>` + `<tone_and_style>` but Claude treats it as a soft signal, not strict truncation. Affects BOTH callers (`hq-generate-venue-about` and `vs-compile-summaries` Pass 2) since the prompt is shared. Likely fix: tighten the cap restatement, drop softer adjectives in `<word_use_guidance>`, possibly shorten one of the 6 examples to anchor a tighter target. Collaborative prompt-tuning territory; routes to **5.12.6 prompt audit**. (RESOLVED `1a01d3e`: cap widened from 90-110 to 90-120 (producer call: 120 reads as the honest upper bound; tightening to 110 forces output Jimmie's hand-revised examples wouldn't themselves pass). `<task>` block rewritten with cut-or-merge fallback ("If your draft exceeds 120 words, cut sentences or merge clauses until it fits before you reply.") + count-before-returning ("Count the words in your draft before returning."); "Every sentence must earn its place" moves to the tail as a closer. `<output_format>` block gains a new length-check paragraph appended inside the existing block: "Before returning, count the words in your draft. If above 120, cut a sentence or merge two clauses and recount. Do not return a paragraph above 120 words." 6 in-prompt example INPUT lines reshape from long narrative `Key features:` strings to short tag-shaped `Key features (tags):` lists (Studio 525 / Chelsea Industrial / 8626 Melrose Ave / The Flat NYC / AT&T Discovery District / Astor Place) matching the post-5.12.13.1 user-message label. All 6 example OUTPUTS stay byte-for-byte (Jimmie's hand-revised voice anchors; the cap widening to 120 is the lever that lets them hold without editing — The Flat NYC at 112 words sits inside the new range). `<input_fields>` `Key features (tags)` line gains a description body ("short evergreen feature tags... Interpret each tag as a feature anchor; expand on what it describes in the paragraph. Do not list the tags verbatim and do not pad the paragraph to cover every tag.") + opportunistic em-dash kill on line 64. Softer-adjective allowlist tightening NOT applied this ship (deferred unless smoke shows post-cap bloat is allowlist-driven; failure-mode tuning levers documented in spec § 11). Server-side word-count trim helper NOT added (lever stays in SYSTEM; re-open as 5.12.13.2.1 only if smoke shows 3+ of 5 overshoot the 120 ceiling). Two edge functions redeploy out-of-band: `hq-generate-venue-about` + `vs-compile-summaries`; one-time prompt-cache invalidation per consumer accepted per `feedback_tool_choice_collapse`. POST-.2 SMOKE CORRECTION `1a01d3e` (Jimmie 2026-05-25, ~5-6 fresh regenerates): the .2 120-word hard cap did NOT land. 5 of 6 outputs landed 130-175 words (The Glasshouse 175 / NYA Studios 163 / AT&T Discovery District 159 + 3 others 130-145; only 1 within range). Producer call: quality of the longer outputs is right; the 120 cap was too tight for venues with material to work with. .2.1 ships the corrective cap widen 90-120 -> 100-150 (numerical edits to `<task>` + `<output_format>`) + tag-priority guidance on the `<input_fields>` Key features (tags) line ("favor the ones most appealing or distinctive for this specific venue, especially when expanding on more would push the paragraph above the 150-word cap"). 5 locked decisions in `docs/decisions.md` § Phase 5.12.13.2.1. Edge #4 stays RESOLVED at the cap level; .2.1 is the producer-honest re-calibration to where high-quality outputs actually land.) |
| 5 | 2026-05-23 | `supabase/functions/_shared/venueTypes.ts` (`CANONICAL_TYPES`) + `src/lib/venue-scout/venueTypes.ts` mirror | `n/a` | ☑ | ☑ | [med] | Phase 5.12.0 smoke (Jimmie): VS canonicalizes venue types against a hard-coded `CANONICAL_TYPES` array (server + frontend lockstep), separate from HQ's `venue_types` lookup table that admins manage from Settings > Lookup Lists. Drift is real: White Box was added to the canonical list in 5.12.0 with a seed insert, but any future producer-added type in HQ Venues won't be recognized by VS until a code change. Intent (Jimmie): VS reads the lookup table at runtime so admin adds/revisions flow through automatically. Architectural change — touches `canonicalizeType`, `parseTypes`, `TYPE_STYLES` (palette needs a strategy: lookup-table column, algorithmic assignment, or fall back to `TYPE_FALLBACK_STYLE` for non-canonical types), the FILL_TOOL `venue_type` schema description (Claude needs to know the valid list at prompt time — pass dynamic list in the user message), and the frontend filter chip lists. **Locked as Phase 5.12.5** (Jimmie 2026-05-23); prompt audit bumped to 5.12.6 + UX smoke to 5.12.7 so the audit can be informed by the new dynamic-types model. (RESOLVED `1a01d3e`: VS now reads `public.venue_types` at runtime via shared `useLookup` cache on the frontend (new `useVenueTypes` hook) + per-request `getVenueTypesCanonicalSet` fetch in 5 edge functions. Schema descriptions + system prompts stay static for prompt-cache stability; live canonical list rides in per-call user message. `TYPE_STYLES` keeps the 9 legacy palette keys; producer-added types render via `TYPE_FALLBACK_STYLE` (neutral grey). Single migration adds LOWER expression unique index (mirrors cities) + rewrites `bulk_import_commit_venues` to use `ON CONFLICT DO NOTHING`. Legacy `canonicalizeType` + `canonicalizeMultiType` helpers deleted from both lockstep files.) |
| 6 | 2026-05-24 | `supabase/functions/vs-generate-deck/index.ts:597-616` (pre-5.12.4.1 path) | `256fd10` | ☑ | ☑ | [high] | Codex adversarial review of the 5.12.4 diff surfaced a duplicate-invocation TOCTOU race on `vs-generate-deck`'s kickoff path (mirror of Edge #1 on the deck-generation surface). The pre-5.12.4.1 inline path read `brief_data.deck_generation_started_at`, compared age to `IN_FLIGHT_GRACE_MS=90000`, then non-atomically UPDATEd the timestamp — two near-simultaneous invocations (producer double-clicking Generate) could both pass the grace-window check before either committed its timestamp, then both call `pushVenuesToHq` and INSERT duplicate `venues` rows. Aggravator: no UNIQUE constraint on venue identity columns, so concurrent winners both succeed at the INSERT and produce unrecoverable duplicate master venues. (RESOLVED `1a01d3e`: new SECURITY DEFINER `vs_deck_try_acquire_kickoff` RPC mirrors `vs_research_try_acquire_kickoff` exactly — `pg_try_advisory_xact_lock` under `vs-deck:<scout_id>` namespace + same-transaction read-and-write of `brief_data.deck_generation_started_at`; `vs-generate-deck` swaps the inline path for the RPC call. Migration `20260604120000_phase_5_12_4_1_deck_kickoff_lock.sql`. GRANT EXECUTE TO service_role only.) |
| 7 | 2026-05-24 | `supabase/functions/_shared/venueDedupe.ts:307-308` (pre-5.12.4.1 eligible filter) | `dc717c5` | ☑ | ☑ | [high] | Codex adversarial review of the 5.12.4 diff surfaced an identity-signal gap in the 5.12.3 points-based dedupe ladder. Pre-5.12.4.1 the eligible filter required only `!s.vetoed && s.score.total >= threshold` (threshold 60). Address full match (50) + city bonus (10) = 60 cleared the threshold alone, so two distinct venues at the same building address with the same city ("75 Rockefeller Plaza - Top of the Rock" and "75 Rockefeller Plaza - Studio 8H") would have merged because the cross-field VETO only fires on name FULL match. Both consumers affected: `pushVenuesToHq` at deck time could link a Claude-research candidate to the wrong HQ venue and overwrite `about_venue`; `vs-research-venues` cross-rail dedupe could drop a research row against the wrong HQ pool entry. (RESOLVED `1a01d3e`: identity-signal gate added — eligible filter now requires `s.score.name >= 1 || s.score.website >= 1` on top of the threshold check. Address-only matches with no name-or-website co-signal fall through to `no_merge_identity_gate` (new telemetry decision string). Cross-field VETO preserved unchanged.) |
| 8 | 2026-05-24 | `supabase/functions/vs-generate-deck/index.ts:85,90` + `supabase/functions/vs-compile-summaries/index.ts:97,120` + `supabase/functions/vs-research-venues/index.ts:1497,1502` (pre-5.12.4.2 constants) | `e3d2c83` | ☑ | ☑ | [high] | Codex adversarial review round 2 on the 5.12.4.1 squash flagged that the new `vs_deck_try_acquire_kickoff` RPC's grace window (90s) was shorter than the deck `WORK_TIMEOUT_MS` (180s), leaving a 90s race window where a producer refresh could reacquire the kickoff while the first work() invocation was still mid-Slides-API, re-opening the duplicate-HQ-venue race the 5.12.4.1 RPC was meant to close. Same shape on `vs-compile-summaries` (grace 90s, timeout 240s → 150s race window) and `vs-research-venues` (grace 360s == timeout 360s → hairline race since the RPC uses `<` for the age check). (RESOLVED `1a01d3e`: bumped `IN_FLIGHT_GRACE_MS` on all three VS edge functions to exceed `WORK_TIMEOUT_MS` + 60s buffer for writeFailure / failWithCode UPDATE to land. `vs-generate-deck` 90_000 -> 240_000; `vs-compile-summaries` 90_000 -> 300_000; `vs-research-venues` 360_000 -> 420_000. Producer's explicit Re-Generate flow unaffected (reset RPCs clear kickoff timestamps directly). Carry-forward: durable run-id pattern is the longer-term proper fix.) |
| 9 | 2026-05-24 | `supabase/functions/vs-compile-summaries/index.ts:699-707` + `supabase/functions/vs-research-venues/index.ts:2452-2461` + `supabase/functions/vs-generate-deck/index.ts:1160-1170` (pre-5.12.4.2 final UPDATEs) | `e3d2c83` | ☑ | ☑ | [high] | Codex adversarial review round 2 flagged that `Promise.race([work, timeoutPromise])` rejects on timeout + writes failure, but JavaScript can't cancel the losing work() promise — a slow Claude call (per-fetch ceiling 180s + 1 pause_turn continuation = up to ~360s real wall-clock) can resolve after the timeout, then apply per-venue patches, then overwrite the failure stamp with success state via the final UPDATE (which had no CAS on `pipeline_error` pre-5.12.4.2). Result: UI and DB disagree on whether compile / research / deck succeeded or failed. All three VS edge functions affected. (RESOLVED `1a01d3e`: all three final success UPDATEs now CAS on `current_step=<expected>` AND `pipeline_error IS NULL`. The kickoff RPC clears `pipeline_error` to NULL at acquisition; writeFailure / failWithCode stamps a non-null `pipeline_error`; so the CAS no-ops cleanly on the timeout-then-late-success race. Each UPDATE uses `{ count: "exact" }` so the no-op branch logs a warn + returns without raising, so the outer catch doesn't double-write failure on top of failure. Carry-forward: thread AbortSignal/run-token through callClaude for proper cancellation.) |
| 10 | 2026-05-24 | `supabase/functions/vs-generate-deck/index.ts:717-738` (pre-5.12.4.2 pushVenuesToHq position) | `256fd10` | ☑ | ☑ | [med] | Codex adversarial review round 2 flagged that `pushVenuesToHq` (which INSERTs new `venues` rows + writes `linked_venue_id` on `vs_candidate_venues`) ran at step 2.5 (immediately after the venues load), BEFORE the env-var check + template copy + Slides API. A missing `GOOGLE_TEMPLATE_FILE_ID` / `GOOGLE_OUTPUT_FOLDER_ID` config error at step 4 would leave HQ state permanently mutated for a deck the producer was told was NOT generated. No rollback. (RESOLVED `1a01d3e`: env-var existence check pulled up to new step 2.5 BEFORE pushVenuesToHq. The actual `copyTemplate` Drive call stays at step 4 (it's a mutating Drive API call). Remaining HQ-mutated-but-deck-failed window: Drive `copyTemplate` throw or Slides API throw AFTER the push — both fail via the appropriate code; `pushVenuesToHq` is idempotent on `linked_venue_id` so the producer's Re-Generate path re-runs without re-INSERTing. Carry-forward: move HQ push AFTER Slides success for the strongest guarantee.) |
| 11 | 2026-05-24 | `supabase/functions/_shared/venueFill.ts` (`buildFillUserMsg` brief block) | `1a01d3e` | ☑ | ☑ | [low] | Phase 5.12.7 carry-forward (Feature B widened the Pass 1 caller set to research-time enrichment of `hq_pool` rows). `buildFillUserMsg` does NOT yet consume `target_audience` or `vibe_aesthetic` from the brief — the shared brief block uses `client_name` / `event_name` / `city` / `live_dates` / `budget` / `expected_guest_count` / `event_overview` / `Additional brief notes`, plus the HQ CANONICAL ABOUT block when set. This omission is shared with `vs-compile-summaries` Pass 1 (same helper). Phase 5.12.5's array-shape flip on those two brief fields landed in `vs-research-venues` Phase B's `userMsgBriefPrefix` + `rescueHqPoolFitVetoedVenues` only; the Pass 1 shared `buildFillUserMsg` was left out of scope. Net effect: enrichment fires per-brief recs/cons/rank without the two 5.12.5 brief signals. Defer to a dedicated sub-phase that can review the prompt diff for Pass 1 prompt-shape regression risk across all three callers (`vs-parse-sheet` enrichment, `vs-compile-summaries` Pass 1, `vs-research-venues` Feature B). (RESOLVED `<5.16.1.1>`: `buildFillUserMsg` now surfaces `target_audience` + `vibe_aesthetic` (tolerating the legacy single-string shape) with the Phase B `userMsgBriefPrefix` labels + "(not set)" fallback. Note: `vs-parse-sheet` no longer runs AI enrichment (moved to vs-research-venues at 4.10.3-port), so the live callers are vs-research-venues / vs-compile-summaries / vs-research-single-venue, all redeployed out-of-band. Deployed 2026-05-28; smoke pending. See `docs/decisions.md` § Phase 5.16.1.1 D11.) |
| 12 | 2026-05-24 | `supabase/functions/vs-research-venues/index.ts` (Phase B `SYSTEM` retail-priority directive) | `1a01d3e` | ☑ | ☑ | [med] | Phase 5.12.7 post-smoke (Jimmie, scout 39a9fab2-90ee-497f-ab48-3da8356bb0f4): Phase B research consistently returns mostly / all `Retail` venues despite the broader CANONICAL_TYPES set being available. Specific run: 4/4 net-new research rows were all Retail (Beverly Connection storefront, Buttercup Pop Up #1 WeHo retail, Melrose Ave Storefront at La Cienega retail, Robertson Boulevard Storefront retail). Likely root cause: the `SYSTEM` prompt's strong priority directive — "Strongly prioritize ground-floor commercial / storefront / retail-vacancy properties — a significant share of Mirror's selected venues are empty storefronts and ground-floor commercial spaces" + the "TYPE CONSTRAINT (strict)" block instructing Retail for ground-floor commercial — over-anchors the model toward Retail at the expense of brief-driven variety (Event Venue, Industrial, Warehouse, Gallery, Studio, Outdoor, Mobile). Producer ask is a way to de-prioritize this so the matrix shows more variety informed by the brief's `venue_types` signal instead of the SYSTEM bias. Lever lives in the prompt audit phase (`docs/roadmap.md` Phase 5.12.13). Carry-forward note: this is one to discuss collaboratively rather than tighten unilaterally — there is a real reason the retail bias exists (Mirror's actual selected-venue distribution) and a poorly-aimed lever could swing too far toward variety-for-variety's-sake. (RESOLVED `1a01d3e`: Phase B `SYSTEM` block "Strongly prioritize" framing rewrites to "Mirror often books... so surface them when the brief allows. Equal-weight the other canonical venue types..."; TYPE CONSTRAINT's added 5.12.10 "prefer Retail when..." sentence dropped; em dash on line 1814 killed. Phase B userMsg "Return X" line "Weight... heavily in ranking_score" replaced with brief-criteria-balanced rank line. `FILL_SYSTEM` "Prefer Retail for ground-floor commercial / storefront / vacancy spaces." sentence dropped. `requirementLines` venueTypes "must be one of" softens to "should be" with 1-2 outliers permitted when very-high-rank justifies the mismatch. `requirementLines` strict-neighborhoods ternary tightens both branches in lockstep (strict allows 1-2 adjacent; non-strict explicitly allows adjacent + reasonable radius). Three edge functions redeploy out-of-band; one-time prompt-cache invalidation accepted per consumer. Producer-evidence smoke baseline already confirmed in the working session that bias is brief-sensitive: a non-Retail brief surfaced 0/10 Retail at 9/10 Pop Up. Spec at `OUTPUTS/historical/phase-5-12-13-3-spec.md`.) |
| 13 | 2026-05-24 | `supabase/functions/vs-research-venues/index.ts` Phase B `TOOL.venues[].name` schema description | `1a01d3e` | ☑ | ☑ | [med] | Phase 5.12.7 post-smoke (Jimmie, retail-row pattern): sourced venue names from Phase B come back with descriptor / source / cross-street cruft that should not appear in the name field. Live examples from the same smoke run: `Sunset Strip Storefront (Peerspace — Sunset Strip Lounge)` (carries the listing-DB source + sub-listing name in parens) and `Melrose Ave Storefront at La Cienega` (carries a cross-street qualifier). Acceptable shapes for those same venues are `Sunset Strip Storefront` and `Melrose Ave Storefront` respectively. Producer rules for unnamed / unbranded vacancies: (a) NO listing-database citations in the name (no "(Peerspace — ...)", "(LoopNet)", "(NMRK)" etc; those belong on `website_url` and nowhere else); (b) NO cross-street / "at <street>" qualifier unless it's part of the property's verified name; (c) acceptable v1 fallback for unnamed properties is a "<neighborhood-or-street> <space-type>" identifier like "Sunset Strip Storefront" or "Melrose Ave Storefront". This LAST rule conflicts with the current `name` schema description in `vs-research-venues` lines 1577-1581, which says "If the property is genuinely an unbranded vacancy with no name, use just the address (e.g. '10250 Santa Monica Blvd')" AND "Do NOT add descriptive suffixes like 'Storefront' ... Vacancy ... Retail Vacancy ...". Producer's revised stance is that "<location> Storefront" IS acceptable; only listing-DB citations + cross-street qualifiers are unwanted. Concentrated on retail-sourced rows (composes with row 12 retail-bias). Lever: the `name` schema description string. Defer to Phase 5.12.13 prompt audit because the right wording needs Jimmie + a real eye on a fresh batch of Phase B outputs (under-tightening leaves the cruft, over-tightening reverts to bare addresses again). (5.16.1.1 initial rewrite SHIPPED + deployed out-of-band 2026-05-28: the `name` schema description now encodes the 3 producer rules (no listing-DB citations / no cross-street qualifiers / "<location> Storefront" acceptable for unbranded vacancies). R stays OPEN pending a fresh Phase B smoke batch + Jimmie's wording lock; 1-2 iterations expected (human-in-the-loop, not auto-iterated). See `docs/decisions.md` § Phase 5.16.1.1 D10.) (LOCKED 5.16.1.2 2026-05-28: a fresh Phase B run returned 5 names all passing the 3 rules — Abbot Kinney / Century Park / Culver City Main Street / Robertson Blvd / Santa Monica Blvd Storefront; 0 wording iterations needed. Locked on Jimmie's confirmation. See `docs/decisions.md` § Phase 5.16.1.2 D13.) |
| 14 | 2026-05-24 | `supabase/functions/_shared/venueFill.ts` (`FILL_TOOL.key_features` description) + `supabase/functions/vs-research-venues/index.ts` Phase B `TOOL.venues[].features` description | `1a01d3e` | ☑ | ☑ | [med] | Phase 5.12.7 post-smoke (Jimmie, VS-side mirror of the HQ-side issue already slated for 5.12.13): VS-generated venue features come back as long narrative sentences instead of the intended short keyword tag list. Live examples: `Standalone creative-conversion building, ~24,806 sq ft total leasable area`, `Brand-new creative conversion finish in Hollywood Media District`, `62 secured parking spaces across surface and subterranean lots, valet can add ~25%`, `Walk Score of 96 — highly walkable block on N Cahuenga Blvd`, `Zoned MR2-2 / LAMR1, standalone building with full-floor tenancy option`, `Neighbors include Netflix, Red Studios, Paramount Studios, Sunset Las Palmas Studios`. Intended shape is a SHORT keyword tag list, 1-4 words MAXIMUM (usually 1-2), evergreen + generic enough to be useful across diverse venues. Example tags: Courtyard, Skylight, High Ceilings, Garage Doors, Ground Floor, Multi-Level, Waterfront, Rooftop, Garden, Wood Floors, Exposed Brick, Balcony, Storefront, Vaulted Ceilings, Multiple Stages, Outdoor, Floor-to-Ceiling Windows. Hard rules: (a) NO venue-type values (Warehouse, Hotel, Industrial, etc — those live in `venue_type` / `venue_venue_types`); (b) NO specific named landmarks / neighborhoods / architects (Waterfront not "Hudson River Waterfront", Townhouse not "West Village townhouse", no "Statue of Liberty views" / "Miles van der Rohe"); (c) NO narrative sentences carrying square-footage / zoning / walk-score / neighbor lists; (d) 2-10 tags per venue, certainty + quality over volume. Originally Phase 5.12.13 plan scoped this only to HQ's `hq-generate-venue-about`; the VS side has the same problem on the same shape so the prompt audit phase needs to cover BOTH surfaces in lockstep. Same FILL_TOOL is the lever for `vs-compile-summaries` Pass 1, `vs-parse-sheet` enrichment, AND `vs-research-venues` Phase A's per-row fill; the Phase B `submit_research` tool is a separate but parallel surface. Roadmap entry for 5.12.13 updated to call this out explicitly. (RESOLVED `1a01d3e`: shipped together with Edge #3 as the first ship of the prompt audit. FILL_TOOL.key_features schema rewrites to 2-10 evergreen 1-4 word tags (description + items.maxLength 35 + minItems 2 + maxItems 10); Phase B `vs-research-venues` `TOOL.venues[].key_features` mirrors the FILL_TOOL shape; new `sanitizeTagShape` helper in `_shared/venueTypes.ts` chains on the outside of `stripPlaceholders` at every write site including a new HQ-pool fill gate inside `enrichHqPoolForResearch`. Pre-5.12.13.1 the HQ pool branch deliberately skipped key_features writes; Jimmie's call locked the policy that empty-features HQ rows now receive sanitized AI tags (never overwrites, never additive). User-message label sync `Key features:` -> `Key features (tags):` in `buildOverviewUserMsgFromVenue` + `buildOverviewUserMsgForVsRow` signals to the About-paragraph generator that the field arrives as tags-to-interpret rather than narrative-to-echo.) |
| 15 | 2026-05-24 | `src/lib/hq/bulkImport/refEnumerate.ts` (`splitMulti` line 58-63 + `enumerateUnresolvedRefs` split at line 30) + `supabase/functions/vs-parse-sheet/index.ts` (`venue_type` pick at line ~175; no downstream split at parse time) | `n/a` | ☑ | ☑ | [med] | Phase 5.12.13.1 smoke (Jimmie): cross-cutting delimiter divergence across HQ + VS parsers. HQ Bulk Import splits multi-value cells on `\|` ([refEnumerate.ts:30 + splitMulti at :60](src/lib/hq/bulkImport/refEnumerate.ts:30)); `vs-parse-sheet` does NOT split `venue_type` at all (stores the raw cell verbatim); downstream VS Phase A `sanitizeMultiAgainst` + frontend `parseTypes` both split on `/` and `,`. Producer uploaded a sheet with `Retail\|Event Venue` per the HQ-import `\|` convention; vs-parse-sheet wrote the literal string to `vs_candidate_venues.venue_type`; Phase A `sanitizeMultiAgainst` (split on `/` + `,`) failed canonicalize and skipped the write; matrix rendered a single fallback-styled chip with the full `Retail\|Event Venue` text. No new row inserted into `public.venue_types`; the symptom is the stale-token-as-chip rendering via `TypeTogglePopover`'s stale-token union. Locked target (Jimmie 2026-05-24): `/` + `,` universally (the union; mirrors what VS Phase A + frontend `parseTypes` already accept). Switch path: (1) update `refEnumerate.ts splitMulti` to split on `/` + `,` (or both, with `\|` accepted during a transition window); (2) extend `vs-parse-sheet` `venue_type` parse to split on the same delimiters before INSERT into `vs_candidate_venues.venue_type`; (3) update CSV templates + bulk-import producer-facing copy. Lands as a dedicated cross-cutting sub-phase (5.12.16-ish) so parser + templates + docs move in lockstep. (RESOLVED `<5.16.1.1>`: shared `splitMultiValue` util (`src/lib/multiValue.ts` + byte-equivalent `supabase/functions/_shared/multiValue.ts` mirror) splits on `/` + `,`, accepts legacy `\|` during a logged transition window. `refEnumerate.ts` `splitMulti` + `enumerateUnresolvedRefs` consume it; `vs-parse-sheet` now splits `venue_type` and re-joins with " / "; the 3 CSV templates + RefResolvedCell placeholder use `/`. Transition banner skipped (owner call). `vs-parse-sheet` deployed out-of-band. See `docs/decisions.md` § Phase 5.16.1.1 D9.) (SUPERSEDED at v1.0 / Phase 6.0: the multi-value delimiter contract flipped to PIPE `|` ONLY. Slash `/` is now literal and never splits (valid values like `Theatre/Auditorium`, `Indoor/Outdoor` stay whole); comma `,` is the CSV column delimiter only. `splitMultiValue` splits on `|` alone; the `/` + `,` canonical set is retired. The VS-internal `venue_type` (slash-joined, slash-split by `parseTypes` / `sanitizeMultiAgainst` / the AI emission) is unchanged and flagged as a separate parser. See `docs/decisions.md` § Phase 6.0.) |
| 16 | 2026-05-28 | `supabase/functions/vs-research-venues/index.ts` (`rescueHqPoolFitVetoedVenues`) | `n/a` | ☑ | ☑ | [low] | Phase 5.14 spec § 8 edge-cases table lists "loadHqVenuesIntoPool rescue path inserts a candidate → Same photo pre-population logic applies", but the implementation prompt specified only the deterministic-keep path and seed-then-drop path. The `rescueHqPoolFitVetoedVenues` INSERT (line ~1694) does not seed `vs_venue_photos` from HQ photos. Rescued candidates with HQ venues that have stored photos will start with empty photo slots in Review. Low severity: rescued venues are the third-lane fallback (fit-filter-vetoed venues; typically fewer than deterministic-keep rows) and the producer can still upload photos manually. Proper fix: change the INSERT to `.select("id").maybeSingle()` and call `seedHqPhotosToVs` after, mirroring the deterministic-keep path. (RESOLVED Phase 5.14: INSERT extended to `.select("id").maybeSingle()`; `photoSeedCandidates` collected during verdict loop; `Promise.allSettled` seeding added after Step 8 with `path=rescue` telemetry qualifier.) |
| 17 | 2026-05-28 | `supabase/functions/ts-pull-candidates/index.ts` (attachments accumulator + `uploadAttachmentToStorage` call) | `n/a` | ☐ | ☐ | [low] | Encountered during the Phase 5.16.1.1 §3a no-explicit-any sweep. The per-candidate `attachments` array previously annotated `mimeType: string`, but the value comes from a Gmail MIME part's `mimeType` which the REST shape makes optional (`string \| undefined`). Typing pass widened the field to `string \| undefined` (honest) and added a type-only `as string` cast at the single `uploadAttachmentToStorage({ mimeType })` call site (that helper requires a non-optional `string`). In practice Gmail always emits a mimeType for attachment parts, so the cast never masks a real runtime gap; flagging only because the helper's required-`string` contract vs the part's optional field is a latent mismatch that a future refactor of `_shared/attachmentStorage.ts` could surface. |
| 18 | 2026-05-28 | `supabase/functions/` (whole tree) | `n/a` | ☑ | ☑ | [low] | Encountered during the Phase 5.16.1.1 §3a no-explicit-any sweep. No edge function imports the generated `Database` types (they live under `src/integrations/supabase/types.ts`, the frontend tree). The established convention is an untyped service client (`ReturnType<typeof createClient>` with no `Database` generic) plus hand-rolled narrow interfaces / inline casts for each `.select(...)` projection. Consequence: row reads off the client are effectively `any`-shaped at the type level, and projection mismatches (selecting columns that don't exist, or mis-typing a column) are invisible since there is no tsc gate on edge functions. Not a bug; noting the typing-coverage gap in case a future pass wants to wire the generated `Database` types into the edge tree (would require a path that doesn't pull the whole frontend types file through esm/Deno resolution). (RESOLVED `<5.16.1.2>`: `supabase/functions/_shared/database.types.ts` generated (3061 lines, matches the frontend schema; via /tmp + test -s + mv). The "pull the whole frontend types file" concern is moot — the generated file is self-contained in the Deno tree. Existing intentionally-narrow `.select()` interfaces left as-is per spec §3k (a full `Database[...]["Row"]` would over-fetch the type surface); forward convention documented in `docs/edge-functions.md` + `docs/conventions.md`. See `docs/decisions.md` § Phase 5.16.1.2 D12.) |
| 19 | 2026-05-28 | `supabase/functions/_shared/buildClaudeEvalRequest.ts` (`logClaudeUsage`) | `n/a` | ☑ | ☑ | [low] | Surfaced during the Phase 5.16.1.1 §3a no-explicit-any sweep (shared-group agent): `logClaudeUsage` has zero callers anywhere under `supabase/functions/` (grep-confirmed) — dead code. Left in place per the no-refactor-during-a-typing-pass rule; flagged for a future cleanup pass to delete it (and its newly-narrowed `EvalUsage` param type if nothing else adopts it). (RESOLVED `<5.16.1.2>`: deleted `logClaudeUsage` + its now-dead `EvalUsage` type from `buildClaudeEvalRequest.ts`; grep-confirmed zero references repo-wide. The functions importing this `_shared` module are behavior-identical (dead code) and align to source on the staged 5.16.1.1 parity redeploys.) |
| 20 | 2026-05-28 | `supabase/functions/bulk-import/index.ts` (commit RPC call sites ~95/126/157) | `81cdaf2` | ☑ | ☐ | [low] | Flagged by migration-reviewer during the Phase 5.16.1.2 advisor migration: the three bulk-import commit RPCs are invoked as `sb.rpc(...)` where `sb` is the service-role `adminClient` passed into `handler.commit` (only `bulk_import_undo` calls `adminClient.rpc(...)` explicitly). Reading the file cold, `sb.rpc(...)` reads like it could be the user-JWT client, but the 5.16.1.2 service-role-only GRANT lockdown (decisions D1) relies on these being the service-role client. Harmless today (they ARE service-role); add a one-line comment at the call sites clarifying `sb === adminClient` if the file is touched again. |
| 21 | 2026-05-28 | `supabase/functions/vs-parse-sheet/index.ts:51` | `d7f6fe0` | ☑ | ☑ | [med] | Surfaced by code-reviewer during the Phase 5.16.1.2 xlsx swap: the server-side sheet parser still imports `https://esm.sh/xlsx@0.18.5` — the SAME unpatched prototype-pollution + ReDoS version that 5.16.1.2 removed from the frontend npm graph. `npm audit` 0 covers only the npm tree, not Deno esm.sh imports, so the vuln persists server-side on producer-uploaded sheet parsing (admin/producer-gated → internal threat model). esm.sh cannot serve a patched build (npm `xlsx` tops out at 0.18.5; that is why SheetJS moved to their CDN), so the fix mirrors the frontend: point the import at `https://cdn.sheetjs.com/xlsx-0.20.3/package/xlsx.mjs`. Out of the locked 5.16.1.2 scope (spec §3h = frontend npm only); needs a `vs-parse-sheet` redeploy + a real sheet-parse smoke. Recommend folding into this phase's edge work or a fast follow-up. (RESOLVED `<5.16.1.2>`: the cdn.sheetjs.com swap FAILED at the Supabase edge deploy bundler ("Cannot import from cdn.sheetjs.com:443" — the bundler rejects that host; the frontend is unaffected since it builds through Vite + imports the CDN at browser runtime). Per Jimmie's call, vendored the patched SheetJS 0.20.3 ESM build locally at `supabase/functions/_shared/vendor/xlsx.mjs` (+ provenance README) and imported it from `vs-parse-sheet` via `../_shared/vendor/xlsx.mjs` — the bundler bundles local files fine. Deployed to prod 2026-05-28; the server-side xlsx vuln is closed. Same `read` + `utils.sheet_to_json` API; a PDF/xlsx/csv parse smoke is recommended to confirm 0.18.5→0.20.3 parity, API-identical so low risk.) |
| 22 | 2026-05-29 | `supabase/functions/vs-compile-summaries/index.ts:233-252` | `n/a` | ☐ | ☐ | [high] | Still uses the old non-atomic kickoff (read `compile_started_at`, check grace, then a separate non-atomic UPDATE) while `vs-research-venues` + `vs-generate-deck` were migrated to atomic `pg_try_advisory_xact_lock` RPCs to close this exact TOCTOU, so compile has the same double-Claude-spend race (the 5.12.4.2 comment claims it extends the deck fix but only bumped the grace window). Rec: add a `vs_compile_try_acquire_kickoff` RPC mirroring the research/deck ones and call it in place of the read-check-write block. (audit F007) |
| 23 | 2026-05-29 | `supabase/functions/vs-research-venues/index.ts:2231,3080` | `n/a` | ☐ | ☐ | [high] | `await loadHqVenuesIntoPool(...)` runs in the `Deno.serve` handler before `waitUntil(work())`, and that helper does a venues SELECT, fit-scoring, up to 10 INSERTs, and `seedHqPhotosToVs` (sequential per-photo storage download+upload+insert, up to ~40 round-trips), so the browser blocks on all of it before the 200, defeating the `waitUntil` fast-return design for any city-anchored scout with HQ photos. Rec: move the `loadHqVenuesIntoPool` call (and thus photo seeding) inside `work()`, or at minimum push `seedHqPhotosToVs` to the background. (audit F008) |
| 24 | 2026-05-29 | `supabase/functions/vs-research-venues/index.ts:887-1072` | `n/a` | ☐ | ☐ | [high] | `scoreVenueFitForBrief` is a pure, branch-heavy gate (weighted type/neighborhood/features/sqft/capacity scoring, 3 veto classes, an `unscored > applicableCount/2` aggregate veto, a legacy `sq_ft_min` fallback) deciding which HQ venues enter every producer's matrix, with zero tests, so a regression silently drops or floods venues. Rec: extract the pure scorer to `_shared/venueFit.ts` (also F029) and add a focused unit test covering each veto path, the `> half` boundary, the legacy fallback, and the zero-applicable case. (audit F010) |
| 25 | 2026-05-29 | `supabase/functions/vs-research-venues/index.ts:1-3087` | `n/a` | ☐ | ☐ | [med] | Largest file in the repo (3086 LOC): 2 Claude prompt/tool pairs, the entire pure fit scorer (~230 LOC), `loadHqVenuesIntoPool`, rescue/enrich helpers, `seedHqPhotosToVs`, plus the handler and Phase B, where the pure scorer (887-1072) and photo helper (734-797) have no request-context dependency. Rec: extract the pure fit-scoring cluster to `_shared/venueFit.ts` (enables F024 above) and `seedHqPhotosToVs` to a `_shared` module (no behavior change). (audit F029) |
| 26 | 2026-05-29 | `supabase/functions/vs-research-venues/index.ts:2609-2682` | `n/a` | ☐ | ☐ | [med] | Phase B `submit_research` output writes `derived_attrs` to the DB with only a `typeof === 'object'` check, but the schema promises a string-valued `{column_id:'yes'|'maybe'|'no'}` map and nothing enforces value shapes before INSERT into `vs_candidate_venues.derived_attrs`, so Claude can emit arbitrary nested objects. Rec: validate/coerce `derived_attrs` entries to the documented string-valued shape before INSERT, matching the placeholder-stripping already done for recommendations/key_features. (audit F030) |
| 27 | 2026-05-29 | `supabase/functions/ts-pull-candidates/index.ts:283-610` | `n/a` | ☐ | ☐ | [med] | The forwarded-email/referral attribution pipeline (`parseForwardedEmail`, `extractManagerNote`, segment/sender parsers) decides who the candidate is, who referred them, and what manager note feeds the first Claude eval (a multi-stage regex pipeline with no tests, functions already `export`ed), so a regex regression silently misattributes or skips applicants. Rec: add a Deno test exercising `parseForwardedEmail`+`extractManagerNote` against captured real forward shapes (Gmail, Apple Mail, nested A->B->jobs@, reply-quote). (audit F031) |
| 28 | 2026-05-29 | `supabase/functions/ts-evaluate-candidate/index.ts:187-217` | `n/a` | ☐ | ☐ | [med] | Re-eval fetches/parses every attachment with no size or budget guard (full fetch into Storage upload into PDF/DOCX parse), while `ts-pull-candidates` guards the identical path with `MAX_PARSE_ATTACHMENT_BYTES` (2.5MB), `MAX_TOTAL_PARSED_ATTACHMENT_BYTES` (5MB), `MAX_STORAGE_PERSIST_BYTES` (10MB) that the re-eval port dropped, so a large/zip-bomb DOCX (looped 25/batch by `ts-bulk-reevaluate`) can exhaust the memory/time budget. Rec: port the same three size caps plus a "too large to parse safely" skip into the re-eval attachment loop. (audit F032) |
| 29 | 2026-05-29 | `supabase/functions/vs-generate-deck/index.ts:1312-1404` | `n/a` | ☐ | ☐ | [med] | The per-venue slide loop calls `getPresentation(deckId, token)` twice per venue (hyperlink pass + photo pass), both operating on the same slide, so for N venues that is up to 2N full presentation reads compounding `WORK_TIMEOUT_MS` pressure. Rec: fetch the presentation once per venue and reuse it for both passes. (audit F034) |
| 30 | 2026-05-29 | `supabase/functions/vs-compile-summaries/index.ts:243-252` | `n/a` | ☐ | ☐ | [med] | The kickoff UPDATE that writes `compile_started_at` and clears `pipeline_error` is unchecked, so if it fails `pipeline_error` stays set and the final success CAS (which guards on `.is('pipeline_error', null)`) no-ops (`scoutUpdCount===0`), leaving the scout stuck looking failed though work completed. Rec: capture and check the kickoff UPDATE error and on failure return 500 / log loudly rather than proceeding into `work()`. (audit F036) |
| 31 | 2026-05-29 | `supabase/functions/vs-research-venues/index.ts:2722-2727` | `n/a` | ☐ | ☐ | [med] | The Phase B cross-rail dedupe re-runs the exact same `venues` SELECT (same columns, same `.ilike('city', ...)`) that `loadHqVenuesIntoPool` already ran at line 1120, but `HqPoolLoadResult.hqRows` is available and unused, a redundant round-trip plus drift risk if the two column lists diverge. Rec: thread `hqRows` from `hqPoolLoadResult` into `work()` and reuse, or document why a fresh read is required (rows inserted by rescue between the two points). (audit F037) |
| 32 | 2026-05-29 | `supabase/functions/_shared/database.types.ts:1-3061` | `n/a` | ☐ | ☐ | [med] | Imported by zero edge functions (grep clean) and already drifted from `src/integrations/supabase/types.ts` (src has a `venue_files` table the edge copy lacks), dead weight that would mislead a future dev into a stale schema (relates to the now-resolved Edge #18 / D12 which generated this file as "wired but adoption deferred"). Rec: delete it (nothing imports it) or add it to the regen step so it stops drifting, and update Edge #18 / D12 to reflect "exists, unimported, drifting." (audit F038) |
| 33 | 2026-05-29 | `supabase/functions/vs-research-venues/index.ts:2505-2573` | `n/a` | ☐ | ☐ | [med] | Phase B races `callClaude` against `WORK_TIMEOUT_MS=360s`, but `callClaude` self-aborts at 180s (`anthropic.ts:383`), so the 360s timer can never fire for the Claude call and only bounds the surrounding sequential work, while the comment frames it as guarding "a hanging Anthropic call" the inner 180s already covers. Rec: realign the comment (inner 180s covers the hang, `WORK_TIMEOUT_MS` bounds the aggregate pipeline) or lower it to a value that actually bounds the observed worst case. (audit F044) |
| 34 | 2026-05-29 | `supabase/functions/_shared/venueTypes.ts` (`parseTypes` + `sanitizeMultiAgainst`, `split(/[/,]/)`) + `src/lib/venue-scout/venueTypes.ts` (`parseTypes` mirror) + the slash-separated AI `venue_type` emission | `n/a` | ☐ | ☐ | [med] | v1.0 delimiter flip (`docs/decisions.md` § Phase 6.0 D12): the producer-facing bulk-import + VS-sheet multi-value delimiter is now pipe `|` only, but the VS-INTERNAL `venue_type` representation is still slash-joined (`vs-parse-sheet` re-joins piped tokens with " / ") and slash-split downstream by `parseTypes` / `sanitizeMultiAgainst`, and the AI is prompted to emit slash-separated types. So a canonical venue type whose NAME contains `/` (e.g. "Theatre/Auditorium") still splits into two pills in the VS matrix and can fail Phase A canonicalize, even though HQ bulk-import now stores it correctly as one value. Accepted for v1.0 (producer input is pipe, so a slash survives the initial parse); the full realign is a post-v1 refactor. Rec: flip the VS-internal separator + the AI prompt + `parseTypes` + `sanitizeMultiAgainst` + the matrix together so slash becomes literal end-to-end. (post-v1.0 backlog) |

## Database

(Migrations, schema, RLS, triggers, RPCs.)

| # | Date | File | Hash | V | R | Severity | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-28 | `storage.objects` policies (`briefs` bucket) | `n/a` | ☑ | ☑ | [low] | Surfaced in the Phase 5.16.0 `pg_policies` snapshot: the `briefs` storage bucket carries TWO redundant ALL policies — `briefs_authenticated_all` and `storage_briefs_all`, both `bucket_id = 'briefs'` USING/WITH CHECK `true`. Looks like an initial-schema name plus a later re-create under the `storage_*_all` convention that never dropped the original. Harmless (both grant identical access) but should collapse to one in the 5.16.1 sweep. Not touched by 5.16.0 (briefs is out of the tier-hardening scope). (5.16.1.1: migration `20260611000000_phase_5_16_1_1_briefs_dedupe_policy.sql` written, dropping the Studio-orphan `briefs_authenticated_all` (no migration ever defined it) and keeping the version-controlled `storage_briefs_all`. migration-reviewer verdict GO-with-caveat. APPLIED to prod 2026-05-28 via `supabase db push --linked`; the drop is provably safe since the kept `storage_briefs_all` already grants `bucket_id='briefs'` to all `authenticated` (the maximum for the role), so dropping the orphan cannot tighten access. RESOLVED `<5.16.1.1>`.) |

## Build & Tooling

(Vite config, Tailwind config, tsconfig, scripts, Netlify config, package.json.)

| # | Date | File | Hash | V | R | Severity | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-17 | `src/integrations/supabase/types.ts:1` | `c29c8bd` | ☑ | ☑ | [med] | The committed types.ts since Phase 5.6.3 starts with a `Initialising login role...` line (stderr from the supabase CLI captured into the file by the `> types.ts` redirect when stderr wasn't suppressed). The file is unparseable so `tsc --noEmit` errors out on every line of types.ts and never reaches the rest of the codebase. Vite build still passes because Vite skips type-checking by default. Resolved during the Phase 5.6.5 regen (`supabase gen types typescript --linked 2>/dev/null > types.ts`); the resulting clean parse exposed 14 pre-existing TS errors in unrelated files (TaskDetail, VendorDetail, VenueDetail, ProjectDetail, ClientDetail, PersonDetail, DeliverableDetail, TeamMemberEdit, SettingsPage, PullDetail, ErrorState, lookups.ts, vendors/queries.ts) that the corruption was masking. Those errors are out-of-scope for 5.6.5; flagging as a follow-on cleanup pass. |
| 2 | 2026-05-22 | `package.json` (`@testing-library/react`) | `n/a` | ☑ | ☑ | [low] | Codebase-audit pass: `@testing-library/react` is a devDep but no test imports it (only `@testing-library/jest-dom` is wired via `src/test/setup.ts`; the lone `src/test/example.test.ts` doesn't render components). Held back from removal pending Jimmie's call on whether component tests are planned. Remove if not. (`@tailwindcss/typography` + `@types/dompurify` were the clear-cut unused devDeps and were removed in the audit; `googleapis`/`tsx` are real, used by `scripts/verify-service-account.ts`.) (RESOLVED `<5.16.1.1>`: `@testing-library/react` dropped (Round 3); `@testing-library/jest-dom` stays (wired via `src/test/setup.ts`); the example test still passes.) |
| 3 | 2026-05-22 | `eslint.config.js` / `supabase/functions/**` | `n/a` | ☑ | ☑ | [med] | Codebase-audit pass: `npm run lint` reports 192 problems (161 errors), almost all `@typescript-eslint/no-explicit-any`, spread across edge functions AND frontend files (FilterBar, ViewSwitch, CalendarPage, useAuth, briefForm, several TS/VS pages, etc.). Pre-existing baseline unrelated to the audit; `npm run build` does not gate on lint so it ships green. Future call: either fix the `any`s incrementally or scope eslint so the Deno `supabase/functions/**` tree isn't linted by the frontend config. (NOTE 5.16.1.0: scope narrowed by excluding `supabase/functions/**` from `eslint.config.js`. New baseline is 30 frontend problems — 4 errors / 26 warnings; full repo incl. edge functions is 191 — 165 errors / 26 warnings, so the Deno tree was 161 of the errors. NOT resolved; 5.16.1.1 burns the frontend 30 to zero.) (RESOLVED `<5.16.1.1>`: full repo burned to 0. The `supabase/functions/**` eslint exclusion was REVERTED (edge tree permanently re-linted); the 161 edge errors (152 `no-explicit-any` + 9 `no-useless-escape`) were typed away with real types + 0 inline disables; the 30 frontend problems fixed. See `docs/decisions.md` § Phase 5.16.1.1 D1.) |
| 4 | 2026-05-27 | `src/components/bulk-import/StickyActionBar.tsx` | `81cdaf2` | ☑ | ☑ | [low] | Uses a custom `sticky bottom-0` implementation instead of `.actionbar`. Not in 5.13.2a scope (bulk-import is a separate surface); should be converted to `.actionbar` in a future bulk-import touch pass. (RESOLVED `<5.16.1.1>`: StickyActionBar outer converted to `.actionbar` (inner flex keeps `px-6 py-4`; unsaved indicator uses `.dirty`); BulkImportPage gains `pb-28` so the now-fixed bar doesn't overlap content.) |
| 5 | 2026-05-28 | `src/index.css` (`@media (max-width:640px)` block) + Tailwind JIT | `n/a` | ☑ | ☑ | [med] | Surfaced by the Phase 5.16.1.0 Vite 8 upgrade. Tailwind v3's JIT scanner extracts false-positive class candidates from idiomatic JS negations (`!row` from `const unread = !row.read;` in NotificationRow.tsx etc.) and generates spurious `!important` variants of our `@layer` component rules; some emerge as **empty-selector** rules (a rule body with no selector before the `{`), e.g. `{padding:16px !important; ...}` and `{font-size:26px !important;}` derived from the `@media (max-width:640px)` block. esbuild's CSS minifier silently drops them (warns `Unexpected "{"`); Vite 8's default Lightning CSS minifier hard-errors. Workaround shipped 5.16.1.0: pin `build.cssMinify: 'esbuild'` in `vite.config.ts`. Real fix (5.16.1.1): stop the empty-selector generation at the Tailwind source (e.g. tighten content extraction or the `!`-candidate surface), then drop the `cssMinify` override so Vite 8's Lightning CSS default takes over. (RESOLVED `<5.16.1.1>`: root-caused; `!row` (from `!row.read`) is scanned as an important-modified candidate matching the `.row` component, whose rule lives in a nested `@media` inside `@layer components`, so Tailwind emits the media block's other rules under an empty selector. Fixed with a `tailwind.config.ts` `content.transform` (DEFAULT) that strips bare-word `!`-negation candidates (`![a-z]+` not followed by a class-continuation char) before the scanner; hyphenated/bracketed important utilities like `!border-t-2` / `!bg-[...]` are preserved (none single-word exist). `build.cssMinify` override + comment removed; production build green on the Lightning CSS default (0 empty selectors, bundle 584 KB). See `docs/decisions.md` § Phase 5.16.1.1 (D8 sibling) + `tailwind.config.ts` comment.) |
| 6 | 2026-05-28 | `vite.config.ts` (`@vitejs/plugin-react-swc`) | `n/a` | ☑ | ☑ | [low] | Vite 8 emits an informational notice on dev/build/test: `[vite:react-swc] We recommend switching to @vitejs/plugin-react for improved performance as no swc plugins are used` (https://vite.dev/rolldown). With Rolldown + Oxc as the Vite 8 engine and no SWC plugins in use, the oxc-based `@vitejs/plugin-react` would be faster. Not acted on in 5.16.1.0 (plugin swap changes the build; out of scope for the tooling-upgrade phase). Optional 5.16.1.1 perf follow-up: swap `@vitejs/plugin-react-swc` → `@vitejs/plugin-react` and re-smoke. (RESOLVED `<5.16.1.1>`: swapped to `@vitejs/plugin-react@6.0.2` in `package.json` + `vite.config.ts` + `vitest.config.ts` (the vitest config also imported the swc plugin); dev/build/test/typecheck + a dev-boot HTTP 200 all clean, no more swc notice.) |
| 7 | 2026-05-29 | `eslint.config.js` (ignores) | `n/a` | ☐ | ☐ | [low] | Encountered while running baseline gates for the tech-debt audit: `eslint .` lints the `.claude/` dir (not in eslint's ignores, not gitignored), so the untracked Workflow helper `.claude/wf-tech-debt-audit.js` makes `npm run lint` exit non-zero with `192:1 Parsing error: 'return' outside of function` (top-level `return` is valid in the Workflow runtime but not to eslint's parser). The source tree itself is clean (0 errors). Any `.js` dropped into `.claude/` will redden the lint gate. Fix: add `.claude/**` (and likely `.codex/**` / `.agents/**`) to the `ignores` in `eslint.config.js`. Untracked file, so no production impact; affects only local lint runs that have the helper present. |
| 8 | 2026-05-29 | `~/.claude/settings.json` (global; `supabase@claude-plugins-official` MCP, not repo source) | `n/a` | ☐ | ☐ | [med] | Claude Code setup audit: the enabled Supabase plugin MCP is an unscoped HTTP endpoint (`https://mcp.supabase.com/mcp`, no `read_only` / project-ref flags), so it is write-capable and account-wide when authenticated, reaching any project including prod `amipjjmphblfxpghjnel`. Currently dormant (flagged needs-auth; only `authenticate` / `complete_authentication` exposed). Keep dormant or scope OAuth to read-only / non-prod at auth time unless a concrete write workflow is approved. Scope is controlled at the remote (OAuth consent + dashboard), not a local flag, so there is no repo-side fix. |

## Docs

(`docs/`, root-level markdown.)

| # | Date | File | Hash | V | R | Severity | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-16 | `docs/schema.md` | `n/a` | ☑ | ☑ | [med] | Schema doc still describes the pre-5.2.3 unified `organizations` table; the 5.2.3 split into vendors + clients was never reflected in a structural rewrite (only callout notes were added in Phase 5.2 cleanup `523e1d6` for the cleanup-specific column add + GRANT fix). Canonical column lists currently live in the migration files + `src/integrations/supabase/types.ts`; a full schema.md rewrite reflecting the split shape + the new `vendor_capabilities`, `vendor_categories`, `cities`, `project_categories` lookup tables is doc-debt carried forward to a future polish pass. Code-reviewer flagged in 5.2 cleanup. (5.8.2 triage: verify-and-defer to Phase 5.8.3. Explicit slot in 5.8.3 (MD doc audit + cleanup pass). This is the biggest known target for that pass per `OUTPUTS/phase-5-8-plan.md`.) (RESOLVED `fc7f42d`: the 5.8.3 doc audit rewrote schema.md into the split shape — `### vendors (renamed from organizations)`, `### clients`, `### organizations table (DROPPED)` — plus the `vendor_categories` / `vendor_subcategories` / `vendor_capabilities` / `cities` lookup tables. Triage 2026-05-22 confirmed present; flipping R now.) |
| 2 | 2026-05-16 | `docs/working-with-claude.md` (§ 4.5) | `523e1d6` | ☑ | ☑ | [low] | The Phase 5.2 cleanup amendment reordered the squash flow so the SHIPPED block in `OUTPUTS/COWORK_SYNC.md` is written (step 5d) BEFORE Jimmie runs the manual `git push origin main` (step 5e). If Jimmie reviews the local commits and decides NOT to push, the SHIPPED block already claims the work shipped; a subsequent Cowork session reading that file would get stale state until the next AWAITING-block overwrite. Mitigated by the block's self-documenting "pending push" line, but worth revisiting if the manual-push step becomes a frequent abort point. Resolved by `85cb765`: pre-added `Bash(git push origin main)` permission rule to `.claude/settings.json`; § 4.5 restructured so step 5c is autonomous push and step 5e SHIPPED-block overwrite happens AFTER push lands. |
| 3 | 2026-05-16 | `docs/working-with-claude.md` (§ 4.5 step 5c) | `1c21ea9` | ☑ | ☑ | [med] | The Phase 5.3 ship discovered that Netlify evaluates `[skip netlify]` against the HEAD commit of each push, not per-commit. The original § 4.5 step 5c put the squash commit + the `[skip netlify]` CHECKPOINT backfill in the same `git push origin main`; Netlify saw `[skip netlify]` at HEAD and skipped the entire push, leaving the squash deploy-worthy but un-deployed. Jimmie triggered the deploy manually. Resolved by splitting step 5c into 5b (push the squash alone, deploy fires) + 5d (push the backfill alone, correctly skipped). New § 4.5.b documents the Netlify trigger semantics + the empirical confirmation. |
| 4 | 2026-05-19 | `CHECKPOINT.md` | `n/a` | ☑ | ☑ | [low] | Encountered while doing the Phase 5.9.3 CHECKPOINT update: the file is ~410 KB / ~69k tokens and now exceeds the Read tool's size limit, so it can only be opened via `sed`/`awk` line ranges. The "Latest ship" / "Prior ship" prose entries + the never-trimmed Recent commits log (15+ entries despite the file's own "keep last 5" footer rule) are the bulk. Future polish: archive ship narratives older than the current phase to a `docs/v*-changelog.md` and actually trim Recent commits to ~5, so the living-state doc stays tool-readable. Not touched structurally in 5.9.3. (RESOLVED `<5.16.1.1>`: CHECKPOINT.md rewritten lean: Recent commits trimmed to 5, older ship narratives dropped (the per-phase detail lives in `docs/v1-changelog.md`), so the file reads whole again.) |
| 5 | 2026-05-23 | `docs/design-system.md` + `docs/mirror-style-guide.md` | `n/a` | ☑ | ☑ | [low] | Codex UX audit source-of-truth check: the docs conflicted in a few current-state places. `design-system.md` still pointed to `.hq-pill--<token>` after the hq chrome cleanup, while `mirror-style-guide.md` still referenced removed `.btn-base` and `.input-base`; the two docs also differed on button casing and card surface language. (RESOLVED `3e59a4d`: docs now point to `.pill.p-<token>`, `.btn`, and `.input`, and clarify button source casing plus `--surface` vs `--surface-alt` card usage.) |
| 6 | 2026-05-29 | `docs/decisions.md` + `docs/design-system.md` | `n/a` | ☑ | ☑ | [med] | Phase 6.0 M2(b) doc-sweep: the 6.1-6.5 cluster commits (`0b6f800`/`37a293e`/`5044439`/`c9ec706`/`54c1f35`) each carried a "Carry-forward to the 6.0 close sync" doc-intent list (design-system.md / decisions.md / changelog / roadmap edits), but `git diff f138c23..HEAD` showed those doc edits were never actually committed (design-system.md was byte-identical to the 5.16 base; decisions.md held only the tech-debt + F001 blocks). The close sweep applied them (folds into `<pending 6.0 squash>`). Pattern to watch: "carry-forward to the close sync" doc intent in cluster commits is NOT self-executing; the close sweep must reconcile against the live docs, never trust the commit-message bullets. (R flipped: the M2(b) sweep applied the missing maintenance.) |
| 7 | 2026-05-29 | tracked `.md` files (pre-6.0 content) | `n/a` | ☑ | ☐ | [low] | Repo-wide pre-existing em-dash debt. The Phase 6.0 final doc sweep stripped em dashes from every 6.0-ADDED markdown line (verified: `git diff f138c23 -- '*.md'` reports 0 em-dash additions), but ~79 em dashes remain on pre-6.0 lines this sweep did not touch: `code-observations.md` (38 older rows), `docs/schema.md` (11), `docs/auth-model.md` (10), `docs/design-system.md` (5), `docs/v1-changelog.md` (5), `README.md` (3), `docs/edge-functions.md` (3), `CHECKPOINT.md` (1), plus 2 small subdir files, and the tooling files under `.claude/` + `.agents/` (out of cleanup scope). The no-em-dash house rule holds going forward; a future opportunistic pass can strip the pre-existing population (each is a 1-char fix). Not blocking v1.0. |

## Other

(Catch-all for anything that does not fit.)

| # | Date | File | Hash | V | R | Severity | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
